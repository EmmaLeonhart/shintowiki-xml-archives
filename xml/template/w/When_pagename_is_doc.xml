<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Template:When pagename is/doc</title>
    <ns>10</ns>
    <id>11327</id>
    <revision>
      <id>1046876</id>
      <timestamp>2003-01-01T00:00:00Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Auto generating page. date placed far in the past so it is attributed to Immanuelle and FireflyBot sends them messages if it is ever imported to Wikipedia.</comment>
      <origin>1046876</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="211" sha1="6nj7dn46k3z6ugz1hcd5oabdoyfc0zu" xml:space="preserve">#redirect[[en:Template:When pagename is/doc]]
&lt;noinclude&gt;[[Category:New automatic wikipedia redirects]]&lt;/noinclude&gt;&lt;includeonly&gt;[[Category:Erroneous transclusions of Template:When pagename is/doc]]&lt;/includeonly&gt;</text>
      <sha1>6nj7dn46k3z6ugz1hcd5oabdoyfc0zu</sha1>
    </revision>
    <revision>
      <id>1046877</id>
      <parentid>1046876</parentid>
      <timestamp>2025-02-22T20:57:21Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: importing redirects I created with chatGPT</comment>
      <origin>1046876</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="211" sha1="6nj7dn46k3z6ugz1hcd5oabdoyfc0zu" xml:space="preserve">#redirect[[en:Template:When pagename is/doc]]
&lt;noinclude&gt;[[Category:New automatic wikipedia redirects]]&lt;/noinclude&gt;&lt;includeonly&gt;[[Category:Erroneous transclusions of Template:When pagename is/doc]]&lt;/includeonly&gt;</text>
      <sha1>6nj7dn46k3z6ugz1hcd5oabdoyfc0zu</sha1>
    </revision>
    <revision>
      <id>1046878</id>
      <parentid>1046877</parentid>
      <timestamp>2025-03-24T06:05:24Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "#redirect[[en:Template" to "#redirect[[:en:Template"</comment>
      <origin>1046878</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="212" sha1="s2hxwifx2r33y8vqjgjixnjvbc6mkye" xml:space="preserve">#redirect[[:en:Template:When pagename is/doc]]
&lt;noinclude&gt;[[Category:New automatic wikipedia redirects]]&lt;/noinclude&gt;&lt;includeonly&gt;[[Category:Erroneous transclusions of Template:When pagename is/doc]]&lt;/includeonly&gt;</text>
      <sha1>s2hxwifx2r33y8vqjgjixnjvbc6mkye</sha1>
    </revision>
    <revision>
      <id>1046879</id>
      <parentid>1046878</parentid>
      <timestamp>2025-05-19T10:08:59Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: sync with enwiki</comment>
      <origin>1046879</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="7106" sha1="i2oo1e420ujolw3hvqfewvmez26dkat" xml:space="preserve">{{Documentation subpage}}
&lt;!--Categories where indicated at the bottom of this page, please; interwikis at Wikidata (see [[Wikipedia:Wikidata]])--&gt;

This is the {{tl|When pagename is}} meta-template. It helps other templates detect what page they are on, using pattern matching on the pagename.

== Usage ==

This template takes one or more parameters. Most of the parameters don't have fixed names, instead they are part of the pattern matching. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
| /doc = Doc page text
| other = Other pages text
}}
&lt;/syntaxhighlight&gt;

If the template is on "Template:Example/doc" the code above will return this:
: {{When pagename is
  | /doc = Doc page text
  | other = Other pages text
  | page = Template:Example/doc
 }}

If the template is on any ''other'' page than a /doc page it will return this:

: {{When pagename is
   | /doc = Doc page text
   | other = Other pages text
   | page = Template:Example
  }}

Here is a description of the full pattern matching:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
&lt;!-- Match on full pagename --&gt;
 | User:Example/test = Text for "User:Example/test".
 | User:Example = Text for "User:Example".
 | User talk:Example = Text for "User talk:Example".

&lt;!-- Match on full basepage name, when on a basepage or its subpages --&gt;
 | User:Example/+ = Matches "User:Example" and "User:Example/test".

&lt;!-- Match on pagename, when on a basepage --&gt;
 | Example = Matches "User:Example", "User talk:Example", "Template:Example" 
             and so on, but not "User:Example/something".

&lt;!-- Match on full basepage name, when on a subpage --&gt;
 | User:Example/* = Matches "User:Example/something", but not "User:Example".
 | User talk:Example/* = Matches "User talk:Example/something".

&lt;!-- Match on basepage name, when on a subpage --&gt;
 | Example/* = Matches "User:Example/something" and "User talk:Example/something".

&lt;!-- Match on subpage name --&gt;
 | /something = Any pagename that ends in "/something".
 | /doc = Any pagename that ends in "/doc".

&lt;!-- Match on partial subpage name (case-insensitive) --&gt;
 | /some* = Any subpage name beginning with "/some" or "/Some".
 | /arch* = Matches "User talk:Example/Archive 1".

&lt;!-- Defaults --&gt;
 | basepage = Text for any basepage.
 | subpage = Text for any subpage.
 | other = Text for any page.
}}
&lt;/syntaxhighlight&gt;

The matching goes from top to bottom, and returns the first parameter that matches. "''Top to bottom''" means the order shown above, not the order you happen to feed the parameters.

There's no limit to the number of parameters that you can use, other than what the servers and MediaWiki system can handle. 

Most of the matching is case-sensitive. For instance "&lt;code&gt;/test&lt;/code&gt;" matches "User:Example/test" but not "User:Example/Test". 

Matching on partial subpage names such as "&lt;code&gt;/some*&lt;/code&gt;" has some limitations, see [[#Partial subpage names|its own section]] below.

If an empty (but defined) parameter matches, the matching stops and the template returns an empty string. That's on purpose and can be used like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /doc =
 | /sandbox = Sandbox text
 | other = Other pages text
}}
&lt;/syntaxhighlight&gt;

The code above will render nothing when on a /doc page. But when on a /sandbox page it will return this: 

: {{When pagename is
   | /doc =
   | /sandbox = Sandbox text
   | other = Other pages text
   | page = Template:Example/sandbox
  }}

And when on any other page it will return this:
: {{When pagename is
   | /doc =
   | /sandbox = Sandbox text
   | other = Other pages text
   | page = Template:Example
  }}

== Partial subpage names ==

This template can also match on partial subpage names. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /archiv* = Archive page text
 | other = Other pages text
}}
&lt;/syntaxhighlight&gt;

If on "User:Example/Archive 1" the code above will return this:
: {{When pagename is
   | /archiv* = Archive page text
   | other = Other pages text
   | page = User:Example/Archive 1
  }}

The parameter name "&lt;code&gt;/some*&lt;/code&gt;" must be lower case. But it matches subpage names in both upper and lower case such as "User:Example/SomeThing" and "User:Example/something".

The partial matching only supports matching on 4, 6 and 8 characters. Thus using "&lt;code&gt;/some*&lt;/code&gt;", "&lt;code&gt;/someth*&lt;/code&gt;" and "&lt;code&gt;/somethin*&lt;/code&gt;" works, but using "&lt;code&gt;/som*&lt;/code&gt;" or "&lt;code&gt;/somet*&lt;/code&gt;" doesn't work.

Longer patterns match first, thus if both "&lt;code&gt;/somethin*&lt;/code&gt;" and "&lt;code&gt;/some*&lt;/code&gt;" are defined, and the current page is "User:Example/Something", then the data from "&lt;code&gt;/somethin*&lt;/code&gt;" will be used.

== "page" parameter ==

For testing and demonstration purposes this template can take a parameter named '''page'''. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /test = Test pages text
 | other = Other pages text
 | page = Template:Example/test
}}
&lt;/syntaxhighlight&gt;

No matter on what kind of page the code above is used it will return this:
: {{When pagename is
   | /test = Test pages text
   | other = Other pages text
   | page = Template:Example/test
  }}

The '''page''' parameter makes this template behave exactly as if on that page. The pagename doesn't have to be an existing page. 

If the '''page''' parameter is empty or undefined, the name of the current page determines the result.

You can make it so your template also understands the '''page''' parameter. That means you can demonstrate the different appearances of your template in the documentation for your template. Then do like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /test = Test pages text
 | other = Other pages text
 | page = {{{page|}}}
}}
&lt;/syntaxhighlight&gt;

== Namespace matching ==

This template doesn't have namespace matching. If you need that then combine this template with one of the namespace-detection templates such as {{tl|When on template page}}. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When on template page
 | {{When pagename is
    | /doc = Template doc page text.
   }}
 | &lt;!--(action if not on a Template: page)--&gt;
}}
&lt;/syntaxhighlight&gt;

If on "User:Example/doc" the code above will return nothing. But if on "Template:Example/doc" it will return this:
: {{When on template page |demospace=template
   | {{When pagename is
      | /doc = Template doc page text.
      | page = Template:Example/doc
     }}
  | &lt;!--(action if not on a Template: page)--&gt;
 }}

== Technical details ==

This template detects subpages even when used in namespaces that don't have the MediaWiki [[meta:Link#Subpage feature|subpage feature]] enabled. Thus this template works the same in all namespaces.

For more technical details, see the [[Template talk:When pagename is|talk page]].

{{Namespace and pagename-detecting templates}}

&lt;includeonly&gt;{{Sandbox other|
| &lt;!--Categories below this line, please; interwikis at Wikidata--&gt;
[[Category:If-then-else templates]]
[[Category:Pagename manipulation templates]]
[[Category:Wikipedia metatemplates]]
}}&lt;/includeonly&gt;</text>
      <sha1>i2oo1e420ujolw3hvqfewvmez26dkat</sha1>
    </revision>
    <revision>
      <id>1046880</id>
      <parentid>1046879</parentid>
      <timestamp>2025-11-08T04:10:07Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: add {{wikidata link|Q...}} or [[Category:Templates without wikidata]]</comment>
      <origin>1046880</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="7169" sha1="i94gla2i0cpz4afs4turwryv2qmusca" xml:space="preserve">{{Documentation subpage}}
&lt;!--Categories where indicated at the bottom of this page, please; interwikis at Wikidata (see [[Wikipedia:Wikidata]])--&gt;

This is the {{tl|When pagename is}} meta-template. It helps other templates detect what page they are on, using pattern matching on the pagename.

== Usage ==

This template takes one or more parameters. Most of the parameters don't have fixed names, instead they are part of the pattern matching. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
| /doc = Doc page text
| other = Other pages text
}}
&lt;/syntaxhighlight&gt;

If the template is on "Template:Example/doc" the code above will return this:
: {{When pagename is
  | /doc = Doc page text
  | other = Other pages text
  | page = Template:Example/doc
 }}

If the template is on any ''other'' page than a /doc page it will return this:

: {{When pagename is
   | /doc = Doc page text
   | other = Other pages text
   | page = Template:Example
  }}

Here is a description of the full pattern matching:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
&lt;!-- Match on full pagename --&gt;
 | User:Example/test = Text for "User:Example/test".
 | User:Example = Text for "User:Example".
 | User talk:Example = Text for "User talk:Example".

&lt;!-- Match on full basepage name, when on a basepage or its subpages --&gt;
 | User:Example/+ = Matches "User:Example" and "User:Example/test".

&lt;!-- Match on pagename, when on a basepage --&gt;
 | Example = Matches "User:Example", "User talk:Example", "Template:Example" 
             and so on, but not "User:Example/something".

&lt;!-- Match on full basepage name, when on a subpage --&gt;
 | User:Example/* = Matches "User:Example/something", but not "User:Example".
 | User talk:Example/* = Matches "User talk:Example/something".

&lt;!-- Match on basepage name, when on a subpage --&gt;
 | Example/* = Matches "User:Example/something" and "User talk:Example/something".

&lt;!-- Match on subpage name --&gt;
 | /something = Any pagename that ends in "/something".
 | /doc = Any pagename that ends in "/doc".

&lt;!-- Match on partial subpage name (case-insensitive) --&gt;
 | /some* = Any subpage name beginning with "/some" or "/Some".
 | /arch* = Matches "User talk:Example/Archive 1".

&lt;!-- Defaults --&gt;
 | basepage = Text for any basepage.
 | subpage = Text for any subpage.
 | other = Text for any page.
}}
&lt;/syntaxhighlight&gt;

The matching goes from top to bottom, and returns the first parameter that matches. "''Top to bottom''" means the order shown above, not the order you happen to feed the parameters.

There's no limit to the number of parameters that you can use, other than what the servers and MediaWiki system can handle. 

Most of the matching is case-sensitive. For instance "&lt;code&gt;/test&lt;/code&gt;" matches "User:Example/test" but not "User:Example/Test". 

Matching on partial subpage names such as "&lt;code&gt;/some*&lt;/code&gt;" has some limitations, see [[#Partial subpage names|its own section]] below.

If an empty (but defined) parameter matches, the matching stops and the template returns an empty string. That's on purpose and can be used like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /doc =
 | /sandbox = Sandbox text
 | other = Other pages text
}}
&lt;/syntaxhighlight&gt;

The code above will render nothing when on a /doc page. But when on a /sandbox page it will return this: 

: {{When pagename is
   | /doc =
   | /sandbox = Sandbox text
   | other = Other pages text
   | page = Template:Example/sandbox
  }}

And when on any other page it will return this:
: {{When pagename is
   | /doc =
   | /sandbox = Sandbox text
   | other = Other pages text
   | page = Template:Example
  }}

== Partial subpage names ==

This template can also match on partial subpage names. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /archiv* = Archive page text
 | other = Other pages text
}}
&lt;/syntaxhighlight&gt;

If on "User:Example/Archive 1" the code above will return this:
: {{When pagename is
   | /archiv* = Archive page text
   | other = Other pages text
   | page = User:Example/Archive 1
  }}

The parameter name "&lt;code&gt;/some*&lt;/code&gt;" must be lower case. But it matches subpage names in both upper and lower case such as "User:Example/SomeThing" and "User:Example/something".

The partial matching only supports matching on 4, 6 and 8 characters. Thus using "&lt;code&gt;/some*&lt;/code&gt;", "&lt;code&gt;/someth*&lt;/code&gt;" and "&lt;code&gt;/somethin*&lt;/code&gt;" works, but using "&lt;code&gt;/som*&lt;/code&gt;" or "&lt;code&gt;/somet*&lt;/code&gt;" doesn't work.

Longer patterns match first, thus if both "&lt;code&gt;/somethin*&lt;/code&gt;" and "&lt;code&gt;/some*&lt;/code&gt;" are defined, and the current page is "User:Example/Something", then the data from "&lt;code&gt;/somethin*&lt;/code&gt;" will be used.

== "page" parameter ==

For testing and demonstration purposes this template can take a parameter named '''page'''. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /test = Test pages text
 | other = Other pages text
 | page = Template:Example/test
}}
&lt;/syntaxhighlight&gt;

No matter on what kind of page the code above is used it will return this:
: {{When pagename is
   | /test = Test pages text
   | other = Other pages text
   | page = Template:Example/test
  }}

The '''page''' parameter makes this template behave exactly as if on that page. The pagename doesn't have to be an existing page. 

If the '''page''' parameter is empty or undefined, the name of the current page determines the result.

You can make it so your template also understands the '''page''' parameter. That means you can demonstrate the different appearances of your template in the documentation for your template. Then do like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /test = Test pages text
 | other = Other pages text
 | page = {{{page|}}}
}}
&lt;/syntaxhighlight&gt;

== Namespace matching ==

This template doesn't have namespace matching. If you need that then combine this template with one of the namespace-detection templates such as {{tl|When on template page}}. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When on template page
 | {{When pagename is
    | /doc = Template doc page text.
   }}
 | &lt;!--(action if not on a Template: page)--&gt;
}}
&lt;/syntaxhighlight&gt;

If on "User:Example/doc" the code above will return nothing. But if on "Template:Example/doc" it will return this:
: {{When on template page |demospace=template
   | {{When pagename is
      | /doc = Template doc page text.
      | page = Template:Example/doc
     }}
  | &lt;!--(action if not on a Template: page)--&gt;
 }}

== Technical details ==

This template detects subpages even when used in namespaces that don't have the MediaWiki [[meta:Link#Subpage feature|subpage feature]] enabled. Thus this template works the same in all namespaces.

For more technical details, see the [[Template talk:When pagename is|talk page]].

{{Namespace and pagename-detecting templates}}

&lt;includeonly&gt;{{Sandbox other|
| &lt;!--Categories below this line, please; interwikis at Wikidata--&gt;
[[Category:If-then-else templates]]
[[Category:Pagename manipulation templates]]
[[Category:Wikipedia metatemplates]]
}}&lt;/includeonly&gt;
&lt;noinclude&gt;[[Category:Templates without wikidata]]&lt;/noinclude&gt;</text>
      <sha1>i94gla2i0cpz4afs4turwryv2qmusca</sha1>
    </revision>
    <revision>
      <id>11328</id>
      <timestamp>2025-11-08T04:44:41Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was Text replacement - "↵&lt;noinclude&gt;[[Category:Templates without wikidata]]&lt;/noinclude&gt;" to ""</comment>
      <origin>11328</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="7106" sha1="i2oo1e420ujolw3hvqfewvmez26dkat" xml:space="preserve">{{Documentation subpage}}
&lt;!--Categories where indicated at the bottom of this page, please; interwikis at Wikidata (see [[Wikipedia:Wikidata]])--&gt;

This is the {{tl|When pagename is}} meta-template. It helps other templates detect what page they are on, using pattern matching on the pagename.

== Usage ==

This template takes one or more parameters. Most of the parameters don't have fixed names, instead they are part of the pattern matching. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
| /doc = Doc page text
| other = Other pages text
}}
&lt;/syntaxhighlight&gt;

If the template is on "Template:Example/doc" the code above will return this:
: {{When pagename is
  | /doc = Doc page text
  | other = Other pages text
  | page = Template:Example/doc
 }}

If the template is on any ''other'' page than a /doc page it will return this:

: {{When pagename is
   | /doc = Doc page text
   | other = Other pages text
   | page = Template:Example
  }}

Here is a description of the full pattern matching:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
&lt;!-- Match on full pagename --&gt;
 | User:Example/test = Text for "User:Example/test".
 | User:Example = Text for "User:Example".
 | User talk:Example = Text for "User talk:Example".

&lt;!-- Match on full basepage name, when on a basepage or its subpages --&gt;
 | User:Example/+ = Matches "User:Example" and "User:Example/test".

&lt;!-- Match on pagename, when on a basepage --&gt;
 | Example = Matches "User:Example", "User talk:Example", "Template:Example" 
             and so on, but not "User:Example/something".

&lt;!-- Match on full basepage name, when on a subpage --&gt;
 | User:Example/* = Matches "User:Example/something", but not "User:Example".
 | User talk:Example/* = Matches "User talk:Example/something".

&lt;!-- Match on basepage name, when on a subpage --&gt;
 | Example/* = Matches "User:Example/something" and "User talk:Example/something".

&lt;!-- Match on subpage name --&gt;
 | /something = Any pagename that ends in "/something".
 | /doc = Any pagename that ends in "/doc".

&lt;!-- Match on partial subpage name (case-insensitive) --&gt;
 | /some* = Any subpage name beginning with "/some" or "/Some".
 | /arch* = Matches "User talk:Example/Archive 1".

&lt;!-- Defaults --&gt;
 | basepage = Text for any basepage.
 | subpage = Text for any subpage.
 | other = Text for any page.
}}
&lt;/syntaxhighlight&gt;

The matching goes from top to bottom, and returns the first parameter that matches. "''Top to bottom''" means the order shown above, not the order you happen to feed the parameters.

There's no limit to the number of parameters that you can use, other than what the servers and MediaWiki system can handle. 

Most of the matching is case-sensitive. For instance "&lt;code&gt;/test&lt;/code&gt;" matches "User:Example/test" but not "User:Example/Test". 

Matching on partial subpage names such as "&lt;code&gt;/some*&lt;/code&gt;" has some limitations, see [[#Partial subpage names|its own section]] below.

If an empty (but defined) parameter matches, the matching stops and the template returns an empty string. That's on purpose and can be used like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /doc =
 | /sandbox = Sandbox text
 | other = Other pages text
}}
&lt;/syntaxhighlight&gt;

The code above will render nothing when on a /doc page. But when on a /sandbox page it will return this: 

: {{When pagename is
   | /doc =
   | /sandbox = Sandbox text
   | other = Other pages text
   | page = Template:Example/sandbox
  }}

And when on any other page it will return this:
: {{When pagename is
   | /doc =
   | /sandbox = Sandbox text
   | other = Other pages text
   | page = Template:Example
  }}

== Partial subpage names ==

This template can also match on partial subpage names. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /archiv* = Archive page text
 | other = Other pages text
}}
&lt;/syntaxhighlight&gt;

If on "User:Example/Archive 1" the code above will return this:
: {{When pagename is
   | /archiv* = Archive page text
   | other = Other pages text
   | page = User:Example/Archive 1
  }}

The parameter name "&lt;code&gt;/some*&lt;/code&gt;" must be lower case. But it matches subpage names in both upper and lower case such as "User:Example/SomeThing" and "User:Example/something".

The partial matching only supports matching on 4, 6 and 8 characters. Thus using "&lt;code&gt;/some*&lt;/code&gt;", "&lt;code&gt;/someth*&lt;/code&gt;" and "&lt;code&gt;/somethin*&lt;/code&gt;" works, but using "&lt;code&gt;/som*&lt;/code&gt;" or "&lt;code&gt;/somet*&lt;/code&gt;" doesn't work.

Longer patterns match first, thus if both "&lt;code&gt;/somethin*&lt;/code&gt;" and "&lt;code&gt;/some*&lt;/code&gt;" are defined, and the current page is "User:Example/Something", then the data from "&lt;code&gt;/somethin*&lt;/code&gt;" will be used.

== "page" parameter ==

For testing and demonstration purposes this template can take a parameter named '''page'''. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /test = Test pages text
 | other = Other pages text
 | page = Template:Example/test
}}
&lt;/syntaxhighlight&gt;

No matter on what kind of page the code above is used it will return this:
: {{When pagename is
   | /test = Test pages text
   | other = Other pages text
   | page = Template:Example/test
  }}

The '''page''' parameter makes this template behave exactly as if on that page. The pagename doesn't have to be an existing page. 

If the '''page''' parameter is empty or undefined, the name of the current page determines the result.

You can make it so your template also understands the '''page''' parameter. That means you can demonstrate the different appearances of your template in the documentation for your template. Then do like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /test = Test pages text
 | other = Other pages text
 | page = {{{page|}}}
}}
&lt;/syntaxhighlight&gt;

== Namespace matching ==

This template doesn't have namespace matching. If you need that then combine this template with one of the namespace-detection templates such as {{tl|When on template page}}. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When on template page
 | {{When pagename is
    | /doc = Template doc page text.
   }}
 | &lt;!--(action if not on a Template: page)--&gt;
}}
&lt;/syntaxhighlight&gt;

If on "User:Example/doc" the code above will return nothing. But if on "Template:Example/doc" it will return this:
: {{When on template page |demospace=template
   | {{When pagename is
      | /doc = Template doc page text.
      | page = Template:Example/doc
     }}
  | &lt;!--(action if not on a Template: page)--&gt;
 }}

== Technical details ==

This template detects subpages even when used in namespaces that don't have the MediaWiki [[meta:Link#Subpage feature|subpage feature]] enabled. Thus this template works the same in all namespaces.

For more technical details, see the [[Template talk:When pagename is|talk page]].

{{Namespace and pagename-detecting templates}}

&lt;includeonly&gt;{{Sandbox other|
| &lt;!--Categories below this line, please; interwikis at Wikidata--&gt;
[[Category:If-then-else templates]]
[[Category:Pagename manipulation templates]]
[[Category:Wikipedia metatemplates]]
}}&lt;/includeonly&gt;</text>
      <sha1>i2oo1e420ujolw3hvqfewvmez26dkat</sha1>
    </revision>
    <revision>
      <id>176105</id>
      <parentid>11328</parentid>
      <timestamp>2026-02-06T00:51:01Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: Adding dummy category to make categories work after wiki restoration</comment>
      <origin>176105</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="7129" sha1="8z5nb6dicbdjkfevca0ffi0d6ngywsi" xml:space="preserve">{{Documentation subpage}}
&lt;!--Categories where indicated at the bottom of this page, please; interwikis at Wikidata (see [[Wikipedia:Wikidata]])--&gt;

This is the {{tl|When pagename is}} meta-template. It helps other templates detect what page they are on, using pattern matching on the pagename.

== Usage ==

This template takes one or more parameters. Most of the parameters don't have fixed names, instead they are part of the pattern matching. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
| /doc = Doc page text
| other = Other pages text
}}
&lt;/syntaxhighlight&gt;

If the template is on "Template:Example/doc" the code above will return this:
: {{When pagename is
  | /doc = Doc page text
  | other = Other pages text
  | page = Template:Example/doc
 }}

If the template is on any ''other'' page than a /doc page it will return this:

: {{When pagename is
   | /doc = Doc page text
   | other = Other pages text
   | page = Template:Example
  }}

Here is a description of the full pattern matching:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
&lt;!-- Match on full pagename --&gt;
 | User:Example/test = Text for "User:Example/test".
 | User:Example = Text for "User:Example".
 | User talk:Example = Text for "User talk:Example".

&lt;!-- Match on full basepage name, when on a basepage or its subpages --&gt;
 | User:Example/+ = Matches "User:Example" and "User:Example/test".

&lt;!-- Match on pagename, when on a basepage --&gt;
 | Example = Matches "User:Example", "User talk:Example", "Template:Example" 
             and so on, but not "User:Example/something".

&lt;!-- Match on full basepage name, when on a subpage --&gt;
 | User:Example/* = Matches "User:Example/something", but not "User:Example".
 | User talk:Example/* = Matches "User talk:Example/something".

&lt;!-- Match on basepage name, when on a subpage --&gt;
 | Example/* = Matches "User:Example/something" and "User talk:Example/something".

&lt;!-- Match on subpage name --&gt;
 | /something = Any pagename that ends in "/something".
 | /doc = Any pagename that ends in "/doc".

&lt;!-- Match on partial subpage name (case-insensitive) --&gt;
 | /some* = Any subpage name beginning with "/some" or "/Some".
 | /arch* = Matches "User talk:Example/Archive 1".

&lt;!-- Defaults --&gt;
 | basepage = Text for any basepage.
 | subpage = Text for any subpage.
 | other = Text for any page.
}}
&lt;/syntaxhighlight&gt;

The matching goes from top to bottom, and returns the first parameter that matches. "''Top to bottom''" means the order shown above, not the order you happen to feed the parameters.

There's no limit to the number of parameters that you can use, other than what the servers and MediaWiki system can handle. 

Most of the matching is case-sensitive. For instance "&lt;code&gt;/test&lt;/code&gt;" matches "User:Example/test" but not "User:Example/Test". 

Matching on partial subpage names such as "&lt;code&gt;/some*&lt;/code&gt;" has some limitations, see [[#Partial subpage names|its own section]] below.

If an empty (but defined) parameter matches, the matching stops and the template returns an empty string. That's on purpose and can be used like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /doc =
 | /sandbox = Sandbox text
 | other = Other pages text
}}
&lt;/syntaxhighlight&gt;

The code above will render nothing when on a /doc page. But when on a /sandbox page it will return this: 

: {{When pagename is
   | /doc =
   | /sandbox = Sandbox text
   | other = Other pages text
   | page = Template:Example/sandbox
  }}

And when on any other page it will return this:
: {{When pagename is
   | /doc =
   | /sandbox = Sandbox text
   | other = Other pages text
   | page = Template:Example
  }}

== Partial subpage names ==

This template can also match on partial subpage names. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /archiv* = Archive page text
 | other = Other pages text
}}
&lt;/syntaxhighlight&gt;

If on "User:Example/Archive 1" the code above will return this:
: {{When pagename is
   | /archiv* = Archive page text
   | other = Other pages text
   | page = User:Example/Archive 1
  }}

The parameter name "&lt;code&gt;/some*&lt;/code&gt;" must be lower case. But it matches subpage names in both upper and lower case such as "User:Example/SomeThing" and "User:Example/something".

The partial matching only supports matching on 4, 6 and 8 characters. Thus using "&lt;code&gt;/some*&lt;/code&gt;", "&lt;code&gt;/someth*&lt;/code&gt;" and "&lt;code&gt;/somethin*&lt;/code&gt;" works, but using "&lt;code&gt;/som*&lt;/code&gt;" or "&lt;code&gt;/somet*&lt;/code&gt;" doesn't work.

Longer patterns match first, thus if both "&lt;code&gt;/somethin*&lt;/code&gt;" and "&lt;code&gt;/some*&lt;/code&gt;" are defined, and the current page is "User:Example/Something", then the data from "&lt;code&gt;/somethin*&lt;/code&gt;" will be used.

== "page" parameter ==

For testing and demonstration purposes this template can take a parameter named '''page'''. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /test = Test pages text
 | other = Other pages text
 | page = Template:Example/test
}}
&lt;/syntaxhighlight&gt;

No matter on what kind of page the code above is used it will return this:
: {{When pagename is
   | /test = Test pages text
   | other = Other pages text
   | page = Template:Example/test
  }}

The '''page''' parameter makes this template behave exactly as if on that page. The pagename doesn't have to be an existing page. 

If the '''page''' parameter is empty or undefined, the name of the current page determines the result.

You can make it so your template also understands the '''page''' parameter. That means you can demonstrate the different appearances of your template in the documentation for your template. Then do like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /test = Test pages text
 | other = Other pages text
 | page = {{{page|}}}
}}
&lt;/syntaxhighlight&gt;

== Namespace matching ==

This template doesn't have namespace matching. If you need that then combine this template with one of the namespace-detection templates such as {{tl|When on template page}}. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When on template page
 | {{When pagename is
    | /doc = Template doc page text.
   }}
 | &lt;!--(action if not on a Template: page)--&gt;
}}
&lt;/syntaxhighlight&gt;

If on "User:Example/doc" the code above will return nothing. But if on "Template:Example/doc" it will return this:
: {{When on template page |demospace=template
   | {{When pagename is
      | /doc = Template doc page text.
      | page = Template:Example/doc
     }}
  | &lt;!--(action if not on a Template: page)--&gt;
 }}

== Technical details ==

This template detects subpages even when used in namespaces that don't have the MediaWiki [[meta:Link#Subpage feature|subpage feature]] enabled. Thus this template works the same in all namespaces.

For more technical details, see the [[Template talk:When pagename is|talk page]].

{{Namespace and pagename-detecting templates}}

&lt;includeonly&gt;{{Sandbox other|
| &lt;!--Categories below this line, please; interwikis at Wikidata--&gt;
[[Category:If-then-else templates]]
[[Category:Pagename manipulation templates]]
[[Category:Wikipedia metatemplates]]
}}&lt;/includeonly&gt;
[[Category:Templates]]</text>
      <sha1>8z5nb6dicbdjkfevca0ffi0d6ngywsi</sha1>
    </revision>
    <revision>
      <id>3575988</id>
      <parentid>176105</parentid>
      <timestamp>2026-03-28T21:20:07Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <comment>Bot: wrap 4 stray tag(s) in noinclude [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23693191906|commit triggered pipeline]]</comment>
      <origin>3575988</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="7198" sha1="o8wqadpqdq5u72bbyafwckvnkqgws5h" xml:space="preserve">{{Documentation subpage}}
&lt;!--Categories where indicated at the bottom of this page, please; interwikis at Wikidata (see [[Wikipedia:Wikidata]])--&gt;

This is the {{tl|When pagename is}} meta-template. It helps other templates detect what page they are on, using pattern matching on the pagename.

== Usage ==

This template takes one or more parameters. Most of the parameters don't have fixed names, instead they are part of the pattern matching. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
| /doc = Doc page text
| other = Other pages text
}}
&lt;/syntaxhighlight&gt;

If the template is on "Template:Example/doc" the code above will return this:
: {{When pagename is
  | /doc = Doc page text
  | other = Other pages text
  | page = Template:Example/doc
 }}

If the template is on any ''other'' page than a /doc page it will return this:

: {{When pagename is
   | /doc = Doc page text
   | other = Other pages text
   | page = Template:Example
  }}

Here is a description of the full pattern matching:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
&lt;!-- Match on full pagename --&gt;
 | User:Example/test = Text for "User:Example/test".
 | User:Example = Text for "User:Example".
 | User talk:Example = Text for "User talk:Example".

&lt;!-- Match on full basepage name, when on a basepage or its subpages --&gt;
 | User:Example/+ = Matches "User:Example" and "User:Example/test".

&lt;!-- Match on pagename, when on a basepage --&gt;
 | Example = Matches "User:Example", "User talk:Example", "Template:Example" 
             and so on, but not "User:Example/something".

&lt;!-- Match on full basepage name, when on a subpage --&gt;
 | User:Example/* = Matches "User:Example/something", but not "User:Example".
 | User talk:Example/* = Matches "User talk:Example/something".

&lt;!-- Match on basepage name, when on a subpage --&gt;
 | Example/* = Matches "User:Example/something" and "User talk:Example/something".

&lt;!-- Match on subpage name --&gt;
 | /something = Any pagename that ends in "/something".
 | /doc = Any pagename that ends in "/doc".

&lt;!-- Match on partial subpage name (case-insensitive) --&gt;
 | /some* = Any subpage name beginning with "/some" or "/Some".
 | /arch* = Matches "User talk:Example/Archive 1".

&lt;!-- Defaults --&gt;
 | basepage = Text for any basepage.
 | subpage = Text for any subpage.
 | other = Text for any page.
}}
&lt;/syntaxhighlight&gt;

The matching goes from top to bottom, and returns the first parameter that matches. "''Top to bottom''" means the order shown above, not the order you happen to feed the parameters.

There's no limit to the number of parameters that you can use, other than what the servers and MediaWiki system can handle. 

Most of the matching is case-sensitive. For instance "&lt;code&gt;/test&lt;/code&gt;" matches "User:Example/test" but not "User:Example/Test". 

Matching on partial subpage names such as "&lt;code&gt;/some*&lt;/code&gt;" has some limitations, see [[#Partial subpage names|its own section]] below.

If an empty (but defined) parameter matches, the matching stops and the template returns an empty string. That's on purpose and can be used like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /doc =
 | /sandbox = Sandbox text
 | other = Other pages text
}}
&lt;/syntaxhighlight&gt;

The code above will render nothing when on a /doc page. But when on a /sandbox page it will return this: 

: {{When pagename is
   | /doc =
   | /sandbox = Sandbox text
   | other = Other pages text
   | page = Template:Example/sandbox
  }}

And when on any other page it will return this:
: {{When pagename is
   | /doc =
   | /sandbox = Sandbox text
   | other = Other pages text
   | page = Template:Example
  }}

== Partial subpage names ==

This template can also match on partial subpage names. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /archiv* = Archive page text
 | other = Other pages text
}}
&lt;/syntaxhighlight&gt;

If on "User:Example/Archive 1" the code above will return this:
: {{When pagename is
   | /archiv* = Archive page text
   | other = Other pages text
   | page = User:Example/Archive 1
  }}

The parameter name "&lt;code&gt;/some*&lt;/code&gt;" must be lower case. But it matches subpage names in both upper and lower case such as "User:Example/SomeThing" and "User:Example/something".

The partial matching only supports matching on 4, 6 and 8 characters. Thus using "&lt;code&gt;/some*&lt;/code&gt;", "&lt;code&gt;/someth*&lt;/code&gt;" and "&lt;code&gt;/somethin*&lt;/code&gt;" works, but using "&lt;code&gt;/som*&lt;/code&gt;" or "&lt;code&gt;/somet*&lt;/code&gt;" doesn't work.

Longer patterns match first, thus if both "&lt;code&gt;/somethin*&lt;/code&gt;" and "&lt;code&gt;/some*&lt;/code&gt;" are defined, and the current page is "User:Example/Something", then the data from "&lt;code&gt;/somethin*&lt;/code&gt;" will be used.

== "page" parameter ==

For testing and demonstration purposes this template can take a parameter named '''page'''. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /test = Test pages text
 | other = Other pages text
 | page = Template:Example/test
}}
&lt;/syntaxhighlight&gt;

No matter on what kind of page the code above is used it will return this:
: {{When pagename is
   | /test = Test pages text
   | other = Other pages text
   | page = Template:Example/test
  }}

The '''page''' parameter makes this template behave exactly as if on that page. The pagename doesn't have to be an existing page. 

If the '''page''' parameter is empty or undefined, the name of the current page determines the result.

You can make it so your template also understands the '''page''' parameter. That means you can demonstrate the different appearances of your template in the documentation for your template. Then do like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /test = Test pages text
 | other = Other pages text
 | page = {{{page|}}}
}}
&lt;/syntaxhighlight&gt;

== Namespace matching ==

This template doesn't have namespace matching. If you need that then combine this template with one of the namespace-detection templates such as {{tl|When on template page}}. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When on template page
 | {{When pagename is
    | /doc = Template doc page text.
   }}
 | &lt;!--(action if not on a Template: page)--&gt;
}}
&lt;/syntaxhighlight&gt;

If on "User:Example/doc" the code above will return nothing. But if on "Template:Example/doc" it will return this:
: {{When on template page |demospace=template
   | {{When pagename is
      | /doc = Template doc page text.
      | page = Template:Example/doc
     }}
  | &lt;!--(action if not on a Template: page)--&gt;
 }}

== Technical details ==

This template detects subpages even when used in namespaces that don't have the MediaWiki [[meta:Link#Subpage feature|subpage feature]] enabled. Thus this template works the same in all namespaces.

For more technical details, see the [[Template talk:When pagename is|talk page]].

{{Namespace and pagename-detecting templates}}

&lt;includeonly&gt;{{Sandbox other|
| &lt;!--Categories below this line, please; interwikis at Wikidata--&gt;
}}&lt;/includeonly&gt;
&lt;noinclude&gt;
[[Category:If-then-else templates]]
[[Category:Pagename manipulation templates]]
[[Category:Wikipedia metatemplates]]
[[Category:Templates]]
[[Category:Templates fixed with noinclude]]
&lt;/noinclude&gt;</text>
      <sha1>o8wqadpqdq5u72bbyafwckvnkqgws5h</sha1>
    </revision>
    <revision>
      <id>3675665</id>
      <parentid>3575988</parentid>
      <timestamp>2026-04-27T21:12:40Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <comment>Bot: strip HTML comments [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/24999656626|time triggered pipeline]]</comment>
      <origin>3675665</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="6562" sha1="o1f76mqhkl3zlc9ggz4jjmosgxipagz" xml:space="preserve">{{Documentation subpage}}

This is the {{tl|When pagename is}} meta-template. It helps other templates detect what page they are on, using pattern matching on the pagename.

== Usage ==

This template takes one or more parameters. Most of the parameters don't have fixed names, instead they are part of the pattern matching. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
| /doc = Doc page text
| other = Other pages text
}}
&lt;/syntaxhighlight&gt;

If the template is on "Template:Example/doc" the code above will return this:
: {{When pagename is
  | /doc = Doc page text
  | other = Other pages text
  | page = Template:Example/doc
 }}

If the template is on any ''other'' page than a /doc page it will return this:

: {{When pagename is
   | /doc = Doc page text
   | other = Other pages text
   | page = Template:Example
  }}

Here is a description of the full pattern matching:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is

 | User:Example/test = Text for "User:Example/test".
 | User:Example = Text for "User:Example".
 | User talk:Example = Text for "User talk:Example".

 | User:Example/+ = Matches "User:Example" and "User:Example/test".

 | Example = Matches "User:Example", "User talk:Example", "Template:Example" 
             and so on, but not "User:Example/something".

 | User:Example/* = Matches "User:Example/something", but not "User:Example".
 | User talk:Example/* = Matches "User talk:Example/something".

 | Example/* = Matches "User:Example/something" and "User talk:Example/something".

 | /something = Any pagename that ends in "/something".
 | /doc = Any pagename that ends in "/doc".

 | /some* = Any subpage name beginning with "/some" or "/Some".
 | /arch* = Matches "User talk:Example/Archive 1".

 | basepage = Text for any basepage.
 | subpage = Text for any subpage.
 | other = Text for any page.
}}
&lt;/syntaxhighlight&gt;

The matching goes from top to bottom, and returns the first parameter that matches. "''Top to bottom''" means the order shown above, not the order you happen to feed the parameters.

There's no limit to the number of parameters that you can use, other than what the servers and MediaWiki system can handle. 

Most of the matching is case-sensitive. For instance "&lt;code&gt;/test&lt;/code&gt;" matches "User:Example/test" but not "User:Example/Test". 

Matching on partial subpage names such as "&lt;code&gt;/some*&lt;/code&gt;" has some limitations, see [[#Partial subpage names|its own section]] below.

If an empty (but defined) parameter matches, the matching stops and the template returns an empty string. That's on purpose and can be used like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /doc =
 | /sandbox = Sandbox text
 | other = Other pages text
}}
&lt;/syntaxhighlight&gt;

The code above will render nothing when on a /doc page. But when on a /sandbox page it will return this: 

: {{When pagename is
   | /doc =
   | /sandbox = Sandbox text
   | other = Other pages text
   | page = Template:Example/sandbox
  }}

And when on any other page it will return this:
: {{When pagename is
   | /doc =
   | /sandbox = Sandbox text
   | other = Other pages text
   | page = Template:Example
  }}

== Partial subpage names ==

This template can also match on partial subpage names. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /archiv* = Archive page text
 | other = Other pages text
}}
&lt;/syntaxhighlight&gt;

If on "User:Example/Archive 1" the code above will return this:
: {{When pagename is
   | /archiv* = Archive page text
   | other = Other pages text
   | page = User:Example/Archive 1
  }}

The parameter name "&lt;code&gt;/some*&lt;/code&gt;" must be lower case. But it matches subpage names in both upper and lower case such as "User:Example/SomeThing" and "User:Example/something".

The partial matching only supports matching on 4, 6 and 8 characters. Thus using "&lt;code&gt;/some*&lt;/code&gt;", "&lt;code&gt;/someth*&lt;/code&gt;" and "&lt;code&gt;/somethin*&lt;/code&gt;" works, but using "&lt;code&gt;/som*&lt;/code&gt;" or "&lt;code&gt;/somet*&lt;/code&gt;" doesn't work.

Longer patterns match first, thus if both "&lt;code&gt;/somethin*&lt;/code&gt;" and "&lt;code&gt;/some*&lt;/code&gt;" are defined, and the current page is "User:Example/Something", then the data from "&lt;code&gt;/somethin*&lt;/code&gt;" will be used.

== "page" parameter ==

For testing and demonstration purposes this template can take a parameter named '''page'''. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /test = Test pages text
 | other = Other pages text
 | page = Template:Example/test
}}
&lt;/syntaxhighlight&gt;

No matter on what kind of page the code above is used it will return this:
: {{When pagename is
   | /test = Test pages text
   | other = Other pages text
   | page = Template:Example/test
  }}

The '''page''' parameter makes this template behave exactly as if on that page. The pagename doesn't have to be an existing page. 

If the '''page''' parameter is empty or undefined, the name of the current page determines the result.

You can make it so your template also understands the '''page''' parameter. That means you can demonstrate the different appearances of your template in the documentation for your template. Then do like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When pagename is
 | /test = Test pages text
 | other = Other pages text
 | page = {{{page|}}}
}}
&lt;/syntaxhighlight&gt;

== Namespace matching ==

This template doesn't have namespace matching. If you need that then combine this template with one of the namespace-detection templates such as {{tl|When on template page}}. Like this:
&lt;syntaxhighlight lang="wikitext"&gt;
{{When on template page
 | {{When pagename is
    | /doc = Template doc page text.
   }}
 | 
}}
&lt;/syntaxhighlight&gt;

If on "User:Example/doc" the code above will return nothing. But if on "Template:Example/doc" it will return this:
: {{When on template page |demospace=template
   | {{When pagename is
      | /doc = Template doc page text.
      | page = Template:Example/doc
     }}
  | 
 }}

== Technical details ==

This template detects subpages even when used in namespaces that don't have the MediaWiki [[meta:Link#Subpage feature|subpage feature]] enabled. Thus this template works the same in all namespaces.

For more technical details, see the [[Template talk:When pagename is|talk page]].

{{Namespace and pagename-detecting templates}}

&lt;includeonly&gt;{{Sandbox other|
| 
}}&lt;/includeonly&gt;
&lt;noinclude&gt;
[[Category:If-then-else templates]]
[[Category:Pagename manipulation templates]]
[[Category:Wikipedia metatemplates]]
[[Category:Templates]]
[[Category:Templates fixed with noinclude]]
&lt;/noinclude&gt;</text>
      <sha1>o1f76mqhkl3zlc9ggz4jjmosgxipagz</sha1>
    </revision>
  </page>
</mediawiki>
