<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Template:User other/doc</title>
    <ns>10</ns>
    <id>10298</id>
    <revision>
      <id>1009240</id>
      <timestamp>2023-05-21T15:55:55Z</timestamp>
      <contributor>
        <username>en&gt;WOSlinker</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>syntaxhighlight lang="wikitext"</comment>
      <origin>1009240</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="2649" sha1="hs9nh76mwcqo8y4qty7rea7ed5sn6g7" xml:space="preserve">{{Documentation subpage}}
{{Template shortcut|ns2}}
{{High-use|150,000+}}
This is the {{tl|user other}} meta-template.

This template helps other templates detect whether they are on a "User:" page or some "other" type of page.

===Usage===
This template usually takes two parameters, like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|User-page text|Other-pages text}}
&lt;/syntaxhighlight&gt;

If the template is on a "User:" page, it will return this:

:{{user other|demospace=user|User-page text|Other-pages text}}

If the template is on any other type of page, it will return this:

:{{user other|User-page text|Other-pages text}}

Using this meta-template can make a template that adds a category when it is invoked on User pages, but not when it is invoked on non-User pages. Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|[[Category:Some user category]]}}
&lt;/syntaxhighlight&gt;

This meta-template can warn that a template should not be used on non-User pages:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other||This template should only be used on user pages.}}
&lt;/syntaxhighlight&gt;

In the first case above, the "other" parameter was not used. In the second example, the "user" parameter was left empty.

===Demospace===

For testing and demonstration purposes, this template can take a named parameter, '''demospace'''.

* If demospace has the value '''user''', it returns the user-page text.
* It demospace has the value '''other''', or any other value such as the name of some other namespace it returns the other-pages text.
* If demospace is empty or undefined, the actual page type determines the result.

Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace=user
}}
&lt;/syntaxhighlight&gt;

No matter on what type of page the code above is used, it will return this:

:{{user other
| User-page text
| Other-pages text
| demospace=user
}}

You can make it so your template also understands the '''demospace''' parameter. That means you can demonstrate the different appearances of your template in the documentation for your template. Then do like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace={{{demospace|}}}
}}
&lt;/syntaxhighlight&gt;

===Technical details===
This template detects "User talk:" pages as type '''other'''.

This template works like {{tl|main talk other}}. For more examples and technical details, see full documentation there.

{{Namespace and pagename-detecting templates}}

&lt;includeonly&gt;{{Sandbox other||
[[Category:User namespace templates]]
[[Category:Namespace manipulation templates]]
}}&lt;/includeonly&gt;</text>
      <sha1>hs9nh76mwcqo8y4qty7rea7ed5sn6g7</sha1>
    </revision>
    <revision>
      <id>1009241</id>
      <parentid>1009240</parentid>
      <timestamp>2025-01-28T11:08:51Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: importing templates</comment>
      <origin>1009240</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="2649" sha1="hs9nh76mwcqo8y4qty7rea7ed5sn6g7" xml:space="preserve">{{Documentation subpage}}
{{Template shortcut|ns2}}
{{High-use|150,000+}}
This is the {{tl|user other}} meta-template.

This template helps other templates detect whether they are on a "User:" page or some "other" type of page.

===Usage===
This template usually takes two parameters, like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|User-page text|Other-pages text}}
&lt;/syntaxhighlight&gt;

If the template is on a "User:" page, it will return this:

:{{user other|demospace=user|User-page text|Other-pages text}}

If the template is on any other type of page, it will return this:

:{{user other|User-page text|Other-pages text}}

Using this meta-template can make a template that adds a category when it is invoked on User pages, but not when it is invoked on non-User pages. Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|[[Category:Some user category]]}}
&lt;/syntaxhighlight&gt;

This meta-template can warn that a template should not be used on non-User pages:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other||This template should only be used on user pages.}}
&lt;/syntaxhighlight&gt;

In the first case above, the "other" parameter was not used. In the second example, the "user" parameter was left empty.

===Demospace===

For testing and demonstration purposes, this template can take a named parameter, '''demospace'''.

* If demospace has the value '''user''', it returns the user-page text.
* It demospace has the value '''other''', or any other value such as the name of some other namespace it returns the other-pages text.
* If demospace is empty or undefined, the actual page type determines the result.

Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace=user
}}
&lt;/syntaxhighlight&gt;

No matter on what type of page the code above is used, it will return this:

:{{user other
| User-page text
| Other-pages text
| demospace=user
}}

You can make it so your template also understands the '''demospace''' parameter. That means you can demonstrate the different appearances of your template in the documentation for your template. Then do like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace={{{demospace|}}}
}}
&lt;/syntaxhighlight&gt;

===Technical details===
This template detects "User talk:" pages as type '''other'''.

This template works like {{tl|main talk other}}. For more examples and technical details, see full documentation there.

{{Namespace and pagename-detecting templates}}

&lt;includeonly&gt;{{Sandbox other||
[[Category:User namespace templates]]
[[Category:Namespace manipulation templates]]
}}&lt;/includeonly&gt;</text>
      <sha1>hs9nh76mwcqo8y4qty7rea7ed5sn6g7</sha1>
    </revision>
    <revision>
      <id>1009242</id>
      <parentid>1009241</parentid>
      <timestamp>2025-01-28T11:22:35Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Adding [[Category:refreshing imports]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>1009242</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="2681" sha1="f2nsx1ls6jbtjcyanb2x60yrn61o4lo" xml:space="preserve">{{Documentation subpage}}
{{Template shortcut|ns2}}
{{High-use|150,000+}}
This is the {{tl|user other}} meta-template.

This template helps other templates detect whether they are on a "User:" page or some "other" type of page.

===Usage===
This template usually takes two parameters, like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|User-page text|Other-pages text}}
&lt;/syntaxhighlight&gt;

If the template is on a "User:" page, it will return this:

:{{user other|demospace=user|User-page text|Other-pages text}}

If the template is on any other type of page, it will return this:

:{{user other|User-page text|Other-pages text}}

Using this meta-template can make a template that adds a category when it is invoked on User pages, but not when it is invoked on non-User pages. Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|[[Category:Some user category]]}}
&lt;/syntaxhighlight&gt;

This meta-template can warn that a template should not be used on non-User pages:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other||This template should only be used on user pages.}}
&lt;/syntaxhighlight&gt;

In the first case above, the "other" parameter was not used. In the second example, the "user" parameter was left empty.

===Demospace===

For testing and demonstration purposes, this template can take a named parameter, '''demospace'''.

* If demospace has the value '''user''', it returns the user-page text.
* It demospace has the value '''other''', or any other value such as the name of some other namespace it returns the other-pages text.
* If demospace is empty or undefined, the actual page type determines the result.

Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace=user
}}
&lt;/syntaxhighlight&gt;

No matter on what type of page the code above is used, it will return this:

:{{user other
| User-page text
| Other-pages text
| demospace=user
}}

You can make it so your template also understands the '''demospace''' parameter. That means you can demonstrate the different appearances of your template in the documentation for your template. Then do like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace={{{demospace|}}}
}}
&lt;/syntaxhighlight&gt;

===Technical details===
This template detects "User talk:" pages as type '''other'''.

This template works like {{tl|main talk other}}. For more examples and technical details, see full documentation there.

{{Namespace and pagename-detecting templates}}

&lt;includeonly&gt;{{Sandbox other||
[[Category:User namespace templates]]
[[Category:Namespace manipulation templates]]
[[Category:refreshing imports]]
}}&lt;/includeonly&gt;</text>
      <sha1>f2nsx1ls6jbtjcyanb2x60yrn61o4lo</sha1>
    </revision>
    <revision>
      <id>1009243</id>
      <parentid>1009242</parentid>
      <timestamp>2025-04-24T07:06:59Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "refreshing imports" to "qq"</comment>
      <origin>1009243</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="2665" sha1="fnb136w32n8k0pp79mxj0upncetm6ic" xml:space="preserve">{{Documentation subpage}}
{{Template shortcut|ns2}}
{{High-use|150,000+}}
This is the {{tl|user other}} meta-template.

This template helps other templates detect whether they are on a "User:" page or some "other" type of page.

===Usage===
This template usually takes two parameters, like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|User-page text|Other-pages text}}
&lt;/syntaxhighlight&gt;

If the template is on a "User:" page, it will return this:

:{{user other|demospace=user|User-page text|Other-pages text}}

If the template is on any other type of page, it will return this:

:{{user other|User-page text|Other-pages text}}

Using this meta-template can make a template that adds a category when it is invoked on User pages, but not when it is invoked on non-User pages. Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|[[Category:Some user category]]}}
&lt;/syntaxhighlight&gt;

This meta-template can warn that a template should not be used on non-User pages:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other||This template should only be used on user pages.}}
&lt;/syntaxhighlight&gt;

In the first case above, the "other" parameter was not used. In the second example, the "user" parameter was left empty.

===Demospace===

For testing and demonstration purposes, this template can take a named parameter, '''demospace'''.

* If demospace has the value '''user''', it returns the user-page text.
* It demospace has the value '''other''', or any other value such as the name of some other namespace it returns the other-pages text.
* If demospace is empty or undefined, the actual page type determines the result.

Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace=user
}}
&lt;/syntaxhighlight&gt;

No matter on what type of page the code above is used, it will return this:

:{{user other
| User-page text
| Other-pages text
| demospace=user
}}

You can make it so your template also understands the '''demospace''' parameter. That means you can demonstrate the different appearances of your template in the documentation for your template. Then do like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace={{{demospace|}}}
}}
&lt;/syntaxhighlight&gt;

===Technical details===
This template detects "User talk:" pages as type '''other'''.

This template works like {{tl|main talk other}}. For more examples and technical details, see full documentation there.

{{Namespace and pagename-detecting templates}}

&lt;includeonly&gt;{{Sandbox other||
[[Category:User namespace templates]]
[[Category:Namespace manipulation templates]]
[[Category:qq]]
}}&lt;/includeonly&gt;</text>
      <sha1>fnb136w32n8k0pp79mxj0upncetm6ic</sha1>
    </revision>
    <revision>
      <id>1009244</id>
      <parentid>1009243</parentid>
      <timestamp>2025-04-29T10:47:09Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "[[Category:qq]]" to ""</comment>
      <origin>1009244</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="2650" sha1="7w52fty3rlvu0n9wxrtphwqa74x3daa" xml:space="preserve">{{Documentation subpage}}
{{Template shortcut|ns2}}
{{High-use|150,000+}}
This is the {{tl|user other}} meta-template.

This template helps other templates detect whether they are on a "User:" page or some "other" type of page.

===Usage===
This template usually takes two parameters, like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|User-page text|Other-pages text}}
&lt;/syntaxhighlight&gt;

If the template is on a "User:" page, it will return this:

:{{user other|demospace=user|User-page text|Other-pages text}}

If the template is on any other type of page, it will return this:

:{{user other|User-page text|Other-pages text}}

Using this meta-template can make a template that adds a category when it is invoked on User pages, but not when it is invoked on non-User pages. Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|[[Category:Some user category]]}}
&lt;/syntaxhighlight&gt;

This meta-template can warn that a template should not be used on non-User pages:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other||This template should only be used on user pages.}}
&lt;/syntaxhighlight&gt;

In the first case above, the "other" parameter was not used. In the second example, the "user" parameter was left empty.

===Demospace===

For testing and demonstration purposes, this template can take a named parameter, '''demospace'''.

* If demospace has the value '''user''', it returns the user-page text.
* It demospace has the value '''other''', or any other value such as the name of some other namespace it returns the other-pages text.
* If demospace is empty or undefined, the actual page type determines the result.

Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace=user
}}
&lt;/syntaxhighlight&gt;

No matter on what type of page the code above is used, it will return this:

:{{user other
| User-page text
| Other-pages text
| demospace=user
}}

You can make it so your template also understands the '''demospace''' parameter. That means you can demonstrate the different appearances of your template in the documentation for your template. Then do like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace={{{demospace|}}}
}}
&lt;/syntaxhighlight&gt;

===Technical details===
This template detects "User talk:" pages as type '''other'''.

This template works like {{tl|main talk other}}. For more examples and technical details, see full documentation there.

{{Namespace and pagename-detecting templates}}

&lt;includeonly&gt;{{Sandbox other||
[[Category:User namespace templates]]
[[Category:Namespace manipulation templates]]

}}&lt;/includeonly&gt;</text>
      <sha1>7w52fty3rlvu0n9wxrtphwqa74x3daa</sha1>
    </revision>
    <revision>
      <id>1009245</id>
      <parentid>1009244</parentid>
      <timestamp>2025-11-08T04:04:25Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: add {{wikidata link|Q...}} or [[Category:Templates without wikidata]]</comment>
      <origin>1009245</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="2713" sha1="7tc1i2aiuxom01l57c9e1l5vw3fpyxy" xml:space="preserve">{{Documentation subpage}}
{{Template shortcut|ns2}}
{{High-use|150,000+}}
This is the {{tl|user other}} meta-template.

This template helps other templates detect whether they are on a "User:" page or some "other" type of page.

===Usage===
This template usually takes two parameters, like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|User-page text|Other-pages text}}
&lt;/syntaxhighlight&gt;

If the template is on a "User:" page, it will return this:

:{{user other|demospace=user|User-page text|Other-pages text}}

If the template is on any other type of page, it will return this:

:{{user other|User-page text|Other-pages text}}

Using this meta-template can make a template that adds a category when it is invoked on User pages, but not when it is invoked on non-User pages. Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|[[Category:Some user category]]}}
&lt;/syntaxhighlight&gt;

This meta-template can warn that a template should not be used on non-User pages:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other||This template should only be used on user pages.}}
&lt;/syntaxhighlight&gt;

In the first case above, the "other" parameter was not used. In the second example, the "user" parameter was left empty.

===Demospace===

For testing and demonstration purposes, this template can take a named parameter, '''demospace'''.

* If demospace has the value '''user''', it returns the user-page text.
* It demospace has the value '''other''', or any other value such as the name of some other namespace it returns the other-pages text.
* If demospace is empty or undefined, the actual page type determines the result.

Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace=user
}}
&lt;/syntaxhighlight&gt;

No matter on what type of page the code above is used, it will return this:

:{{user other
| User-page text
| Other-pages text
| demospace=user
}}

You can make it so your template also understands the '''demospace''' parameter. That means you can demonstrate the different appearances of your template in the documentation for your template. Then do like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace={{{demospace|}}}
}}
&lt;/syntaxhighlight&gt;

===Technical details===
This template detects "User talk:" pages as type '''other'''.

This template works like {{tl|main talk other}}. For more examples and technical details, see full documentation there.

{{Namespace and pagename-detecting templates}}

&lt;includeonly&gt;{{Sandbox other||
[[Category:User namespace templates]]
[[Category:Namespace manipulation templates]]

}}&lt;/includeonly&gt;
&lt;noinclude&gt;[[Category:Templates without wikidata]]&lt;/noinclude&gt;</text>
      <sha1>7tc1i2aiuxom01l57c9e1l5vw3fpyxy</sha1>
    </revision>
    <revision>
      <id>10299</id>
      <timestamp>2025-11-08T04:44:26Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was Text replacement - "↵&lt;noinclude&gt;[[Category:Templates without wikidata]]&lt;/noinclude&gt;" to ""</comment>
      <origin>10299</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="2650" sha1="7w52fty3rlvu0n9wxrtphwqa74x3daa" xml:space="preserve">{{Documentation subpage}}
{{Template shortcut|ns2}}
{{High-use|150,000+}}
This is the {{tl|user other}} meta-template.

This template helps other templates detect whether they are on a "User:" page or some "other" type of page.

===Usage===
This template usually takes two parameters, like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|User-page text|Other-pages text}}
&lt;/syntaxhighlight&gt;

If the template is on a "User:" page, it will return this:

:{{user other|demospace=user|User-page text|Other-pages text}}

If the template is on any other type of page, it will return this:

:{{user other|User-page text|Other-pages text}}

Using this meta-template can make a template that adds a category when it is invoked on User pages, but not when it is invoked on non-User pages. Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|[[Category:Some user category]]}}
&lt;/syntaxhighlight&gt;

This meta-template can warn that a template should not be used on non-User pages:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other||This template should only be used on user pages.}}
&lt;/syntaxhighlight&gt;

In the first case above, the "other" parameter was not used. In the second example, the "user" parameter was left empty.

===Demospace===

For testing and demonstration purposes, this template can take a named parameter, '''demospace'''.

* If demospace has the value '''user''', it returns the user-page text.
* It demospace has the value '''other''', or any other value such as the name of some other namespace it returns the other-pages text.
* If demospace is empty or undefined, the actual page type determines the result.

Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace=user
}}
&lt;/syntaxhighlight&gt;

No matter on what type of page the code above is used, it will return this:

:{{user other
| User-page text
| Other-pages text
| demospace=user
}}

You can make it so your template also understands the '''demospace''' parameter. That means you can demonstrate the different appearances of your template in the documentation for your template. Then do like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace={{{demospace|}}}
}}
&lt;/syntaxhighlight&gt;

===Technical details===
This template detects "User talk:" pages as type '''other'''.

This template works like {{tl|main talk other}}. For more examples and technical details, see full documentation there.

{{Namespace and pagename-detecting templates}}

&lt;includeonly&gt;{{Sandbox other||
[[Category:User namespace templates]]
[[Category:Namespace manipulation templates]]

}}&lt;/includeonly&gt;</text>
      <sha1>7w52fty3rlvu0n9wxrtphwqa74x3daa</sha1>
    </revision>
    <revision>
      <id>175773</id>
      <parentid>10299</parentid>
      <timestamp>2026-02-06T00:44:44Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: Adding dummy category to make categories work after wiki restoration</comment>
      <origin>175773</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="2673" sha1="8y3nwzdf6plvufcmlp4jivtjqww1og9" xml:space="preserve">{{Documentation subpage}}
{{Template shortcut|ns2}}
{{High-use|150,000+}}
This is the {{tl|user other}} meta-template.

This template helps other templates detect whether they are on a "User:" page or some "other" type of page.

===Usage===
This template usually takes two parameters, like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|User-page text|Other-pages text}}
&lt;/syntaxhighlight&gt;

If the template is on a "User:" page, it will return this:

:{{user other|demospace=user|User-page text|Other-pages text}}

If the template is on any other type of page, it will return this:

:{{user other|User-page text|Other-pages text}}

Using this meta-template can make a template that adds a category when it is invoked on User pages, but not when it is invoked on non-User pages. Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|[[Category:Some user category]]}}
&lt;/syntaxhighlight&gt;

This meta-template can warn that a template should not be used on non-User pages:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other||This template should only be used on user pages.}}
&lt;/syntaxhighlight&gt;

In the first case above, the "other" parameter was not used. In the second example, the "user" parameter was left empty.

===Demospace===

For testing and demonstration purposes, this template can take a named parameter, '''demospace'''.

* If demospace has the value '''user''', it returns the user-page text.
* It demospace has the value '''other''', or any other value such as the name of some other namespace it returns the other-pages text.
* If demospace is empty or undefined, the actual page type determines the result.

Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace=user
}}
&lt;/syntaxhighlight&gt;

No matter on what type of page the code above is used, it will return this:

:{{user other
| User-page text
| Other-pages text
| demospace=user
}}

You can make it so your template also understands the '''demospace''' parameter. That means you can demonstrate the different appearances of your template in the documentation for your template. Then do like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace={{{demospace|}}}
}}
&lt;/syntaxhighlight&gt;

===Technical details===
This template detects "User talk:" pages as type '''other'''.

This template works like {{tl|main talk other}}. For more examples and technical details, see full documentation there.

{{Namespace and pagename-detecting templates}}

&lt;includeonly&gt;{{Sandbox other||
[[Category:User namespace templates]]
[[Category:Namespace manipulation templates]]

}}&lt;/includeonly&gt;
[[Category:Templates]]</text>
      <sha1>8y3nwzdf6plvufcmlp4jivtjqww1og9</sha1>
    </revision>
    <revision>
      <id>3575516</id>
      <parentid>175773</parentid>
      <timestamp>2026-03-28T20:03:19Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <comment>Bot: wrap 4 stray tag(s) in noinclude [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23692667944|commit triggered pipeline]]</comment>
      <origin>3575516</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="2743" sha1="r4okbthkrcu1lwoql4paziu9w7i2ga5" xml:space="preserve">{{Documentation subpage}}
{{Template shortcut|ns2}}
{{High-use|150,000+}}
This is the {{tl|user other}} meta-template.

This template helps other templates detect whether they are on a "User:" page or some "other" type of page.

===Usage===
This template usually takes two parameters, like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|User-page text|Other-pages text}}
&lt;/syntaxhighlight&gt;

If the template is on a "User:" page, it will return this:

:{{user other|demospace=user|User-page text|Other-pages text}}

If the template is on any other type of page, it will return this:

:{{user other|User-page text|Other-pages text}}

Using this meta-template can make a template that adds a category when it is invoked on User pages, but not when it is invoked on non-User pages. Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other|}}
&lt;/syntaxhighlight&gt;

This meta-template can warn that a template should not be used on non-User pages:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other||This template should only be used on user pages.}}
&lt;/syntaxhighlight&gt;

In the first case above, the "other" parameter was not used. In the second example, the "user" parameter was left empty.

===Demospace===

For testing and demonstration purposes, this template can take a named parameter, '''demospace'''.

* If demospace has the value '''user''', it returns the user-page text.
* It demospace has the value '''other''', or any other value such as the name of some other namespace it returns the other-pages text.
* If demospace is empty or undefined, the actual page type determines the result.

Like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace=user
}}
&lt;/syntaxhighlight&gt;

No matter on what type of page the code above is used, it will return this:

:{{user other
| User-page text
| Other-pages text
| demospace=user
}}

You can make it so your template also understands the '''demospace''' parameter. That means you can demonstrate the different appearances of your template in the documentation for your template. Then do like this:

&lt;syntaxhighlight lang="wikitext"&gt;
{{user other
| User-page text
| Other-pages text
| demospace={{{demospace|}}}
}}
&lt;/syntaxhighlight&gt;

===Technical details===
This template detects "User talk:" pages as type '''other'''.

This template works like {{tl|main talk other}}. For more examples and technical details, see full documentation there.

{{Namespace and pagename-detecting templates}}

&lt;includeonly&gt;{{Sandbox other||

}}&lt;/includeonly&gt;
&lt;noinclude&gt;
[[Category:Some user category]]
[[Category:User namespace templates]]
[[Category:Namespace manipulation templates]]
[[Category:Templates]]
[[Category:Templates fixed with noinclude]]
&lt;/noinclude&gt;</text>
      <sha1>r4okbthkrcu1lwoql4paziu9w7i2ga5</sha1>
    </revision>
  </page>
</mediawiki>
