<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Help:Purge</title>
    <ns>12</ns>
    <id>4879</id>
    <revision>
      <id>778555</id>
      <timestamp>2024-10-28T18:36:00Z</timestamp>
      <contributor>
        <username>en&gt;Riod456</username>
        <id>0</id>
      </contributor>
      <minor/>
      <origin>778555</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12492" sha1="l54e9qj05pph39jbru2qnt2skirhxtg" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]]</text>
      <sha1>l54e9qj05pph39jbru2qnt2skirhxtg</sha1>
    </revision>
    <revision>
      <id>778556</id>
      <parentid>778555</parentid>
      <timestamp>2024-12-19T20:11:08Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: mass importing wikipedia pages</comment>
      <origin>778555</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12492" sha1="l54e9qj05pph39jbru2qnt2skirhxtg" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]]</text>
      <sha1>l54e9qj05pph39jbru2qnt2skirhxtg</sha1>
    </revision>
    <revision>
      <id>778557</id>
      <parentid>778556</parentid>
      <timestamp>2025-02-28T06:19:14Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>778557</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12508" sha1="2tcxux98u7n9w9g4r9ih8zk8uuppcf5" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:New]]</text>
      <sha1>2tcxux98u7n9w9g4r9ih8zk8uuppcf5</sha1>
    </revision>
    <revision>
      <id>778558</id>
      <parentid>778557</parentid>
      <timestamp>2025-03-08T18:21:08Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>778558</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12557" sha1="gwsv3gpyx2j54cdp7dgc11xgx6r04b5" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:New]]
[[Category:Pages and Categories from Wikipedia]]</text>
      <sha1>gwsv3gpyx2j54cdp7dgc11xgx6r04b5</sha1>
    </revision>
    <revision>
      <id>778559</id>
      <parentid>778558</parentid>
      <timestamp>2025-03-11T22:45:08Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>778559</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12574" sha1="sqyh2q7fn6w4zuap38j4r8e9h6uynxh" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:New]]</text>
      <sha1>sqyh2q7fn6w4zuap38j4r8e9h6uynxh</sha1>
    </revision>
    <revision>
      <id>778560</id>
      <parentid>778559</parentid>
      <timestamp>2025-03-15T23:49:33Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>778560</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12591" sha1="28ulq1g13tm5siw88tv0qcohg7y9pxh" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>28ulq1g13tm5siw88tv0qcohg7y9pxh</sha1>
    </revision>
    <revision>
      <id>778561</id>
      <parentid>778560</parentid>
      <timestamp>2025-03-24T20:43:07Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>778561</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12608" sha1="2yjxxhxxs21ibs3j6yxsecd09gr5rin" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>2yjxxhxxs21ibs3j6yxsecd09gr5rin</sha1>
    </revision>
    <revision>
      <id>778562</id>
      <parentid>778561</parentid>
      <timestamp>2025-03-24T21:38:05Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Adding [[Category:qq]][[sn:{{subst:PAGENAME}}]][[Category:qq]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>778562</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12651" sha1="4hl5negb45oelzgetr7a18l0wlw1t0o" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:qq]][[sn:Purge]][[Category:qq]]</text>
      <sha1>4hl5negb45oelzgetr7a18l0wlw1t0o</sha1>
    </revision>
    <revision>
      <id>778563</id>
      <parentid>778562</parentid>
      <timestamp>2025-03-25T04:44:17Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>778563</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12668" sha1="sl233umg6eui4boza7vfnizc7171hiu" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:qq]][[sn:Purge]][[Category:qq]]
[[Category:New]]</text>
      <sha1>sl233umg6eui4boza7vfnizc7171hiu</sha1>
    </revision>
    <revision>
      <id>778564</id>
      <parentid>778563</parentid>
      <timestamp>2025-03-27T12:35:15Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>778564</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12685" sha1="jqvp4s8v1z5frc2jc29hazd4qaq4yg8" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:qq]][[sn:Purge]][[Category:qq]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>jqvp4s8v1z5frc2jc29hazd4qaq4yg8</sha1>
    </revision>
    <revision>
      <id>778565</id>
      <parentid>778564</parentid>
      <timestamp>2025-03-30T01:07:52Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:Pages and Categories from Wikipedia 8]] to [[Category:qq]][[sn:{{subst:PAGENAME}}]][[Category:qq]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>778565</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12728" sha1="8u3od16j00y9g41k7jwu3odr5c90lnb" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:qq]][[sn:Purge]][[Category:qq]]
[[Category:New]]
[[Category:New]]
[[Category:qq]][[sn:Purge]][[Category:qq]]</text>
      <sha1>8u3od16j00y9g41k7jwu3odr5c90lnb</sha1>
    </revision>
    <revision>
      <id>778566</id>
      <parentid>778565</parentid>
      <timestamp>2025-03-30T01:25:41Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>778566</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12745" sha1="lkmpmm4mma1qcfij7p3gevu0yxsd5fd" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:qq]][[sn:Purge]][[Category:qq]]
[[Category:New]]
[[Category:New]]
[[Category:qq]][[sn:Purge]][[Category:qq]]
[[Category:New]]</text>
      <sha1>lkmpmm4mma1qcfij7p3gevu0yxsd5fd</sha1>
    </revision>
    <revision>
      <id>778567</id>
      <parentid>778566</parentid>
      <timestamp>2025-03-30T04:28:52Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Adding [[Category:qq]][[sn:{{subst:PAGENAME}}]][[Category:qq]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>778567</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12788" sha1="jauqsukaxsl434ryy74o4uwi38fbobx" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:qq]][[sn:Purge]][[Category:qq]]
[[Category:New]]
[[Category:New]]
[[Category:qq]][[sn:Purge]][[Category:qq]]
[[Category:New]]
[[Category:qq]][[sn:Purge]][[Category:qq]]</text>
      <sha1>jauqsukaxsl434ryy74o4uwi38fbobx</sha1>
    </revision>
    <revision>
      <id>778568</id>
      <parentid>778567</parentid>
      <timestamp>2025-04-09T02:13:58Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Removing from [[Category:Qq]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>778568</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12696" sha1="4a5qy3637f0rpetgj54ist681n28353" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[sn:Purge]][[Category:New]]
[[Category:New]]
[[sn:Purge]][[Category:New]]
[[sn:Purge]]</text>
      <sha1>4a5qy3637f0rpetgj54ist681n28353</sha1>
    </revision>
    <revision>
      <id>778569</id>
      <parentid>778568</parentid>
      <timestamp>2025-04-13T05:42:31Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>778569</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12713" sha1="se4gu0gpbha7o6w0afxnhqd2i97ps4h" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[sn:Purge]][[Category:New]]
[[Category:New]]
[[sn:Purge]][[Category:New]]
[[sn:Purge]]
[[Category:New]]</text>
      <sha1>se4gu0gpbha7o6w0afxnhqd2i97ps4h</sha1>
    </revision>
    <revision>
      <id>778570</id>
      <parentid>778569</parentid>
      <timestamp>2025-04-15T21:59:45Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:Pages and Categories from Wikipedia 8]] to [[Category:qq]][[sn:{{subst:PAGENAME}}]][[Category:qq]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>778570</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12756" sha1="759bbiz2kpdxg0zjjaltj4jcwjgbebq" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[sn:Purge]][[Category:New]]
[[Category:New]]
[[sn:Purge]][[Category:New]]
[[sn:Purge]]
[[Category:New]]
[[Category:qq]][[sn:Purge]][[Category:qq]]</text>
      <sha1>759bbiz2kpdxg0zjjaltj4jcwjgbebq</sha1>
    </revision>
    <revision>
      <id>778571</id>
      <parentid>778570</parentid>
      <timestamp>2025-04-15T23:13:07Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>778571</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12773" sha1="nob6sikkh0tk1ewqkrehr6aswhi8wep" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[sn:Purge]][[Category:New]]
[[Category:New]]
[[sn:Purge]][[Category:New]]
[[sn:Purge]]
[[Category:New]]
[[Category:qq]][[sn:Purge]][[Category:qq]]
[[Category:New]]</text>
      <sha1>nob6sikkh0tk1ewqkrehr6aswhi8wep</sha1>
    </revision>
    <revision>
      <id>778572</id>
      <parentid>778571</parentid>
      <timestamp>2025-04-27T09:44:26Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Removing from [[Category:New]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>778572</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12620" sha1="ro3r7gua5nmwf0istcwwrmxn7a4kyqq" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:Pages and Categories from Wikipedia]]
[[sn:Purge]][[sn:Purge]][[sn:Purge]]
[[Category:qq]][[sn:Purge]][[Category:qq]]</text>
      <sha1>ro3r7gua5nmwf0istcwwrmxn7a4kyqq</sha1>
    </revision>
    <revision>
      <id>778573</id>
      <parentid>778572</parentid>
      <timestamp>2025-04-29T10:47:13Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "[[Category:qq]]" to ""</comment>
      <origin>778573</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12590" sha1="ds41q2a5ph7i9dbr9lanzee2kzh4wrl" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:Pages and Categories from Wikipedia]]
[[sn:Purge]][[sn:Purge]][[sn:Purge]]
[[sn:Purge]]</text>
      <sha1>ds41q2a5ph7i9dbr9lanzee2kzh4wrl</sha1>
    </revision>
    <revision>
      <id>778574</id>
      <parentid>778573</parentid>
      <timestamp>2025-05-05T22:51:39Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:Pages and Categories from Wikipedia]] to [[Category:pages to be overwritten]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>778574</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="12627" sha1="kajsnnl0hcyaeb26cyx37oi78fqpb2p" xml:space="preserve">{{Short description|Wikimedia project page}}
{{pp-semi-indef|small=yes}}
{{Wikipedia how-to|WP:PURGE}}
{{nutshell|A common way to purge a page is to append &lt;code&gt;&lt;nowiki&gt;?action=purge&lt;/nowiki&gt;&lt;/code&gt; to the URL.}}

'''Purge''' is a function that is sometimes necessary to use to update a wiki page whenever template- or subpage-[[Help:Transclusion|transclusion]]s are involved. Purging clears the page's server [[Web cache|cache]], and the page is rebuilt. Before purging, you may want to try first to [[Wikipedia:Bypass your cache|refresh the page]] using your [[web browser]].

For updating a page display, any purge methods do the job, but for [[Wikipedia:Categorization|categories]] and [[Help:What links here|backlinks]] a '''[[#Null edit|null edit]]''' explained below is required, and other methods don't work. Update of images is explained in [[#Images|a section below]].

The page to purge is the one that ''transcludes'', not the page that gets transcluded. Purge consumes a small amount of additional processing power to rebuild the page.

== How to purge ==
{{Hatnote|Note: Purging may take a few minutes, though it usually happens immediately.}}

===By clicking a link===
====Sometimes already provided====
Some pages already provide a link to purge the page, such as the [[Wikipedia:Templates for discussion]] page ([[WP:TFD]]). Simply click on the link to purge the page.
([[WP:TFD]] transcludes subpages like [[Wikipedia:Templates for discussion/Log/{{CURRENTYEAR}} {{CURRENTMONTHNAME}} {{CURRENTDAY}}]], and updates from these subpages are not always reflected in [[WP:TFD]]. By purging, you will see the most recent version.)

===={{tlf|purge}}====
The {{tl|purge}} template may be used to place a purge link on a page.

====Gadgets====
There are also two [[Wikipedia:Gadget|gadgets]] that can add a purge tab or link to every page.  These may be found in [[Special:Preferences#mw-prefsection-gadgets|the Gadgets section of your Wikipedia Preferences]], under "Appearance":
*The "Purgetab" gadget ("{{int:Gadget-purgetab}}"), and
*The "UTCLiveClock" gadget ("{{int:Gadget-UTCLiveClock}}").

The "purge" option of the former can also be found in the Vector [[WP:SKIN|skin]] by navigating to the drop-down menu to the left of the Search box. In the MonoBook skin, it is in a separate tab.

To change the text shown on the "Purgetab" gadget's menu, add the following to [[Special:MyPage/common.js|your common.js file]] (or create the file using it):
&lt;syntaxhighlight lang=javascript&gt;
/* Customise purge tab's text */
$( document ).ready( function() {
	$( 'a', '#ca-purge' ).text( 'p' );
});
&lt;/syntaxhighlight&gt;
In this case, the &lt;code&gt;'p'&lt;/code&gt; in the third line sets the text shown on the tab to the lowercase letter p. You can change this to whatever you prefer (&lt;code&gt;'purge'&lt;/code&gt;, &lt;code&gt;'purge page'&lt;/code&gt;, etc).
&lt;!---With thanks to [[User:Werieth]] for this information---&gt;

=== Purge request to server ===
[[File:MediaWiki-Purging-URL.png|thumb|300px|Purge cache by adding &lt;code&gt;?action=purge&lt;/code&gt; to the URL]]
[[File:MediaWiki-Purging-for-IPs.png|thumb|300px|You have to confirm purging. This is normal, just click "Yes". [[WP:NOBREAK|You can't break anything]] by doing it.]]

You can also request the MediaWiki server to purge using a special URL. The URL should be in the format &lt;code&gt;//en.wikipedia.org/w/index.php?title=''page_title''&amp;action=purge&lt;/code&gt; (it can be prefixed with &lt;code&gt;http:&lt;/code&gt;, &lt;code&gt;https:&lt;/code&gt;, or with nothing).

Note that this will not work with URLs of the form &lt;code&gt;//en.wikipedia.org/wiki/''page_title''&lt;/code&gt;, for which a slightly different form is required: &lt;code&gt;//en.wikipedia.org/wiki/''page_title''?action=purge&lt;/code&gt;. In both cases the text needs to be placed before the [[URL fragment]] if one is there.

Another way is using the page [[Special:Purge]]. It can be used directly by accessing it and providing the title of the target page, or through accessing a link that already supplies the page, like &lt;code&gt;//en.wikipedia.org/wiki/Special:Purge/page_title&lt;/code&gt;.

It may be easiest to make the URL by clicking "edit" or "view source" tab of the page, and changing the last portion of the URL's [[query string]] from &lt;code&gt;action=edit&lt;/code&gt; to &lt;code&gt;action=purge&lt;/code&gt; (anything in the URL that appears after &lt;code&gt;action=edit&lt;/code&gt; can be deleted).

This kind of URL is used to make the links and templates outlined in the [[#By clicking a link|Just by clicking a link]] section above.

=== Null edit ===
{{Shortcut|WP:NULL|WP:NULLEDIT}}
{{distinguish|text=a [[Help:Dummy edit|dummy edit]], which slightly changes the page}}
If a category or backlink page isn't showing linked articles known to exist, the page may be null-edited (refreshed) and will then show all properly linked articles.

A '''null edit''' purges the target page (including category and backlink pages) if the page can be edited:
# Click any "Edit" or "Edit source" link on the page.
# Click "Publish changes" at the bottom of the edit box without making ''any changes''.

With a null edit, nothing is saved, and no edit is recorded. If you leave text in the edit summary, it will be discarded. Adding or deleting blank lines ''only at the end'' of the page is also usually a null edit. Trailing blank lines are largely ignored. Adding or deleting blank lines in section edits, however, sometimes results in changes, and gets saved.

A page move or protection counts as a null edit as well, although an edit is recorded in the page history.

'''Advantage'''

None of the other purge methods apply to categorization, nor to "what links here" changes from template edits.

'''Disadvantage'''

If you can't edit the page, you can't perform a null edit to it. If you're looking for category or backlink updates, but cannot perform a null edit to the page, all you need to do is simply wait, or ask another editor who is capable of editing the page to do it for you. ''Changes in category links are put into the [[Help:Job queue|job queue]], and re-cached when the server load is low. Depending on how busy the server is, the page may take days, or even months, to be updated.''

Do not confuse a ''null edit'' with a ''[[Help:Dummy edit|dummy edit]]''. A dummy edit ''does'' modify the page source (even if only slightly), and ''does'' result in the edit being saved and logged to the page's edit history.

If you have access to use the [[WP:AWB|AutoWikiBrowser]] editor and need to purge a large number of pages (e.g., after changing a transcluded template), one way to do this is to click the save button without making any changes. No changes will be made, but the page will be purged.

When you need to null edit a larger number of pages in a category (existing or not-existing), one of [[Wikipedia:User scripts/List#Purging|these tools]] may be used (e.g. [[User:Ahecht/Scripts/refresh.js]]).

=== Purge local browser cache ===

{| class="wikitable"

|-
|[[Mozilla Firefox|Firefox]] || [[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Ctrl|Shift|R}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (reload page and rewrite cache)

|-
|[[Google Chrome]]||[[Microsoft Windows]] and [[Linux]]:&lt;br /&gt;{{key|Ctrl|F5}} or {{key|Shift|F5}}
----
[[macOS]]:&lt;br /&gt;
{{key|⌘|R}} (reload page) or {{key|⌘|Shift|R}} (force reload page)

|-
|[[Konqueror]] || {{key|Ctrl|R}}

|-
|[[Microsoft Edge]]||{{key|F5}} or {{key|Ctrl|R}}

|-
|[[Opera (web browser)|Opera]]
|{{key|Ctrl|F5}} or {{key|Shift|F5}}

|-
|[[Safari (software)|Safari]] ||{{key|⌘|⌥|E}} (clearing browser cache) or {{key|⌘|R}} (update)

|}

===forcerecursivelinkupdate===
The behaviour of null edits changed around July 2013. Null edits previously added all pages which transcluded the page to the job queue to be reparsed. Now a null edit will reparse the page edited just as it always has (and therefore fix category membership and such), but it will no longer queue every transcluding page for reparse too.

A new "forcerecursivelinkupdate" parameter was added to the API's action=purge to get the old behavior if necessary. So &lt;code&gt;action=purge&amp;forcerecursivelinkupdate=1&lt;/code&gt; in the URL (see [[#Purge request to server|above]]) will get the old behaviour. Only use this responsibly. 

These changes ''do not'' affect normal edits that change the content of a page.

=== User preference ===
Logged-in users have some options in their [[Special:Preferences]] to help purging and bypassing browser caches. See also [[Special:Gadgets]]. For purge gadgets in Commons, see [[commons:Help:purge]].

==== Automating the confirmation screen ====
Logged in users may automate the "OK" confirmation by modifying [[Special:MyPage/common.js|their custom common.js file]].  An example script is below:
&lt;syntaxhighlight lang="javascript"&gt;
/* Automate purge confirmation dialog */
if (mw.config.get('wgAction') === 'purge') {
    $('form[action*="action=purge"]').submit();
}
&lt;/syntaxhighlight&gt;

== Server cache ==
When a change is made to a page, the [[MediaWiki]] software saves the change to the main database. Thereafter a copy of that page is made to a "server cache" on secondary servers, and until the page is changed again, that cached page will be shown. A change in a template puts updates of articles that use the template into a waiting list, called the [[Wikipedia:Job queue|Job queue]], and the page is updated once it has reached its turn. Smaller changes to the cache are done when pages are deleted or restored, in that case only the links are updated. Similarly when an image is updated, thumbnails on all pages that use that image are updated.&lt;ref&gt;[[:mw:Manual:Job_queue#HTML_cache_invalidation]]&lt;/ref&gt;

=== Category counts ===
Counts in categories are normally done by adding to the count when a page is added to an category and subtracting when an page is removed. In categories with less than 5000 members users can purge the category to update the count. Since April 2022, larger categories are recounted monthly.&lt;ref&gt;[[phab:T299823]]&lt;/ref&gt;

=== Templates ===
In a future update, in templates, the &lt;code&gt;#time&lt;/code&gt; parser function and the &lt;code&gt;formatDate()&lt;/code&gt; function in Modules will update server cache once the resulting time is up.&lt;ref&gt;[[gerrit:c/mediawiki/core/+/135887/]]&lt;/ref&gt; If none of the previous conditions are met, then the page will be updated 30 days after the last edit.&lt;ref&gt;[[gerrit:c/operations/puppet/+/230808/2/modules/role/manifests/cache/text.pp]]&lt;/ref&gt; Some pages are updated more frequently, like the Main page, which is updated every hour.
----
*{{tl|Purge}}
{{Purge}}{{-}}
-----
*{{tl|Purge box}}
{{Purge box}}{{-}}
----
*{{tl|Purge button}}
{{Purge button}}{{-}}
-----
*{{tl|Purge link portals}}
{{Purge link portals}}{{-}}
----
*{{tl|Purge page}}
{{Purge page}}{{-}}
----
*{{tlu|User:UBX/Purge}}
{{User:UBX/Purge}}{{-}}
----

=== Images ===
{{seealso|Commons:Help:Purge}}
To refresh thumbnails, you may have to purge either or both of the image page and/or the page that loads the image.
 
First go to the image description page by clicking the image. You should go further to Commons if the image is from Commons. Then purge. (Purge gadgets seem to work consistently. See [[commons:Help:Purge]] for Commons gadgets.) It may take a while for MediaWiki to complete rescaling, especially for an animated GIF.  It depends on how busy the MediaWiki software is.

After a while, go back to the page with thumbnails and reload, [[Wikipedia:Bypass your cache|bypassing your browser cache]] (Ctrl-F5 on Firefox). If the page still shows the old thumbnails, then purge this page.

If an image thumbnail will not yet regenerate successfully, it is probably because the server has cached an error page with the URL of a thumbnail, but the page is not purged because the actual thumbnail does not exist.  The solution is to request the thumbnail with an unusual URL, say by appending "&lt;code&gt;?1&lt;/code&gt;" to the end, then do a usual purge. If there is still a problem, ask for help at [[Wikipedia:Village pump (technical)]].

==See also==
* [[Help:Transclusion]]
* [[mw:Manual:Purge|Manual:Purge on MediaWiki]], [[mw:Manual:Cache|Manual:Cache on MediaWiki]], and [[mw:Manual:Job queue|Manual:Job queue on MediaWiki]]
* [[Wikipedia:Bypass your cache]], related but different concept of clearing a cache locally

== References ==
{{reflist}}

{{Purge Pages}}
{{Wikipedia technical help|collapsed}}

[[Category:Wikipedia how-to|Purge]]
[[Category:Wikipedia features|Purge]]
[[Category:Wikipedia sidebar help]][[Category:Pages and Categories from Wikipedia]]
[[Category:pages to be overwritten]]
[[sn:Purge]][[sn:Purge]][[sn:Purge]]
[[sn:Purge]]</text>
      <sha1>kajsnnl0hcyaeb26cyx37oi78fqpb2p</sha1>
    </revision>
    <revision>
      <id>778575</id>
      <parentid>778574</parentid>
      <timestamp>2025-05-05T23:00:03Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Auto generating page. date placed far in the past so it is attributed to Immanuelle and FireflyBot sends them messages if it is ever imported to Wikipedia.</comment>
      <origin>778575</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="161" sha1="d9tbz219lsmy26ov2v2pi65tl3it4h8" xml:space="preserve">#redirect[[en:Help:Purge]]
&lt;includeonly&gt;[[Category:Erroneous transclusions of Help:Purge]]&lt;/includeonly&gt;
&lt;noinclude&gt;[[Category:Wikipedia overwrites]]&lt;/noinclude&gt;</text>
      <sha1>d9tbz219lsmy26ov2v2pi65tl3it4h8</sha1>
    </revision>
    <revision>
      <id>4880</id>
      <timestamp>2025-05-20T08:44:00Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was Bot: remove deleted category Wikipedia overwrites</comment>
      <origin>4880</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="128" sha1="dpa3p3fifmu9q9sgj007th45se603hk" xml:space="preserve">#redirect[[en:Help:Purge]]
&lt;includeonly&gt;[[Category:Erroneous transclusions of Help:Purge]]&lt;/includeonly&gt;
&lt;noinclude&gt;&lt;/noinclude&gt;</text>
      <sha1>dpa3p3fifmu9q9sgj007th45se603hk</sha1>
    </revision>
    <revision>
      <id>3695782</id>
      <parentid>4880</parentid>
      <timestamp>2026-05-02T22:28:30Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>removing these dumb broken links that cause problems</comment>
      <origin>3695782</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="128" sha1="6tk3amf59ajyw0ez4nji5gyjb2ifxrx" xml:space="preserve">redirect[[:en:Help:Purge]]
&lt;includeonly&gt;[[Category:Erroneous transclusions of Help:Purge]]&lt;/includeonly&gt;
&lt;noinclude&gt;&lt;/noinclude&gt;</text>
      <sha1>6tk3amf59ajyw0ez4nji5gyjb2ifxrx</sha1>
    </revision>
  </page>
</mediawiki>
