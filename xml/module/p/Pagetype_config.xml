<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Module:Pagetype/config</title>
    <ns>828</ns>
    <id>28</id>
    <revision>
      <id>431887</id>
      <timestamp>2014-04-04T04:21:03Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>copy config data from [[Module:Namespace detect]] and format it</comment>
      <origin>431887</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3660" sha1="d5dmy06qv0sktp18n4y20zfwinn7as9" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the values to use for "main=true", "user=true", etc. Keys to
-- this table should be namespace parameters that can be used with
-- [[Module:Namespace detect]]. 
cfg.pagetypes = {
	['main']              = 'article',
	['user']              = 'user page',
	['project']           = 'project page',
	['wikipedia']         = 'project page',
	['wp']                = 'project page',
	['file']              = 'file',
	['image']             = 'file',
	['mediawiki']         = 'interface page',
	['template']          = 'template',
	['help']              = 'help page',
	['category']          = 'category',
	['portal']            = 'portal',
	['book']              = 'book',
	['draft']             = 'draft',
	['education program'] = 'education program page',
	['timedtext']         = 'Timed Text page',
	['module']            = 'module',
	['talk']              = 'talk page',
	['special']           = 'special page',
	['media']             = 'file'
}

-- This table holds the names of the namespaces to be looked up from
-- cfg.pagetypes by default.
cfg.defaultNamespaces = {
	'main',
	'file',
	'template',
	'category',
	'module',
	'book'
}

-- This table holds the names of the namespaces to be looked up from
-- cfg.pagetypes if cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	'main',
	'user',
	'project',
	'file',
	'mediawiki',
	'template',
	'category',
	'help',
	'portal',
	'module',
	'book',
	'draft'
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	'disambiguation',
	'disambig',
	'disamb',
	'dab'
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {'na', 'n/a'}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>d5dmy06qv0sktp18n4y20zfwinn7as9</sha1>
    </revision>
    <revision>
      <id>431888</id>
      <parentid>431887</parentid>
      <timestamp>2014-04-04T05:40:04Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>Protected Module:Pagetype/config: [[Wikipedia:Lua/Modules|High-risk Lua module]] ([Edit=Allow only template editors and admins] (indefinite) [Move=Allow only template editors and admins] (indefinite))</comment>
      <origin>431887</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3660" sha1="d5dmy06qv0sktp18n4y20zfwinn7as9" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the values to use for "main=true", "user=true", etc. Keys to
-- this table should be namespace parameters that can be used with
-- [[Module:Namespace detect]]. 
cfg.pagetypes = {
	['main']              = 'article',
	['user']              = 'user page',
	['project']           = 'project page',
	['wikipedia']         = 'project page',
	['wp']                = 'project page',
	['file']              = 'file',
	['image']             = 'file',
	['mediawiki']         = 'interface page',
	['template']          = 'template',
	['help']              = 'help page',
	['category']          = 'category',
	['portal']            = 'portal',
	['book']              = 'book',
	['draft']             = 'draft',
	['education program'] = 'education program page',
	['timedtext']         = 'Timed Text page',
	['module']            = 'module',
	['talk']              = 'talk page',
	['special']           = 'special page',
	['media']             = 'file'
}

-- This table holds the names of the namespaces to be looked up from
-- cfg.pagetypes by default.
cfg.defaultNamespaces = {
	'main',
	'file',
	'template',
	'category',
	'module',
	'book'
}

-- This table holds the names of the namespaces to be looked up from
-- cfg.pagetypes if cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	'main',
	'user',
	'project',
	'file',
	'mediawiki',
	'template',
	'category',
	'help',
	'portal',
	'module',
	'book',
	'draft'
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	'disambiguation',
	'disambig',
	'disamb',
	'dab'
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {'na', 'n/a'}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>d5dmy06qv0sktp18n4y20zfwinn7as9</sha1>
    </revision>
    <revision>
      <id>431889</id>
      <parentid>431888</parentid>
      <timestamp>2014-10-03T13:27:53Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>add Flow boards</comment>
      <origin>431889</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3694" sha1="4ct6zmhtm1p7mhm9t55efxnids6dn0w" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the values to use for "main=true", "user=true", etc. Keys to
-- this table should be namespace parameters that can be used with
-- [[Module:Namespace detect]]. 
cfg.pagetypes = {
	['main']              = 'article',
	['user']              = 'user page',
	['project']           = 'project page',
	['wikipedia']         = 'project page',
	['wp']                = 'project page',
	['file']              = 'file',
	['image']             = 'file',
	['mediawiki']         = 'interface page',
	['template']          = 'template',
	['help']              = 'help page',
	['category']          = 'category',
	['portal']            = 'portal',
	['book']              = 'book',
	['draft']             = 'draft',
	['education program'] = 'education program page',
	['timedtext']         = 'Timed Text page',
	['module']            = 'module',
	['topic']             = 'topic',
	['talk']              = 'talk page',
	['special']           = 'special page',
	['media']             = 'file'
}

-- This table holds the names of the namespaces to be looked up from
-- cfg.pagetypes by default.
cfg.defaultNamespaces = {
	'main',
	'file',
	'template',
	'category',
	'module',
	'book'
}

-- This table holds the names of the namespaces to be looked up from
-- cfg.pagetypes if cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	'main',
	'user',
	'project',
	'file',
	'mediawiki',
	'template',
	'category',
	'help',
	'portal',
	'module',
	'book',
	'draft'
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	'disambiguation',
	'disambig',
	'disamb',
	'dab'
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {'na', 'n/a'}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>4ct6zmhtm1p7mhm9t55efxnids6dn0w</sha1>
    </revision>
    <revision>
      <id>431890</id>
      <parentid>431889</parentid>
      <timestamp>2015-09-28T03:41:25Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>add the gadget and gadget definition namespaces per protected edit request by [[User:GeoffreyT2000]]</comment>
      <origin>431890</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3776" sha1="nsjvdnxzxja6ehw5fddooj4hmniipbh" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the values to use for "main=true", "user=true", etc. Keys to
-- this table should be namespace parameters that can be used with
-- [[Module:Namespace detect]]. 
cfg.pagetypes = {
	['main']              = 'article',
	['user']              = 'user page',
	['project']           = 'project page',
	['wikipedia']         = 'project page',
	['wp']                = 'project page',
	['file']              = 'file',
	['image']             = 'file',
	['mediawiki']         = 'interface page',
	['template']          = 'template',
	['help']              = 'help page',
	['category']          = 'category',
	['portal']            = 'portal',
	['book']              = 'book',
	['draft']             = 'draft',
	['education program'] = 'education program page',
	['timedtext']         = 'Timed Text page',
	['module']            = 'module',
	['topic']             = 'topic',
	['gadget']            = 'gadget',
	['gadget definition'] = 'gadget definition',
	['talk']              = 'talk page',
	['special']           = 'special page',
	['media']             = 'file',
}

-- This table holds the names of the namespaces to be looked up from
-- cfg.pagetypes by default.
cfg.defaultNamespaces = {
	'main',
	'file',
	'template',
	'category',
	'module',
	'book'
}

-- This table holds the names of the namespaces to be looked up from
-- cfg.pagetypes if cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	'main',
	'user',
	'project',
	'file',
	'mediawiki',
	'template',
	'category',
	'help',
	'portal',
	'module',
	'book',
	'draft'
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	'disambiguation',
	'disambig',
	'disamb',
	'dab'
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {'na', 'n/a'}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>nsjvdnxzxja6ehw5fddooj4hmniipbh</sha1>
    </revision>
    <revision>
      <id>431891</id>
      <parentid>431890</parentid>
      <timestamp>2019-04-22T21:09:58Z</timestamp>
      <contributor>
        <username>wikipedia&gt;El C</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>Changed protection level for "[[Module:Pagetype/config]]": [[WP:High-risk templates|High-risk Lua module]] ([Edit=Require administrator access] (indefinite) [Move=Require administrator access] (indefinite))</comment>
      <origin>431890</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3776" sha1="nsjvdnxzxja6ehw5fddooj4hmniipbh" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the values to use for "main=true", "user=true", etc. Keys to
-- this table should be namespace parameters that can be used with
-- [[Module:Namespace detect]]. 
cfg.pagetypes = {
	['main']              = 'article',
	['user']              = 'user page',
	['project']           = 'project page',
	['wikipedia']         = 'project page',
	['wp']                = 'project page',
	['file']              = 'file',
	['image']             = 'file',
	['mediawiki']         = 'interface page',
	['template']          = 'template',
	['help']              = 'help page',
	['category']          = 'category',
	['portal']            = 'portal',
	['book']              = 'book',
	['draft']             = 'draft',
	['education program'] = 'education program page',
	['timedtext']         = 'Timed Text page',
	['module']            = 'module',
	['topic']             = 'topic',
	['gadget']            = 'gadget',
	['gadget definition'] = 'gadget definition',
	['talk']              = 'talk page',
	['special']           = 'special page',
	['media']             = 'file',
}

-- This table holds the names of the namespaces to be looked up from
-- cfg.pagetypes by default.
cfg.defaultNamespaces = {
	'main',
	'file',
	'template',
	'category',
	'module',
	'book'
}

-- This table holds the names of the namespaces to be looked up from
-- cfg.pagetypes if cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	'main',
	'user',
	'project',
	'file',
	'mediawiki',
	'template',
	'category',
	'help',
	'portal',
	'module',
	'book',
	'draft'
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	'disambiguation',
	'disambig',
	'disamb',
	'dab'
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {'na', 'n/a'}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>nsjvdnxzxja6ehw5fddooj4hmniipbh</sha1>
    </revision>
    <revision>
      <id>431892</id>
      <parentid>431891</parentid>
      <timestamp>2020-06-01T17:52:20Z</timestamp>
      <contributor>
        <username>wikipedia&gt;RexxS</username>
        <id>0</id>
      </contributor>
      <comment>update from sandbox</comment>
      <origin>431892</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3949" sha1="tb6svg2he6570yy55mfxge7s1pk60ma" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the values to use for "main=true", "user=true", etc. Keys to
-- this table should be namespace parameters that can be used with
-- [[Module:Namespace detect]]. 
cfg.pagetypes = {
	['main']              = 'article',
	['user']              = 'user page',
	['project']           = 'project page',
	['wikipedia']         = 'project page',
	['wp']                = 'project page',
	['file']              = 'file',
	['image']             = 'file',
	['mediawiki']         = 'interface page',
	['template']          = 'template',
	['help']              = 'help page',
	['category']          = 'category',
	['portal']            = 'portal',
	['book']              = 'book',
	['draft']             = 'draft',
	['education program'] = 'education program page',
	['timedtext']         = 'Timed Text page',
	['module']            = 'module',
	['topic']             = 'topic',
	['gadget']            = 'gadget',
	['gadget definition'] = 'gadget definition',
	['talk']              = 'talk page',
	['special']           = 'special page',
	['media']             = 'file',
}

-- This table holds the names of the namespaces to be looked up from
-- cfg.pagetypes by default.
cfg.defaultNamespaces = {
	'main',
	'file',
	'template',
	'category',
	'module',
	'book'
}

-- This table holds the names of the namespaces to be looked up from
-- cfg.pagetypes if cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	'main',
	'user',
	'project',
	'file',
	'mediawiki',
	'template',
	'category',
	'help',
	'portal',
	'module',
	'book',
	'draft'
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	'disambiguation',
	'disambig',
	'disamb',
	'dab'
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {'na', 'n/a'}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>tb6svg2he6570yy55mfxge7s1pk60ma</sha1>
    </revision>
    <revision>
      <id>431893</id>
      <parentid>431892</parentid>
      <timestamp>2021-06-21T19:44:00Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Trialpears</username>
        <id>0</id>
      </contributor>
      <comment>Education program namespace is no more</comment>
      <origin>431893</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3898" sha1="lv8r4iyvyhbqdoc2xg6czkywezjd1px" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the values to use for "main=true", "user=true", etc. Keys to
-- this table should be namespace parameters that can be used with
-- [[Module:Namespace detect]]. 
cfg.pagetypes = {
	['main']              = 'article',
	['user']              = 'user page',
	['project']           = 'project page',
	['wikipedia']         = 'project page',
	['wp']                = 'project page',
	['file']              = 'file',
	['image']             = 'file',
	['mediawiki']         = 'interface page',
	['template']          = 'template',
	['help']              = 'help page',
	['category']          = 'category',
	['portal']            = 'portal',
	['book']              = 'book',
	['draft']             = 'draft',
	['timedtext']         = 'Timed Text page',
	['module']            = 'module',
	['topic']             = 'topic',
	['gadget']            = 'gadget',
	['gadget definition'] = 'gadget definition',
	['talk']              = 'talk page',
	['special']           = 'special page',
	['media']             = 'file',
}

-- This table holds the names of the namespaces to be looked up from
-- cfg.pagetypes by default.
cfg.defaultNamespaces = {
	'main',
	'file',
	'template',
	'category',
	'module',
	'book'
}

-- This table holds the names of the namespaces to be looked up from
-- cfg.pagetypes if cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	'main',
	'user',
	'project',
	'file',
	'mediawiki',
	'template',
	'category',
	'help',
	'portal',
	'module',
	'book',
	'draft'
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	'disambiguation',
	'disambig',
	'disamb',
	'dab'
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {'na', 'n/a'}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>lv8r4iyvyhbqdoc2xg6czkywezjd1px</sha1>
    </revision>
    <revision>
      <id>431894</id>
      <parentid>431893</parentid>
      <timestamp>2021-07-10T15:47:32Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Trialpears</username>
        <id>0</id>
      </contributor>
      <comment>Book namespace removal will happen within a few days</comment>
      <origin>431894</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3847" sha1="qi8m5o0e66j9quw6kiawd2otsulzq9m" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the values to use for "main=true", "user=true", etc. Keys to
-- this table should be namespace parameters that can be used with
-- [[Module:Namespace detect]]. 
cfg.pagetypes = {
	['main']              = 'article',
	['user']              = 'user page',
	['project']           = 'project page',
	['wikipedia']         = 'project page',
	['wp']                = 'project page',
	['file']              = 'file',
	['image']             = 'file',
	['mediawiki']         = 'interface page',
	['template']          = 'template',
	['help']              = 'help page',
	['category']          = 'category',
	['portal']            = 'portal',
	['draft']             = 'draft',
	['timedtext']         = 'Timed Text page',
	['module']            = 'module',
	['topic']             = 'topic',
	['gadget']            = 'gadget',
	['gadget definition'] = 'gadget definition',
	['talk']              = 'talk page',
	['special']           = 'special page',
	['media']             = 'file',
}

-- This table holds the names of the namespaces to be looked up from
-- cfg.pagetypes by default.
cfg.defaultNamespaces = {
	'main',
	'file',
	'template',
	'category',
	'module'
}

-- This table holds the names of the namespaces to be looked up from
-- cfg.pagetypes if cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	'main',
	'user',
	'project',
	'file',
	'mediawiki',
	'template',
	'category',
	'help',
	'portal',
	'module',
	'draft'
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	'disambiguation',
	'disambig',
	'disamb',
	'dab'
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {'na', 'n/a'}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>qi8m5o0e66j9quw6kiawd2otsulzq9m</sha1>
    </revision>
    <revision>
      <id>431895</id>
      <parentid>431894</parentid>
      <timestamp>2023-08-28T13:36:34Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>update from [[Module:Pagetype/config/sandbox]]</comment>
      <origin>431895</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3949" sha1="9qmc982q65l2h7aj1wblije3j52xkbx" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>9qmc982q65l2h7aj1wblije3j52xkbx</sha1>
    </revision>
    <revision>
      <id>431896</id>
      <parentid>431895</parentid>
      <timestamp>2024-01-31T19:43:55Z</timestamp>
      <contributor>
        <username>wikipedia&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>add cfg for soft redirects and SIAs</comment>
      <origin>431896</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4066" sha1="skpulsais2cx5v5l6zs9kcycf2ezrmi" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>skpulsais2cx5v5l6zs9kcycf2ezrmi</sha1>
    </revision>
    <revision>
      <id>431883</id>
      <timestamp>2024-03-12T20:10:21Z</timestamp>
      <contributor>
        <username>simple&gt;Ferien</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>Protected "[[Module:Pagetype/config]]": Highly visible template ([Edit=Allow only administrators] (indefinite) [Move=Allow only administrators] (indefinite))</comment>
      <origin>431883</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3949" sha1="9qmc982q65l2h7aj1wblije3j52xkbx" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>9qmc982q65l2h7aj1wblije3j52xkbx</sha1>
    </revision>
    <revision>
      <id>431897</id>
      <parentid>431883</parentid>
      <timestamp>2024-05-09T08:50:37Z</timestamp>
      <contributor>
        <username>wikipedia&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <comment>add rfd</comment>
      <origin>431897</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4116" sha1="b11w63qzip05yx5s26i15jk97d3x4s8" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>b11w63qzip05yx5s26i15jk97d3x4s8</sha1>
    </revision>
    <revision>
      <id>431881</id>
      <timestamp>2024-05-16T12:23:09Z</timestamp>
      <contributor>
        <username>wikipedia&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>431881</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>431882</id>
      <parentid>431881</parentid>
      <timestamp>2024-06-25T10:18:02Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported from [[:wikipedia:Module:Pagetype/config]]: importing for the list. This is a canonical list of Shrines to work with</comment>
      <origin>431881</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>431884</id>
      <parentid>431882</parentid>
      <timestamp>2025-01-09T00:01:15Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: importing templates</comment>
      <origin>431881</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>431885</id>
      <parentid>431884</parentid>
      <timestamp>2025-03-11T09:00:38Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>431881</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>431886</id>
      <parentid>431885</parentid>
      <timestamp>2025-03-11T09:00:39Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: more imports</comment>
      <origin>431881</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>431898</id>
      <parentid>431886</parentid>
      <timestamp>2025-03-12T23:59:31Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>11 revisions imported from [[:wikipedia:Module:Pagetype/config]]: importing because this actually has redlinks for a lot of the articles</comment>
      <origin>431881</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>431899</id>
      <parentid>431898</parentid>
      <timestamp>2025-03-15T19:25:21Z</timestamp>
      <contributor>
        <username>en&gt;Unknown user</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>431881</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>431900</id>
      <parentid>431899</parentid>
      <timestamp>2025-03-15T19:25:22Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: I hope this actually fixes things</comment>
      <origin>431881</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>431901</id>
      <parentid>431900</parentid>
      <timestamp>2025-04-27T22:10:57Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>431881</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>431902</id>
      <parentid>431901</parentid>
      <timestamp>2025-04-27T22:10:58Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: chat is this real</comment>
      <origin>431881</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>431903</id>
      <parentid>431902</parentid>
      <timestamp>2025-04-27T22:12:24Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>431881</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>431904</id>
      <parentid>431903</parentid>
      <timestamp>2025-04-27T22:13:17Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>431881</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>431905</id>
      <parentid>431904</parentid>
      <timestamp>2025-04-27T22:13:18Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: chat is this real</comment>
      <origin>431881</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>431906</id>
      <parentid>431905</parentid>
      <timestamp>2025-04-27T22:16:58Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>431881</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>431907</id>
      <parentid>431906</parentid>
      <timestamp>2025-04-27T22:16:59Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: chat is this real</comment>
      <origin>431881</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>431908</id>
      <parentid>431907</parentid>
      <timestamp>2025-04-28T02:49:13Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>431881</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>431909</id>
      <parentid>431908</parentid>
      <timestamp>2025-04-28T02:49:14Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>431881</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>29</id>
      <timestamp>2025-05-05T21:16:10Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was 1 revision imported: lets hope this fixes it since the qq=draft was showing up</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3304490</id>
      <parentid>29</parentid>
      <timestamp>2026-02-26T20:18:52Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3304492</id>
      <parentid>3304490</parentid>
      <timestamp>2026-02-26T20:18:52Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: importing this file, and also doing a full refresh import of all dependencies, because I am concerned this is one with a lot of broken dependencies. Intended end result is the template [[Template:Talk page header]] which is gonna be highly customized to our usage later on</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3365348</id>
      <parentid>3304492</parentid>
      <timestamp>2026-03-02T16:21:05Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3365349</id>
      <parentid>3365348</parentid>
      <timestamp>2026-03-02T16:21:05Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: import overwrite</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3368025</id>
      <parentid>3365349</parentid>
      <timestamp>2026-03-02T18:53:57Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3368026</id>
      <parentid>3368025</parentid>
      <timestamp>2026-03-02T18:53:57Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3368499</id>
      <parentid>3368026</parentid>
      <timestamp>2026-03-02T18:56:21Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3368500</id>
      <parentid>3368499</parentid>
      <timestamp>2026-03-02T18:56:21Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3368973</id>
      <parentid>3368500</parentid>
      <timestamp>2026-03-02T19:02:42Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3368974</id>
      <parentid>3368973</parentid>
      <timestamp>2026-03-02T19:02:43Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3407117</id>
      <parentid>3368974</parentid>
      <timestamp>2026-03-12T20:03:23Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3407118</id>
      <parentid>3407117</parentid>
      <timestamp>2026-03-12T20:03:23Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3408712</id>
      <parentid>3407118</parentid>
      <timestamp>2026-03-12T20:38:45Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3408713</id>
      <parentid>3408712</parentid>
      <timestamp>2026-03-12T20:38:45Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3412109</id>
      <parentid>3408713</parentid>
      <timestamp>2026-03-13T00:15:58Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3412110</id>
      <parentid>3412109</parentid>
      <timestamp>2026-03-13T00:15:58Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: importing</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3412478</id>
      <parentid>3412110</parentid>
      <timestamp>2026-03-13T00:17:03Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3412479</id>
      <parentid>3412478</parentid>
      <timestamp>2026-03-13T00:17:03Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3553975</id>
      <parentid>3412479</parentid>
      <timestamp>2026-03-20T18:19:45Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3553976</id>
      <parentid>3553975</parentid>
      <timestamp>2026-03-20T18:19:45Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3554189</id>
      <parentid>3553976</parentid>
      <timestamp>2026-03-20T18:20:48Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3554190</id>
      <parentid>3554189</parentid>
      <timestamp>2026-03-20T18:20:48Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3554403</id>
      <parentid>3554190</parentid>
      <timestamp>2026-03-20T18:22:17Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3554404</id>
      <parentid>3554403</parentid>
      <timestamp>2026-03-20T18:22:18Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3554617</id>
      <parentid>3554404</parentid>
      <timestamp>2026-03-20T18:24:18Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3554618</id>
      <parentid>3554617</parentid>
      <timestamp>2026-03-20T18:24:18Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3554831</id>
      <parentid>3554618</parentid>
      <timestamp>2026-03-20T18:26:48Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3554832</id>
      <parentid>3554831</parentid>
      <timestamp>2026-03-20T18:26:48Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3555045</id>
      <parentid>3554832</parentid>
      <timestamp>2026-03-20T18:29:49Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3555046</id>
      <parentid>3555045</parentid>
      <timestamp>2026-03-20T18:29:50Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3555259</id>
      <parentid>3555046</parentid>
      <timestamp>2026-03-20T18:33:20Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3555260</id>
      <parentid>3555259</parentid>
      <timestamp>2026-03-20T18:33:20Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3555473</id>
      <parentid>3555260</parentid>
      <timestamp>2026-03-20T18:37:24Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3555474</id>
      <parentid>3555473</parentid>
      <timestamp>2026-03-20T18:37:24Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3555687</id>
      <parentid>3555474</parentid>
      <timestamp>2026-03-20T18:41:53Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3555688</id>
      <parentid>3555687</parentid>
      <timestamp>2026-03-20T18:41:53Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3555901</id>
      <parentid>3555688</parentid>
      <timestamp>2026-03-20T18:46:56Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3555902</id>
      <parentid>3555901</parentid>
      <timestamp>2026-03-20T18:46:56Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3556115</id>
      <parentid>3555902</parentid>
      <timestamp>2026-03-20T18:52:25Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3556116</id>
      <parentid>3556115</parentid>
      <timestamp>2026-03-20T18:52:26Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3556329</id>
      <parentid>3556116</parentid>
      <timestamp>2026-03-20T18:58:31Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3556330</id>
      <parentid>3556329</parentid>
      <timestamp>2026-03-20T18:58:31Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3556543</id>
      <parentid>3556330</parentid>
      <timestamp>2026-03-20T19:05:01Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3556544</id>
      <parentid>3556543</parentid>
      <timestamp>2026-03-20T19:05:01Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3556757</id>
      <parentid>3556544</parentid>
      <timestamp>2026-03-20T19:12:02Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3556758</id>
      <parentid>3556757</parentid>
      <timestamp>2026-03-20T19:12:03Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3557013</id>
      <parentid>3556758</parentid>
      <timestamp>2026-03-20T19:37:45Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3557014</id>
      <parentid>3557013</parentid>
      <timestamp>2026-03-20T19:37:45Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3557336</id>
      <parentid>3557014</parentid>
      <timestamp>2026-03-20T19:41:41Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3557337</id>
      <parentid>3557336</parentid>
      <timestamp>2026-03-20T19:41:41Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3557849</id>
      <parentid>3557337</parentid>
      <timestamp>2026-03-20T19:46:15Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3557850</id>
      <parentid>3557849</parentid>
      <timestamp>2026-03-20T19:46:15Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3558722</id>
      <parentid>3557850</parentid>
      <timestamp>2026-03-20T19:55:16Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3558723</id>
      <parentid>3558722</parentid>
      <timestamp>2026-03-20T19:55:16Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3562116</id>
      <parentid>3558723</parentid>
      <timestamp>2026-03-22T18:47:29Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3562117</id>
      <parentid>3562116</parentid>
      <timestamp>2026-03-22T18:47:29Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3646100</id>
      <parentid>3562117</parentid>
      <timestamp>2026-04-24T16:30:26Z</timestamp>
      <contributor>
        <username>en&gt;MSGJ</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>+cfg for non-existent pages</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
    <revision>
      <id>3646101</id>
      <parentid>3646100</parentid>
      <timestamp>2026-04-24T16:30:26Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: trying to do a last fix of these, they will be fixed and not bloat the site</comment>
      <origin>29</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4216" sha1="61h7tsfs7sjoixfi1zfjharglkpqbji" xml:space="preserve">--------------------------------------------------------------------------------
--                  Module:Pagetype configuration data                        --
-- This page holds localisation and configuration data for Module:Pagetype.   --
--------------------------------------------------------------------------------

local cfg = {} -- Don't edit this line.

--------------------------------------------------------------------------------
--                        Start configuration data                            --
--------------------------------------------------------------------------------

-- This table holds the default page types for each namespace. Keys to this
-- table should be integers that can be used as keys to mw.site.namespaces.
cfg.pagetypes = {
	[0]    = 'article', -- Main namespace
	[2]    = 'user page',
	[4]    = 'project page',
	[6]    = 'file',
	[8]    = 'interface page', -- MediaWiki namespace
	[10]   = 'template',
	[12]   = 'help page',
	[14]   = 'category',
	[100]  = 'portal',
	[118]  = 'draft',
	[710]  = 'Timed Text page',
	[828]  = 'module',
	[2300] = 'gadget',
	[2302] = 'gadget definition',
	[-1]   = 'special page',
	[-2]   = 'file', -- Media namespace
}

-- This table holds the namespaces to be looked up from cfg.pagetypes by
-- default.
cfg.defaultNamespaces = {
	[0] = true,   -- main
	[6] = true,   -- file
	[10] = true,  -- template
	[14] = true,  -- category
	[828] = true, -- module
}

-- This table holds the namespaces to be looked up from cfg.pagetypes if
-- cfg.defaultnsExtended is set.
cfg.extendedNamespaces = {
	[0] = true,   -- main
	[2] = true,   -- user
	[4] = true,   -- project
	[6] = true,   -- file
	[8] = true,   -- mediawiki
	[10] = true,  -- template
	[12] = true,  -- help
	[14] = true,  -- category
	[100] = true, -- portal
	[118] = true, -- draft
	[828] = true, -- module
}

-- This table holds custom aliases for each namespace.
cfg.customNamespaceAliases = {
	[0] = {'main'},
}

-- The parameter name to set which default namespace values to be looked up from
-- cfg.pagetypes.
cfg.defaultns = 'defaultns'

-- The value of cfg.defaultns to set all namespaces, including talk.
cfg.defaultnsAll = 'all'

-- The value of cfg.defaultns to set the namespaces listed in
-- cfg.extendedNamespaces
cfg.defaultnsExtended = 'extended'

-- The value of cfg.defaultns to set no default namespaces.
cfg.defaultnsNone = 'none'

-- The parameter name to use for talk pages.
cfg.talk = 'talk'

-- The default value for talk pages.
cfg.talkDefault = 'talk page'

-- The parameter name to use for disambiguation pages page.
cfg.dab = 'dab'

-- The parameter name to use for non-existent pages.
cfg.ne = 'nonexistent'
cfg.neDefault = 'page'

cfg.softRedirect = 'soft_redirect'
cfg.softRedirectDefault = 'redirect'

cfg.sia = 'sia'
cfg.siaDefault = 'article'

cfg.rfd = 'redirect'
cfg.rfdDefault = 'redirect'

-- This table holds the different possible aliases for disambiguation-class
-- pages. These should be lower-case.
cfg.dabAliases = {
	['disambiguation'] = true,
	['disambig'] = true,
	['disamb'] = true,
	['dab'] = true,
}

-- The default value for disambiguation pages.
cfg.dabDefault = 'page'

-- The parameter name to use for N/A-class page.
cfg.na = 'na'

-- This table holds the different possible aliases for N/A-class pages. These
-- should be lower-case.
cfg.naAliases = {
	['na'] = true,
	['n/a'] = true,
}

-- The default value for N/A-class pages.
cfg.naDefault = 'page'

-- The parameter name to use for redirects.
cfg.redirect = 'redirect'

-- The default value to use for redirects.
cfg.redirectDefault = 'redirect'

-- The parameter name for undefined namespaces.
cfg.other = 'other'

-- The value used if the module detects an undefined namespace.
cfg.otherDefault = 'page'

-- The usual suffix denoting a plural.
cfg.plural = 's'

-- This table holds plurals not formed by a simple suffix.
cfg.irregularPlurals = {
	["category"] = "categories"
}

--------------------------------------------------------------------------------
--                        End configuration data                              --
--------------------------------------------------------------------------------

return cfg -- Don't edit this line</text>
      <sha1>61h7tsfs7sjoixfi1zfjharglkpqbji</sha1>
    </revision>
  </page>
</mediawiki>
