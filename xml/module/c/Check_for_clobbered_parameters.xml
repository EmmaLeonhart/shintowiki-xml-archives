<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Module:Check for clobbered parameters</title>
    <ns>828</ns>
    <id>474</id>
    <revision>
      <id>494524</id>
      <timestamp>2019-11-15T20:47:51Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Frietjes</username>
        <id>0</id>
      </contributor>
      <comment>generic clobbered parameter checker based on the one I made for infobox school (probably has some bugs)</comment>
      <origin>494524</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1614" sha1="lkutcovmw2s00t2o8boffca00pcrkji" xml:space="preserve">local p = {}

local function trim(s)
	return s:match('^%s*(.-)%s*$')
end

local function isnotempty(s)
	return s and s:match('%S')
end

function p.check(frame)
	local args = frame.args
	local pargs = frame:getParent().args
	local checknested = isnotempty(args['nested'])
	local delimiter = isnotempty(args['delimiter']) and args['delimiter'] or '='
	local cat = ''
	if args['cat'] and mw.ustring.match(args['cat'],'^[Cc][Aa][Tt][Ee][Gg][Oo][Rr][Yy]:') then
		cat = args['cat']
	end
	local res = ''

	local argpairs = {}
	for k, v in pairs(args) do
		if type(k) == 'number' then
			local plist = mw.text.split(v, '[\s]*' .. delimiter .. '[\s]*')
			local pfound = {}
			for ii, vv in ipairs(plist) do
				if checknested and pargs[vv] or isnotempty(pargs[vv]) then
					table.insert(pfound, vv)
				end
			end
			if pfound.length &gt; 1 then
				table.insert(argpairs, pfound)
			end
		end
	end
	
	local warnmsg = {}
	if #argpairs &gt; 0 then
		local nocat = isnotblank(args['nocat'])
		for i, v in ipairs( argpairs ) do
			table.insert(warnmsg, 'Using more than one of the following parameters: &lt;code&gt;' ..
				table.concat(v, '&lt;/code&gt;, &lt;code&gt;') .. '&lt;/code&gt;')
			if not nocat and cat ~= '' then
				res = res .. '[[' .. cat .. '|' .. (v[1] == '' and ' ' or '') .. v[1] .. ']]'
			end	
		end
	end
	
	if #warnmsg &gt; 0 then
		if frame:preprocess( "{{REVISIONID}}" ) == "" then
			local p = args['template'] and args['template'] .. ' warning' or 'Warning'
			res = '&lt;div class="hatnote" style="color:red"&gt;&lt;strong&gt;' .. p .. ':&lt;/strong&gt; ' .. table.concat(warnmsg, '&lt;br&gt;') .. '&lt;/div&gt;' .. res
		end
	end
	
	return res
end

return p</text>
      <sha1>lkutcovmw2s00t2o8boffca00pcrkji</sha1>
    </revision>
    <revision>
      <id>494525</id>
      <parentid>494524</parentid>
      <timestamp>2019-11-15T20:51:29Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Frietjes</username>
        <id>0</id>
      </contributor>
      <origin>494525</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1614" sha1="n7alr4qhbqiqddp5crdpw9fmagsdaxf" xml:space="preserve">local p = {}

local function trim(s)
	return s:match('^%s*(.-)%s*$')
end

local function isnotempty(s)
	return s and s:match('%S')
end

function p.check(frame)
	local args = frame.args
	local pargs = frame:getParent().args
	local checknested = isnotempty(args['nested'])
	local delimiter = isnotempty(args['delimiter']) and args['delimiter'] or ';'
	local cat = ''
	if args['cat'] and mw.ustring.match(args['cat'],'^[Cc][Aa][Tt][Ee][Gg][Oo][Rr][Yy]:') then
		cat = args['cat']
	end
	local res = ''

	local argpairs = {}
	for k, v in pairs(args) do
		if type(k) == 'number' then
			local plist = mw.text.split(v, '[\s]*' .. delimiter .. '[\s]*')
			local pfound = {}
			for ii, vv in ipairs(plist) do
				if checknested and pargs[vv] or isnotempty(pargs[vv]) then
					table.insert(pfound, vv)
				end
			end
			if pfound.length &gt; 1 then
				table.insert(argpairs, pfound)
			end
		end
	end
	
	local warnmsg = {}
	if #argpairs &gt; 0 then
		local nocat = isnotblank(args['nocat'])
		for i, v in ipairs( argpairs ) do
			table.insert(warnmsg, 'Using more than one of the following parameters: &lt;code&gt;' ..
				table.concat(v, '&lt;/code&gt;, &lt;code&gt;') .. '&lt;/code&gt;')
			if not nocat and cat ~= '' then
				res = res .. '[[' .. cat .. '|' .. (v[1] == '' and ' ' or '') .. v[1] .. ']]'
			end	
		end
	end
	
	if #warnmsg &gt; 0 then
		if frame:preprocess( "{{REVISIONID}}" ) == "" then
			local p = args['template'] and args['template'] .. ' warning' or 'Warning'
			res = '&lt;div class="hatnote" style="color:red"&gt;&lt;strong&gt;' .. p .. ':&lt;/strong&gt; ' .. table.concat(warnmsg, '&lt;br&gt;') .. '&lt;/div&gt;' .. res
		end
	end
	
	return res
end

return p</text>
      <sha1>n7alr4qhbqiqddp5crdpw9fmagsdaxf</sha1>
    </revision>
    <revision>
      <id>494526</id>
      <parentid>494525</parentid>
      <timestamp>2019-11-15T20:52:01Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Frietjes</username>
        <id>0</id>
      </contributor>
      <origin>494526</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1632" sha1="7qno0vk1xfg6zb0rim6tnx7uz4o76g8" xml:space="preserve">local p = {}

local function trim(s)
	return s:match('^%s*(.-)%s*$')
end

local function isnotempty(s)
	return s and s:match('%S')
end

function p.check(frame)
	local args = frame.args
	local pargs = frame:getParent().args
	local checknested = isnotempty(args['nested'])
	local delimiter = isnotempty(args['delimiter']) and args['delimiter'] or ';'
	local cat = ''
	if args['cat'] and mw.ustring.match(args['cat'],'^[Cc][Aa][Tt][Ee][Gg][Oo][Rr][Yy]:') then
		cat = args['cat']
	end
	local res = ''

	local argpairs = {}
	for k, v in pairs(args) do
		if type(k) == 'number' then
			local plist = mw.text.split(v, '[\s]*' .. delimiter .. '[\s]*')
			local pfound = {}
			for ii, vv in ipairs(plist) do
				vv = trim(vv)
				if checknested and pargs[vv] or isnotempty(pargs[vv]) then
					table.insert(pfound, vv)
				end
			end
			if pfound.length &gt; 1 then
				table.insert(argpairs, pfound)
			end
		end
	end
	
	local warnmsg = {}
	if #argpairs &gt; 0 then
		local nocat = isnotblank(args['nocat'])
		for i, v in ipairs( argpairs ) do
			table.insert(warnmsg, 'Using more than one of the following parameters: &lt;code&gt;' ..
				table.concat(v, '&lt;/code&gt;, &lt;code&gt;') .. '&lt;/code&gt;')
			if not nocat and cat ~= '' then
				res = res .. '[[' .. cat .. '|' .. (v[1] == '' and ' ' or '') .. v[1] .. ']]'
			end	
		end
	end
	
	if #warnmsg &gt; 0 then
		if frame:preprocess( "{{REVISIONID}}" ) == "" then
			local p = args['template'] and args['template'] .. ' warning' or 'Warning'
			res = '&lt;div class="hatnote" style="color:red"&gt;&lt;strong&gt;' .. p .. ':&lt;/strong&gt; ' .. table.concat(warnmsg, '&lt;br&gt;') .. '&lt;/div&gt;' .. res
		end
	end
	
	return res
end

return p</text>
      <sha1>7qno0vk1xfg6zb0rim6tnx7uz4o76g8</sha1>
    </revision>
    <revision>
      <id>494527</id>
      <parentid>494526</parentid>
      <timestamp>2019-11-15T20:53:03Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Frietjes</username>
        <id>0</id>
      </contributor>
      <origin>494527</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1610" sha1="mjuar5ys8u4pceos87jq37ihhlw83tz" xml:space="preserve">local p = {}

local function trim(s)
	return s:match('^%s*(.-)%s*$')
end

local function isnotempty(s)
	return s and s:match('%S')
end

function p.check(frame)
	local args = frame.args
	local pargs = frame:getParent().args
	local checknested = isnotempty(args['nested'])
	local delimiter = isnotempty(args['delimiter']) and args['delimiter'] or ';'
	local cat = ''
	if args['cat'] and mw.ustring.match(args['cat'],'^[Cc][Aa][Tt][Ee][Gg][Oo][Rr][Yy]:') then
		cat = args['cat']
	end
	local res = ''

	local argpairs = {}
	for k, v in pairs(args) do
		if type(k) == 'number' then
			local plist = mw.text.split(v, delimiter)
			local pfound = {}
			for ii, vv in ipairs(plist) do
				vv = trim(vv)
				if checknested and pargs[vv] or isnotempty(pargs[vv]) then
					table.insert(pfound, vv)
				end
			end
			if pfound.length &gt; 1 then
				table.insert(argpairs, pfound)
			end
		end
	end
	
	local warnmsg = {}
	if #argpairs &gt; 0 then
		local nocat = isnotblank(args['nocat'])
		for i, v in ipairs( argpairs ) do
			table.insert(warnmsg, 'Using more than one of the following parameters: &lt;code&gt;' ..
				table.concat(v, '&lt;/code&gt;, &lt;code&gt;') .. '&lt;/code&gt;')
			if not nocat and cat ~= '' then
				res = res .. '[[' .. cat .. '|' .. (v[1] == '' and ' ' or '') .. v[1] .. ']]'
			end	
		end
	end
	
	if #warnmsg &gt; 0 then
		if frame:preprocess( "{{REVISIONID}}" ) == "" then
			local p = args['template'] and args['template'] .. ' warning' or 'Warning'
			res = '&lt;div class="hatnote" style="color:red"&gt;&lt;strong&gt;' .. p .. ':&lt;/strong&gt; ' .. table.concat(warnmsg, '&lt;br&gt;') .. '&lt;/div&gt;' .. res
		end
	end
	
	return res
end

return p</text>
      <sha1>mjuar5ys8u4pceos87jq37ihhlw83tz</sha1>
    </revision>
    <revision>
      <id>494528</id>
      <parentid>494527</parentid>
      <timestamp>2019-11-15T21:03:05Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Frietjes</username>
        <id>0</id>
      </contributor>
      <origin>494528</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1644" sha1="3oa7zyhmyuvjmrsx4u5jy1rsama1pu2" xml:space="preserve">local p = {}

local function trim(s)
	return s:match('^%s*(.-)%s*$')
end

local function isnotempty(s)
	return s and s:match('%S')
end

function p.check(frame)
	local args = frame.args
	local pargs = frame:getParent().args
	local checknested = isnotempty(args['nested'])
	local delimiter = isnotempty(args['delimiter']) and args['delimiter'] or ';'
	local cat = ''
	if args['cat'] and mw.ustring.match(args['cat'],'^[Cc][Aa][Tt][Ee][Gg][Oo][Rr][Yy]:') then
		cat = args['cat']
	end
	local res = ''

	local argpairs = {}
	for k, v in pairs(args) do
		if type(k) == 'number' then
			local plist = mw.text.split(v, delimiter)
			local pfound = {}
			local count = 0
			for ii, vv in ipairs(plist) do
				vv = trim(vv)
				if checknested and pargs[vv] or isnotempty(pargs[vv]) then
					count = count + 1
					table.insert(pfound, vv)
				end
			end
			if count &gt; 1 then
				table.insert(argpairs, pfound)
			end
		end
	end
	
	local warnmsg = {}
	if #argpairs &gt; 0 then
		local nocat = isnotblank(args['nocat'])
		for i, v in ipairs( argpairs ) do
			table.insert(warnmsg, 'Using more than one of the following parameters: &lt;code&gt;' ..
				table.concat(v, '&lt;/code&gt;, &lt;code&gt;') .. '&lt;/code&gt;')
			if not nocat and cat ~= '' then
				res = res .. '[[' .. cat .. '|' .. (v[1] == '' and ' ' or '') .. v[1] .. ']]'
			end	
		end
	end
	
	if #warnmsg &gt; 0 then
		if frame:preprocess( "{{REVISIONID}}" ) == "" then
			local p = args['template'] and args['template'] .. ' warning' or 'Warning'
			res = '&lt;div class="hatnote" style="color:red"&gt;&lt;strong&gt;' .. p .. ':&lt;/strong&gt; ' .. table.concat(warnmsg, '&lt;br&gt;') .. '&lt;/div&gt;' .. res
		end
	end
	
	return res
end

return p</text>
      <sha1>3oa7zyhmyuvjmrsx4u5jy1rsama1pu2</sha1>
    </revision>
    <revision>
      <id>494529</id>
      <parentid>494528</parentid>
      <timestamp>2019-11-15T21:08:35Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Frietjes</username>
        <id>0</id>
      </contributor>
      <origin>494529</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1588" sha1="awuk8qm8cpv4k15p5l70p7hy7rk4laa" xml:space="preserve">local p = {}

local function trim(s)
	return s:match('^%s*(.-)%s*$')
end

local function isnotempty(s)
	return s and s:match('%S')
end

function p.check(frame)
	local args = frame.args
	local pargs = frame:getParent().args
	local checknested = isnotempty(args['nested'])
	local delimiter = isnotempty(args['delimiter']) and args['delimiter'] or ';'
	local cat = ''
	if args['cat'] and mw.ustring.match(args['cat'],'^[Cc][Aa][Tt][Ee][Gg][Oo][Rr][Yy]:') then
		cat = args['cat']
	end
	local res = ''

	local argpairs = {}
	for k, v in pairs(args) do
		if type(k) == 'number' then
			local plist = mw.text.split(v, delimiter)
			local pfound = {}
			local count = 0
			for ii, vv in ipairs(plist) do
				vv = trim(vv)
				if checknested and pargs[vv] or isnotempty(pargs[vv]) then
					count = count + 1
					table.insert(pfound, vv)
				end
			end
			if count &gt; 1 then
				table.insert(argpairs, pfound)
			end
		end
	end
	
	local warnmsg = {}
	if #argpairs &gt; 0 then
		for i, v in ipairs( argpairs ) do
			table.insert(warnmsg, 'Using more than one of the following parameters: &lt;code&gt;' ..
				table.concat(v, '&lt;/code&gt;, &lt;code&gt;') .. '&lt;/code&gt;')
			if cat ~= '' then
				res = res .. '[[' .. cat .. '|' .. (v[1] == '' and ' ' or '') .. v[1] .. ']]'
			end	
		end
	end
	
	if #warnmsg &gt; 0 then
		if frame:preprocess( "{{REVISIONID}}" ) == "" then
			local p = args['template'] and args['template'] .. ' warning' or 'Warning'
			res = '&lt;div class="hatnote" style="color:red"&gt;&lt;strong&gt;' .. p .. ':&lt;/strong&gt; ' .. table.concat(warnmsg, '&lt;br&gt;') .. '&lt;/div&gt;' .. res
		end
	end
	
	return res
end

return p</text>
      <sha1>awuk8qm8cpv4k15p5l70p7hy7rk4laa</sha1>
    </revision>
    <revision>
      <id>494530</id>
      <parentid>494529</parentid>
      <timestamp>2019-11-15T21:12:13Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Frietjes</username>
        <id>0</id>
      </contributor>
      <origin>494530</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1618" sha1="tllwcgchd7vj98brjuqc4mfh3jshcv0" xml:space="preserve">local p = {}

local function trim(s)
	return s:match('^%s*(.-)%s*$')
end

local function isnotempty(s)
	return s and s:match('%S')
end

function p.check(frame)
	local args = frame.args
	local pargs = frame:getParent().args
	local checknested = isnotempty(args['nested'])
	local delimiter = isnotempty(args['delimiter']) and args['delimiter'] or ';'
	local cat = ''
	if args['cat'] and mw.ustring.match(args['cat'],'^[Cc][Aa][Tt][Ee][Gg][Oo][Rr][Yy]:') then
		cat = args['cat']
	end
	local res = ''

	local argpairs = {}
	for k, v in pairs(args) do
		if type(k) == 'number' then
			local plist = mw.text.split(v, delimiter)
			local pfound = {}
			local count = 0
			for ii, vv in ipairs(plist) do
				vv = trim(vv)
				if checknested and pargs[vv] or isnotempty(pargs[vv]) then
					count = count + 1
					table.insert(pfound, vv)
				end
			end
			if count &gt; 1 then
				table.insert(argpairs, pfound)
			end
		end
	end
	
	local warnmsg = {}
	if #argpairs &gt; 0 then
		for i, v in ipairs( argpairs ) do
			table.insert(warnmsg, 'Using more than one of the following parameters (one may override the others): &lt;code&gt;' ..
				table.concat(v, '&lt;/code&gt;, &lt;code&gt;') .. '&lt;/code&gt;')
			if cat ~= '' then
				res = res .. '[[' .. cat .. '|' .. (v[1] == '' and ' ' or '') .. v[1] .. ']]'
			end	
		end
	end
	
	if #warnmsg &gt; 0 then
		if frame:preprocess( "{{REVISIONID}}" ) == "" then
			local p = args['template'] and args['template'] .. ' warning' or 'Warning'
			res = '&lt;div class="hatnote" style="color:red"&gt;&lt;strong&gt;' .. p .. ':&lt;/strong&gt; ' .. table.concat(warnmsg, '&lt;br&gt;') .. '&lt;/div&gt;' .. res
		end
	end
	
	return res
end

return p</text>
      <sha1>tllwcgchd7vj98brjuqc4mfh3jshcv0</sha1>
    </revision>
    <revision>
      <id>494531</id>
      <parentid>494530</parentid>
      <timestamp>2019-11-15T21:44:44Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Frietjes</username>
        <id>0</id>
      </contributor>
      <origin>494531</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1624" sha1="j2fenu2077jn31xkyfcjubqqca0y0fo" xml:space="preserve">local p = {}

local function trim(s)
	return s:match('^%s*(.-)%s*$')
end

local function isnotempty(s)
	return s and s:match('%S')
end

function p.check(frame)
	local args = frame.args
	local pargs = frame:getParent().args
	local checknested = isnotempty(args['nested'])
	local delimiter = isnotempty(args['delimiter']) and args['delimiter'] or ';'
	local cat = ''
	if args['cat'] and mw.ustring.match(args['cat'],'^[Cc][Aa][Tt][Ee][Gg][Oo][Rr][Yy]:') then
		cat = args['cat']
	end
	local res = ''

	local argpairs = {}
	for k, v in pairs(args) do
		if type(k) == 'number' then
			local plist = mw.text.split(v, delimiter)
			local pfound = {}
			local count = 0
			for ii, vv in ipairs(plist) do
				vv = trim(vv)
				if checknested and pargs[vv] or isnotempty(pargs[vv]) then
					count = count + 1
					table.insert(pfound, vv)
				end
			end
			if count &gt; 1 then
				table.insert(argpairs, pfound)
			end
		end
	end
	
	local warnmsg = {}
	if #argpairs &gt; 0 then
		for i, v in ipairs( argpairs ) do
			table.insert(warnmsg, 'Using more than one of the following parameters (one may override the others): &lt;code&gt;' ..
				table.concat(v, '&lt;/code&gt;, &lt;code&gt;') .. '&lt;/code&gt;')
			if cat ~= '' then
				res = res .. '[[' .. cat .. '|' .. (v[1] == '' and ' ' or '') .. v[1] .. ']]'
			end	
		end
	end
	
	if #warnmsg &gt; 0 then
		if frame:preprocess( "{{REVISIONID}}" ) == "" then
			local ptxt = args['template'] and args['template'] .. ' warning' or 'Warning'
			res = '&lt;div class="hatnote" style="color:red"&gt;&lt;strong&gt;' .. ptxt .. ':&lt;/strong&gt; ' .. table.concat(warnmsg, '&lt;br&gt;') .. '&lt;/div&gt;' .. res
		end
	end
	
	return res
end

return p</text>
      <sha1>j2fenu2077jn31xkyfcjubqqca0y0fo</sha1>
    </revision>
    <revision>
      <id>494532</id>
      <parentid>494531</parentid>
      <timestamp>2019-11-15T21:47:19Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Frietjes</username>
        <id>0</id>
      </contributor>
      <origin>494532</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1594" sha1="oa62l8h6k4arzwhdoix5dw61evefwwd" xml:space="preserve">local p = {}

local function trim(s)
	return s:match('^%s*(.-)%s*$')
end

local function isnotempty(s)
	return s and s:match('%S')
end

function p.check(frame)
	local args = frame.args
	local pargs = frame:getParent().args
	local checknested = isnotempty(args['nested'])
	local delimiter = isnotempty(args['delimiter']) and args['delimiter'] or ';'
	local cat = ''
	if args['cat'] and mw.ustring.match(args['cat'],'^[Cc][Aa][Tt][Ee][Gg][Oo][Rr][Yy]:') then
		cat = args['cat']
	end
	local res = ''

	local argpairs = {}
	for k, v in pairs(args) do
		if type(k) == 'number' then
			local plist = mw.text.split(v, delimiter)
			local pfound = {}
			local count = 0
			for ii, vv in ipairs(plist) do
				vv = trim(vv)
				if checknested and pargs[vv] or isnotempty(pargs[vv]) then
					count = count + 1
					table.insert(pfound, vv)
				end
			end
			if count &gt; 1 then
				table.insert(argpairs, pfound)
			end
		end
	end
	
	local warnmsg = {}
	if #argpairs &gt; 0 then
		for i, v in ipairs( argpairs ) do
			table.insert(warnmsg, 'Using more than one of the following parameters: &lt;code&gt;' ..
				table.concat(v, '&lt;/code&gt;, &lt;code&gt;') .. '&lt;/code&gt;')
			if cat ~= '' then
				res = res .. '[[' .. cat .. '|' .. (v[1] == '' and ' ' or '') .. v[1] .. ']]'
			end	
		end
	end
	
	if #warnmsg &gt; 0 then
		if frame:preprocess( "{{REVISIONID}}" ) == "" then
			local ptxt = args['template'] and args['template'] .. ' warning' or 'Warning'
			res = '&lt;div class="hatnote" style="color:red"&gt;&lt;strong&gt;' .. ptxt .. ':&lt;/strong&gt; ' .. table.concat(warnmsg, '&lt;br&gt;') .. '&lt;/div&gt;' .. res
		end
	end
	
	return res
end

return p</text>
      <sha1>oa62l8h6k4arzwhdoix5dw61evefwwd</sha1>
    </revision>
    <revision>
      <id>494533</id>
      <parentid>494532</parentid>
      <timestamp>2019-11-16T17:59:47Z</timestamp>
      <contributor>
        <username>wikipedia&gt;MusikBot II</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>Protected "[[Module:Check for clobbered parameters]]": [[Wikipedia:High-risk templates|High-risk template or module]] ([[User:MusikBot II/TemplateProtector|more info]]) ([Edit=Require template editor access] (indefinite) [Move=Require template editor access] (indefinite))</comment>
      <origin>494532</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1594" sha1="oa62l8h6k4arzwhdoix5dw61evefwwd" xml:space="preserve">local p = {}

local function trim(s)
	return s:match('^%s*(.-)%s*$')
end

local function isnotempty(s)
	return s and s:match('%S')
end

function p.check(frame)
	local args = frame.args
	local pargs = frame:getParent().args
	local checknested = isnotempty(args['nested'])
	local delimiter = isnotempty(args['delimiter']) and args['delimiter'] or ';'
	local cat = ''
	if args['cat'] and mw.ustring.match(args['cat'],'^[Cc][Aa][Tt][Ee][Gg][Oo][Rr][Yy]:') then
		cat = args['cat']
	end
	local res = ''

	local argpairs = {}
	for k, v in pairs(args) do
		if type(k) == 'number' then
			local plist = mw.text.split(v, delimiter)
			local pfound = {}
			local count = 0
			for ii, vv in ipairs(plist) do
				vv = trim(vv)
				if checknested and pargs[vv] or isnotempty(pargs[vv]) then
					count = count + 1
					table.insert(pfound, vv)
				end
			end
			if count &gt; 1 then
				table.insert(argpairs, pfound)
			end
		end
	end
	
	local warnmsg = {}
	if #argpairs &gt; 0 then
		for i, v in ipairs( argpairs ) do
			table.insert(warnmsg, 'Using more than one of the following parameters: &lt;code&gt;' ..
				table.concat(v, '&lt;/code&gt;, &lt;code&gt;') .. '&lt;/code&gt;')
			if cat ~= '' then
				res = res .. '[[' .. cat .. '|' .. (v[1] == '' and ' ' or '') .. v[1] .. ']]'
			end	
		end
	end
	
	if #warnmsg &gt; 0 then
		if frame:preprocess( "{{REVISIONID}}" ) == "" then
			local ptxt = args['template'] and args['template'] .. ' warning' or 'Warning'
			res = '&lt;div class="hatnote" style="color:red"&gt;&lt;strong&gt;' .. ptxt .. ':&lt;/strong&gt; ' .. table.concat(warnmsg, '&lt;br&gt;') .. '&lt;/div&gt;' .. res
		end
	end
	
	return res
end

return p</text>
      <sha1>oa62l8h6k4arzwhdoix5dw61evefwwd</sha1>
    </revision>
    <revision>
      <id>494521</id>
      <timestamp>2021-05-07T15:16:54Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izno</username>
        <id>0</id>
      </contributor>
      <comment>use if preview</comment>
      <origin>494521</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1541" sha1="59n770hna40q9pw4oa0dsp86euaks0u" xml:space="preserve">local p = {}

local function trim(s)
	return s:match('^%s*(.-)%s*$')
end

local function isnotempty(s)
	return s and s:match('%S')
end

function p.check(frame)
	local args = frame.args
	local pargs = frame:getParent().args
	local checknested = isnotempty(args['nested'])
	local delimiter = isnotempty(args['delimiter']) and args['delimiter'] or ';'
	local argpairs = {}
	
	for k, v in pairs(args) do
		if type(k) == 'number' then
			local plist = mw.text.split(v, delimiter)
			local pfound = {}
			local count = 0
			for ii, vv in ipairs(plist) do
				vv = trim(vv)
				if checknested and pargs[vv] or isnotempty(pargs[vv]) then
					count = count + 1
					table.insert(pfound, vv)
				end
			end
			if count &gt; 1 then
				table.insert(argpairs, pfound)
			end
		end
	end
	
	local warnmsg = {}
	local res = ''
	local cat = ''
	if args['cat'] and mw.ustring.match(args['cat'],'^[Cc][Aa][Tt][Ee][Gg][Oo][Rr][Yy]:') then
		cat = args['cat']
	end
	local template = args['template'] and ' in ' .. args['template']  or ''
	if #argpairs &gt; 0 then
		for i, v in ipairs( argpairs ) do
			table.insert(
				warnmsg,
				mw.ustring.format(
					'Using more than one of the following parameters%s: &lt;code&gt;%s&lt;/code&gt;.',
					template,
					table.concat(v, '&lt;/code&gt;, &lt;code&gt;')
				)
			)
			if cat ~= '' then
				res = res .. '[[' .. cat .. '|' .. (v[1] == '' and ' ' or '') .. v[1] .. ']]'
			end	
		end
	end
	
	if #warnmsg &gt; 0 then
		res = require('Module:If preview')._warning({
			table.concat(warnmsg, '&lt;br&gt;')
		}) .. res
	end
	
	return res
end

return p</text>
      <sha1>59n770hna40q9pw4oa0dsp86euaks0u</sha1>
    </revision>
    <revision>
      <id>494522</id>
      <parentid>494521</parentid>
      <timestamp>2024-06-26T02:51:33Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported from [[:wikipedia:Module:Check_for_clobbered_parameters]]</comment>
      <origin>494521</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1541" sha1="59n770hna40q9pw4oa0dsp86euaks0u" xml:space="preserve">local p = {}

local function trim(s)
	return s:match('^%s*(.-)%s*$')
end

local function isnotempty(s)
	return s and s:match('%S')
end

function p.check(frame)
	local args = frame.args
	local pargs = frame:getParent().args
	local checknested = isnotempty(args['nested'])
	local delimiter = isnotempty(args['delimiter']) and args['delimiter'] or ';'
	local argpairs = {}
	
	for k, v in pairs(args) do
		if type(k) == 'number' then
			local plist = mw.text.split(v, delimiter)
			local pfound = {}
			local count = 0
			for ii, vv in ipairs(plist) do
				vv = trim(vv)
				if checknested and pargs[vv] or isnotempty(pargs[vv]) then
					count = count + 1
					table.insert(pfound, vv)
				end
			end
			if count &gt; 1 then
				table.insert(argpairs, pfound)
			end
		end
	end
	
	local warnmsg = {}
	local res = ''
	local cat = ''
	if args['cat'] and mw.ustring.match(args['cat'],'^[Cc][Aa][Tt][Ee][Gg][Oo][Rr][Yy]:') then
		cat = args['cat']
	end
	local template = args['template'] and ' in ' .. args['template']  or ''
	if #argpairs &gt; 0 then
		for i, v in ipairs( argpairs ) do
			table.insert(
				warnmsg,
				mw.ustring.format(
					'Using more than one of the following parameters%s: &lt;code&gt;%s&lt;/code&gt;.',
					template,
					table.concat(v, '&lt;/code&gt;, &lt;code&gt;')
				)
			)
			if cat ~= '' then
				res = res .. '[[' .. cat .. '|' .. (v[1] == '' and ' ' or '') .. v[1] .. ']]'
			end	
		end
	end
	
	if #warnmsg &gt; 0 then
		res = require('Module:If preview')._warning({
			table.concat(warnmsg, '&lt;br&gt;')
		}) .. res
	end
	
	return res
end

return p</text>
      <sha1>59n770hna40q9pw4oa0dsp86euaks0u</sha1>
    </revision>
    <revision>
      <id>494523</id>
      <parentid>494522</parentid>
      <timestamp>2025-03-15T19:25:54Z</timestamp>
      <contributor>
        <username>en&gt;Unknown user</username>
        <id>0</id>
      </contributor>
      <comment>use if preview</comment>
      <origin>494521</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1541" sha1="59n770hna40q9pw4oa0dsp86euaks0u" xml:space="preserve">local p = {}

local function trim(s)
	return s:match('^%s*(.-)%s*$')
end

local function isnotempty(s)
	return s and s:match('%S')
end

function p.check(frame)
	local args = frame.args
	local pargs = frame:getParent().args
	local checknested = isnotempty(args['nested'])
	local delimiter = isnotempty(args['delimiter']) and args['delimiter'] or ';'
	local argpairs = {}
	
	for k, v in pairs(args) do
		if type(k) == 'number' then
			local plist = mw.text.split(v, delimiter)
			local pfound = {}
			local count = 0
			for ii, vv in ipairs(plist) do
				vv = trim(vv)
				if checknested and pargs[vv] or isnotempty(pargs[vv]) then
					count = count + 1
					table.insert(pfound, vv)
				end
			end
			if count &gt; 1 then
				table.insert(argpairs, pfound)
			end
		end
	end
	
	local warnmsg = {}
	local res = ''
	local cat = ''
	if args['cat'] and mw.ustring.match(args['cat'],'^[Cc][Aa][Tt][Ee][Gg][Oo][Rr][Yy]:') then
		cat = args['cat']
	end
	local template = args['template'] and ' in ' .. args['template']  or ''
	if #argpairs &gt; 0 then
		for i, v in ipairs( argpairs ) do
			table.insert(
				warnmsg,
				mw.ustring.format(
					'Using more than one of the following parameters%s: &lt;code&gt;%s&lt;/code&gt;.',
					template,
					table.concat(v, '&lt;/code&gt;, &lt;code&gt;')
				)
			)
			if cat ~= '' then
				res = res .. '[[' .. cat .. '|' .. (v[1] == '' and ' ' or '') .. v[1] .. ']]'
			end	
		end
	end
	
	if #warnmsg &gt; 0 then
		res = require('Module:If preview')._warning({
			table.concat(warnmsg, '&lt;br&gt;')
		}) .. res
	end
	
	return res
end

return p</text>
      <sha1>59n770hna40q9pw4oa0dsp86euaks0u</sha1>
    </revision>
    <revision>
      <id>475</id>
      <timestamp>2025-03-25T05:23:37Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was 10 revisions imported from [[:wikipedia:Module:Check_for_clobbered_parameters]]</comment>
      <origin>475</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1541" sha1="59n770hna40q9pw4oa0dsp86euaks0u" xml:space="preserve">local p = {}

local function trim(s)
	return s:match('^%s*(.-)%s*$')
end

local function isnotempty(s)
	return s and s:match('%S')
end

function p.check(frame)
	local args = frame.args
	local pargs = frame:getParent().args
	local checknested = isnotempty(args['nested'])
	local delimiter = isnotempty(args['delimiter']) and args['delimiter'] or ';'
	local argpairs = {}
	
	for k, v in pairs(args) do
		if type(k) == 'number' then
			local plist = mw.text.split(v, delimiter)
			local pfound = {}
			local count = 0
			for ii, vv in ipairs(plist) do
				vv = trim(vv)
				if checknested and pargs[vv] or isnotempty(pargs[vv]) then
					count = count + 1
					table.insert(pfound, vv)
				end
			end
			if count &gt; 1 then
				table.insert(argpairs, pfound)
			end
		end
	end
	
	local warnmsg = {}
	local res = ''
	local cat = ''
	if args['cat'] and mw.ustring.match(args['cat'],'^[Cc][Aa][Tt][Ee][Gg][Oo][Rr][Yy]:') then
		cat = args['cat']
	end
	local template = args['template'] and ' in ' .. args['template']  or ''
	if #argpairs &gt; 0 then
		for i, v in ipairs( argpairs ) do
			table.insert(
				warnmsg,
				mw.ustring.format(
					'Using more than one of the following parameters%s: &lt;code&gt;%s&lt;/code&gt;.',
					template,
					table.concat(v, '&lt;/code&gt;, &lt;code&gt;')
				)
			)
			if cat ~= '' then
				res = res .. '[[' .. cat .. '|' .. (v[1] == '' and ' ' or '') .. v[1] .. ']]'
			end	
		end
	end
	
	if #warnmsg &gt; 0 then
		res = require('Module:If preview')._warning({
			table.concat(warnmsg, '&lt;br&gt;')
		}) .. res
	end
	
	return res
end

return p</text>
      <sha1>59n770hna40q9pw4oa0dsp86euaks0u</sha1>
    </revision>
  </page>
</mediawiki>
