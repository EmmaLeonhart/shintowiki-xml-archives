<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Module:RfD</title>
    <ns>828</ns>
    <id>17350</id>
    <revision>
      <id>1165384</id>
      <timestamp>2025-04-27T22:11:26Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>NOINDEX only if actually displaying the RfD notice</comment>
      <origin>1165384</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="8418" sha1="oxxtlhxa8rn1n3a7oqgwnbfa56oxuq6" xml:space="preserve">require('strict')

local p = {}

local getTargetFromText = require('Module:Redirect').getTargetFromText
local messageBox

local mboxText = "'''The purpose of this redirect is currently being discussed by the Wikipedia community."
	.. " The outcome of the discussion may result in a change of this page, or possibly its deletion in accordance with Wikipedia's [[Wikipedia:Deletion policy|deletion policy]].''' &lt;br /&gt;"
	.. " Please share your thoughts on the matter at '''[[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|this redirect's entry]]''' on the [[Wikipedia:Redirects for discussion|redirects for discussion]] page.&lt;br /&gt;"
	.. "  '''Click on the link below''' to go to the current destination page.&lt;br /&gt;"
	.. "&lt;small&gt;Please notify the good-faith creator and any main contributors of the redirect by placing &lt;code&gt;&amp;#123;&amp;#123;[[Wikipedia:Substitution|subst]]:[[Template:Rfd notice|Rfd notice]]&amp;#124;%s&amp;#125;&amp;#125; &amp;#126;&amp;#126;&amp;#126;&amp;#126;&lt;/code&gt; on their talk page.&lt;/small&gt;__EXPECTUNUSEDTEMPLATE__"
local deleteReason = '[[Wikipedia:Redirects for discussion]] debate closed as delete'
local messageOnTransclusionsStandard = '&amp;lsaquo;The template below is included via a redirect'
    .. ' (%s) that is under discussion. See [[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|redirects for discussion]]'
    .. ' to help reach a consensus.&amp;rsaquo;'
local messageOnTransclusionsTiny = '&amp;lsaquo;[[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|See RfD]]&amp;rsaquo;'

local function makeTransclusionTag(args, redirect)
	local discussionPage = args[1] and mw.text.trim(args[1])
	if not discussionPage or discussionPage == '' then
		discussionPage = redirect
	end
	local visClasses = {
		['autoconfirmed']='autoconfirmed-show',
		['extendedconfirmed']='extendedconfirmed-show sysop-show'
	} -- More can be added if justified by situation
	local sizeAttrs = {
		['standard'] = {
			['tags'] = {'&lt;div ', '&lt;/div&gt;'},
			['style'] = ' border-bottom: 1px solid #AAAAAA;',
			['message'] = messageOnTransclusionsStandard:format(redirect, args.year, args.month, args.day, discussionPage)
		},
		['tiny'] = {
			['tags'] = {'&lt;span ', '&lt;/span&gt; '},
			['style'] = '',
			['message'] = messageOnTransclusionsTiny:format(args.year, args.month, args.day, discussionPage)
		},
	}
	local size = sizeAttrs[args.showontransclusion] and args.showontransclusion or 'standard'
	local tags = sizeAttrs[size]['tags']
	local visibility = visClasses[args.transclusiontagvisibility] or ''
	local classes = 'class="boilerplate metadata plainlinks ' .. visibility .. '" '
	local id = 'id="rfd-t" '
	local style = 'style="font-size: 85%; text-align: left;' .. sizeAttrs[size]['style'] .. '" '
	return tags[1] .. classes .. id .. style .. '&gt;' .. sizeAttrs[size]['message'] .. tags[2]
end

local function makeRfdNotice(args)
	local currentTitle = mw.title.getCurrentTitle()
	if not messageBox then
		messageBox = require('Module:Message box')
	end
	local discussionPage = args[1] and mw.text.trim(args[1])
	if discussionPage == '' then
		discussionPage = nil
	end
	local target = getTargetFromText(args.content)
	local isError = not target or not mw.title.new(target)
	local category
	if args.category then
		category = args.category
	elseif args.timestop then
                -- Extract stable year and month from timestop; args.month and args.year can change if the discussion is relisted (see [[Special:Diff/896302321]])
                local lang = mw.language.getContentLanguage()
                local catMonth = lang:formatDate('F', args.timestop)
                local catYear = lang:formatDate('Y', args.timestop)
		category = string.format('[[Category:Redirects for discussion from %s %s|%s]][[Category:All redirects for discussion|%s]]', catMonth, catYear, currentTitle.text, currentTitle.text)
	else
		category = string.format('[[Category:Redirects for discussion|%s]][[Category:All redirects for discussion|%s]]', currentTitle.text, currentTitle.text)
	end
	if category then category = category..'[[Category:Temporary maintenance holdings/RFD]]' end
	local noindex = mw.getCurrentFrame():expandTemplate{title="NOINDEX"}
	return string.format('%s%s&lt;span id="delete-reason" style="display: none;"&gt;%s&lt;/span&gt;%s%s',
		noindex,
		messageBox.main('mbox', {
			type = 'delete',
			name = 'RfD',
			image = 'none',
			text = string.format(mboxText, args.year, args.month, args.day, discussionPage or currentTitle.prefixedText, mw.text.nowiki(currentTitle.prefixedText))
		}),
		mw.uri.encode(deleteReason),
		category,
		isError and '[[Category:RfD errors]]' or ''
	)
end

p[''] = function(frame)
	local args = frame.args
	if not args.content or mw.text.trim(args.content) == '' then
		return '&lt;span class="error"&gt;Error: No content was provided. The original text of the page (the #REDIRECT line and any templates) must be placed inside of the content parameter.[[Category:RfD errors]]&lt;/span&gt;'
	end
	local pframe = frame:getParent()
	if pframe:preprocess('&lt;includeonly&gt;1&lt;/includeonly&gt;') == '1' then
		-- We're being transcluded, so display the content of our target.
		local target = getTargetFromText(args.content)
		if target then
			target = mw.title.new(target)
		end
		local redirect = pframe:getTitle()
		if target and not target.isRedirect and target ~= redirect then
			-- We should actually be calling expandTemplate on the grandparent rather than on the parent, but we can't do that yet
			-- Since we don't have grandparent access, though, it means the thing we're calling doesn't either, so it doesn't really matter yet
			local parsedTarget = pframe:expandTemplate{title = ':' .. target.prefixedText, args = pframe.args}
			local showontransclusion = frame.args.showontransclusion
			showontransclusion = require('Module:Yesno')(showontransclusion, showontransclusion)
			if showontransclusion and not (showontransclusion == '' or mw.isSubsting()) then
				return makeTransclusionTag(args, redirect) .. parsedTarget
			end
			return parsedTarget
		end
	end
	-- We're not being transcluded, or we can't figure out how to display our target.
	-- Check if we can find the target.
	local target = getTargetFromText(args.content)
	messageBox = require('Module:Message box')
	local redirbox
	if target then -- target isn't nil
		local firstChar = string.sub(target, 1, 1)
		if firstChar ~= ":" then
			target = ":" .. target
		end
		redirbox = messageBox.main('mbox', {
			type = 'notice',
			name = 'RedirectBox',
			image = '[[File:Symbol redirect blue.svg|40px]]',
			text = "'''This title is currently a redirect ''' to [[" .. target .. "]]; click there to go to the current target. The full content of this redirect page, including all [[Wikipedia:Categorizing redirects|redirect categories]], is displayed below."
		})
	else
		redirbox = messageBox.main('mbox', {
			type = 'notice',
			name = 'RedirectBox',
			image = '[[File:Symbol redirect vote 4.svg|40px]]',
			text = "'''This redirect's target could not be determined.''' This page may be a [[Wikipedia:Soft redirect|soft redirect]], or not a redirect at all. The content of the page is displayed below."
		})
	end
	-- Display the RfD banner.
	return makeRfdNotice(frame.args) .. '\n' .. redirbox .. frame.args.content
end

local substText = "{{&lt;includeonly&gt;safesubst:&lt;/includeonly&gt;#invoke:RfD||%s%s|%s%s\n"
	.. "&lt;!-- The above content is generated by {{subst:Rfd}}. --&gt;\n&lt;!-- End of RFD message. Don't edit anything above here. Feel free to edit below here, but do NOT change the redirect's target. --&gt;|content=\n%s\n"
	.. "&lt;!-- Don't add anything after this line unless you're drafting a disambiguation page or article to replace the redirect. --&gt;\n}}"
local dateText = 'month = %B\n|day = %e\n|year = %Y\n|time = %R\n|timestop = %Y%m%d%H%M%S'

-- called during subst when the template is initially placed on the page
function p.main(frame)
	local pframe = frame:getParent()
	local pargs = pframe.args
	local Date
	if pargs.days then
		Date = os.date(dateText, os.time() - 86400*pargs.days)
	else
		Date = os.date(dateText)
	end
	local retval = string.format(substText, pargs.FULLPAGENAME or pargs[1] or '', pargs.showontransclusion and '|showontransclusion=1' or '', Date, pframe:getTitle() == mw.title.getCurrentTitle().prefixedText and '|category=' or '', pargs.content or '')
	if mw.isSubsting() then
		return retval
	else
		return frame:expandTemplate{title = 'Template:Error:must be substituted', args = {'rfd'}} .. frame:preprocess(retval)
	end
end

return p</text>
      <sha1>oxxtlhxa8rn1n3a7oqgwnbfa56oxuq6</sha1>
    </revision>
    <revision>
      <id>1165385</id>
      <parentid>1165384</parentid>
      <timestamp>2025-04-27T22:11:27Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: chat is this real</comment>
      <origin>1165384</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="8418" sha1="oxxtlhxa8rn1n3a7oqgwnbfa56oxuq6" xml:space="preserve">require('strict')

local p = {}

local getTargetFromText = require('Module:Redirect').getTargetFromText
local messageBox

local mboxText = "'''The purpose of this redirect is currently being discussed by the Wikipedia community."
	.. " The outcome of the discussion may result in a change of this page, or possibly its deletion in accordance with Wikipedia's [[Wikipedia:Deletion policy|deletion policy]].''' &lt;br /&gt;"
	.. " Please share your thoughts on the matter at '''[[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|this redirect's entry]]''' on the [[Wikipedia:Redirects for discussion|redirects for discussion]] page.&lt;br /&gt;"
	.. "  '''Click on the link below''' to go to the current destination page.&lt;br /&gt;"
	.. "&lt;small&gt;Please notify the good-faith creator and any main contributors of the redirect by placing &lt;code&gt;&amp;#123;&amp;#123;[[Wikipedia:Substitution|subst]]:[[Template:Rfd notice|Rfd notice]]&amp;#124;%s&amp;#125;&amp;#125; &amp;#126;&amp;#126;&amp;#126;&amp;#126;&lt;/code&gt; on their talk page.&lt;/small&gt;__EXPECTUNUSEDTEMPLATE__"
local deleteReason = '[[Wikipedia:Redirects for discussion]] debate closed as delete'
local messageOnTransclusionsStandard = '&amp;lsaquo;The template below is included via a redirect'
    .. ' (%s) that is under discussion. See [[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|redirects for discussion]]'
    .. ' to help reach a consensus.&amp;rsaquo;'
local messageOnTransclusionsTiny = '&amp;lsaquo;[[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|See RfD]]&amp;rsaquo;'

local function makeTransclusionTag(args, redirect)
	local discussionPage = args[1] and mw.text.trim(args[1])
	if not discussionPage or discussionPage == '' then
		discussionPage = redirect
	end
	local visClasses = {
		['autoconfirmed']='autoconfirmed-show',
		['extendedconfirmed']='extendedconfirmed-show sysop-show'
	} -- More can be added if justified by situation
	local sizeAttrs = {
		['standard'] = {
			['tags'] = {'&lt;div ', '&lt;/div&gt;'},
			['style'] = ' border-bottom: 1px solid #AAAAAA;',
			['message'] = messageOnTransclusionsStandard:format(redirect, args.year, args.month, args.day, discussionPage)
		},
		['tiny'] = {
			['tags'] = {'&lt;span ', '&lt;/span&gt; '},
			['style'] = '',
			['message'] = messageOnTransclusionsTiny:format(args.year, args.month, args.day, discussionPage)
		},
	}
	local size = sizeAttrs[args.showontransclusion] and args.showontransclusion or 'standard'
	local tags = sizeAttrs[size]['tags']
	local visibility = visClasses[args.transclusiontagvisibility] or ''
	local classes = 'class="boilerplate metadata plainlinks ' .. visibility .. '" '
	local id = 'id="rfd-t" '
	local style = 'style="font-size: 85%; text-align: left;' .. sizeAttrs[size]['style'] .. '" '
	return tags[1] .. classes .. id .. style .. '&gt;' .. sizeAttrs[size]['message'] .. tags[2]
end

local function makeRfdNotice(args)
	local currentTitle = mw.title.getCurrentTitle()
	if not messageBox then
		messageBox = require('Module:Message box')
	end
	local discussionPage = args[1] and mw.text.trim(args[1])
	if discussionPage == '' then
		discussionPage = nil
	end
	local target = getTargetFromText(args.content)
	local isError = not target or not mw.title.new(target)
	local category
	if args.category then
		category = args.category
	elseif args.timestop then
                -- Extract stable year and month from timestop; args.month and args.year can change if the discussion is relisted (see [[Special:Diff/896302321]])
                local lang = mw.language.getContentLanguage()
                local catMonth = lang:formatDate('F', args.timestop)
                local catYear = lang:formatDate('Y', args.timestop)
		category = string.format('[[Category:Redirects for discussion from %s %s|%s]][[Category:All redirects for discussion|%s]]', catMonth, catYear, currentTitle.text, currentTitle.text)
	else
		category = string.format('[[Category:Redirects for discussion|%s]][[Category:All redirects for discussion|%s]]', currentTitle.text, currentTitle.text)
	end
	if category then category = category..'[[Category:Temporary maintenance holdings/RFD]]' end
	local noindex = mw.getCurrentFrame():expandTemplate{title="NOINDEX"}
	return string.format('%s%s&lt;span id="delete-reason" style="display: none;"&gt;%s&lt;/span&gt;%s%s',
		noindex,
		messageBox.main('mbox', {
			type = 'delete',
			name = 'RfD',
			image = 'none',
			text = string.format(mboxText, args.year, args.month, args.day, discussionPage or currentTitle.prefixedText, mw.text.nowiki(currentTitle.prefixedText))
		}),
		mw.uri.encode(deleteReason),
		category,
		isError and '[[Category:RfD errors]]' or ''
	)
end

p[''] = function(frame)
	local args = frame.args
	if not args.content or mw.text.trim(args.content) == '' then
		return '&lt;span class="error"&gt;Error: No content was provided. The original text of the page (the #REDIRECT line and any templates) must be placed inside of the content parameter.[[Category:RfD errors]]&lt;/span&gt;'
	end
	local pframe = frame:getParent()
	if pframe:preprocess('&lt;includeonly&gt;1&lt;/includeonly&gt;') == '1' then
		-- We're being transcluded, so display the content of our target.
		local target = getTargetFromText(args.content)
		if target then
			target = mw.title.new(target)
		end
		local redirect = pframe:getTitle()
		if target and not target.isRedirect and target ~= redirect then
			-- We should actually be calling expandTemplate on the grandparent rather than on the parent, but we can't do that yet
			-- Since we don't have grandparent access, though, it means the thing we're calling doesn't either, so it doesn't really matter yet
			local parsedTarget = pframe:expandTemplate{title = ':' .. target.prefixedText, args = pframe.args}
			local showontransclusion = frame.args.showontransclusion
			showontransclusion = require('Module:Yesno')(showontransclusion, showontransclusion)
			if showontransclusion and not (showontransclusion == '' or mw.isSubsting()) then
				return makeTransclusionTag(args, redirect) .. parsedTarget
			end
			return parsedTarget
		end
	end
	-- We're not being transcluded, or we can't figure out how to display our target.
	-- Check if we can find the target.
	local target = getTargetFromText(args.content)
	messageBox = require('Module:Message box')
	local redirbox
	if target then -- target isn't nil
		local firstChar = string.sub(target, 1, 1)
		if firstChar ~= ":" then
			target = ":" .. target
		end
		redirbox = messageBox.main('mbox', {
			type = 'notice',
			name = 'RedirectBox',
			image = '[[File:Symbol redirect blue.svg|40px]]',
			text = "'''This title is currently a redirect ''' to [[" .. target .. "]]; click there to go to the current target. The full content of this redirect page, including all [[Wikipedia:Categorizing redirects|redirect categories]], is displayed below."
		})
	else
		redirbox = messageBox.main('mbox', {
			type = 'notice',
			name = 'RedirectBox',
			image = '[[File:Symbol redirect vote 4.svg|40px]]',
			text = "'''This redirect's target could not be determined.''' This page may be a [[Wikipedia:Soft redirect|soft redirect]], or not a redirect at all. The content of the page is displayed below."
		})
	end
	-- Display the RfD banner.
	return makeRfdNotice(frame.args) .. '\n' .. redirbox .. frame.args.content
end

local substText = "{{&lt;includeonly&gt;safesubst:&lt;/includeonly&gt;#invoke:RfD||%s%s|%s%s\n"
	.. "&lt;!-- The above content is generated by {{subst:Rfd}}. --&gt;\n&lt;!-- End of RFD message. Don't edit anything above here. Feel free to edit below here, but do NOT change the redirect's target. --&gt;|content=\n%s\n"
	.. "&lt;!-- Don't add anything after this line unless you're drafting a disambiguation page or article to replace the redirect. --&gt;\n}}"
local dateText = 'month = %B\n|day = %e\n|year = %Y\n|time = %R\n|timestop = %Y%m%d%H%M%S'

-- called during subst when the template is initially placed on the page
function p.main(frame)
	local pframe = frame:getParent()
	local pargs = pframe.args
	local Date
	if pargs.days then
		Date = os.date(dateText, os.time() - 86400*pargs.days)
	else
		Date = os.date(dateText)
	end
	local retval = string.format(substText, pargs.FULLPAGENAME or pargs[1] or '', pargs.showontransclusion and '|showontransclusion=1' or '', Date, pframe:getTitle() == mw.title.getCurrentTitle().prefixedText and '|category=' or '', pargs.content or '')
	if mw.isSubsting() then
		return retval
	else
		return frame:expandTemplate{title = 'Template:Error:must be substituted', args = {'rfd'}} .. frame:preprocess(retval)
	end
end

return p</text>
      <sha1>oxxtlhxa8rn1n3a7oqgwnbfa56oxuq6</sha1>
    </revision>
    <revision>
      <id>1165386</id>
      <parentid>1165385</parentid>
      <timestamp>2025-04-27T22:12:49Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>NOINDEX only if actually displaying the RfD notice</comment>
      <origin>1165384</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="8418" sha1="oxxtlhxa8rn1n3a7oqgwnbfa56oxuq6" xml:space="preserve">require('strict')

local p = {}

local getTargetFromText = require('Module:Redirect').getTargetFromText
local messageBox

local mboxText = "'''The purpose of this redirect is currently being discussed by the Wikipedia community."
	.. " The outcome of the discussion may result in a change of this page, or possibly its deletion in accordance with Wikipedia's [[Wikipedia:Deletion policy|deletion policy]].''' &lt;br /&gt;"
	.. " Please share your thoughts on the matter at '''[[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|this redirect's entry]]''' on the [[Wikipedia:Redirects for discussion|redirects for discussion]] page.&lt;br /&gt;"
	.. "  '''Click on the link below''' to go to the current destination page.&lt;br /&gt;"
	.. "&lt;small&gt;Please notify the good-faith creator and any main contributors of the redirect by placing &lt;code&gt;&amp;#123;&amp;#123;[[Wikipedia:Substitution|subst]]:[[Template:Rfd notice|Rfd notice]]&amp;#124;%s&amp;#125;&amp;#125; &amp;#126;&amp;#126;&amp;#126;&amp;#126;&lt;/code&gt; on their talk page.&lt;/small&gt;__EXPECTUNUSEDTEMPLATE__"
local deleteReason = '[[Wikipedia:Redirects for discussion]] debate closed as delete'
local messageOnTransclusionsStandard = '&amp;lsaquo;The template below is included via a redirect'
    .. ' (%s) that is under discussion. See [[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|redirects for discussion]]'
    .. ' to help reach a consensus.&amp;rsaquo;'
local messageOnTransclusionsTiny = '&amp;lsaquo;[[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|See RfD]]&amp;rsaquo;'

local function makeTransclusionTag(args, redirect)
	local discussionPage = args[1] and mw.text.trim(args[1])
	if not discussionPage or discussionPage == '' then
		discussionPage = redirect
	end
	local visClasses = {
		['autoconfirmed']='autoconfirmed-show',
		['extendedconfirmed']='extendedconfirmed-show sysop-show'
	} -- More can be added if justified by situation
	local sizeAttrs = {
		['standard'] = {
			['tags'] = {'&lt;div ', '&lt;/div&gt;'},
			['style'] = ' border-bottom: 1px solid #AAAAAA;',
			['message'] = messageOnTransclusionsStandard:format(redirect, args.year, args.month, args.day, discussionPage)
		},
		['tiny'] = {
			['tags'] = {'&lt;span ', '&lt;/span&gt; '},
			['style'] = '',
			['message'] = messageOnTransclusionsTiny:format(args.year, args.month, args.day, discussionPage)
		},
	}
	local size = sizeAttrs[args.showontransclusion] and args.showontransclusion or 'standard'
	local tags = sizeAttrs[size]['tags']
	local visibility = visClasses[args.transclusiontagvisibility] or ''
	local classes = 'class="boilerplate metadata plainlinks ' .. visibility .. '" '
	local id = 'id="rfd-t" '
	local style = 'style="font-size: 85%; text-align: left;' .. sizeAttrs[size]['style'] .. '" '
	return tags[1] .. classes .. id .. style .. '&gt;' .. sizeAttrs[size]['message'] .. tags[2]
end

local function makeRfdNotice(args)
	local currentTitle = mw.title.getCurrentTitle()
	if not messageBox then
		messageBox = require('Module:Message box')
	end
	local discussionPage = args[1] and mw.text.trim(args[1])
	if discussionPage == '' then
		discussionPage = nil
	end
	local target = getTargetFromText(args.content)
	local isError = not target or not mw.title.new(target)
	local category
	if args.category then
		category = args.category
	elseif args.timestop then
                -- Extract stable year and month from timestop; args.month and args.year can change if the discussion is relisted (see [[Special:Diff/896302321]])
                local lang = mw.language.getContentLanguage()
                local catMonth = lang:formatDate('F', args.timestop)
                local catYear = lang:formatDate('Y', args.timestop)
		category = string.format('[[Category:Redirects for discussion from %s %s|%s]][[Category:All redirects for discussion|%s]]', catMonth, catYear, currentTitle.text, currentTitle.text)
	else
		category = string.format('[[Category:Redirects for discussion|%s]][[Category:All redirects for discussion|%s]]', currentTitle.text, currentTitle.text)
	end
	if category then category = category..'[[Category:Temporary maintenance holdings/RFD]]' end
	local noindex = mw.getCurrentFrame():expandTemplate{title="NOINDEX"}
	return string.format('%s%s&lt;span id="delete-reason" style="display: none;"&gt;%s&lt;/span&gt;%s%s',
		noindex,
		messageBox.main('mbox', {
			type = 'delete',
			name = 'RfD',
			image = 'none',
			text = string.format(mboxText, args.year, args.month, args.day, discussionPage or currentTitle.prefixedText, mw.text.nowiki(currentTitle.prefixedText))
		}),
		mw.uri.encode(deleteReason),
		category,
		isError and '[[Category:RfD errors]]' or ''
	)
end

p[''] = function(frame)
	local args = frame.args
	if not args.content or mw.text.trim(args.content) == '' then
		return '&lt;span class="error"&gt;Error: No content was provided. The original text of the page (the #REDIRECT line and any templates) must be placed inside of the content parameter.[[Category:RfD errors]]&lt;/span&gt;'
	end
	local pframe = frame:getParent()
	if pframe:preprocess('&lt;includeonly&gt;1&lt;/includeonly&gt;') == '1' then
		-- We're being transcluded, so display the content of our target.
		local target = getTargetFromText(args.content)
		if target then
			target = mw.title.new(target)
		end
		local redirect = pframe:getTitle()
		if target and not target.isRedirect and target ~= redirect then
			-- We should actually be calling expandTemplate on the grandparent rather than on the parent, but we can't do that yet
			-- Since we don't have grandparent access, though, it means the thing we're calling doesn't either, so it doesn't really matter yet
			local parsedTarget = pframe:expandTemplate{title = ':' .. target.prefixedText, args = pframe.args}
			local showontransclusion = frame.args.showontransclusion
			showontransclusion = require('Module:Yesno')(showontransclusion, showontransclusion)
			if showontransclusion and not (showontransclusion == '' or mw.isSubsting()) then
				return makeTransclusionTag(args, redirect) .. parsedTarget
			end
			return parsedTarget
		end
	end
	-- We're not being transcluded, or we can't figure out how to display our target.
	-- Check if we can find the target.
	local target = getTargetFromText(args.content)
	messageBox = require('Module:Message box')
	local redirbox
	if target then -- target isn't nil
		local firstChar = string.sub(target, 1, 1)
		if firstChar ~= ":" then
			target = ":" .. target
		end
		redirbox = messageBox.main('mbox', {
			type = 'notice',
			name = 'RedirectBox',
			image = '[[File:Symbol redirect blue.svg|40px]]',
			text = "'''This title is currently a redirect ''' to [[" .. target .. "]]; click there to go to the current target. The full content of this redirect page, including all [[Wikipedia:Categorizing redirects|redirect categories]], is displayed below."
		})
	else
		redirbox = messageBox.main('mbox', {
			type = 'notice',
			name = 'RedirectBox',
			image = '[[File:Symbol redirect vote 4.svg|40px]]',
			text = "'''This redirect's target could not be determined.''' This page may be a [[Wikipedia:Soft redirect|soft redirect]], or not a redirect at all. The content of the page is displayed below."
		})
	end
	-- Display the RfD banner.
	return makeRfdNotice(frame.args) .. '\n' .. redirbox .. frame.args.content
end

local substText = "{{&lt;includeonly&gt;safesubst:&lt;/includeonly&gt;#invoke:RfD||%s%s|%s%s\n"
	.. "&lt;!-- The above content is generated by {{subst:Rfd}}. --&gt;\n&lt;!-- End of RFD message. Don't edit anything above here. Feel free to edit below here, but do NOT change the redirect's target. --&gt;|content=\n%s\n"
	.. "&lt;!-- Don't add anything after this line unless you're drafting a disambiguation page or article to replace the redirect. --&gt;\n}}"
local dateText = 'month = %B\n|day = %e\n|year = %Y\n|time = %R\n|timestop = %Y%m%d%H%M%S'

-- called during subst when the template is initially placed on the page
function p.main(frame)
	local pframe = frame:getParent()
	local pargs = pframe.args
	local Date
	if pargs.days then
		Date = os.date(dateText, os.time() - 86400*pargs.days)
	else
		Date = os.date(dateText)
	end
	local retval = string.format(substText, pargs.FULLPAGENAME or pargs[1] or '', pargs.showontransclusion and '|showontransclusion=1' or '', Date, pframe:getTitle() == mw.title.getCurrentTitle().prefixedText and '|category=' or '', pargs.content or '')
	if mw.isSubsting() then
		return retval
	else
		return frame:expandTemplate{title = 'Template:Error:must be substituted', args = {'rfd'}} .. frame:preprocess(retval)
	end
end

return p</text>
      <sha1>oxxtlhxa8rn1n3a7oqgwnbfa56oxuq6</sha1>
    </revision>
    <revision>
      <id>1165387</id>
      <parentid>1165386</parentid>
      <timestamp>2025-04-27T22:13:44Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>NOINDEX only if actually displaying the RfD notice</comment>
      <origin>1165384</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="8418" sha1="oxxtlhxa8rn1n3a7oqgwnbfa56oxuq6" xml:space="preserve">require('strict')

local p = {}

local getTargetFromText = require('Module:Redirect').getTargetFromText
local messageBox

local mboxText = "'''The purpose of this redirect is currently being discussed by the Wikipedia community."
	.. " The outcome of the discussion may result in a change of this page, or possibly its deletion in accordance with Wikipedia's [[Wikipedia:Deletion policy|deletion policy]].''' &lt;br /&gt;"
	.. " Please share your thoughts on the matter at '''[[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|this redirect's entry]]''' on the [[Wikipedia:Redirects for discussion|redirects for discussion]] page.&lt;br /&gt;"
	.. "  '''Click on the link below''' to go to the current destination page.&lt;br /&gt;"
	.. "&lt;small&gt;Please notify the good-faith creator and any main contributors of the redirect by placing &lt;code&gt;&amp;#123;&amp;#123;[[Wikipedia:Substitution|subst]]:[[Template:Rfd notice|Rfd notice]]&amp;#124;%s&amp;#125;&amp;#125; &amp;#126;&amp;#126;&amp;#126;&amp;#126;&lt;/code&gt; on their talk page.&lt;/small&gt;__EXPECTUNUSEDTEMPLATE__"
local deleteReason = '[[Wikipedia:Redirects for discussion]] debate closed as delete'
local messageOnTransclusionsStandard = '&amp;lsaquo;The template below is included via a redirect'
    .. ' (%s) that is under discussion. See [[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|redirects for discussion]]'
    .. ' to help reach a consensus.&amp;rsaquo;'
local messageOnTransclusionsTiny = '&amp;lsaquo;[[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|See RfD]]&amp;rsaquo;'

local function makeTransclusionTag(args, redirect)
	local discussionPage = args[1] and mw.text.trim(args[1])
	if not discussionPage or discussionPage == '' then
		discussionPage = redirect
	end
	local visClasses = {
		['autoconfirmed']='autoconfirmed-show',
		['extendedconfirmed']='extendedconfirmed-show sysop-show'
	} -- More can be added if justified by situation
	local sizeAttrs = {
		['standard'] = {
			['tags'] = {'&lt;div ', '&lt;/div&gt;'},
			['style'] = ' border-bottom: 1px solid #AAAAAA;',
			['message'] = messageOnTransclusionsStandard:format(redirect, args.year, args.month, args.day, discussionPage)
		},
		['tiny'] = {
			['tags'] = {'&lt;span ', '&lt;/span&gt; '},
			['style'] = '',
			['message'] = messageOnTransclusionsTiny:format(args.year, args.month, args.day, discussionPage)
		},
	}
	local size = sizeAttrs[args.showontransclusion] and args.showontransclusion or 'standard'
	local tags = sizeAttrs[size]['tags']
	local visibility = visClasses[args.transclusiontagvisibility] or ''
	local classes = 'class="boilerplate metadata plainlinks ' .. visibility .. '" '
	local id = 'id="rfd-t" '
	local style = 'style="font-size: 85%; text-align: left;' .. sizeAttrs[size]['style'] .. '" '
	return tags[1] .. classes .. id .. style .. '&gt;' .. sizeAttrs[size]['message'] .. tags[2]
end

local function makeRfdNotice(args)
	local currentTitle = mw.title.getCurrentTitle()
	if not messageBox then
		messageBox = require('Module:Message box')
	end
	local discussionPage = args[1] and mw.text.trim(args[1])
	if discussionPage == '' then
		discussionPage = nil
	end
	local target = getTargetFromText(args.content)
	local isError = not target or not mw.title.new(target)
	local category
	if args.category then
		category = args.category
	elseif args.timestop then
                -- Extract stable year and month from timestop; args.month and args.year can change if the discussion is relisted (see [[Special:Diff/896302321]])
                local lang = mw.language.getContentLanguage()
                local catMonth = lang:formatDate('F', args.timestop)
                local catYear = lang:formatDate('Y', args.timestop)
		category = string.format('[[Category:Redirects for discussion from %s %s|%s]][[Category:All redirects for discussion|%s]]', catMonth, catYear, currentTitle.text, currentTitle.text)
	else
		category = string.format('[[Category:Redirects for discussion|%s]][[Category:All redirects for discussion|%s]]', currentTitle.text, currentTitle.text)
	end
	if category then category = category..'[[Category:Temporary maintenance holdings/RFD]]' end
	local noindex = mw.getCurrentFrame():expandTemplate{title="NOINDEX"}
	return string.format('%s%s&lt;span id="delete-reason" style="display: none;"&gt;%s&lt;/span&gt;%s%s',
		noindex,
		messageBox.main('mbox', {
			type = 'delete',
			name = 'RfD',
			image = 'none',
			text = string.format(mboxText, args.year, args.month, args.day, discussionPage or currentTitle.prefixedText, mw.text.nowiki(currentTitle.prefixedText))
		}),
		mw.uri.encode(deleteReason),
		category,
		isError and '[[Category:RfD errors]]' or ''
	)
end

p[''] = function(frame)
	local args = frame.args
	if not args.content or mw.text.trim(args.content) == '' then
		return '&lt;span class="error"&gt;Error: No content was provided. The original text of the page (the #REDIRECT line and any templates) must be placed inside of the content parameter.[[Category:RfD errors]]&lt;/span&gt;'
	end
	local pframe = frame:getParent()
	if pframe:preprocess('&lt;includeonly&gt;1&lt;/includeonly&gt;') == '1' then
		-- We're being transcluded, so display the content of our target.
		local target = getTargetFromText(args.content)
		if target then
			target = mw.title.new(target)
		end
		local redirect = pframe:getTitle()
		if target and not target.isRedirect and target ~= redirect then
			-- We should actually be calling expandTemplate on the grandparent rather than on the parent, but we can't do that yet
			-- Since we don't have grandparent access, though, it means the thing we're calling doesn't either, so it doesn't really matter yet
			local parsedTarget = pframe:expandTemplate{title = ':' .. target.prefixedText, args = pframe.args}
			local showontransclusion = frame.args.showontransclusion
			showontransclusion = require('Module:Yesno')(showontransclusion, showontransclusion)
			if showontransclusion and not (showontransclusion == '' or mw.isSubsting()) then
				return makeTransclusionTag(args, redirect) .. parsedTarget
			end
			return parsedTarget
		end
	end
	-- We're not being transcluded, or we can't figure out how to display our target.
	-- Check if we can find the target.
	local target = getTargetFromText(args.content)
	messageBox = require('Module:Message box')
	local redirbox
	if target then -- target isn't nil
		local firstChar = string.sub(target, 1, 1)
		if firstChar ~= ":" then
			target = ":" .. target
		end
		redirbox = messageBox.main('mbox', {
			type = 'notice',
			name = 'RedirectBox',
			image = '[[File:Symbol redirect blue.svg|40px]]',
			text = "'''This title is currently a redirect ''' to [[" .. target .. "]]; click there to go to the current target. The full content of this redirect page, including all [[Wikipedia:Categorizing redirects|redirect categories]], is displayed below."
		})
	else
		redirbox = messageBox.main('mbox', {
			type = 'notice',
			name = 'RedirectBox',
			image = '[[File:Symbol redirect vote 4.svg|40px]]',
			text = "'''This redirect's target could not be determined.''' This page may be a [[Wikipedia:Soft redirect|soft redirect]], or not a redirect at all. The content of the page is displayed below."
		})
	end
	-- Display the RfD banner.
	return makeRfdNotice(frame.args) .. '\n' .. redirbox .. frame.args.content
end

local substText = "{{&lt;includeonly&gt;safesubst:&lt;/includeonly&gt;#invoke:RfD||%s%s|%s%s\n"
	.. "&lt;!-- The above content is generated by {{subst:Rfd}}. --&gt;\n&lt;!-- End of RFD message. Don't edit anything above here. Feel free to edit below here, but do NOT change the redirect's target. --&gt;|content=\n%s\n"
	.. "&lt;!-- Don't add anything after this line unless you're drafting a disambiguation page or article to replace the redirect. --&gt;\n}}"
local dateText = 'month = %B\n|day = %e\n|year = %Y\n|time = %R\n|timestop = %Y%m%d%H%M%S'

-- called during subst when the template is initially placed on the page
function p.main(frame)
	local pframe = frame:getParent()
	local pargs = pframe.args
	local Date
	if pargs.days then
		Date = os.date(dateText, os.time() - 86400*pargs.days)
	else
		Date = os.date(dateText)
	end
	local retval = string.format(substText, pargs.FULLPAGENAME or pargs[1] or '', pargs.showontransclusion and '|showontransclusion=1' or '', Date, pframe:getTitle() == mw.title.getCurrentTitle().prefixedText and '|category=' or '', pargs.content or '')
	if mw.isSubsting() then
		return retval
	else
		return frame:expandTemplate{title = 'Template:Error:must be substituted', args = {'rfd'}} .. frame:preprocess(retval)
	end
end

return p</text>
      <sha1>oxxtlhxa8rn1n3a7oqgwnbfa56oxuq6</sha1>
    </revision>
    <revision>
      <id>1165388</id>
      <parentid>1165387</parentid>
      <timestamp>2025-04-27T22:17:25Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>NOINDEX only if actually displaying the RfD notice</comment>
      <origin>1165384</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="8418" sha1="oxxtlhxa8rn1n3a7oqgwnbfa56oxuq6" xml:space="preserve">require('strict')

local p = {}

local getTargetFromText = require('Module:Redirect').getTargetFromText
local messageBox

local mboxText = "'''The purpose of this redirect is currently being discussed by the Wikipedia community."
	.. " The outcome of the discussion may result in a change of this page, or possibly its deletion in accordance with Wikipedia's [[Wikipedia:Deletion policy|deletion policy]].''' &lt;br /&gt;"
	.. " Please share your thoughts on the matter at '''[[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|this redirect's entry]]''' on the [[Wikipedia:Redirects for discussion|redirects for discussion]] page.&lt;br /&gt;"
	.. "  '''Click on the link below''' to go to the current destination page.&lt;br /&gt;"
	.. "&lt;small&gt;Please notify the good-faith creator and any main contributors of the redirect by placing &lt;code&gt;&amp;#123;&amp;#123;[[Wikipedia:Substitution|subst]]:[[Template:Rfd notice|Rfd notice]]&amp;#124;%s&amp;#125;&amp;#125; &amp;#126;&amp;#126;&amp;#126;&amp;#126;&lt;/code&gt; on their talk page.&lt;/small&gt;__EXPECTUNUSEDTEMPLATE__"
local deleteReason = '[[Wikipedia:Redirects for discussion]] debate closed as delete'
local messageOnTransclusionsStandard = '&amp;lsaquo;The template below is included via a redirect'
    .. ' (%s) that is under discussion. See [[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|redirects for discussion]]'
    .. ' to help reach a consensus.&amp;rsaquo;'
local messageOnTransclusionsTiny = '&amp;lsaquo;[[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|See RfD]]&amp;rsaquo;'

local function makeTransclusionTag(args, redirect)
	local discussionPage = args[1] and mw.text.trim(args[1])
	if not discussionPage or discussionPage == '' then
		discussionPage = redirect
	end
	local visClasses = {
		['autoconfirmed']='autoconfirmed-show',
		['extendedconfirmed']='extendedconfirmed-show sysop-show'
	} -- More can be added if justified by situation
	local sizeAttrs = {
		['standard'] = {
			['tags'] = {'&lt;div ', '&lt;/div&gt;'},
			['style'] = ' border-bottom: 1px solid #AAAAAA;',
			['message'] = messageOnTransclusionsStandard:format(redirect, args.year, args.month, args.day, discussionPage)
		},
		['tiny'] = {
			['tags'] = {'&lt;span ', '&lt;/span&gt; '},
			['style'] = '',
			['message'] = messageOnTransclusionsTiny:format(args.year, args.month, args.day, discussionPage)
		},
	}
	local size = sizeAttrs[args.showontransclusion] and args.showontransclusion or 'standard'
	local tags = sizeAttrs[size]['tags']
	local visibility = visClasses[args.transclusiontagvisibility] or ''
	local classes = 'class="boilerplate metadata plainlinks ' .. visibility .. '" '
	local id = 'id="rfd-t" '
	local style = 'style="font-size: 85%; text-align: left;' .. sizeAttrs[size]['style'] .. '" '
	return tags[1] .. classes .. id .. style .. '&gt;' .. sizeAttrs[size]['message'] .. tags[2]
end

local function makeRfdNotice(args)
	local currentTitle = mw.title.getCurrentTitle()
	if not messageBox then
		messageBox = require('Module:Message box')
	end
	local discussionPage = args[1] and mw.text.trim(args[1])
	if discussionPage == '' then
		discussionPage = nil
	end
	local target = getTargetFromText(args.content)
	local isError = not target or not mw.title.new(target)
	local category
	if args.category then
		category = args.category
	elseif args.timestop then
                -- Extract stable year and month from timestop; args.month and args.year can change if the discussion is relisted (see [[Special:Diff/896302321]])
                local lang = mw.language.getContentLanguage()
                local catMonth = lang:formatDate('F', args.timestop)
                local catYear = lang:formatDate('Y', args.timestop)
		category = string.format('[[Category:Redirects for discussion from %s %s|%s]][[Category:All redirects for discussion|%s]]', catMonth, catYear, currentTitle.text, currentTitle.text)
	else
		category = string.format('[[Category:Redirects for discussion|%s]][[Category:All redirects for discussion|%s]]', currentTitle.text, currentTitle.text)
	end
	if category then category = category..'[[Category:Temporary maintenance holdings/RFD]]' end
	local noindex = mw.getCurrentFrame():expandTemplate{title="NOINDEX"}
	return string.format('%s%s&lt;span id="delete-reason" style="display: none;"&gt;%s&lt;/span&gt;%s%s',
		noindex,
		messageBox.main('mbox', {
			type = 'delete',
			name = 'RfD',
			image = 'none',
			text = string.format(mboxText, args.year, args.month, args.day, discussionPage or currentTitle.prefixedText, mw.text.nowiki(currentTitle.prefixedText))
		}),
		mw.uri.encode(deleteReason),
		category,
		isError and '[[Category:RfD errors]]' or ''
	)
end

p[''] = function(frame)
	local args = frame.args
	if not args.content or mw.text.trim(args.content) == '' then
		return '&lt;span class="error"&gt;Error: No content was provided. The original text of the page (the #REDIRECT line and any templates) must be placed inside of the content parameter.[[Category:RfD errors]]&lt;/span&gt;'
	end
	local pframe = frame:getParent()
	if pframe:preprocess('&lt;includeonly&gt;1&lt;/includeonly&gt;') == '1' then
		-- We're being transcluded, so display the content of our target.
		local target = getTargetFromText(args.content)
		if target then
			target = mw.title.new(target)
		end
		local redirect = pframe:getTitle()
		if target and not target.isRedirect and target ~= redirect then
			-- We should actually be calling expandTemplate on the grandparent rather than on the parent, but we can't do that yet
			-- Since we don't have grandparent access, though, it means the thing we're calling doesn't either, so it doesn't really matter yet
			local parsedTarget = pframe:expandTemplate{title = ':' .. target.prefixedText, args = pframe.args}
			local showontransclusion = frame.args.showontransclusion
			showontransclusion = require('Module:Yesno')(showontransclusion, showontransclusion)
			if showontransclusion and not (showontransclusion == '' or mw.isSubsting()) then
				return makeTransclusionTag(args, redirect) .. parsedTarget
			end
			return parsedTarget
		end
	end
	-- We're not being transcluded, or we can't figure out how to display our target.
	-- Check if we can find the target.
	local target = getTargetFromText(args.content)
	messageBox = require('Module:Message box')
	local redirbox
	if target then -- target isn't nil
		local firstChar = string.sub(target, 1, 1)
		if firstChar ~= ":" then
			target = ":" .. target
		end
		redirbox = messageBox.main('mbox', {
			type = 'notice',
			name = 'RedirectBox',
			image = '[[File:Symbol redirect blue.svg|40px]]',
			text = "'''This title is currently a redirect ''' to [[" .. target .. "]]; click there to go to the current target. The full content of this redirect page, including all [[Wikipedia:Categorizing redirects|redirect categories]], is displayed below."
		})
	else
		redirbox = messageBox.main('mbox', {
			type = 'notice',
			name = 'RedirectBox',
			image = '[[File:Symbol redirect vote 4.svg|40px]]',
			text = "'''This redirect's target could not be determined.''' This page may be a [[Wikipedia:Soft redirect|soft redirect]], or not a redirect at all. The content of the page is displayed below."
		})
	end
	-- Display the RfD banner.
	return makeRfdNotice(frame.args) .. '\n' .. redirbox .. frame.args.content
end

local substText = "{{&lt;includeonly&gt;safesubst:&lt;/includeonly&gt;#invoke:RfD||%s%s|%s%s\n"
	.. "&lt;!-- The above content is generated by {{subst:Rfd}}. --&gt;\n&lt;!-- End of RFD message. Don't edit anything above here. Feel free to edit below here, but do NOT change the redirect's target. --&gt;|content=\n%s\n"
	.. "&lt;!-- Don't add anything after this line unless you're drafting a disambiguation page or article to replace the redirect. --&gt;\n}}"
local dateText = 'month = %B\n|day = %e\n|year = %Y\n|time = %R\n|timestop = %Y%m%d%H%M%S'

-- called during subst when the template is initially placed on the page
function p.main(frame)
	local pframe = frame:getParent()
	local pargs = pframe.args
	local Date
	if pargs.days then
		Date = os.date(dateText, os.time() - 86400*pargs.days)
	else
		Date = os.date(dateText)
	end
	local retval = string.format(substText, pargs.FULLPAGENAME or pargs[1] or '', pargs.showontransclusion and '|showontransclusion=1' or '', Date, pframe:getTitle() == mw.title.getCurrentTitle().prefixedText and '|category=' or '', pargs.content or '')
	if mw.isSubsting() then
		return retval
	else
		return frame:expandTemplate{title = 'Template:Error:must be substituted', args = {'rfd'}} .. frame:preprocess(retval)
	end
end

return p</text>
      <sha1>oxxtlhxa8rn1n3a7oqgwnbfa56oxuq6</sha1>
    </revision>
    <revision>
      <id>1165389</id>
      <parentid>1165388</parentid>
      <timestamp>2025-04-27T22:17:26Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: chat is this real</comment>
      <origin>1165384</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="8418" sha1="oxxtlhxa8rn1n3a7oqgwnbfa56oxuq6" xml:space="preserve">require('strict')

local p = {}

local getTargetFromText = require('Module:Redirect').getTargetFromText
local messageBox

local mboxText = "'''The purpose of this redirect is currently being discussed by the Wikipedia community."
	.. " The outcome of the discussion may result in a change of this page, or possibly its deletion in accordance with Wikipedia's [[Wikipedia:Deletion policy|deletion policy]].''' &lt;br /&gt;"
	.. " Please share your thoughts on the matter at '''[[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|this redirect's entry]]''' on the [[Wikipedia:Redirects for discussion|redirects for discussion]] page.&lt;br /&gt;"
	.. "  '''Click on the link below''' to go to the current destination page.&lt;br /&gt;"
	.. "&lt;small&gt;Please notify the good-faith creator and any main contributors of the redirect by placing &lt;code&gt;&amp;#123;&amp;#123;[[Wikipedia:Substitution|subst]]:[[Template:Rfd notice|Rfd notice]]&amp;#124;%s&amp;#125;&amp;#125; &amp;#126;&amp;#126;&amp;#126;&amp;#126;&lt;/code&gt; on their talk page.&lt;/small&gt;__EXPECTUNUSEDTEMPLATE__"
local deleteReason = '[[Wikipedia:Redirects for discussion]] debate closed as delete'
local messageOnTransclusionsStandard = '&amp;lsaquo;The template below is included via a redirect'
    .. ' (%s) that is under discussion. See [[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|redirects for discussion]]'
    .. ' to help reach a consensus.&amp;rsaquo;'
local messageOnTransclusionsTiny = '&amp;lsaquo;[[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|See RfD]]&amp;rsaquo;'

local function makeTransclusionTag(args, redirect)
	local discussionPage = args[1] and mw.text.trim(args[1])
	if not discussionPage or discussionPage == '' then
		discussionPage = redirect
	end
	local visClasses = {
		['autoconfirmed']='autoconfirmed-show',
		['extendedconfirmed']='extendedconfirmed-show sysop-show'
	} -- More can be added if justified by situation
	local sizeAttrs = {
		['standard'] = {
			['tags'] = {'&lt;div ', '&lt;/div&gt;'},
			['style'] = ' border-bottom: 1px solid #AAAAAA;',
			['message'] = messageOnTransclusionsStandard:format(redirect, args.year, args.month, args.day, discussionPage)
		},
		['tiny'] = {
			['tags'] = {'&lt;span ', '&lt;/span&gt; '},
			['style'] = '',
			['message'] = messageOnTransclusionsTiny:format(args.year, args.month, args.day, discussionPage)
		},
	}
	local size = sizeAttrs[args.showontransclusion] and args.showontransclusion or 'standard'
	local tags = sizeAttrs[size]['tags']
	local visibility = visClasses[args.transclusiontagvisibility] or ''
	local classes = 'class="boilerplate metadata plainlinks ' .. visibility .. '" '
	local id = 'id="rfd-t" '
	local style = 'style="font-size: 85%; text-align: left;' .. sizeAttrs[size]['style'] .. '" '
	return tags[1] .. classes .. id .. style .. '&gt;' .. sizeAttrs[size]['message'] .. tags[2]
end

local function makeRfdNotice(args)
	local currentTitle = mw.title.getCurrentTitle()
	if not messageBox then
		messageBox = require('Module:Message box')
	end
	local discussionPage = args[1] and mw.text.trim(args[1])
	if discussionPage == '' then
		discussionPage = nil
	end
	local target = getTargetFromText(args.content)
	local isError = not target or not mw.title.new(target)
	local category
	if args.category then
		category = args.category
	elseif args.timestop then
                -- Extract stable year and month from timestop; args.month and args.year can change if the discussion is relisted (see [[Special:Diff/896302321]])
                local lang = mw.language.getContentLanguage()
                local catMonth = lang:formatDate('F', args.timestop)
                local catYear = lang:formatDate('Y', args.timestop)
		category = string.format('[[Category:Redirects for discussion from %s %s|%s]][[Category:All redirects for discussion|%s]]', catMonth, catYear, currentTitle.text, currentTitle.text)
	else
		category = string.format('[[Category:Redirects for discussion|%s]][[Category:All redirects for discussion|%s]]', currentTitle.text, currentTitle.text)
	end
	if category then category = category..'[[Category:Temporary maintenance holdings/RFD]]' end
	local noindex = mw.getCurrentFrame():expandTemplate{title="NOINDEX"}
	return string.format('%s%s&lt;span id="delete-reason" style="display: none;"&gt;%s&lt;/span&gt;%s%s',
		noindex,
		messageBox.main('mbox', {
			type = 'delete',
			name = 'RfD',
			image = 'none',
			text = string.format(mboxText, args.year, args.month, args.day, discussionPage or currentTitle.prefixedText, mw.text.nowiki(currentTitle.prefixedText))
		}),
		mw.uri.encode(deleteReason),
		category,
		isError and '[[Category:RfD errors]]' or ''
	)
end

p[''] = function(frame)
	local args = frame.args
	if not args.content or mw.text.trim(args.content) == '' then
		return '&lt;span class="error"&gt;Error: No content was provided. The original text of the page (the #REDIRECT line and any templates) must be placed inside of the content parameter.[[Category:RfD errors]]&lt;/span&gt;'
	end
	local pframe = frame:getParent()
	if pframe:preprocess('&lt;includeonly&gt;1&lt;/includeonly&gt;') == '1' then
		-- We're being transcluded, so display the content of our target.
		local target = getTargetFromText(args.content)
		if target then
			target = mw.title.new(target)
		end
		local redirect = pframe:getTitle()
		if target and not target.isRedirect and target ~= redirect then
			-- We should actually be calling expandTemplate on the grandparent rather than on the parent, but we can't do that yet
			-- Since we don't have grandparent access, though, it means the thing we're calling doesn't either, so it doesn't really matter yet
			local parsedTarget = pframe:expandTemplate{title = ':' .. target.prefixedText, args = pframe.args}
			local showontransclusion = frame.args.showontransclusion
			showontransclusion = require('Module:Yesno')(showontransclusion, showontransclusion)
			if showontransclusion and not (showontransclusion == '' or mw.isSubsting()) then
				return makeTransclusionTag(args, redirect) .. parsedTarget
			end
			return parsedTarget
		end
	end
	-- We're not being transcluded, or we can't figure out how to display our target.
	-- Check if we can find the target.
	local target = getTargetFromText(args.content)
	messageBox = require('Module:Message box')
	local redirbox
	if target then -- target isn't nil
		local firstChar = string.sub(target, 1, 1)
		if firstChar ~= ":" then
			target = ":" .. target
		end
		redirbox = messageBox.main('mbox', {
			type = 'notice',
			name = 'RedirectBox',
			image = '[[File:Symbol redirect blue.svg|40px]]',
			text = "'''This title is currently a redirect ''' to [[" .. target .. "]]; click there to go to the current target. The full content of this redirect page, including all [[Wikipedia:Categorizing redirects|redirect categories]], is displayed below."
		})
	else
		redirbox = messageBox.main('mbox', {
			type = 'notice',
			name = 'RedirectBox',
			image = '[[File:Symbol redirect vote 4.svg|40px]]',
			text = "'''This redirect's target could not be determined.''' This page may be a [[Wikipedia:Soft redirect|soft redirect]], or not a redirect at all. The content of the page is displayed below."
		})
	end
	-- Display the RfD banner.
	return makeRfdNotice(frame.args) .. '\n' .. redirbox .. frame.args.content
end

local substText = "{{&lt;includeonly&gt;safesubst:&lt;/includeonly&gt;#invoke:RfD||%s%s|%s%s\n"
	.. "&lt;!-- The above content is generated by {{subst:Rfd}}. --&gt;\n&lt;!-- End of RFD message. Don't edit anything above here. Feel free to edit below here, but do NOT change the redirect's target. --&gt;|content=\n%s\n"
	.. "&lt;!-- Don't add anything after this line unless you're drafting a disambiguation page or article to replace the redirect. --&gt;\n}}"
local dateText = 'month = %B\n|day = %e\n|year = %Y\n|time = %R\n|timestop = %Y%m%d%H%M%S'

-- called during subst when the template is initially placed on the page
function p.main(frame)
	local pframe = frame:getParent()
	local pargs = pframe.args
	local Date
	if pargs.days then
		Date = os.date(dateText, os.time() - 86400*pargs.days)
	else
		Date = os.date(dateText)
	end
	local retval = string.format(substText, pargs.FULLPAGENAME or pargs[1] or '', pargs.showontransclusion and '|showontransclusion=1' or '', Date, pframe:getTitle() == mw.title.getCurrentTitle().prefixedText and '|category=' or '', pargs.content or '')
	if mw.isSubsting() then
		return retval
	else
		return frame:expandTemplate{title = 'Template:Error:must be substituted', args = {'rfd'}} .. frame:preprocess(retval)
	end
end

return p</text>
      <sha1>oxxtlhxa8rn1n3a7oqgwnbfa56oxuq6</sha1>
    </revision>
    <revision>
      <id>17351</id>
      <timestamp>2025-12-07T07:44:47Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was Text replacement - "#REDIRECT" to "#redirect"</comment>
      <origin>17351</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="8418" sha1="ijwsokz6d130xho8ivsjqm170j0txgu" xml:space="preserve">require('strict')

local p = {}

local getTargetFromText = require('Module:Redirect').getTargetFromText
local messageBox

local mboxText = "'''The purpose of this redirect is currently being discussed by the Wikipedia community."
	.. " The outcome of the discussion may result in a change of this page, or possibly its deletion in accordance with Wikipedia's [[Wikipedia:Deletion policy|deletion policy]].''' &lt;br /&gt;"
	.. " Please share your thoughts on the matter at '''[[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|this redirect's entry]]''' on the [[Wikipedia:Redirects for discussion|redirects for discussion]] page.&lt;br /&gt;"
	.. "  '''Click on the link below''' to go to the current destination page.&lt;br /&gt;"
	.. "&lt;small&gt;Please notify the good-faith creator and any main contributors of the redirect by placing &lt;code&gt;&amp;#123;&amp;#123;[[Wikipedia:Substitution|subst]]:[[Template:Rfd notice|Rfd notice]]&amp;#124;%s&amp;#125;&amp;#125; &amp;#126;&amp;#126;&amp;#126;&amp;#126;&lt;/code&gt; on their talk page.&lt;/small&gt;__EXPECTUNUSEDTEMPLATE__"
local deleteReason = '[[Wikipedia:Redirects for discussion]] debate closed as delete'
local messageOnTransclusionsStandard = '&amp;lsaquo;The template below is included via a redirect'
    .. ' (%s) that is under discussion. See [[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|redirects for discussion]]'
    .. ' to help reach a consensus.&amp;rsaquo;'
local messageOnTransclusionsTiny = '&amp;lsaquo;[[Wikipedia:Redirects for discussion/Log/%s %s %s#%s|See RfD]]&amp;rsaquo;'

local function makeTransclusionTag(args, redirect)
	local discussionPage = args[1] and mw.text.trim(args[1])
	if not discussionPage or discussionPage == '' then
		discussionPage = redirect
	end
	local visClasses = {
		['autoconfirmed']='autoconfirmed-show',
		['extendedconfirmed']='extendedconfirmed-show sysop-show'
	} -- More can be added if justified by situation
	local sizeAttrs = {
		['standard'] = {
			['tags'] = {'&lt;div ', '&lt;/div&gt;'},
			['style'] = ' border-bottom: 1px solid #AAAAAA;',
			['message'] = messageOnTransclusionsStandard:format(redirect, args.year, args.month, args.day, discussionPage)
		},
		['tiny'] = {
			['tags'] = {'&lt;span ', '&lt;/span&gt; '},
			['style'] = '',
			['message'] = messageOnTransclusionsTiny:format(args.year, args.month, args.day, discussionPage)
		},
	}
	local size = sizeAttrs[args.showontransclusion] and args.showontransclusion or 'standard'
	local tags = sizeAttrs[size]['tags']
	local visibility = visClasses[args.transclusiontagvisibility] or ''
	local classes = 'class="boilerplate metadata plainlinks ' .. visibility .. '" '
	local id = 'id="rfd-t" '
	local style = 'style="font-size: 85%; text-align: left;' .. sizeAttrs[size]['style'] .. '" '
	return tags[1] .. classes .. id .. style .. '&gt;' .. sizeAttrs[size]['message'] .. tags[2]
end

local function makeRfdNotice(args)
	local currentTitle = mw.title.getCurrentTitle()
	if not messageBox then
		messageBox = require('Module:Message box')
	end
	local discussionPage = args[1] and mw.text.trim(args[1])
	if discussionPage == '' then
		discussionPage = nil
	end
	local target = getTargetFromText(args.content)
	local isError = not target or not mw.title.new(target)
	local category
	if args.category then
		category = args.category
	elseif args.timestop then
                -- Extract stable year and month from timestop; args.month and args.year can change if the discussion is relisted (see [[Special:Diff/896302321]])
                local lang = mw.language.getContentLanguage()
                local catMonth = lang:formatDate('F', args.timestop)
                local catYear = lang:formatDate('Y', args.timestop)
		category = string.format('[[Category:Redirects for discussion from %s %s|%s]][[Category:All redirects for discussion|%s]]', catMonth, catYear, currentTitle.text, currentTitle.text)
	else
		category = string.format('[[Category:Redirects for discussion|%s]][[Category:All redirects for discussion|%s]]', currentTitle.text, currentTitle.text)
	end
	if category then category = category..'[[Category:Temporary maintenance holdings/RFD]]' end
	local noindex = mw.getCurrentFrame():expandTemplate{title="NOINDEX"}
	return string.format('%s%s&lt;span id="delete-reason" style="display: none;"&gt;%s&lt;/span&gt;%s%s',
		noindex,
		messageBox.main('mbox', {
			type = 'delete',
			name = 'RfD',
			image = 'none',
			text = string.format(mboxText, args.year, args.month, args.day, discussionPage or currentTitle.prefixedText, mw.text.nowiki(currentTitle.prefixedText))
		}),
		mw.uri.encode(deleteReason),
		category,
		isError and '[[Category:RfD errors]]' or ''
	)
end

p[''] = function(frame)
	local args = frame.args
	if not args.content or mw.text.trim(args.content) == '' then
		return '&lt;span class="error"&gt;Error: No content was provided. The original text of the page (the #redirect line and any templates) must be placed inside of the content parameter.[[Category:RfD errors]]&lt;/span&gt;'
	end
	local pframe = frame:getParent()
	if pframe:preprocess('&lt;includeonly&gt;1&lt;/includeonly&gt;') == '1' then
		-- We're being transcluded, so display the content of our target.
		local target = getTargetFromText(args.content)
		if target then
			target = mw.title.new(target)
		end
		local redirect = pframe:getTitle()
		if target and not target.isRedirect and target ~= redirect then
			-- We should actually be calling expandTemplate on the grandparent rather than on the parent, but we can't do that yet
			-- Since we don't have grandparent access, though, it means the thing we're calling doesn't either, so it doesn't really matter yet
			local parsedTarget = pframe:expandTemplate{title = ':' .. target.prefixedText, args = pframe.args}
			local showontransclusion = frame.args.showontransclusion
			showontransclusion = require('Module:Yesno')(showontransclusion, showontransclusion)
			if showontransclusion and not (showontransclusion == '' or mw.isSubsting()) then
				return makeTransclusionTag(args, redirect) .. parsedTarget
			end
			return parsedTarget
		end
	end
	-- We're not being transcluded, or we can't figure out how to display our target.
	-- Check if we can find the target.
	local target = getTargetFromText(args.content)
	messageBox = require('Module:Message box')
	local redirbox
	if target then -- target isn't nil
		local firstChar = string.sub(target, 1, 1)
		if firstChar ~= ":" then
			target = ":" .. target
		end
		redirbox = messageBox.main('mbox', {
			type = 'notice',
			name = 'RedirectBox',
			image = '[[File:Symbol redirect blue.svg|40px]]',
			text = "'''This title is currently a redirect ''' to [[" .. target .. "]]; click there to go to the current target. The full content of this redirect page, including all [[Wikipedia:Categorizing redirects|redirect categories]], is displayed below."
		})
	else
		redirbox = messageBox.main('mbox', {
			type = 'notice',
			name = 'RedirectBox',
			image = '[[File:Symbol redirect vote 4.svg|40px]]',
			text = "'''This redirect's target could not be determined.''' This page may be a [[Wikipedia:Soft redirect|soft redirect]], or not a redirect at all. The content of the page is displayed below."
		})
	end
	-- Display the RfD banner.
	return makeRfdNotice(frame.args) .. '\n' .. redirbox .. frame.args.content
end

local substText = "{{&lt;includeonly&gt;safesubst:&lt;/includeonly&gt;#invoke:RfD||%s%s|%s%s\n"
	.. "&lt;!-- The above content is generated by {{subst:Rfd}}. --&gt;\n&lt;!-- End of RFD message. Don't edit anything above here. Feel free to edit below here, but do NOT change the redirect's target. --&gt;|content=\n%s\n"
	.. "&lt;!-- Don't add anything after this line unless you're drafting a disambiguation page or article to replace the redirect. --&gt;\n}}"
local dateText = 'month = %B\n|day = %e\n|year = %Y\n|time = %R\n|timestop = %Y%m%d%H%M%S'

-- called during subst when the template is initially placed on the page
function p.main(frame)
	local pframe = frame:getParent()
	local pargs = pframe.args
	local Date
	if pargs.days then
		Date = os.date(dateText, os.time() - 86400*pargs.days)
	else
		Date = os.date(dateText)
	end
	local retval = string.format(substText, pargs.FULLPAGENAME or pargs[1] or '', pargs.showontransclusion and '|showontransclusion=1' or '', Date, pframe:getTitle() == mw.title.getCurrentTitle().prefixedText and '|category=' or '', pargs.content or '')
	if mw.isSubsting() then
		return retval
	else
		return frame:expandTemplate{title = 'Template:Error:must be substituted', args = {'rfd'}} .. frame:preprocess(retval)
	end
end

return p</text>
      <sha1>ijwsokz6d130xho8ivsjqm170j0txgu</sha1>
    </revision>
  </page>
</mediawiki>
