<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Module:Math/doc</title>
    <ns>828</ns>
    <id>7646</id>
    <revision>
      <id>885134</id>
      <timestamp>2023-08-27T20:07:46Z</timestamp>
      <contributor>
        <username>en&gt;MarMi wiki</username>
        <id>0</id>
      </contributor>
      <comment>clarify</comment>
      <origin>885134</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8412" sha1="ae14yv40ldafmfch6mo3yt8dfmt01as" xml:space="preserve">{{for|''formatting'' mathematical expressions, LATEX-style|Template:Math}}
{{High-risk|1,607,000+}}
{{module rating|protected}}
This module provides a number of mathematical functions. These functions can be used from #invoke or from other Lua modules.

== Use from other Lua modules ==
To use the module from normal wiki pages, no special preparation is needed. If you are using the module from another Lua module, first you need to load it, like this:

&lt;syntaxhighlight lang="lua"&gt;
local mm = require('Module:Math')
&lt;/syntaxhighlight&gt;

(The &lt;code&gt;mm&lt;/code&gt; variable stands for '''M'''odule '''M'''ath; you can choose something more descriptive if you prefer.)

Most functions in the module have a version for Lua and a version for #invoke. It is possible to use the #invoke functions from other Lua modules, but using the Lua functions has the advantage that you do not need to access a Lua [[mw:Extension:Scribunto/Lua reference manual#Frame object|frame object]]. Lua functions are preceded by &lt;code&gt;_&lt;/code&gt;, whereas #invoke functions are not.

== random ==
{{see also|Module:Random}}

 &amp;#123;{#invoke:math|random}}
 &amp;#123;{#invoke:math|random|''max_value''}}
 &amp;#123;{#invoke:math|random|''min_value''|''max_value''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._random()
mm._random(max_value)
mm._random(min_value, max_value)
&lt;/syntaxhighlight&gt;

Generates a random number.

* If no arguments are specified, the number produced is greater than or equal to 0 and less than 1.
* If one argument is provided, the number produced is an integer between 1 and that argument. The argument must be a positive integer.
* If two arguments are provided, the number produced is an integer between the first and second arguments. Both arguments must be integers, but can be negative.

This function will not work properly for numbers less than &amp;minus;2&lt;sup&gt;32&lt;/sup&gt; and greater than 2&lt;sup&gt;32&lt;/sup&gt; &amp;minus; 1. If you need to use numbers outside of this range, it is recommended that you use [[Module:Random]].

== order ==

 &amp;#123;{#invoke:math|order|''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._order(n)
&lt;/syntaxhighlight&gt;

Determines the [[order of magnitude]] of a number.

== precision ==

 &amp;#123;{#invoke:math|precision|''n''}}
 &amp;#123;{#invoke:math|precision|x=''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision(number_string)
&lt;/syntaxhighlight&gt;

Detemines the precision of a number. For example, for "4" it will return "0", for "4.567" it will return "3", and for "100" it will return "-2".

The function attempts to parse the string representation of the number, and detects whether the number uses [[Scientific notation#E notation|E notation]]. For this reason, when called from Lua, very large numbers or very precise numbers should be directly input as strings to get accurate results. If they are input as numbers, the Lua interpreter will change them to E notation and this function will return the precision of the E notation rather than that of the original number. This is not a problem when the number is called from #invoke, as all input from #invoke is in string format.

== max ==

 &amp;#123;{#invoke:math|max|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._max(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the maximum value from the values specified. Values that cannot be converted to numbers are ignored.

== median  ==

 &amp;#123;{#invoke:math|median|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._median(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the [[median]]  value from the values specified. Values that cannot be converted to numbers are ignored.

== min ==

 &amp;#123;{#invoke:math|min|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._min(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the minimum value from the values specified. Values that cannot be converted to numbers are ignored.

== sum  ==

 &amp;#123;{#invoke:math|sum|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._sum(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the sum of the values specified. Values that cannot be converted to numbers are ignored.

== average ==

 &amp;#123;{#invoke:math|average|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._average(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the average of the values specified. (More precisely, the value returned is the [[Mean#Arithmetic mean (AM)|arithmetic mean]].) Values that cannot be converted to numbers are ignored.

== round ==

 &amp;#123;{#invoke:math|round|''value''|''precision''}}
 &amp;#123;{#invoke:math|round|value=''value''|precision=''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._round(value, precision)
&lt;/syntaxhighlight&gt;

[[Rounding|Rounds]] a number to the specified precision{{clarify|reason=What method exactly is used?}}.

Note: As of October 2019, there is a bug in the display of some rounded numbers. When trying to round a number that rounds to "n.0", like "1.02", to the nearest tenth of a digit (i.e. {{para|r|1}}), this function should display "1.0", but it unexpectedly displays "1". Use the {{para|precision_format}} parameter instead.

== log10 ==

 &amp;#123;{#invoke:math | log10 | ''x''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._log10(x)
&lt;/syntaxhighlight&gt;

Returns &lt;code&gt;log&lt;sub&gt;10&lt;/sub&gt;(''x'')&lt;/code&gt;, the [[logarithm]] of ''x'' using base 10.

== mod ==

 &amp;#123;{#invoke:math|mod|''x''|''y''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._mod(x, y)
&lt;/syntaxhighlight&gt;

Gets &lt;code&gt;''x''&lt;/code&gt; [[Modulo operation|modulo]] &lt;code&gt;''y''&lt;/code&gt;, or the remainder after &lt;code&gt;''x''&lt;/code&gt; has been divided by &lt;code&gt;''y''&lt;/code&gt;. This is accurate for integers up to 2&lt;sup&gt;53&lt;/sup&gt;; for larger integers Lua's modulo operator may return an erroneous value. This function deals with this problem by returning &lt;code&gt;0&lt;/code&gt; if the modulo given by Lua's modulo operator is less than 0 or greater than &lt;code&gt;''y''&lt;/code&gt;.

== gcd ==

 &amp;#123;{#invoke:math|gcd|''v1''|''v2''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._gcd(v1, v2, ...)
&lt;/syntaxhighlight&gt;

Finds the [[greatest common divisor]] of the values specified. Values that cannot be converted to numbers are ignored.

== precision_format ==

 &amp;#123;{#invoke:math|precision_format|''value_string''|''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision_format(value_string, precision)
&lt;/syntaxhighlight&gt;

Rounds a number to the specified precision and formats according to rules originally used for {{tl|Rnd}}.  Output is a string.

Parameter &lt;var&gt;precision&lt;/var&gt; should be an integer number of digits after the decimal point. Negative values are permitted. Non-integers give unexpected results. Positive values greater than the input precision add zero-padding, negative values greater than the input order can consume all digits.

Formatting 8,765.567 with {{tlc|#invoke:Math|precision_format|8765.567|&lt;var&gt;precision&lt;/var&gt;}} gives:
{|class="wikitable"
|+
!&lt;code&gt;&lt;var&gt;precision&lt;/var&gt;&lt;/code&gt;!!Result
|-
|style="text-align:right"|2||{{#invoke:Math|precision_format|8765.567|2}}
|-
|style="text-align:right"| -2||{{#invoke:Math|precision_format|8765.567|-2}}
|-
|style="text-align:right"|6||{{#invoke:Math|precision_format|8765.567|6}}
|-
|style="text-align:right"| -6||{{#invoke:Math|precision_format|8765.567|-6}}
|-
|style="text-align:right"|2.5||{{#invoke:Math|precision_format|8765.567|2.5}}
|-
|style="text-align:right"| -2.5||{{#invoke:Math|precision_format|8765.567|-2.5}}
|}

== divide ==

 &amp;#123;{#invoke:Math|divide|''x''|''y''|''round=''|''precision=''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._divide(x, y, round, precision)
&lt;/syntaxhighlight&gt;

Divide x by y.

* If y if not a number, it is returned.
* Otherwise, if x is not a number, it is returned.
* If round is true ("yes" for #invoke), the result has no decimals
* Precision indicates how many digits of precision the result should have

If any of the arguments contain HTML tags, they are returned unchanged, allowing any errors in calculating the arguments to the division function to be propagated to the calling template.  

== cleanNumber ==

&lt;syntaxhighlight lang="lua"&gt;
local number, number_string = mm._cleanNumber(number_string)
&lt;/syntaxhighlight&gt;

A helper function that can be called from other Lua modules, but not from #invoke. This takes a string or a number value as input, and if the value can be converted to a number, cleanNumber returns the number and the number string. If the value cannot be converted to a number, cleanNumber returns &lt;code&gt;nil, nil&lt;/code&gt;.

==See also==
{{Math templates}}</text>
      <sha1>ae14yv40ldafmfch6mo3yt8dfmt01as</sha1>
    </revision>
    <revision>
      <id>885135</id>
      <parentid>885134</parentid>
      <timestamp>2025-01-07T10:29:16Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: importing modules</comment>
      <origin>885134</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8412" sha1="ae14yv40ldafmfch6mo3yt8dfmt01as" xml:space="preserve">{{for|''formatting'' mathematical expressions, LATEX-style|Template:Math}}
{{High-risk|1,607,000+}}
{{module rating|protected}}
This module provides a number of mathematical functions. These functions can be used from #invoke or from other Lua modules.

== Use from other Lua modules ==
To use the module from normal wiki pages, no special preparation is needed. If you are using the module from another Lua module, first you need to load it, like this:

&lt;syntaxhighlight lang="lua"&gt;
local mm = require('Module:Math')
&lt;/syntaxhighlight&gt;

(The &lt;code&gt;mm&lt;/code&gt; variable stands for '''M'''odule '''M'''ath; you can choose something more descriptive if you prefer.)

Most functions in the module have a version for Lua and a version for #invoke. It is possible to use the #invoke functions from other Lua modules, but using the Lua functions has the advantage that you do not need to access a Lua [[mw:Extension:Scribunto/Lua reference manual#Frame object|frame object]]. Lua functions are preceded by &lt;code&gt;_&lt;/code&gt;, whereas #invoke functions are not.

== random ==
{{see also|Module:Random}}

 &amp;#123;{#invoke:math|random}}
 &amp;#123;{#invoke:math|random|''max_value''}}
 &amp;#123;{#invoke:math|random|''min_value''|''max_value''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._random()
mm._random(max_value)
mm._random(min_value, max_value)
&lt;/syntaxhighlight&gt;

Generates a random number.

* If no arguments are specified, the number produced is greater than or equal to 0 and less than 1.
* If one argument is provided, the number produced is an integer between 1 and that argument. The argument must be a positive integer.
* If two arguments are provided, the number produced is an integer between the first and second arguments. Both arguments must be integers, but can be negative.

This function will not work properly for numbers less than &amp;minus;2&lt;sup&gt;32&lt;/sup&gt; and greater than 2&lt;sup&gt;32&lt;/sup&gt; &amp;minus; 1. If you need to use numbers outside of this range, it is recommended that you use [[Module:Random]].

== order ==

 &amp;#123;{#invoke:math|order|''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._order(n)
&lt;/syntaxhighlight&gt;

Determines the [[order of magnitude]] of a number.

== precision ==

 &amp;#123;{#invoke:math|precision|''n''}}
 &amp;#123;{#invoke:math|precision|x=''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision(number_string)
&lt;/syntaxhighlight&gt;

Detemines the precision of a number. For example, for "4" it will return "0", for "4.567" it will return "3", and for "100" it will return "-2".

The function attempts to parse the string representation of the number, and detects whether the number uses [[Scientific notation#E notation|E notation]]. For this reason, when called from Lua, very large numbers or very precise numbers should be directly input as strings to get accurate results. If they are input as numbers, the Lua interpreter will change them to E notation and this function will return the precision of the E notation rather than that of the original number. This is not a problem when the number is called from #invoke, as all input from #invoke is in string format.

== max ==

 &amp;#123;{#invoke:math|max|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._max(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the maximum value from the values specified. Values that cannot be converted to numbers are ignored.

== median  ==

 &amp;#123;{#invoke:math|median|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._median(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the [[median]]  value from the values specified. Values that cannot be converted to numbers are ignored.

== min ==

 &amp;#123;{#invoke:math|min|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._min(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the minimum value from the values specified. Values that cannot be converted to numbers are ignored.

== sum  ==

 &amp;#123;{#invoke:math|sum|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._sum(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the sum of the values specified. Values that cannot be converted to numbers are ignored.

== average ==

 &amp;#123;{#invoke:math|average|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._average(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the average of the values specified. (More precisely, the value returned is the [[Mean#Arithmetic mean (AM)|arithmetic mean]].) Values that cannot be converted to numbers are ignored.

== round ==

 &amp;#123;{#invoke:math|round|''value''|''precision''}}
 &amp;#123;{#invoke:math|round|value=''value''|precision=''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._round(value, precision)
&lt;/syntaxhighlight&gt;

[[Rounding|Rounds]] a number to the specified precision{{clarify|reason=What method exactly is used?}}.

Note: As of October 2019, there is a bug in the display of some rounded numbers. When trying to round a number that rounds to "n.0", like "1.02", to the nearest tenth of a digit (i.e. {{para|r|1}}), this function should display "1.0", but it unexpectedly displays "1". Use the {{para|precision_format}} parameter instead.

== log10 ==

 &amp;#123;{#invoke:math | log10 | ''x''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._log10(x)
&lt;/syntaxhighlight&gt;

Returns &lt;code&gt;log&lt;sub&gt;10&lt;/sub&gt;(''x'')&lt;/code&gt;, the [[logarithm]] of ''x'' using base 10.

== mod ==

 &amp;#123;{#invoke:math|mod|''x''|''y''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._mod(x, y)
&lt;/syntaxhighlight&gt;

Gets &lt;code&gt;''x''&lt;/code&gt; [[Modulo operation|modulo]] &lt;code&gt;''y''&lt;/code&gt;, or the remainder after &lt;code&gt;''x''&lt;/code&gt; has been divided by &lt;code&gt;''y''&lt;/code&gt;. This is accurate for integers up to 2&lt;sup&gt;53&lt;/sup&gt;; for larger integers Lua's modulo operator may return an erroneous value. This function deals with this problem by returning &lt;code&gt;0&lt;/code&gt; if the modulo given by Lua's modulo operator is less than 0 or greater than &lt;code&gt;''y''&lt;/code&gt;.

== gcd ==

 &amp;#123;{#invoke:math|gcd|''v1''|''v2''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._gcd(v1, v2, ...)
&lt;/syntaxhighlight&gt;

Finds the [[greatest common divisor]] of the values specified. Values that cannot be converted to numbers are ignored.

== precision_format ==

 &amp;#123;{#invoke:math|precision_format|''value_string''|''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision_format(value_string, precision)
&lt;/syntaxhighlight&gt;

Rounds a number to the specified precision and formats according to rules originally used for {{tl|Rnd}}.  Output is a string.

Parameter &lt;var&gt;precision&lt;/var&gt; should be an integer number of digits after the decimal point. Negative values are permitted. Non-integers give unexpected results. Positive values greater than the input precision add zero-padding, negative values greater than the input order can consume all digits.

Formatting 8,765.567 with {{tlc|#invoke:Math|precision_format|8765.567|&lt;var&gt;precision&lt;/var&gt;}} gives:
{|class="wikitable"
|+
!&lt;code&gt;&lt;var&gt;precision&lt;/var&gt;&lt;/code&gt;!!Result
|-
|style="text-align:right"|2||{{#invoke:Math|precision_format|8765.567|2}}
|-
|style="text-align:right"| -2||{{#invoke:Math|precision_format|8765.567|-2}}
|-
|style="text-align:right"|6||{{#invoke:Math|precision_format|8765.567|6}}
|-
|style="text-align:right"| -6||{{#invoke:Math|precision_format|8765.567|-6}}
|-
|style="text-align:right"|2.5||{{#invoke:Math|precision_format|8765.567|2.5}}
|-
|style="text-align:right"| -2.5||{{#invoke:Math|precision_format|8765.567|-2.5}}
|}

== divide ==

 &amp;#123;{#invoke:Math|divide|''x''|''y''|''round=''|''precision=''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._divide(x, y, round, precision)
&lt;/syntaxhighlight&gt;

Divide x by y.

* If y if not a number, it is returned.
* Otherwise, if x is not a number, it is returned.
* If round is true ("yes" for #invoke), the result has no decimals
* Precision indicates how many digits of precision the result should have

If any of the arguments contain HTML tags, they are returned unchanged, allowing any errors in calculating the arguments to the division function to be propagated to the calling template.  

== cleanNumber ==

&lt;syntaxhighlight lang="lua"&gt;
local number, number_string = mm._cleanNumber(number_string)
&lt;/syntaxhighlight&gt;

A helper function that can be called from other Lua modules, but not from #invoke. This takes a string or a number value as input, and if the value can be converted to a number, cleanNumber returns the number and the number string. If the value cannot be converted to a number, cleanNumber returns &lt;code&gt;nil, nil&lt;/code&gt;.

==See also==
{{Math templates}}</text>
      <sha1>ae14yv40ldafmfch6mo3yt8dfmt01as</sha1>
    </revision>
    <revision>
      <id>885136</id>
      <parentid>885135</parentid>
      <timestamp>2025-04-27T05:45:22Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:S needing substitution checking]] to [[Category:qq]][[sn:{{subst:PAGENAME}}]][[Category:qq]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>885136</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8459" sha1="qbne3176k43bln4ltb7t6n8qqgaepin" xml:space="preserve">{{for|''formatting'' mathematical expressions, LATEX-style|Template:Math}}
{{High-risk|1,607,000+}}
{{module rating|protected}}
This module provides a number of mathematical functions. These functions can be used from #invoke or from other Lua modules.

== Use from other Lua modules ==
To use the module from normal wiki pages, no special preparation is needed. If you are using the module from another Lua module, first you need to load it, like this:

&lt;syntaxhighlight lang="lua"&gt;
local mm = require('Module:Math')
&lt;/syntaxhighlight&gt;

(The &lt;code&gt;mm&lt;/code&gt; variable stands for '''M'''odule '''M'''ath; you can choose something more descriptive if you prefer.)

Most functions in the module have a version for Lua and a version for #invoke. It is possible to use the #invoke functions from other Lua modules, but using the Lua functions has the advantage that you do not need to access a Lua [[mw:Extension:Scribunto/Lua reference manual#Frame object|frame object]]. Lua functions are preceded by &lt;code&gt;_&lt;/code&gt;, whereas #invoke functions are not.

== random ==
{{see also|Module:Random}}

 &amp;#123;{#invoke:math|random}}
 &amp;#123;{#invoke:math|random|''max_value''}}
 &amp;#123;{#invoke:math|random|''min_value''|''max_value''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._random()
mm._random(max_value)
mm._random(min_value, max_value)
&lt;/syntaxhighlight&gt;

Generates a random number.

* If no arguments are specified, the number produced is greater than or equal to 0 and less than 1.
* If one argument is provided, the number produced is an integer between 1 and that argument. The argument must be a positive integer.
* If two arguments are provided, the number produced is an integer between the first and second arguments. Both arguments must be integers, but can be negative.

This function will not work properly for numbers less than &amp;minus;2&lt;sup&gt;32&lt;/sup&gt; and greater than 2&lt;sup&gt;32&lt;/sup&gt; &amp;minus; 1. If you need to use numbers outside of this range, it is recommended that you use [[Module:Random]].

== order ==

 &amp;#123;{#invoke:math|order|''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._order(n)
&lt;/syntaxhighlight&gt;

Determines the [[order of magnitude]] of a number.

== precision ==

 &amp;#123;{#invoke:math|precision|''n''}}
 &amp;#123;{#invoke:math|precision|x=''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision(number_string)
&lt;/syntaxhighlight&gt;

Detemines the precision of a number. For example, for "4" it will return "0", for "4.567" it will return "3", and for "100" it will return "-2".

The function attempts to parse the string representation of the number, and detects whether the number uses [[Scientific notation#E notation|E notation]]. For this reason, when called from Lua, very large numbers or very precise numbers should be directly input as strings to get accurate results. If they are input as numbers, the Lua interpreter will change them to E notation and this function will return the precision of the E notation rather than that of the original number. This is not a problem when the number is called from #invoke, as all input from #invoke is in string format.

== max ==

 &amp;#123;{#invoke:math|max|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._max(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the maximum value from the values specified. Values that cannot be converted to numbers are ignored.

== median  ==

 &amp;#123;{#invoke:math|median|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._median(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the [[median]]  value from the values specified. Values that cannot be converted to numbers are ignored.

== min ==

 &amp;#123;{#invoke:math|min|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._min(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the minimum value from the values specified. Values that cannot be converted to numbers are ignored.

== sum  ==

 &amp;#123;{#invoke:math|sum|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._sum(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the sum of the values specified. Values that cannot be converted to numbers are ignored.

== average ==

 &amp;#123;{#invoke:math|average|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._average(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the average of the values specified. (More precisely, the value returned is the [[Mean#Arithmetic mean (AM)|arithmetic mean]].) Values that cannot be converted to numbers are ignored.

== round ==

 &amp;#123;{#invoke:math|round|''value''|''precision''}}
 &amp;#123;{#invoke:math|round|value=''value''|precision=''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._round(value, precision)
&lt;/syntaxhighlight&gt;

[[Rounding|Rounds]] a number to the specified precision{{clarify|reason=What method exactly is used?}}.

Note: As of October 2019, there is a bug in the display of some rounded numbers. When trying to round a number that rounds to "n.0", like "1.02", to the nearest tenth of a digit (i.e. {{para|r|1}}), this function should display "1.0", but it unexpectedly displays "1". Use the {{para|precision_format}} parameter instead.

== log10 ==

 &amp;#123;{#invoke:math | log10 | ''x''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._log10(x)
&lt;/syntaxhighlight&gt;

Returns &lt;code&gt;log&lt;sub&gt;10&lt;/sub&gt;(''x'')&lt;/code&gt;, the [[logarithm]] of ''x'' using base 10.

== mod ==

 &amp;#123;{#invoke:math|mod|''x''|''y''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._mod(x, y)
&lt;/syntaxhighlight&gt;

Gets &lt;code&gt;''x''&lt;/code&gt; [[Modulo operation|modulo]] &lt;code&gt;''y''&lt;/code&gt;, or the remainder after &lt;code&gt;''x''&lt;/code&gt; has been divided by &lt;code&gt;''y''&lt;/code&gt;. This is accurate for integers up to 2&lt;sup&gt;53&lt;/sup&gt;; for larger integers Lua's modulo operator may return an erroneous value. This function deals with this problem by returning &lt;code&gt;0&lt;/code&gt; if the modulo given by Lua's modulo operator is less than 0 or greater than &lt;code&gt;''y''&lt;/code&gt;.

== gcd ==

 &amp;#123;{#invoke:math|gcd|''v1''|''v2''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._gcd(v1, v2, ...)
&lt;/syntaxhighlight&gt;

Finds the [[greatest common divisor]] of the values specified. Values that cannot be converted to numbers are ignored.

== precision_format ==

 &amp;#123;{#invoke:math|precision_format|''value_string''|''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision_format(value_string, precision)
&lt;/syntaxhighlight&gt;

Rounds a number to the specified precision and formats according to rules originally used for {{tl|Rnd}}.  Output is a string.

Parameter &lt;var&gt;precision&lt;/var&gt; should be an integer number of digits after the decimal point. Negative values are permitted. Non-integers give unexpected results. Positive values greater than the input precision add zero-padding, negative values greater than the input order can consume all digits.

Formatting 8,765.567 with {{tlc|#invoke:Math|precision_format|8765.567|&lt;var&gt;precision&lt;/var&gt;}} gives:
{|class="wikitable"
|+
!&lt;code&gt;&lt;var&gt;precision&lt;/var&gt;&lt;/code&gt;!!Result
|-
|style="text-align:right"|2||{{#invoke:Math|precision_format|8765.567|2}}
|-
|style="text-align:right"| -2||{{#invoke:Math|precision_format|8765.567|-2}}
|-
|style="text-align:right"|6||{{#invoke:Math|precision_format|8765.567|6}}
|-
|style="text-align:right"| -6||{{#invoke:Math|precision_format|8765.567|-6}}
|-
|style="text-align:right"|2.5||{{#invoke:Math|precision_format|8765.567|2.5}}
|-
|style="text-align:right"| -2.5||{{#invoke:Math|precision_format|8765.567|-2.5}}
|}

== divide ==

 &amp;#123;{#invoke:Math|divide|''x''|''y''|''round=''|''precision=''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._divide(x, y, round, precision)
&lt;/syntaxhighlight&gt;

Divide x by y.

* If y if not a number, it is returned.
* Otherwise, if x is not a number, it is returned.
* If round is true ("yes" for #invoke), the result has no decimals
* Precision indicates how many digits of precision the result should have

If any of the arguments contain HTML tags, they are returned unchanged, allowing any errors in calculating the arguments to the division function to be propagated to the calling template.  

== cleanNumber ==

&lt;syntaxhighlight lang="lua"&gt;
local number, number_string = mm._cleanNumber(number_string)
&lt;/syntaxhighlight&gt;

A helper function that can be called from other Lua modules, but not from #invoke. This takes a string or a number value as input, and if the value can be converted to a number, cleanNumber returns the number and the number string. If the value cannot be converted to a number, cleanNumber returns &lt;code&gt;nil, nil&lt;/code&gt;.

==See also==
{{Math templates}}

[[Category:qq]][[sn:Math/doc]][[Category:qq]]</text>
      <sha1>qbne3176k43bln4ltb7t6n8qqgaepin</sha1>
    </revision>
    <revision>
      <id>885137</id>
      <parentid>885136</parentid>
      <timestamp>2025-04-29T10:47:13Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "[[Category:qq]]" to ""</comment>
      <origin>885137</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8429" sha1="lqj8ofk06azbyj0445shj7k2hualnte" xml:space="preserve">{{for|''formatting'' mathematical expressions, LATEX-style|Template:Math}}
{{High-risk|1,607,000+}}
{{module rating|protected}}
This module provides a number of mathematical functions. These functions can be used from #invoke or from other Lua modules.

== Use from other Lua modules ==
To use the module from normal wiki pages, no special preparation is needed. If you are using the module from another Lua module, first you need to load it, like this:

&lt;syntaxhighlight lang="lua"&gt;
local mm = require('Module:Math')
&lt;/syntaxhighlight&gt;

(The &lt;code&gt;mm&lt;/code&gt; variable stands for '''M'''odule '''M'''ath; you can choose something more descriptive if you prefer.)

Most functions in the module have a version for Lua and a version for #invoke. It is possible to use the #invoke functions from other Lua modules, but using the Lua functions has the advantage that you do not need to access a Lua [[mw:Extension:Scribunto/Lua reference manual#Frame object|frame object]]. Lua functions are preceded by &lt;code&gt;_&lt;/code&gt;, whereas #invoke functions are not.

== random ==
{{see also|Module:Random}}

 &amp;#123;{#invoke:math|random}}
 &amp;#123;{#invoke:math|random|''max_value''}}
 &amp;#123;{#invoke:math|random|''min_value''|''max_value''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._random()
mm._random(max_value)
mm._random(min_value, max_value)
&lt;/syntaxhighlight&gt;

Generates a random number.

* If no arguments are specified, the number produced is greater than or equal to 0 and less than 1.
* If one argument is provided, the number produced is an integer between 1 and that argument. The argument must be a positive integer.
* If two arguments are provided, the number produced is an integer between the first and second arguments. Both arguments must be integers, but can be negative.

This function will not work properly for numbers less than &amp;minus;2&lt;sup&gt;32&lt;/sup&gt; and greater than 2&lt;sup&gt;32&lt;/sup&gt; &amp;minus; 1. If you need to use numbers outside of this range, it is recommended that you use [[Module:Random]].

== order ==

 &amp;#123;{#invoke:math|order|''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._order(n)
&lt;/syntaxhighlight&gt;

Determines the [[order of magnitude]] of a number.

== precision ==

 &amp;#123;{#invoke:math|precision|''n''}}
 &amp;#123;{#invoke:math|precision|x=''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision(number_string)
&lt;/syntaxhighlight&gt;

Detemines the precision of a number. For example, for "4" it will return "0", for "4.567" it will return "3", and for "100" it will return "-2".

The function attempts to parse the string representation of the number, and detects whether the number uses [[Scientific notation#E notation|E notation]]. For this reason, when called from Lua, very large numbers or very precise numbers should be directly input as strings to get accurate results. If they are input as numbers, the Lua interpreter will change them to E notation and this function will return the precision of the E notation rather than that of the original number. This is not a problem when the number is called from #invoke, as all input from #invoke is in string format.

== max ==

 &amp;#123;{#invoke:math|max|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._max(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the maximum value from the values specified. Values that cannot be converted to numbers are ignored.

== median  ==

 &amp;#123;{#invoke:math|median|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._median(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the [[median]]  value from the values specified. Values that cannot be converted to numbers are ignored.

== min ==

 &amp;#123;{#invoke:math|min|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._min(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the minimum value from the values specified. Values that cannot be converted to numbers are ignored.

== sum  ==

 &amp;#123;{#invoke:math|sum|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._sum(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the sum of the values specified. Values that cannot be converted to numbers are ignored.

== average ==

 &amp;#123;{#invoke:math|average|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._average(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the average of the values specified. (More precisely, the value returned is the [[Mean#Arithmetic mean (AM)|arithmetic mean]].) Values that cannot be converted to numbers are ignored.

== round ==

 &amp;#123;{#invoke:math|round|''value''|''precision''}}
 &amp;#123;{#invoke:math|round|value=''value''|precision=''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._round(value, precision)
&lt;/syntaxhighlight&gt;

[[Rounding|Rounds]] a number to the specified precision{{clarify|reason=What method exactly is used?}}.

Note: As of October 2019, there is a bug in the display of some rounded numbers. When trying to round a number that rounds to "n.0", like "1.02", to the nearest tenth of a digit (i.e. {{para|r|1}}), this function should display "1.0", but it unexpectedly displays "1". Use the {{para|precision_format}} parameter instead.

== log10 ==

 &amp;#123;{#invoke:math | log10 | ''x''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._log10(x)
&lt;/syntaxhighlight&gt;

Returns &lt;code&gt;log&lt;sub&gt;10&lt;/sub&gt;(''x'')&lt;/code&gt;, the [[logarithm]] of ''x'' using base 10.

== mod ==

 &amp;#123;{#invoke:math|mod|''x''|''y''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._mod(x, y)
&lt;/syntaxhighlight&gt;

Gets &lt;code&gt;''x''&lt;/code&gt; [[Modulo operation|modulo]] &lt;code&gt;''y''&lt;/code&gt;, or the remainder after &lt;code&gt;''x''&lt;/code&gt; has been divided by &lt;code&gt;''y''&lt;/code&gt;. This is accurate for integers up to 2&lt;sup&gt;53&lt;/sup&gt;; for larger integers Lua's modulo operator may return an erroneous value. This function deals with this problem by returning &lt;code&gt;0&lt;/code&gt; if the modulo given by Lua's modulo operator is less than 0 or greater than &lt;code&gt;''y''&lt;/code&gt;.

== gcd ==

 &amp;#123;{#invoke:math|gcd|''v1''|''v2''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._gcd(v1, v2, ...)
&lt;/syntaxhighlight&gt;

Finds the [[greatest common divisor]] of the values specified. Values that cannot be converted to numbers are ignored.

== precision_format ==

 &amp;#123;{#invoke:math|precision_format|''value_string''|''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision_format(value_string, precision)
&lt;/syntaxhighlight&gt;

Rounds a number to the specified precision and formats according to rules originally used for {{tl|Rnd}}.  Output is a string.

Parameter &lt;var&gt;precision&lt;/var&gt; should be an integer number of digits after the decimal point. Negative values are permitted. Non-integers give unexpected results. Positive values greater than the input precision add zero-padding, negative values greater than the input order can consume all digits.

Formatting 8,765.567 with {{tlc|#invoke:Math|precision_format|8765.567|&lt;var&gt;precision&lt;/var&gt;}} gives:
{|class="wikitable"
|+
!&lt;code&gt;&lt;var&gt;precision&lt;/var&gt;&lt;/code&gt;!!Result
|-
|style="text-align:right"|2||{{#invoke:Math|precision_format|8765.567|2}}
|-
|style="text-align:right"| -2||{{#invoke:Math|precision_format|8765.567|-2}}
|-
|style="text-align:right"|6||{{#invoke:Math|precision_format|8765.567|6}}
|-
|style="text-align:right"| -6||{{#invoke:Math|precision_format|8765.567|-6}}
|-
|style="text-align:right"|2.5||{{#invoke:Math|precision_format|8765.567|2.5}}
|-
|style="text-align:right"| -2.5||{{#invoke:Math|precision_format|8765.567|-2.5}}
|}

== divide ==

 &amp;#123;{#invoke:Math|divide|''x''|''y''|''round=''|''precision=''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._divide(x, y, round, precision)
&lt;/syntaxhighlight&gt;

Divide x by y.

* If y if not a number, it is returned.
* Otherwise, if x is not a number, it is returned.
* If round is true ("yes" for #invoke), the result has no decimals
* Precision indicates how many digits of precision the result should have

If any of the arguments contain HTML tags, they are returned unchanged, allowing any errors in calculating the arguments to the division function to be propagated to the calling template.  

== cleanNumber ==

&lt;syntaxhighlight lang="lua"&gt;
local number, number_string = mm._cleanNumber(number_string)
&lt;/syntaxhighlight&gt;

A helper function that can be called from other Lua modules, but not from #invoke. This takes a string or a number value as input, and if the value can be converted to a number, cleanNumber returns the number and the number string. If the value cannot be converted to a number, cleanNumber returns &lt;code&gt;nil, nil&lt;/code&gt;.

==See also==
{{Math templates}}

[[sn:Math/doc]]</text>
      <sha1>lqj8ofk06azbyj0445shj7k2hualnte</sha1>
    </revision>
    <revision>
      <id>885138</id>
      <parentid>885137</parentid>
      <timestamp>2025-05-20T05:53:31Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>885138</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8457" sha1="q26s246pt4ftwmdb4kuki2aq5y7c9pb" xml:space="preserve">{{for|''formatting'' mathematical expressions, LATEX-style|Template:Math}}
{{High-risk|1,607,000+}}
{{module rating|protected}}
This module provides a number of mathematical functions. These functions can be used from #invoke or from other Lua modules.

== Use from other Lua modules ==
To use the module from normal wiki pages, no special preparation is needed. If you are using the module from another Lua module, first you need to load it, like this:

&lt;syntaxhighlight lang="lua"&gt;
local mm = require('Module:Math')
&lt;/syntaxhighlight&gt;

(The &lt;code&gt;mm&lt;/code&gt; variable stands for '''M'''odule '''M'''ath; you can choose something more descriptive if you prefer.)

Most functions in the module have a version for Lua and a version for #invoke. It is possible to use the #invoke functions from other Lua modules, but using the Lua functions has the advantage that you do not need to access a Lua [[mw:Extension:Scribunto/Lua reference manual#Frame object|frame object]]. Lua functions are preceded by &lt;code&gt;_&lt;/code&gt;, whereas #invoke functions are not.

== random ==
{{see also|Module:Random}}

 &amp;#123;{#invoke:math|random}}
 &amp;#123;{#invoke:math|random|''max_value''}}
 &amp;#123;{#invoke:math|random|''min_value''|''max_value''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._random()
mm._random(max_value)
mm._random(min_value, max_value)
&lt;/syntaxhighlight&gt;

Generates a random number.

* If no arguments are specified, the number produced is greater than or equal to 0 and less than 1.
* If one argument is provided, the number produced is an integer between 1 and that argument. The argument must be a positive integer.
* If two arguments are provided, the number produced is an integer between the first and second arguments. Both arguments must be integers, but can be negative.

This function will not work properly for numbers less than &amp;minus;2&lt;sup&gt;32&lt;/sup&gt; and greater than 2&lt;sup&gt;32&lt;/sup&gt; &amp;minus; 1. If you need to use numbers outside of this range, it is recommended that you use [[Module:Random]].

== order ==

 &amp;#123;{#invoke:math|order|''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._order(n)
&lt;/syntaxhighlight&gt;

Determines the [[order of magnitude]] of a number.

== precision ==

 &amp;#123;{#invoke:math|precision|''n''}}
 &amp;#123;{#invoke:math|precision|x=''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision(number_string)
&lt;/syntaxhighlight&gt;

Detemines the precision of a number. For example, for "4" it will return "0", for "4.567" it will return "3", and for "100" it will return "-2".

The function attempts to parse the string representation of the number, and detects whether the number uses [[Scientific notation#E notation|E notation]]. For this reason, when called from Lua, very large numbers or very precise numbers should be directly input as strings to get accurate results. If they are input as numbers, the Lua interpreter will change them to E notation and this function will return the precision of the E notation rather than that of the original number. This is not a problem when the number is called from #invoke, as all input from #invoke is in string format.

== max ==

 &amp;#123;{#invoke:math|max|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._max(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the maximum value from the values specified. Values that cannot be converted to numbers are ignored.

== median  ==

 &amp;#123;{#invoke:math|median|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._median(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the [[median]]  value from the values specified. Values that cannot be converted to numbers are ignored.

== min ==

 &amp;#123;{#invoke:math|min|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._min(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the minimum value from the values specified. Values that cannot be converted to numbers are ignored.

== sum  ==

 &amp;#123;{#invoke:math|sum|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._sum(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the sum of the values specified. Values that cannot be converted to numbers are ignored.

== average ==

 &amp;#123;{#invoke:math|average|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._average(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the average of the values specified. (More precisely, the value returned is the [[Mean#Arithmetic mean (AM)|arithmetic mean]].) Values that cannot be converted to numbers are ignored.

== round ==

 &amp;#123;{#invoke:math|round|''value''|''precision''}}
 &amp;#123;{#invoke:math|round|value=''value''|precision=''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._round(value, precision)
&lt;/syntaxhighlight&gt;

[[Rounding|Rounds]] a number to the specified precision{{clarify|reason=What method exactly is used?}}.

Note: As of October 2019, there is a bug in the display of some rounded numbers. When trying to round a number that rounds to "n.0", like "1.02", to the nearest tenth of a digit (i.e. {{para|r|1}}), this function should display "1.0", but it unexpectedly displays "1". Use the {{para|precision_format}} parameter instead.

== log10 ==

 &amp;#123;{#invoke:math | log10 | ''x''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._log10(x)
&lt;/syntaxhighlight&gt;

Returns &lt;code&gt;log&lt;sub&gt;10&lt;/sub&gt;(''x'')&lt;/code&gt;, the [[logarithm]] of ''x'' using base 10.

== mod ==

 &amp;#123;{#invoke:math|mod|''x''|''y''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._mod(x, y)
&lt;/syntaxhighlight&gt;

Gets &lt;code&gt;''x''&lt;/code&gt; [[Modulo operation|modulo]] &lt;code&gt;''y''&lt;/code&gt;, or the remainder after &lt;code&gt;''x''&lt;/code&gt; has been divided by &lt;code&gt;''y''&lt;/code&gt;. This is accurate for integers up to 2&lt;sup&gt;53&lt;/sup&gt;; for larger integers Lua's modulo operator may return an erroneous value. This function deals with this problem by returning &lt;code&gt;0&lt;/code&gt; if the modulo given by Lua's modulo operator is less than 0 or greater than &lt;code&gt;''y''&lt;/code&gt;.

== gcd ==

 &amp;#123;{#invoke:math|gcd|''v1''|''v2''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._gcd(v1, v2, ...)
&lt;/syntaxhighlight&gt;

Finds the [[:en:Greatest common divisor|Greatest common divisor]] of the values specified. Values that cannot be converted to numbers are ignored.

== precision_format ==

 &amp;#123;{#invoke:math|precision_format|''value_string''|''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision_format(value_string, precision)
&lt;/syntaxhighlight&gt;

Rounds a number to the specified precision and formats according to rules originally used for {{tl|Rnd}}.  Output is a string.

Parameter &lt;var&gt;precision&lt;/var&gt; should be an integer number of digits after the decimal point. Negative values are permitted. Non-integers give unexpected results. Positive values greater than the input precision add zero-padding, negative values greater than the input order can consume all digits.

Formatting 8,765.567 with {{tlc|#invoke:Math|precision_format|8765.567|&lt;var&gt;precision&lt;/var&gt;}} gives:
{|class="wikitable"
|+
!&lt;code&gt;&lt;var&gt;precision&lt;/var&gt;&lt;/code&gt;!!Result
|-
|style="text-align:right"|2||{{#invoke:Math|precision_format|8765.567|2}}
|-
|style="text-align:right"| -2||{{#invoke:Math|precision_format|8765.567|-2}}
|-
|style="text-align:right"|6||{{#invoke:Math|precision_format|8765.567|6}}
|-
|style="text-align:right"| -6||{{#invoke:Math|precision_format|8765.567|-6}}
|-
|style="text-align:right"|2.5||{{#invoke:Math|precision_format|8765.567|2.5}}
|-
|style="text-align:right"| -2.5||{{#invoke:Math|precision_format|8765.567|-2.5}}
|}

== divide ==

 &amp;#123;{#invoke:Math|divide|''x''|''y''|''round=''|''precision=''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._divide(x, y, round, precision)
&lt;/syntaxhighlight&gt;

Divide x by y.

* If y if not a number, it is returned.
* Otherwise, if x is not a number, it is returned.
* If round is true ("yes" for #invoke), the result has no decimals
* Precision indicates how many digits of precision the result should have

If any of the arguments contain HTML tags, they are returned unchanged, allowing any errors in calculating the arguments to the division function to be propagated to the calling template.  

== cleanNumber ==

&lt;syntaxhighlight lang="lua"&gt;
local number, number_string = mm._cleanNumber(number_string)
&lt;/syntaxhighlight&gt;

A helper function that can be called from other Lua modules, but not from #invoke. This takes a string or a number value as input, and if the value can be converted to a number, cleanNumber returns the number and the number string. If the value cannot be converted to a number, cleanNumber returns &lt;code&gt;nil, nil&lt;/code&gt;.

==See also==
{{Math templates}}

[[sn:Math/doc]]</text>
      <sha1>q26s246pt4ftwmdb4kuki2aq5y7c9pb</sha1>
    </revision>
    <revision>
      <id>885139</id>
      <parentid>885138</parentid>
      <timestamp>2025-05-22T23:48:11Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>885139</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8471" sha1="c8dooqnw8scihlrh2ifvypz6k7gt5m8" xml:space="preserve">{{for|''formatting'' mathematical expressions, LATEX-style|Template:Math}}
{{High-risk|1,607,000+}}
{{module rating|protected}}
This module provides a number of mathematical functions. These functions can be used from #invoke or from other Lua modules.

== Use from other Lua modules ==
To use the module from normal wiki pages, no special preparation is needed. If you are using the module from another Lua module, first you need to load it, like this:

&lt;syntaxhighlight lang="lua"&gt;
local mm = require('Module:Math')
&lt;/syntaxhighlight&gt;

(The &lt;code&gt;mm&lt;/code&gt; variable stands for '''M'''odule '''M'''ath; you can choose something more descriptive if you prefer.)

Most functions in the module have a version for Lua and a version for #invoke. It is possible to use the #invoke functions from other Lua modules, but using the Lua functions has the advantage that you do not need to access a Lua [[mw:Extension:Scribunto/Lua reference manual#Frame object|frame object]]. Lua functions are preceded by &lt;code&gt;_&lt;/code&gt;, whereas #invoke functions are not.

== random ==
{{see also|Module:Random}}

 &amp;#123;{#invoke:math|random}}
 &amp;#123;{#invoke:math|random|''max_value''}}
 &amp;#123;{#invoke:math|random|''min_value''|''max_value''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._random()
mm._random(max_value)
mm._random(min_value, max_value)
&lt;/syntaxhighlight&gt;

Generates a random number.

* If no arguments are specified, the number produced is greater than or equal to 0 and less than 1.
* If one argument is provided, the number produced is an integer between 1 and that argument. The argument must be a positive integer.
* If two arguments are provided, the number produced is an integer between the first and second arguments. Both arguments must be integers, but can be negative.

This function will not work properly for numbers less than &amp;minus;2&lt;sup&gt;32&lt;/sup&gt; and greater than 2&lt;sup&gt;32&lt;/sup&gt; &amp;minus; 1. If you need to use numbers outside of this range, it is recommended that you use [[Module:Random]].

== order ==

 &amp;#123;{#invoke:math|order|''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._order(n)
&lt;/syntaxhighlight&gt;

Determines the [[order of magnitude]] of a number.

== precision ==

 &amp;#123;{#invoke:math|precision|''n''}}
 &amp;#123;{#invoke:math|precision|x=''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision(number_string)
&lt;/syntaxhighlight&gt;

Detemines the precision of a number. For example, for "4" it will return "0", for "4.567" it will return "3", and for "100" it will return "-2".

The function attempts to parse the string representation of the number, and detects whether the number uses [[Scientific notation#E notation|E notation]]. For this reason, when called from Lua, very large numbers or very precise numbers should be directly input as strings to get accurate results. If they are input as numbers, the Lua interpreter will change them to E notation and this function will return the precision of the E notation rather than that of the original number. This is not a problem when the number is called from #invoke, as all input from #invoke is in string format.

== max ==

 &amp;#123;{#invoke:math|max|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._max(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the maximum value from the values specified. Values that cannot be converted to numbers are ignored.

== median  ==

 &amp;#123;{#invoke:math|median|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._median(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the [[median]]  value from the values specified. Values that cannot be converted to numbers are ignored.

== min ==

 &amp;#123;{#invoke:math|min|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._min(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the minimum value from the values specified. Values that cannot be converted to numbers are ignored.

== sum  ==

 &amp;#123;{#invoke:math|sum|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._sum(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the sum of the values specified. Values that cannot be converted to numbers are ignored.

== average ==

 &amp;#123;{#invoke:math|average|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._average(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the average of the values specified. (More precisely, the value returned is the [[Mean#Arithmetic mean (AM)|arithmetic mean]].) Values that cannot be converted to numbers are ignored.

== round ==

 &amp;#123;{#invoke:math|round|''value''|''precision''}}
 &amp;#123;{#invoke:math|round|value=''value''|precision=''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._round(value, precision)
&lt;/syntaxhighlight&gt;

[[Rounding|Rounds]] a number to the specified precision{{clarify|reason=What method exactly is used?}}.

Note: As of October 2019, there is a bug in the display of some rounded numbers. When trying to round a number that rounds to "n.0", like "1.02", to the nearest tenth of a digit (i.e. {{para|r|1}}), this function should display "1.0", but it unexpectedly displays "1". Use the {{para|precision_format}} parameter instead.

== log10 ==

 &amp;#123;{#invoke:math | log10 | ''x''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._log10(x)
&lt;/syntaxhighlight&gt;

Returns &lt;code&gt;log&lt;sub&gt;10&lt;/sub&gt;(''x'')&lt;/code&gt;, the [[:en:Logarithm|Logarithm]] of ''x'' using base 10.

== mod ==

 &amp;#123;{#invoke:math|mod|''x''|''y''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._mod(x, y)
&lt;/syntaxhighlight&gt;

Gets &lt;code&gt;''x''&lt;/code&gt; [[Modulo operation|modulo]] &lt;code&gt;''y''&lt;/code&gt;, or the remainder after &lt;code&gt;''x''&lt;/code&gt; has been divided by &lt;code&gt;''y''&lt;/code&gt;. This is accurate for integers up to 2&lt;sup&gt;53&lt;/sup&gt;; for larger integers Lua's modulo operator may return an erroneous value. This function deals with this problem by returning &lt;code&gt;0&lt;/code&gt; if the modulo given by Lua's modulo operator is less than 0 or greater than &lt;code&gt;''y''&lt;/code&gt;.

== gcd ==

 &amp;#123;{#invoke:math|gcd|''v1''|''v2''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._gcd(v1, v2, ...)
&lt;/syntaxhighlight&gt;

Finds the [[:en:Greatest common divisor|Greatest common divisor]] of the values specified. Values that cannot be converted to numbers are ignored.

== precision_format ==

 &amp;#123;{#invoke:math|precision_format|''value_string''|''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision_format(value_string, precision)
&lt;/syntaxhighlight&gt;

Rounds a number to the specified precision and formats according to rules originally used for {{tl|Rnd}}.  Output is a string.

Parameter &lt;var&gt;precision&lt;/var&gt; should be an integer number of digits after the decimal point. Negative values are permitted. Non-integers give unexpected results. Positive values greater than the input precision add zero-padding, negative values greater than the input order can consume all digits.

Formatting 8,765.567 with {{tlc|#invoke:Math|precision_format|8765.567|&lt;var&gt;precision&lt;/var&gt;}} gives:
{|class="wikitable"
|+
!&lt;code&gt;&lt;var&gt;precision&lt;/var&gt;&lt;/code&gt;!!Result
|-
|style="text-align:right"|2||{{#invoke:Math|precision_format|8765.567|2}}
|-
|style="text-align:right"| -2||{{#invoke:Math|precision_format|8765.567|-2}}
|-
|style="text-align:right"|6||{{#invoke:Math|precision_format|8765.567|6}}
|-
|style="text-align:right"| -6||{{#invoke:Math|precision_format|8765.567|-6}}
|-
|style="text-align:right"|2.5||{{#invoke:Math|precision_format|8765.567|2.5}}
|-
|style="text-align:right"| -2.5||{{#invoke:Math|precision_format|8765.567|-2.5}}
|}

== divide ==

 &amp;#123;{#invoke:Math|divide|''x''|''y''|''round=''|''precision=''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._divide(x, y, round, precision)
&lt;/syntaxhighlight&gt;

Divide x by y.

* If y if not a number, it is returned.
* Otherwise, if x is not a number, it is returned.
* If round is true ("yes" for #invoke), the result has no decimals
* Precision indicates how many digits of precision the result should have

If any of the arguments contain HTML tags, they are returned unchanged, allowing any errors in calculating the arguments to the division function to be propagated to the calling template.  

== cleanNumber ==

&lt;syntaxhighlight lang="lua"&gt;
local number, number_string = mm._cleanNumber(number_string)
&lt;/syntaxhighlight&gt;

A helper function that can be called from other Lua modules, but not from #invoke. This takes a string or a number value as input, and if the value can be converted to a number, cleanNumber returns the number and the number string. If the value cannot be converted to a number, cleanNumber returns &lt;code&gt;nil, nil&lt;/code&gt;.

==See also==
{{Math templates}}

[[sn:Math/doc]]</text>
      <sha1>c8dooqnw8scihlrh2ifvypz6k7gt5m8</sha1>
    </revision>
    <revision>
      <id>885140</id>
      <parentid>885139</parentid>
      <timestamp>2025-05-23T03:07:05Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>885140</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8482" sha1="iqdif7lep4fbolcqg3mcm1877ttifm8" xml:space="preserve">{{for|''formatting'' mathematical expressions, LATEX-style|Template:Math}}
{{High-risk|1,607,000+}}
{{module rating|protected}}
This module provides a number of mathematical functions. These functions can be used from #invoke or from other Lua modules.

== Use from other Lua modules ==
To use the module from normal wiki pages, no special preparation is needed. If you are using the module from another Lua module, first you need to load it, like this:

&lt;syntaxhighlight lang="lua"&gt;
local mm = require('Module:Math')
&lt;/syntaxhighlight&gt;

(The &lt;code&gt;mm&lt;/code&gt; variable stands for '''M'''odule '''M'''ath; you can choose something more descriptive if you prefer.)

Most functions in the module have a version for Lua and a version for #invoke. It is possible to use the #invoke functions from other Lua modules, but using the Lua functions has the advantage that you do not need to access a Lua [[mw:Extension:Scribunto/Lua reference manual#Frame object|frame object]]. Lua functions are preceded by &lt;code&gt;_&lt;/code&gt;, whereas #invoke functions are not.

== random ==
{{see also|Module:Random}}

 &amp;#123;{#invoke:math|random}}
 &amp;#123;{#invoke:math|random|''max_value''}}
 &amp;#123;{#invoke:math|random|''min_value''|''max_value''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._random()
mm._random(max_value)
mm._random(min_value, max_value)
&lt;/syntaxhighlight&gt;

Generates a random number.

* If no arguments are specified, the number produced is greater than or equal to 0 and less than 1.
* If one argument is provided, the number produced is an integer between 1 and that argument. The argument must be a positive integer.
* If two arguments are provided, the number produced is an integer between the first and second arguments. Both arguments must be integers, but can be negative.

This function will not work properly for numbers less than &amp;minus;2&lt;sup&gt;32&lt;/sup&gt; and greater than 2&lt;sup&gt;32&lt;/sup&gt; &amp;minus; 1. If you need to use numbers outside of this range, it is recommended that you use [[Module:Random]].

== order ==

 &amp;#123;{#invoke:math|order|''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._order(n)
&lt;/syntaxhighlight&gt;

Determines the [[order of magnitude]] of a number.

== precision ==

 &amp;#123;{#invoke:math|precision|''n''}}
 &amp;#123;{#invoke:math|precision|x=''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision(number_string)
&lt;/syntaxhighlight&gt;

Detemines the precision of a number. For example, for "4" it will return "0", for "4.567" it will return "3", and for "100" it will return "-2".

The function attempts to parse the string representation of the number, and detects whether the number uses [[Scientific notation#E notation|E notation]]. For this reason, when called from Lua, very large numbers or very precise numbers should be directly input as strings to get accurate results. If they are input as numbers, the Lua interpreter will change them to E notation and this function will return the precision of the E notation rather than that of the original number. This is not a problem when the number is called from #invoke, as all input from #invoke is in string format.

== max ==

 &amp;#123;{#invoke:math|max|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._max(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the maximum value from the values specified. Values that cannot be converted to numbers are ignored.

== median  ==

 &amp;#123;{#invoke:math|median|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._median(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the [[:en:Median|Median]]  value from the values specified. Values that cannot be converted to numbers are ignored.

== min ==

 &amp;#123;{#invoke:math|min|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._min(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the minimum value from the values specified. Values that cannot be converted to numbers are ignored.

== sum  ==

 &amp;#123;{#invoke:math|sum|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._sum(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the sum of the values specified. Values that cannot be converted to numbers are ignored.

== average ==

 &amp;#123;{#invoke:math|average|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._average(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the average of the values specified. (More precisely, the value returned is the [[Mean#Arithmetic mean (AM)|arithmetic mean]].) Values that cannot be converted to numbers are ignored.

== round ==

 &amp;#123;{#invoke:math|round|''value''|''precision''}}
 &amp;#123;{#invoke:math|round|value=''value''|precision=''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._round(value, precision)
&lt;/syntaxhighlight&gt;

[[Rounding|Rounds]] a number to the specified precision{{clarify|reason=What method exactly is used?}}.

Note: As of October 2019, there is a bug in the display of some rounded numbers. When trying to round a number that rounds to "n.0", like "1.02", to the nearest tenth of a digit (i.e. {{para|r|1}}), this function should display "1.0", but it unexpectedly displays "1". Use the {{para|precision_format}} parameter instead.

== log10 ==

 &amp;#123;{#invoke:math | log10 | ''x''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._log10(x)
&lt;/syntaxhighlight&gt;

Returns &lt;code&gt;log&lt;sub&gt;10&lt;/sub&gt;(''x'')&lt;/code&gt;, the [[:en:Logarithm|Logarithm]] of ''x'' using base 10.

== mod ==

 &amp;#123;{#invoke:math|mod|''x''|''y''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._mod(x, y)
&lt;/syntaxhighlight&gt;

Gets &lt;code&gt;''x''&lt;/code&gt; [[Modulo operation|modulo]] &lt;code&gt;''y''&lt;/code&gt;, or the remainder after &lt;code&gt;''x''&lt;/code&gt; has been divided by &lt;code&gt;''y''&lt;/code&gt;. This is accurate for integers up to 2&lt;sup&gt;53&lt;/sup&gt;; for larger integers Lua's modulo operator may return an erroneous value. This function deals with this problem by returning &lt;code&gt;0&lt;/code&gt; if the modulo given by Lua's modulo operator is less than 0 or greater than &lt;code&gt;''y''&lt;/code&gt;.

== gcd ==

 &amp;#123;{#invoke:math|gcd|''v1''|''v2''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._gcd(v1, v2, ...)
&lt;/syntaxhighlight&gt;

Finds the [[:en:Greatest common divisor|Greatest common divisor]] of the values specified. Values that cannot be converted to numbers are ignored.

== precision_format ==

 &amp;#123;{#invoke:math|precision_format|''value_string''|''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision_format(value_string, precision)
&lt;/syntaxhighlight&gt;

Rounds a number to the specified precision and formats according to rules originally used for {{tl|Rnd}}.  Output is a string.

Parameter &lt;var&gt;precision&lt;/var&gt; should be an integer number of digits after the decimal point. Negative values are permitted. Non-integers give unexpected results. Positive values greater than the input precision add zero-padding, negative values greater than the input order can consume all digits.

Formatting 8,765.567 with {{tlc|#invoke:Math|precision_format|8765.567|&lt;var&gt;precision&lt;/var&gt;}} gives:
{|class="wikitable"
|+
!&lt;code&gt;&lt;var&gt;precision&lt;/var&gt;&lt;/code&gt;!!Result
|-
|style="text-align:right"|2||{{#invoke:Math|precision_format|8765.567|2}}
|-
|style="text-align:right"| -2||{{#invoke:Math|precision_format|8765.567|-2}}
|-
|style="text-align:right"|6||{{#invoke:Math|precision_format|8765.567|6}}
|-
|style="text-align:right"| -6||{{#invoke:Math|precision_format|8765.567|-6}}
|-
|style="text-align:right"|2.5||{{#invoke:Math|precision_format|8765.567|2.5}}
|-
|style="text-align:right"| -2.5||{{#invoke:Math|precision_format|8765.567|-2.5}}
|}

== divide ==

 &amp;#123;{#invoke:Math|divide|''x''|''y''|''round=''|''precision=''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._divide(x, y, round, precision)
&lt;/syntaxhighlight&gt;

Divide x by y.

* If y if not a number, it is returned.
* Otherwise, if x is not a number, it is returned.
* If round is true ("yes" for #invoke), the result has no decimals
* Precision indicates how many digits of precision the result should have

If any of the arguments contain HTML tags, they are returned unchanged, allowing any errors in calculating the arguments to the division function to be propagated to the calling template.  

== cleanNumber ==

&lt;syntaxhighlight lang="lua"&gt;
local number, number_string = mm._cleanNumber(number_string)
&lt;/syntaxhighlight&gt;

A helper function that can be called from other Lua modules, but not from #invoke. This takes a string or a number value as input, and if the value can be converted to a number, cleanNumber returns the number and the number string. If the value cannot be converted to a number, cleanNumber returns &lt;code&gt;nil, nil&lt;/code&gt;.

==See also==
{{Math templates}}

[[sn:Math/doc]]</text>
      <sha1>iqdif7lep4fbolcqg3mcm1877ttifm8</sha1>
    </revision>
    <revision>
      <id>885141</id>
      <parentid>885140</parentid>
      <timestamp>2025-05-23T07:07:41Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>885141</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8486" sha1="p5fm5lmlq0uphkzno5vort1h4cvz12f" xml:space="preserve">{{for|''formatting'' mathematical expressions, LATEX-style|Template:Math}}
{{High-risk|1,607,000+}}
{{module rating|protected}}
This module provides a number of mathematical functions. These functions can be used from #invoke or from other Lua modules.

== Use from other Lua modules ==
To use the module from normal wiki pages, no special preparation is needed. If you are using the module from another Lua module, first you need to load it, like this:

&lt;syntaxhighlight lang="lua"&gt;
local mm = require('Module:Math')
&lt;/syntaxhighlight&gt;

(The &lt;code&gt;mm&lt;/code&gt; variable stands for '''M'''odule '''M'''ath; you can choose something more descriptive if you prefer.)

Most functions in the module have a version for Lua and a version for #invoke. It is possible to use the #invoke functions from other Lua modules, but using the Lua functions has the advantage that you do not need to access a Lua [[mw:Extension:Scribunto/Lua reference manual#Frame object|frame object]]. Lua functions are preceded by &lt;code&gt;_&lt;/code&gt;, whereas #invoke functions are not.

== random ==
{{see also|Module:Random}}

 &amp;#123;{#invoke:math|random}}
 &amp;#123;{#invoke:math|random|''max_value''}}
 &amp;#123;{#invoke:math|random|''min_value''|''max_value''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._random()
mm._random(max_value)
mm._random(min_value, max_value)
&lt;/syntaxhighlight&gt;

Generates a random number.

* If no arguments are specified, the number produced is greater than or equal to 0 and less than 1.
* If one argument is provided, the number produced is an integer between 1 and that argument. The argument must be a positive integer.
* If two arguments are provided, the number produced is an integer between the first and second arguments. Both arguments must be integers, but can be negative.

This function will not work properly for numbers less than &amp;minus;2&lt;sup&gt;32&lt;/sup&gt; and greater than 2&lt;sup&gt;32&lt;/sup&gt; &amp;minus; 1. If you need to use numbers outside of this range, it is recommended that you use [[Module:Random]].

== order ==

 &amp;#123;{#invoke:math|order|''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._order(n)
&lt;/syntaxhighlight&gt;

Determines the [[order of magnitude]] of a number.

== precision ==

 &amp;#123;{#invoke:math|precision|''n''}}
 &amp;#123;{#invoke:math|precision|x=''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision(number_string)
&lt;/syntaxhighlight&gt;

Detemines the precision of a number. For example, for "4" it will return "0", for "4.567" it will return "3", and for "100" it will return "-2".

The function attempts to parse the string representation of the number, and detects whether the number uses [[Scientific notation#E notation|E notation]]. For this reason, when called from Lua, very large numbers or very precise numbers should be directly input as strings to get accurate results. If they are input as numbers, the Lua interpreter will change them to E notation and this function will return the precision of the E notation rather than that of the original number. This is not a problem when the number is called from #invoke, as all input from #invoke is in string format.

== max ==

 &amp;#123;{#invoke:math|max|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._max(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the maximum value from the values specified. Values that cannot be converted to numbers are ignored.

== median  ==

 &amp;#123;{#invoke:math|median|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._median(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the [[:en:Median|Median]]  value from the values specified. Values that cannot be converted to numbers are ignored.

== min ==

 &amp;#123;{#invoke:math|min|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._min(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the minimum value from the values specified. Values that cannot be converted to numbers are ignored.

== sum  ==

 &amp;#123;{#invoke:math|sum|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._sum(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the sum of the values specified. Values that cannot be converted to numbers are ignored.

== average ==

 &amp;#123;{#invoke:math|average|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._average(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the average of the values specified. (More precisely, the value returned is the [[Mean#Arithmetic mean (AM)|arithmetic mean]].) Values that cannot be converted to numbers are ignored.

== round ==

 &amp;#123;{#invoke:math|round|''value''|''precision''}}
 &amp;#123;{#invoke:math|round|value=''value''|precision=''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._round(value, precision)
&lt;/syntaxhighlight&gt;

[[Rounding|Rounds]] a number to the specified precision{{clarify|reason=What method exactly is used?}}.

Note: As of October 2019, there is a bug in the display of some rounded numbers. When trying to round a number that rounds to "n.0", like "1.02", to the nearest tenth of a digit (i.e. {{para|r|1}}), this function should display "1.0", but it unexpectedly displays "1". Use the {{para|precision_format}} parameter instead.

== log10 ==

 &amp;#123;{#invoke:math | log10 | ''x''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._log10(x)
&lt;/syntaxhighlight&gt;

Returns &lt;code&gt;log&lt;sub&gt;10&lt;/sub&gt;(''x'')&lt;/code&gt;, the [[:en:Logarithm|Logarithm]] of ''x'' using base 10.

== mod ==

 &amp;#123;{#invoke:math|mod|''x''|''y''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._mod(x, y)
&lt;/syntaxhighlight&gt;

Gets &lt;code&gt;''x''&lt;/code&gt; [[:en:Modulo operation|modulo]] &lt;code&gt;''y''&lt;/code&gt;, or the remainder after &lt;code&gt;''x''&lt;/code&gt; has been divided by &lt;code&gt;''y''&lt;/code&gt;. This is accurate for integers up to 2&lt;sup&gt;53&lt;/sup&gt;; for larger integers Lua's modulo operator may return an erroneous value. This function deals with this problem by returning &lt;code&gt;0&lt;/code&gt; if the modulo given by Lua's modulo operator is less than 0 or greater than &lt;code&gt;''y''&lt;/code&gt;.

== gcd ==

 &amp;#123;{#invoke:math|gcd|''v1''|''v2''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._gcd(v1, v2, ...)
&lt;/syntaxhighlight&gt;

Finds the [[:en:Greatest common divisor|Greatest common divisor]] of the values specified. Values that cannot be converted to numbers are ignored.

== precision_format ==

 &amp;#123;{#invoke:math|precision_format|''value_string''|''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision_format(value_string, precision)
&lt;/syntaxhighlight&gt;

Rounds a number to the specified precision and formats according to rules originally used for {{tl|Rnd}}.  Output is a string.

Parameter &lt;var&gt;precision&lt;/var&gt; should be an integer number of digits after the decimal point. Negative values are permitted. Non-integers give unexpected results. Positive values greater than the input precision add zero-padding, negative values greater than the input order can consume all digits.

Formatting 8,765.567 with {{tlc|#invoke:Math|precision_format|8765.567|&lt;var&gt;precision&lt;/var&gt;}} gives:
{|class="wikitable"
|+
!&lt;code&gt;&lt;var&gt;precision&lt;/var&gt;&lt;/code&gt;!!Result
|-
|style="text-align:right"|2||{{#invoke:Math|precision_format|8765.567|2}}
|-
|style="text-align:right"| -2||{{#invoke:Math|precision_format|8765.567|-2}}
|-
|style="text-align:right"|6||{{#invoke:Math|precision_format|8765.567|6}}
|-
|style="text-align:right"| -6||{{#invoke:Math|precision_format|8765.567|-6}}
|-
|style="text-align:right"|2.5||{{#invoke:Math|precision_format|8765.567|2.5}}
|-
|style="text-align:right"| -2.5||{{#invoke:Math|precision_format|8765.567|-2.5}}
|}

== divide ==

 &amp;#123;{#invoke:Math|divide|''x''|''y''|''round=''|''precision=''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._divide(x, y, round, precision)
&lt;/syntaxhighlight&gt;

Divide x by y.

* If y if not a number, it is returned.
* Otherwise, if x is not a number, it is returned.
* If round is true ("yes" for #invoke), the result has no decimals
* Precision indicates how many digits of precision the result should have

If any of the arguments contain HTML tags, they are returned unchanged, allowing any errors in calculating the arguments to the division function to be propagated to the calling template.  

== cleanNumber ==

&lt;syntaxhighlight lang="lua"&gt;
local number, number_string = mm._cleanNumber(number_string)
&lt;/syntaxhighlight&gt;

A helper function that can be called from other Lua modules, but not from #invoke. This takes a string or a number value as input, and if the value can be converted to a number, cleanNumber returns the number and the number string. If the value cannot be converted to a number, cleanNumber returns &lt;code&gt;nil, nil&lt;/code&gt;.

==See also==
{{Math templates}}

[[sn:Math/doc]]</text>
      <sha1>p5fm5lmlq0uphkzno5vort1h4cvz12f</sha1>
    </revision>
    <revision>
      <id>885142</id>
      <parentid>885141</parentid>
      <timestamp>2025-05-24T08:27:46Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>885142</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8509" sha1="qymalbghp7lv3rm2wrzb9m3cmkqfynj" xml:space="preserve">{{for|''formatting'' mathematical expressions, LATEX-style|Template:Math}}
{{High-risk|1,607,000+}}
{{module rating|protected}}
This module provides a number of mathematical functions. These functions can be used from #invoke or from other Lua modules.

== Use from other Lua modules ==
To use the module from normal wiki pages, no special preparation is needed. If you are using the module from another Lua module, first you need to load it, like this:

&lt;syntaxhighlight lang="lua"&gt;
local mm = require('Module:Math')
&lt;/syntaxhighlight&gt;

(The &lt;code&gt;mm&lt;/code&gt; variable stands for '''M'''odule '''M'''ath; you can choose something more descriptive if you prefer.)

Most functions in the module have a version for Lua and a version for #invoke. It is possible to use the #invoke functions from other Lua modules, but using the Lua functions has the advantage that you do not need to access a Lua [[mw:Extension:Scribunto/Lua reference manual#Frame object|frame object]]. Lua functions are preceded by &lt;code&gt;_&lt;/code&gt;, whereas #invoke functions are not.

== random ==
{{see also|Module:Random}}

 &amp;#123;{#invoke:math|random}}
 &amp;#123;{#invoke:math|random|''max_value''}}
 &amp;#123;{#invoke:math|random|''min_value''|''max_value''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._random()
mm._random(max_value)
mm._random(min_value, max_value)
&lt;/syntaxhighlight&gt;

Generates a random number.

* If no arguments are specified, the number produced is greater than or equal to 0 and less than 1.
* If one argument is provided, the number produced is an integer between 1 and that argument. The argument must be a positive integer.
* If two arguments are provided, the number produced is an integer between the first and second arguments. Both arguments must be integers, but can be negative.

This function will not work properly for numbers less than &amp;minus;2&lt;sup&gt;32&lt;/sup&gt; and greater than 2&lt;sup&gt;32&lt;/sup&gt; &amp;minus; 1. If you need to use numbers outside of this range, it is recommended that you use [[Module:Random]].

== order ==

 &amp;#123;{#invoke:math|order|''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._order(n)
&lt;/syntaxhighlight&gt;

Determines the [[:en:Order of magnitude|Order of magnitude]] of a number.

== precision ==

 &amp;#123;{#invoke:math|precision|''n''}}
 &amp;#123;{#invoke:math|precision|x=''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision(number_string)
&lt;/syntaxhighlight&gt;

Detemines the precision of a number. For example, for "4" it will return "0", for "4.567" it will return "3", and for "100" it will return "-2".

The function attempts to parse the string representation of the number, and detects whether the number uses [[Scientific notation#E notation|E notation]]. For this reason, when called from Lua, very large numbers or very precise numbers should be directly input as strings to get accurate results. If they are input as numbers, the Lua interpreter will change them to E notation and this function will return the precision of the E notation rather than that of the original number. This is not a problem when the number is called from #invoke, as all input from #invoke is in string format.

== max ==

 &amp;#123;{#invoke:math|max|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._max(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the maximum value from the values specified. Values that cannot be converted to numbers are ignored.

== median  ==

 &amp;#123;{#invoke:math|median|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._median(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the [[:en:Median|Median]]  value from the values specified. Values that cannot be converted to numbers are ignored.

== min ==

 &amp;#123;{#invoke:math|min|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._min(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the minimum value from the values specified. Values that cannot be converted to numbers are ignored.

== sum  ==

 &amp;#123;{#invoke:math|sum|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._sum(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the sum of the values specified. Values that cannot be converted to numbers are ignored.

== average ==

 &amp;#123;{#invoke:math|average|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._average(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the average of the values specified. (More precisely, the value returned is the [[Mean#Arithmetic mean (AM)|arithmetic mean]].) Values that cannot be converted to numbers are ignored.

== round ==

 &amp;#123;{#invoke:math|round|''value''|''precision''}}
 &amp;#123;{#invoke:math|round|value=''value''|precision=''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._round(value, precision)
&lt;/syntaxhighlight&gt;

[[Rounding|Rounds]] a number to the specified precision{{clarify|reason=What method exactly is used?}}.

Note: As of October 2019, there is a bug in the display of some rounded numbers. When trying to round a number that rounds to "n.0", like "1.02", to the nearest tenth of a digit (i.e. {{para|r|1}}), this function should display "1.0", but it unexpectedly displays "1". Use the {{para|precision_format}} parameter instead.

== log10 ==

 &amp;#123;{#invoke:math | log10 | ''x''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._log10(x)
&lt;/syntaxhighlight&gt;

Returns &lt;code&gt;log&lt;sub&gt;10&lt;/sub&gt;(''x'')&lt;/code&gt;, the [[:en:Logarithm|Logarithm]] of ''x'' using base 10.

== mod ==

 &amp;#123;{#invoke:math|mod|''x''|''y''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._mod(x, y)
&lt;/syntaxhighlight&gt;

Gets &lt;code&gt;''x''&lt;/code&gt; [[:en:Modulo operation|modulo]] &lt;code&gt;''y''&lt;/code&gt;, or the remainder after &lt;code&gt;''x''&lt;/code&gt; has been divided by &lt;code&gt;''y''&lt;/code&gt;. This is accurate for integers up to 2&lt;sup&gt;53&lt;/sup&gt;; for larger integers Lua's modulo operator may return an erroneous value. This function deals with this problem by returning &lt;code&gt;0&lt;/code&gt; if the modulo given by Lua's modulo operator is less than 0 or greater than &lt;code&gt;''y''&lt;/code&gt;.

== gcd ==

 &amp;#123;{#invoke:math|gcd|''v1''|''v2''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._gcd(v1, v2, ...)
&lt;/syntaxhighlight&gt;

Finds the [[:en:Greatest common divisor|Greatest common divisor]] of the values specified. Values that cannot be converted to numbers are ignored.

== precision_format ==

 &amp;#123;{#invoke:math|precision_format|''value_string''|''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision_format(value_string, precision)
&lt;/syntaxhighlight&gt;

Rounds a number to the specified precision and formats according to rules originally used for {{tl|Rnd}}.  Output is a string.

Parameter &lt;var&gt;precision&lt;/var&gt; should be an integer number of digits after the decimal point. Negative values are permitted. Non-integers give unexpected results. Positive values greater than the input precision add zero-padding, negative values greater than the input order can consume all digits.

Formatting 8,765.567 with {{tlc|#invoke:Math|precision_format|8765.567|&lt;var&gt;precision&lt;/var&gt;}} gives:
{|class="wikitable"
|+
!&lt;code&gt;&lt;var&gt;precision&lt;/var&gt;&lt;/code&gt;!!Result
|-
|style="text-align:right"|2||{{#invoke:Math|precision_format|8765.567|2}}
|-
|style="text-align:right"| -2||{{#invoke:Math|precision_format|8765.567|-2}}
|-
|style="text-align:right"|6||{{#invoke:Math|precision_format|8765.567|6}}
|-
|style="text-align:right"| -6||{{#invoke:Math|precision_format|8765.567|-6}}
|-
|style="text-align:right"|2.5||{{#invoke:Math|precision_format|8765.567|2.5}}
|-
|style="text-align:right"| -2.5||{{#invoke:Math|precision_format|8765.567|-2.5}}
|}

== divide ==

 &amp;#123;{#invoke:Math|divide|''x''|''y''|''round=''|''precision=''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._divide(x, y, round, precision)
&lt;/syntaxhighlight&gt;

Divide x by y.

* If y if not a number, it is returned.
* Otherwise, if x is not a number, it is returned.
* If round is true ("yes" for #invoke), the result has no decimals
* Precision indicates how many digits of precision the result should have

If any of the arguments contain HTML tags, they are returned unchanged, allowing any errors in calculating the arguments to the division function to be propagated to the calling template.  

== cleanNumber ==

&lt;syntaxhighlight lang="lua"&gt;
local number, number_string = mm._cleanNumber(number_string)
&lt;/syntaxhighlight&gt;

A helper function that can be called from other Lua modules, but not from #invoke. This takes a string or a number value as input, and if the value can be converted to a number, cleanNumber returns the number and the number string. If the value cannot be converted to a number, cleanNumber returns &lt;code&gt;nil, nil&lt;/code&gt;.

==See also==
{{Math templates}}

[[sn:Math/doc]]</text>
      <sha1>qymalbghp7lv3rm2wrzb9m3cmkqfynj</sha1>
    </revision>
    <revision>
      <id>7647</id>
      <timestamp>2025-05-24T22:20:51Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was Bot: convert local link → enwiki interwiki</comment>
      <origin>7647</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8513" sha1="2l65l40erc9ydj4tdh6ee2lh69kdurc" xml:space="preserve">{{for|''formatting'' mathematical expressions, LATEX-style|Template:Math}}
{{High-risk|1,607,000+}}
{{module rating|protected}}
This module provides a number of mathematical functions. These functions can be used from #invoke or from other Lua modules.

== Use from other Lua modules ==
To use the module from normal wiki pages, no special preparation is needed. If you are using the module from another Lua module, first you need to load it, like this:

&lt;syntaxhighlight lang="lua"&gt;
local mm = require('Module:Math')
&lt;/syntaxhighlight&gt;

(The &lt;code&gt;mm&lt;/code&gt; variable stands for '''M'''odule '''M'''ath; you can choose something more descriptive if you prefer.)

Most functions in the module have a version for Lua and a version for #invoke. It is possible to use the #invoke functions from other Lua modules, but using the Lua functions has the advantage that you do not need to access a Lua [[mw:Extension:Scribunto/Lua reference manual#Frame object|frame object]]. Lua functions are preceded by &lt;code&gt;_&lt;/code&gt;, whereas #invoke functions are not.

== random ==
{{see also|Module:Random}}

 &amp;#123;{#invoke:math|random}}
 &amp;#123;{#invoke:math|random|''max_value''}}
 &amp;#123;{#invoke:math|random|''min_value''|''max_value''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._random()
mm._random(max_value)
mm._random(min_value, max_value)
&lt;/syntaxhighlight&gt;

Generates a random number.

* If no arguments are specified, the number produced is greater than or equal to 0 and less than 1.
* If one argument is provided, the number produced is an integer between 1 and that argument. The argument must be a positive integer.
* If two arguments are provided, the number produced is an integer between the first and second arguments. Both arguments must be integers, but can be negative.

This function will not work properly for numbers less than &amp;minus;2&lt;sup&gt;32&lt;/sup&gt; and greater than 2&lt;sup&gt;32&lt;/sup&gt; &amp;minus; 1. If you need to use numbers outside of this range, it is recommended that you use [[Module:Random]].

== order ==

 &amp;#123;{#invoke:math|order|''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._order(n)
&lt;/syntaxhighlight&gt;

Determines the [[:en:Order of magnitude|Order of magnitude]] of a number.

== precision ==

 &amp;#123;{#invoke:math|precision|''n''}}
 &amp;#123;{#invoke:math|precision|x=''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision(number_string)
&lt;/syntaxhighlight&gt;

Detemines the precision of a number. For example, for "4" it will return "0", for "4.567" it will return "3", and for "100" it will return "-2".

The function attempts to parse the string representation of the number, and detects whether the number uses [[Scientific notation#E notation|E notation]]. For this reason, when called from Lua, very large numbers or very precise numbers should be directly input as strings to get accurate results. If they are input as numbers, the Lua interpreter will change them to E notation and this function will return the precision of the E notation rather than that of the original number. This is not a problem when the number is called from #invoke, as all input from #invoke is in string format.

== max ==

 &amp;#123;{#invoke:math|max|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._max(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the maximum value from the values specified. Values that cannot be converted to numbers are ignored.

== median  ==

 &amp;#123;{#invoke:math|median|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._median(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the [[:en:Median|Median]]  value from the values specified. Values that cannot be converted to numbers are ignored.

== min ==

 &amp;#123;{#invoke:math|min|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._min(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the minimum value from the values specified. Values that cannot be converted to numbers are ignored.

== sum  ==

 &amp;#123;{#invoke:math|sum|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._sum(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the sum of the values specified. Values that cannot be converted to numbers are ignored.

== average ==

 &amp;#123;{#invoke:math|average|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._average(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the average of the values specified. (More precisely, the value returned is the [[Mean#Arithmetic mean (AM)|arithmetic mean]].) Values that cannot be converted to numbers are ignored.

== round ==

 &amp;#123;{#invoke:math|round|''value''|''precision''}}
 &amp;#123;{#invoke:math|round|value=''value''|precision=''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._round(value, precision)
&lt;/syntaxhighlight&gt;

[[:en:Rounding|Rounds]] a number to the specified precision{{clarify|reason=What method exactly is used?}}.

Note: As of October 2019, there is a bug in the display of some rounded numbers. When trying to round a number that rounds to "n.0", like "1.02", to the nearest tenth of a digit (i.e. {{para|r|1}}), this function should display "1.0", but it unexpectedly displays "1". Use the {{para|precision_format}} parameter instead.

== log10 ==

 &amp;#123;{#invoke:math | log10 | ''x''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._log10(x)
&lt;/syntaxhighlight&gt;

Returns &lt;code&gt;log&lt;sub&gt;10&lt;/sub&gt;(''x'')&lt;/code&gt;, the [[:en:Logarithm|Logarithm]] of ''x'' using base 10.

== mod ==

 &amp;#123;{#invoke:math|mod|''x''|''y''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._mod(x, y)
&lt;/syntaxhighlight&gt;

Gets &lt;code&gt;''x''&lt;/code&gt; [[:en:Modulo operation|modulo]] &lt;code&gt;''y''&lt;/code&gt;, or the remainder after &lt;code&gt;''x''&lt;/code&gt; has been divided by &lt;code&gt;''y''&lt;/code&gt;. This is accurate for integers up to 2&lt;sup&gt;53&lt;/sup&gt;; for larger integers Lua's modulo operator may return an erroneous value. This function deals with this problem by returning &lt;code&gt;0&lt;/code&gt; if the modulo given by Lua's modulo operator is less than 0 or greater than &lt;code&gt;''y''&lt;/code&gt;.

== gcd ==

 &amp;#123;{#invoke:math|gcd|''v1''|''v2''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._gcd(v1, v2, ...)
&lt;/syntaxhighlight&gt;

Finds the [[:en:Greatest common divisor|Greatest common divisor]] of the values specified. Values that cannot be converted to numbers are ignored.

== precision_format ==

 &amp;#123;{#invoke:math|precision_format|''value_string''|''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision_format(value_string, precision)
&lt;/syntaxhighlight&gt;

Rounds a number to the specified precision and formats according to rules originally used for {{tl|Rnd}}.  Output is a string.

Parameter &lt;var&gt;precision&lt;/var&gt; should be an integer number of digits after the decimal point. Negative values are permitted. Non-integers give unexpected results. Positive values greater than the input precision add zero-padding, negative values greater than the input order can consume all digits.

Formatting 8,765.567 with {{tlc|#invoke:Math|precision_format|8765.567|&lt;var&gt;precision&lt;/var&gt;}} gives:
{|class="wikitable"
|+
!&lt;code&gt;&lt;var&gt;precision&lt;/var&gt;&lt;/code&gt;!!Result
|-
|style="text-align:right"|2||{{#invoke:Math|precision_format|8765.567|2}}
|-
|style="text-align:right"| -2||{{#invoke:Math|precision_format|8765.567|-2}}
|-
|style="text-align:right"|6||{{#invoke:Math|precision_format|8765.567|6}}
|-
|style="text-align:right"| -6||{{#invoke:Math|precision_format|8765.567|-6}}
|-
|style="text-align:right"|2.5||{{#invoke:Math|precision_format|8765.567|2.5}}
|-
|style="text-align:right"| -2.5||{{#invoke:Math|precision_format|8765.567|-2.5}}
|}

== divide ==

 &amp;#123;{#invoke:Math|divide|''x''|''y''|''round=''|''precision=''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._divide(x, y, round, precision)
&lt;/syntaxhighlight&gt;

Divide x by y.

* If y if not a number, it is returned.
* Otherwise, if x is not a number, it is returned.
* If round is true ("yes" for #invoke), the result has no decimals
* Precision indicates how many digits of precision the result should have

If any of the arguments contain HTML tags, they are returned unchanged, allowing any errors in calculating the arguments to the division function to be propagated to the calling template.  

== cleanNumber ==

&lt;syntaxhighlight lang="lua"&gt;
local number, number_string = mm._cleanNumber(number_string)
&lt;/syntaxhighlight&gt;

A helper function that can be called from other Lua modules, but not from #invoke. This takes a string or a number value as input, and if the value can be converted to a number, cleanNumber returns the number and the number string. If the value cannot be converted to a number, cleanNumber returns &lt;code&gt;nil, nil&lt;/code&gt;.

==See also==
{{Math templates}}

[[sn:Math/doc]]</text>
      <sha1>2l65l40erc9ydj4tdh6ee2lh69kdurc</sha1>
    </revision>
    <revision>
      <id>365206</id>
      <parentid>7647</parentid>
      <timestamp>2026-02-20T01:58:15Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "[[sn:" to "[[Category:sn_crud|TEST"</comment>
      <origin>365206</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8531" sha1="14q0md6u1j9f4gkklgdpejyuy6apse5" xml:space="preserve">{{for|''formatting'' mathematical expressions, LATEX-style|Template:Math}}
{{High-risk|1,607,000+}}
{{module rating|protected}}
This module provides a number of mathematical functions. These functions can be used from #invoke or from other Lua modules.

== Use from other Lua modules ==
To use the module from normal wiki pages, no special preparation is needed. If you are using the module from another Lua module, first you need to load it, like this:

&lt;syntaxhighlight lang="lua"&gt;
local mm = require('Module:Math')
&lt;/syntaxhighlight&gt;

(The &lt;code&gt;mm&lt;/code&gt; variable stands for '''M'''odule '''M'''ath; you can choose something more descriptive if you prefer.)

Most functions in the module have a version for Lua and a version for #invoke. It is possible to use the #invoke functions from other Lua modules, but using the Lua functions has the advantage that you do not need to access a Lua [[mw:Extension:Scribunto/Lua reference manual#Frame object|frame object]]. Lua functions are preceded by &lt;code&gt;_&lt;/code&gt;, whereas #invoke functions are not.

== random ==
{{see also|Module:Random}}

 &amp;#123;{#invoke:math|random}}
 &amp;#123;{#invoke:math|random|''max_value''}}
 &amp;#123;{#invoke:math|random|''min_value''|''max_value''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._random()
mm._random(max_value)
mm._random(min_value, max_value)
&lt;/syntaxhighlight&gt;

Generates a random number.

* If no arguments are specified, the number produced is greater than or equal to 0 and less than 1.
* If one argument is provided, the number produced is an integer between 1 and that argument. The argument must be a positive integer.
* If two arguments are provided, the number produced is an integer between the first and second arguments. Both arguments must be integers, but can be negative.

This function will not work properly for numbers less than &amp;minus;2&lt;sup&gt;32&lt;/sup&gt; and greater than 2&lt;sup&gt;32&lt;/sup&gt; &amp;minus; 1. If you need to use numbers outside of this range, it is recommended that you use [[Module:Random]].

== order ==

 &amp;#123;{#invoke:math|order|''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._order(n)
&lt;/syntaxhighlight&gt;

Determines the [[:en:Order of magnitude|Order of magnitude]] of a number.

== precision ==

 &amp;#123;{#invoke:math|precision|''n''}}
 &amp;#123;{#invoke:math|precision|x=''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision(number_string)
&lt;/syntaxhighlight&gt;

Detemines the precision of a number. For example, for "4" it will return "0", for "4.567" it will return "3", and for "100" it will return "-2".

The function attempts to parse the string representation of the number, and detects whether the number uses [[Scientific notation#E notation|E notation]]. For this reason, when called from Lua, very large numbers or very precise numbers should be directly input as strings to get accurate results. If they are input as numbers, the Lua interpreter will change them to E notation and this function will return the precision of the E notation rather than that of the original number. This is not a problem when the number is called from #invoke, as all input from #invoke is in string format.

== max ==

 &amp;#123;{#invoke:math|max|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._max(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the maximum value from the values specified. Values that cannot be converted to numbers are ignored.

== median  ==

 &amp;#123;{#invoke:math|median|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._median(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the [[:en:Median|Median]]  value from the values specified. Values that cannot be converted to numbers are ignored.

== min ==

 &amp;#123;{#invoke:math|min|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._min(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the minimum value from the values specified. Values that cannot be converted to numbers are ignored.

== sum  ==

 &amp;#123;{#invoke:math|sum|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._sum(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the sum of the values specified. Values that cannot be converted to numbers are ignored.

== average ==

 &amp;#123;{#invoke:math|average|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._average(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the average of the values specified. (More precisely, the value returned is the [[Mean#Arithmetic mean (AM)|arithmetic mean]].) Values that cannot be converted to numbers are ignored.

== round ==

 &amp;#123;{#invoke:math|round|''value''|''precision''}}
 &amp;#123;{#invoke:math|round|value=''value''|precision=''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._round(value, precision)
&lt;/syntaxhighlight&gt;

[[:en:Rounding|Rounds]] a number to the specified precision{{clarify|reason=What method exactly is used?}}.

Note: As of October 2019, there is a bug in the display of some rounded numbers. When trying to round a number that rounds to "n.0", like "1.02", to the nearest tenth of a digit (i.e. {{para|r|1}}), this function should display "1.0", but it unexpectedly displays "1". Use the {{para|precision_format}} parameter instead.

== log10 ==

 &amp;#123;{#invoke:math | log10 | ''x''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._log10(x)
&lt;/syntaxhighlight&gt;

Returns &lt;code&gt;log&lt;sub&gt;10&lt;/sub&gt;(''x'')&lt;/code&gt;, the [[:en:Logarithm|Logarithm]] of ''x'' using base 10.

== mod ==

 &amp;#123;{#invoke:math|mod|''x''|''y''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._mod(x, y)
&lt;/syntaxhighlight&gt;

Gets &lt;code&gt;''x''&lt;/code&gt; [[:en:Modulo operation|modulo]] &lt;code&gt;''y''&lt;/code&gt;, or the remainder after &lt;code&gt;''x''&lt;/code&gt; has been divided by &lt;code&gt;''y''&lt;/code&gt;. This is accurate for integers up to 2&lt;sup&gt;53&lt;/sup&gt;; for larger integers Lua's modulo operator may return an erroneous value. This function deals with this problem by returning &lt;code&gt;0&lt;/code&gt; if the modulo given by Lua's modulo operator is less than 0 or greater than &lt;code&gt;''y''&lt;/code&gt;.

== gcd ==

 &amp;#123;{#invoke:math|gcd|''v1''|''v2''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._gcd(v1, v2, ...)
&lt;/syntaxhighlight&gt;

Finds the [[:en:Greatest common divisor|Greatest common divisor]] of the values specified. Values that cannot be converted to numbers are ignored.

== precision_format ==

 &amp;#123;{#invoke:math|precision_format|''value_string''|''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision_format(value_string, precision)
&lt;/syntaxhighlight&gt;

Rounds a number to the specified precision and formats according to rules originally used for {{tl|Rnd}}.  Output is a string.

Parameter &lt;var&gt;precision&lt;/var&gt; should be an integer number of digits after the decimal point. Negative values are permitted. Non-integers give unexpected results. Positive values greater than the input precision add zero-padding, negative values greater than the input order can consume all digits.

Formatting 8,765.567 with {{tlc|#invoke:Math|precision_format|8765.567|&lt;var&gt;precision&lt;/var&gt;}} gives:
{|class="wikitable"
|+
!&lt;code&gt;&lt;var&gt;precision&lt;/var&gt;&lt;/code&gt;!!Result
|-
|style="text-align:right"|2||{{#invoke:Math|precision_format|8765.567|2}}
|-
|style="text-align:right"| -2||{{#invoke:Math|precision_format|8765.567|-2}}
|-
|style="text-align:right"|6||{{#invoke:Math|precision_format|8765.567|6}}
|-
|style="text-align:right"| -6||{{#invoke:Math|precision_format|8765.567|-6}}
|-
|style="text-align:right"|2.5||{{#invoke:Math|precision_format|8765.567|2.5}}
|-
|style="text-align:right"| -2.5||{{#invoke:Math|precision_format|8765.567|-2.5}}
|}

== divide ==

 &amp;#123;{#invoke:Math|divide|''x''|''y''|''round=''|''precision=''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._divide(x, y, round, precision)
&lt;/syntaxhighlight&gt;

Divide x by y.

* If y if not a number, it is returned.
* Otherwise, if x is not a number, it is returned.
* If round is true ("yes" for #invoke), the result has no decimals
* Precision indicates how many digits of precision the result should have

If any of the arguments contain HTML tags, they are returned unchanged, allowing any errors in calculating the arguments to the division function to be propagated to the calling template.  

== cleanNumber ==

&lt;syntaxhighlight lang="lua"&gt;
local number, number_string = mm._cleanNumber(number_string)
&lt;/syntaxhighlight&gt;

A helper function that can be called from other Lua modules, but not from #invoke. This takes a string or a number value as input, and if the value can be converted to a number, cleanNumber returns the number and the number string. If the value cannot be converted to a number, cleanNumber returns &lt;code&gt;nil, nil&lt;/code&gt;.

==See also==
{{Math templates}}

[[Category:sn_crud|TESTMath/doc]]</text>
      <sha1>14q0md6u1j9f4gkklgdpejyuy6apse5</sha1>
    </revision>
    <revision>
      <id>3604737</id>
      <parentid>365206</parentid>
      <timestamp>2026-04-11T02:06:40Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <comment>Bot: remove [[Category:Sn crud]] (crud category cleanup) [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/24271391609|time triggered pipeline]]</comment>
      <origin>3604737</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8496" sha1="hnmgzk7x1xzxxrzhw63deesdro5rt0b" xml:space="preserve">{{for|''formatting'' mathematical expressions, LATEX-style|Template:Math}}
{{High-risk|1,607,000+}}
{{module rating|protected}}
This module provides a number of mathematical functions. These functions can be used from #invoke or from other Lua modules.

== Use from other Lua modules ==
To use the module from normal wiki pages, no special preparation is needed. If you are using the module from another Lua module, first you need to load it, like this:

&lt;syntaxhighlight lang="lua"&gt;
local mm = require('Module:Math')
&lt;/syntaxhighlight&gt;

(The &lt;code&gt;mm&lt;/code&gt; variable stands for '''M'''odule '''M'''ath; you can choose something more descriptive if you prefer.)

Most functions in the module have a version for Lua and a version for #invoke. It is possible to use the #invoke functions from other Lua modules, but using the Lua functions has the advantage that you do not need to access a Lua [[mw:Extension:Scribunto/Lua reference manual#Frame object|frame object]]. Lua functions are preceded by &lt;code&gt;_&lt;/code&gt;, whereas #invoke functions are not.

== random ==
{{see also|Module:Random}}

 &amp;#123;{#invoke:math|random}}
 &amp;#123;{#invoke:math|random|''max_value''}}
 &amp;#123;{#invoke:math|random|''min_value''|''max_value''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._random()
mm._random(max_value)
mm._random(min_value, max_value)
&lt;/syntaxhighlight&gt;

Generates a random number.

* If no arguments are specified, the number produced is greater than or equal to 0 and less than 1.
* If one argument is provided, the number produced is an integer between 1 and that argument. The argument must be a positive integer.
* If two arguments are provided, the number produced is an integer between the first and second arguments. Both arguments must be integers, but can be negative.

This function will not work properly for numbers less than &amp;minus;2&lt;sup&gt;32&lt;/sup&gt; and greater than 2&lt;sup&gt;32&lt;/sup&gt; &amp;minus; 1. If you need to use numbers outside of this range, it is recommended that you use [[Module:Random]].

== order ==

 &amp;#123;{#invoke:math|order|''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._order(n)
&lt;/syntaxhighlight&gt;

Determines the [[:en:Order of magnitude|Order of magnitude]] of a number.

== precision ==

 &amp;#123;{#invoke:math|precision|''n''}}
 &amp;#123;{#invoke:math|precision|x=''n''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision(number_string)
&lt;/syntaxhighlight&gt;

Detemines the precision of a number. For example, for "4" it will return "0", for "4.567" it will return "3", and for "100" it will return "-2".

The function attempts to parse the string representation of the number, and detects whether the number uses [[Scientific notation#E notation|E notation]]. For this reason, when called from Lua, very large numbers or very precise numbers should be directly input as strings to get accurate results. If they are input as numbers, the Lua interpreter will change them to E notation and this function will return the precision of the E notation rather than that of the original number. This is not a problem when the number is called from #invoke, as all input from #invoke is in string format.

== max ==

 &amp;#123;{#invoke:math|max|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._max(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the maximum value from the values specified. Values that cannot be converted to numbers are ignored.

== median  ==

 &amp;#123;{#invoke:math|median|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._median(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the [[:en:Median|Median]]  value from the values specified. Values that cannot be converted to numbers are ignored.

== min ==

 &amp;#123;{#invoke:math|min|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._min(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the minimum value from the values specified. Values that cannot be converted to numbers are ignored.

== sum  ==

 &amp;#123;{#invoke:math|sum|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._sum(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the sum of the values specified. Values that cannot be converted to numbers are ignored.

== average ==

 &amp;#123;{#invoke:math|average|''v1''|''v2''|''v3''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._average(v1, v2, v3, ...)
&lt;/syntaxhighlight&gt;

Returns the average of the values specified. (More precisely, the value returned is the [[Mean#Arithmetic mean (AM)|arithmetic mean]].) Values that cannot be converted to numbers are ignored.

== round ==

 &amp;#123;{#invoke:math|round|''value''|''precision''}}
 &amp;#123;{#invoke:math|round|value=''value''|precision=''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._round(value, precision)
&lt;/syntaxhighlight&gt;

[[:en:Rounding|Rounds]] a number to the specified precision{{clarify|reason=What method exactly is used?}}.

Note: As of October 2019, there is a bug in the display of some rounded numbers. When trying to round a number that rounds to "n.0", like "1.02", to the nearest tenth of a digit (i.e. {{para|r|1}}), this function should display "1.0", but it unexpectedly displays "1". Use the {{para|precision_format}} parameter instead.

== log10 ==

 &amp;#123;{#invoke:math | log10 | ''x''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._log10(x)
&lt;/syntaxhighlight&gt;

Returns &lt;code&gt;log&lt;sub&gt;10&lt;/sub&gt;(''x'')&lt;/code&gt;, the [[:en:Logarithm|Logarithm]] of ''x'' using base 10.

== mod ==

 &amp;#123;{#invoke:math|mod|''x''|''y''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._mod(x, y)
&lt;/syntaxhighlight&gt;

Gets &lt;code&gt;''x''&lt;/code&gt; [[:en:Modulo operation|modulo]] &lt;code&gt;''y''&lt;/code&gt;, or the remainder after &lt;code&gt;''x''&lt;/code&gt; has been divided by &lt;code&gt;''y''&lt;/code&gt;. This is accurate for integers up to 2&lt;sup&gt;53&lt;/sup&gt;; for larger integers Lua's modulo operator may return an erroneous value. This function deals with this problem by returning &lt;code&gt;0&lt;/code&gt; if the modulo given by Lua's modulo operator is less than 0 or greater than &lt;code&gt;''y''&lt;/code&gt;.

== gcd ==

 &amp;#123;{#invoke:math|gcd|''v1''|''v2''|...}}

&lt;syntaxhighlight lang="lua"&gt;
mm._gcd(v1, v2, ...)
&lt;/syntaxhighlight&gt;

Finds the [[:en:Greatest common divisor|Greatest common divisor]] of the values specified. Values that cannot be converted to numbers are ignored.

== precision_format ==

 &amp;#123;{#invoke:math|precision_format|''value_string''|''precision''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._precision_format(value_string, precision)
&lt;/syntaxhighlight&gt;

Rounds a number to the specified precision and formats according to rules originally used for {{tl|Rnd}}.  Output is a string.

Parameter &lt;var&gt;precision&lt;/var&gt; should be an integer number of digits after the decimal point. Negative values are permitted. Non-integers give unexpected results. Positive values greater than the input precision add zero-padding, negative values greater than the input order can consume all digits.

Formatting 8,765.567 with {{tlc|#invoke:Math|precision_format|8765.567|&lt;var&gt;precision&lt;/var&gt;}} gives:
{|class="wikitable"
|+
!&lt;code&gt;&lt;var&gt;precision&lt;/var&gt;&lt;/code&gt;!!Result
|-
|style="text-align:right"|2||{{#invoke:Math|precision_format|8765.567|2}}
|-
|style="text-align:right"| -2||{{#invoke:Math|precision_format|8765.567|-2}}
|-
|style="text-align:right"|6||{{#invoke:Math|precision_format|8765.567|6}}
|-
|style="text-align:right"| -6||{{#invoke:Math|precision_format|8765.567|-6}}
|-
|style="text-align:right"|2.5||{{#invoke:Math|precision_format|8765.567|2.5}}
|-
|style="text-align:right"| -2.5||{{#invoke:Math|precision_format|8765.567|-2.5}}
|}

== divide ==

 &amp;#123;{#invoke:Math|divide|''x''|''y''|''round=''|''precision=''}}

&lt;syntaxhighlight lang="lua"&gt;
mm._divide(x, y, round, precision)
&lt;/syntaxhighlight&gt;

Divide x by y.

* If y if not a number, it is returned.
* Otherwise, if x is not a number, it is returned.
* If round is true ("yes" for #invoke), the result has no decimals
* Precision indicates how many digits of precision the result should have

If any of the arguments contain HTML tags, they are returned unchanged, allowing any errors in calculating the arguments to the division function to be propagated to the calling template.  

== cleanNumber ==

&lt;syntaxhighlight lang="lua"&gt;
local number, number_string = mm._cleanNumber(number_string)
&lt;/syntaxhighlight&gt;

A helper function that can be called from other Lua modules, but not from #invoke. This takes a string or a number value as input, and if the value can be converted to a number, cleanNumber returns the number and the number string. If the value cannot be converted to a number, cleanNumber returns &lt;code&gt;nil, nil&lt;/code&gt;.

==See also==
{{Math templates}}</text>
      <sha1>hnmgzk7x1xzxxrzhw63deesdro5rt0b</sha1>
    </revision>
  </page>
</mediawiki>
