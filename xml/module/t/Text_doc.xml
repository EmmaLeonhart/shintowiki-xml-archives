<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Module:Text/doc</title>
    <ns>828</ns>
    <id>8266</id>
    <revision>
      <id>916557</id>
      <timestamp>2024-09-21T14:07:43Z</timestamp>
      <contributor>
        <username>en&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>/* Dependencies */ +1</comment>
      <origin>916557</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8558" sha1="450ezbxyoe4a12wg9z5fohnrvyqevgf" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;</text>
      <sha1>450ezbxyoe4a12wg9z5fohnrvyqevgf</sha1>
    </revision>
    <revision>
      <id>916558</id>
      <parentid>916557</parentid>
      <timestamp>2025-01-10T09:34:41Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: IMPORTING ALL WANTED MODULES</comment>
      <origin>916557</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8558" sha1="450ezbxyoe4a12wg9z5fohnrvyqevgf" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;</text>
      <sha1>450ezbxyoe4a12wg9z5fohnrvyqevgf</sha1>
    </revision>
    <revision>
      <id>916559</id>
      <parentid>916558</parentid>
      <timestamp>2025-02-28T06:21:48Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>916559</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8575" sha1="eg7ekqivhe1o4een9xetlp1l6jpp6m5" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:New]]</text>
      <sha1>eg7ekqivhe1o4een9xetlp1l6jpp6m5</sha1>
    </revision>
    <revision>
      <id>916560</id>
      <parentid>916559</parentid>
      <timestamp>2025-03-08T18:21:21Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>916560</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8624" sha1="m34znuoxun7gegpbvznawnm44ag60qs" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:New]]
[[Category:Pages and Categories from Wikipedia]]</text>
      <sha1>m34znuoxun7gegpbvznawnm44ag60qs</sha1>
    </revision>
    <revision>
      <id>916561</id>
      <parentid>916560</parentid>
      <timestamp>2025-03-08T22:28:38Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Adding [[Category:qq]][[sn:{{subst:PAGENAME}}]][[Category:qq]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>916561</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8670" sha1="lqlek5n7gv4kqj9sx4z4n9fpr2s2qrp" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:qq]][[sn:Text/doc]][[Category:qq]]</text>
      <sha1>lqlek5n7gv4kqj9sx4z4n9fpr2s2qrp</sha1>
    </revision>
    <revision>
      <id>916562</id>
      <parentid>916561</parentid>
      <timestamp>2025-03-11T22:45:00Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>916562</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8687" sha1="86nlsh3dvwsfrry0t4z0nx0dun3yxsp" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:qq]][[sn:Text/doc]][[Category:qq]]
[[Category:New]]</text>
      <sha1>86nlsh3dvwsfrry0t4z0nx0dun3yxsp</sha1>
    </revision>
    <revision>
      <id>916563</id>
      <parentid>916562</parentid>
      <timestamp>2025-03-15T23:49:19Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>916563</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8704" sha1="rhvmvoriq7mye797z09qx4o47dp4esb" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:qq]][[sn:Text/doc]][[Category:qq]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>rhvmvoriq7mye797z09qx4o47dp4esb</sha1>
    </revision>
    <revision>
      <id>916564</id>
      <parentid>916563</parentid>
      <timestamp>2025-03-20T00:03:16Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "qq" to "11"</comment>
      <origin>916564</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8704" sha1="du7seki8r3e6wsak4fdu2zmkv8nu149" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:11]][[sn:Text/doc]][[Category:11]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>du7seki8r3e6wsak4fdu2zmkv8nu149</sha1>
    </revision>
    <revision>
      <id>916565</id>
      <parentid>916564</parentid>
      <timestamp>2025-03-24T20:42:50Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>916565</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8721" sha1="dgqxdyuvawjcxs69koevrrl5i9fzumt" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:11]][[sn:Text/doc]][[Category:11]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>dgqxdyuvawjcxs69koevrrl5i9fzumt</sha1>
    </revision>
    <revision>
      <id>916566</id>
      <parentid>916565</parentid>
      <timestamp>2025-03-25T04:44:02Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>916566</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8738" sha1="tq1ykal230sl048z08qc51nspt5g566" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:11]][[sn:Text/doc]][[Category:11]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>tq1ykal230sl048z08qc51nspt5g566</sha1>
    </revision>
    <revision>
      <id>916567</id>
      <parentid>916566</parentid>
      <timestamp>2025-03-27T12:34:58Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>916567</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8755" sha1="c8wkw2ndftgbe7js7lhvvcjh1ghnx8y" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:11]][[sn:Text/doc]][[Category:11]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>c8wkw2ndftgbe7js7lhvvcjh1ghnx8y</sha1>
    </revision>
    <revision>
      <id>916568</id>
      <parentid>916567</parentid>
      <timestamp>2025-03-30T00:58:30Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:Pages and Categories from Wikipedia 6]] to [[Category:qq]][[sn:{{subst:PAGENAME}}]][[Category:qq]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>916568</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8801" sha1="tbqdeoucs6l5bteo92xvwrcufj9eblq" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:11]][[sn:Text/doc]][[Category:11]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:qq]][[sn:Text/doc]][[Category:qq]]</text>
      <sha1>tbqdeoucs6l5bteo92xvwrcufj9eblq</sha1>
    </revision>
    <revision>
      <id>916569</id>
      <parentid>916568</parentid>
      <timestamp>2025-04-08T20:48:25Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Removing from [[Category:Qq]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>916569</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8771" sha1="o8bqstrdgswv7w2lpd1f5z89j5h1oph" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:11]][[sn:Text/doc]][[Category:11]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[sn:Text/doc]]</text>
      <sha1>o8bqstrdgswv7w2lpd1f5z89j5h1oph</sha1>
    </revision>
    <revision>
      <id>916570</id>
      <parentid>916569</parentid>
      <timestamp>2025-04-13T01:11:33Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Removing from [[Category:New]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>916570</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8669" sha1="a8lgv1d799gntxtlljwtfifcdn1z87s" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:Pages and Categories from Wikipedia]]
[[Category:11]][[sn:Text/doc]][[Category:11]]
[[sn:Text/doc]]</text>
      <sha1>a8lgv1d799gntxtlljwtfifcdn1z87s</sha1>
    </revision>
    <revision>
      <id>916571</id>
      <parentid>916570</parentid>
      <timestamp>2025-04-15T21:57:28Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:Pages and Categories from Wikipedia 6]] to [[Category:qq]][[sn:{{subst:PAGENAME}}]][[Category:qq]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>916571</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8715" sha1="em2dyw10tt9ui4re5gtr8w2wvjrq0ix" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:Pages and Categories from Wikipedia]]
[[Category:11]][[sn:Text/doc]][[Category:11]]
[[Category:qq]][[sn:Text/doc]][[Category:qq]]
[[sn:Text/doc]]</text>
      <sha1>em2dyw10tt9ui4re5gtr8w2wvjrq0ix</sha1>
    </revision>
    <revision>
      <id>916572</id>
      <parentid>916571</parentid>
      <timestamp>2025-04-15T23:12:39Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>916572</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8732" sha1="cokztq9bpcqsa684be0rjscvq1woy7s" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:Pages and Categories from Wikipedia]]
[[Category:11]][[sn:Text/doc]][[Category:11]]
[[Category:qq]][[sn:Text/doc]][[Category:qq]]
[[sn:Text/doc]]
[[Category:New]]</text>
      <sha1>cokztq9bpcqsa684be0rjscvq1woy7s</sha1>
    </revision>
    <revision>
      <id>916579</id>
      <parentid>916572</parentid>
      <timestamp>2025-04-25T15:36:11Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>clearer doc for zip</comment>
      <origin>916579</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8486" sha1="b5m6h4fai5y74k611d2eyvenm6f0rhs" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a,b,c" and list2 = "1,2,3", then&lt;br&gt;&lt;code&gt;zip(list1, list2, sep = ",", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;</text>
      <sha1>b5m6h4fai5y74k611d2eyvenm6f0rhs</sha1>
    </revision>
    <revision>
      <id>916573</id>
      <parentid>916572</parentid>
      <timestamp>2025-04-29T10:46:22Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "[[Category:11]]" to ""</comment>
      <origin>916573</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8702" sha1="dn9hetwahztwf7tu125b6kwkt8znyu7" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:Pages and Categories from Wikipedia]]
[[sn:Text/doc]]
[[Category:qq]][[sn:Text/doc]][[Category:qq]]
[[sn:Text/doc]]
[[Category:New]]</text>
      <sha1>dn9hetwahztwf7tu125b6kwkt8znyu7</sha1>
    </revision>
    <revision>
      <id>916574</id>
      <parentid>916573</parentid>
      <timestamp>2025-04-29T10:47:14Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "[[Category:qq]]" to ""</comment>
      <origin>916574</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8672" sha1="8hzvfvzred97i0ove4s9d6lojz4ynvz" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:Pages and Categories from Wikipedia]]
[[sn:Text/doc]]
[[sn:Text/doc]]
[[sn:Text/doc]]
[[Category:New]]</text>
      <sha1>8hzvfvzred97i0ove4s9d6lojz4ynvz</sha1>
    </revision>
    <revision>
      <id>916575</id>
      <parentid>916574</parentid>
      <timestamp>2025-04-29T11:56:00Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "[[Category:New]]" to ""</comment>
      <origin>916575</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8655" sha1="ihe7f48yt28icq0npdzccnramdlesko" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:Pages and Categories from Wikipedia]]
[[sn:Text/doc]]
[[sn:Text/doc]]
[[sn:Text/doc]]</text>
      <sha1>ihe7f48yt28icq0npdzccnramdlesko</sha1>
    </revision>
    <revision>
      <id>916576</id>
      <parentid>916575</parentid>
      <timestamp>2025-05-06T06:31:01Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Moving from [[Category:Pages and Categories from Wikipedia]] to [[Category:pages to be overwritten]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>916576</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8643" sha1="spcipipf93siovsb51sqbp65h2tyom8" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a b c" and list2 = "1 2 3", then&lt;br&gt;&lt;code&gt;zip(liste1, liste2, sep = " ", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;
* &lt;s&gt;&lt;code&gt;Text.zip(…)&lt;/code&gt;&lt;/s&gt;
* ''&lt;code&gt;Text.test( s )&lt;/code&gt;''

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;
[[Category:pages to be overwritten]]
[[sn:Text/doc]]
[[sn:Text/doc]]
[[sn:Text/doc]]</text>
      <sha1>spcipipf93siovsb51sqbp65h2tyom8</sha1>
    </revision>
    <revision>
      <id>916577</id>
      <parentid>916576</parentid>
      <timestamp>2025-05-06T06:58:14Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Auto generating page. date placed far in the past so it is attributed to Immanuelle and FireflyBot sends them messages if it is ever imported to Wikipedia.</comment>
      <origin>916577</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="171" sha1="fnf4ihusd0co8v2k71gdehc19es78gc" xml:space="preserve">#redirect[[en:Module:Text/doc]]
&lt;includeonly&gt;[[Category:Erroneous transclusions of Module:Text/doc]]&lt;/includeonly&gt;
&lt;noinclude&gt;[[Category:Wikipedia overwrites]]&lt;/noinclude&gt;</text>
      <sha1>fnf4ihusd0co8v2k71gdehc19es78gc</sha1>
    </revision>
    <revision>
      <id>916578</id>
      <parentid>916577</parentid>
      <timestamp>2025-05-20T08:48:00Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: remove deleted category Wikipedia overwrites</comment>
      <origin>916578</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="138" sha1="5i97v11de7jlgn5y6a8j52pgc9yv8ue" xml:space="preserve">#redirect[[en:Module:Text/doc]]
&lt;includeonly&gt;[[Category:Erroneous transclusions of Module:Text/doc]]&lt;/includeonly&gt;
&lt;noinclude&gt;&lt;/noinclude&gt;</text>
      <sha1>5i97v11de7jlgn5y6a8j52pgc9yv8ue</sha1>
    </revision>
    <revision>
      <id>8267</id>
      <timestamp>2025-06-13T06:14:19Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was 1 revision imported from [[:wikipedia:Module:Text/doc]]</comment>
      <origin>8267</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="138" sha1="5i97v11de7jlgn5y6a8j52pgc9yv8ue" xml:space="preserve">#redirect[[en:Module:Text/doc]]
&lt;includeonly&gt;[[Category:Erroneous transclusions of Module:Text/doc]]&lt;/includeonly&gt;
&lt;noinclude&gt;&lt;/noinclude&gt;</text>
      <sha1>5i97v11de7jlgn5y6a8j52pgc9yv8ue</sha1>
    </revision>
    <revision>
      <id>3557575</id>
      <parentid>8267</parentid>
      <timestamp>2026-03-20T19:43:01Z</timestamp>
      <contributor>
        <username>en&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>clearer doc for zip</comment>
      <origin>3557575</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8486" sha1="b5m6h4fai5y74k611d2eyvenm6f0rhs" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a,b,c" and list2 = "1,2,3", then&lt;br&gt;&lt;code&gt;zip(list1, list2, sep = ",", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;</text>
      <sha1>b5m6h4fai5y74k611d2eyvenm6f0rhs</sha1>
    </revision>
    <revision>
      <id>3557576</id>
      <parentid>3557575</parentid>
      <timestamp>2026-03-20T19:43:02Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>3557575</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="8486" sha1="b5m6h4fai5y74k611d2eyvenm6f0rhs" xml:space="preserve">{{high use}}
{{Lua|Module:Yesno}}
'''&lt;code&gt;Text&lt;/code&gt;''' – Module containing methods for the manipulation of text, wikimarkup and some HTML.

== {{Anchor|Vorlage}} Functions for templates ==

All methods have an unnamed parameter containing the text.

The return value is an empty string if the parameter does not meet the conditions. When the condition is matched or some result is successfully found, strings of at least one character are returned.

; char {{Anchor|char}}
: Creates a string from a list of character codes.
::; 1
::: Space-separated list of character codes
::; *
::: Number of repetitions of the list in parameter '''1'''; (Default 1).
::; errors
::: &lt;code&gt;0&lt;/code&gt; – Silence errors
; concatParams {{Anchor|concatParams}}
: Combine any number of elements into a list, like &lt;code&gt;table.concat()&lt;/code&gt; in Lua.
: From a template:
::; 1
::: First element; missing and empty elements are ignored.
::; 2 3 4 5 6 …
::: Further list elements
: From Lua
::; args
::: ''table (sequence)'' of the elements
::; apply
::: Separator between elements; defaults to &lt;code&gt;|&lt;/code&gt;
::; adapt
::: optional formatting, which will be applied to each element; must contain &lt;code&gt;%s&lt;/code&gt;.
; containsCJK {{Anchor|containsCJK}}
: Returns whether the input string contains any [[CJK characters]]
:* Returns nothing if there are no CJK characters
; removeDelimited
: Remove all text between delimiters, including the delimiters themselves.
; getPlain{{Anchor|getPlain}}
: Remove wikimarkup (except templates): comments, tags, bold, italic, nbsp
; isLatinRange {{Anchor|isLatinRange}}
: Returns some content, unless the string contains a character that would not normally be found in Latin text.
:* Returns nothing if there is a non-Latin string.
; isQuote {{Anchor|isQuote}}
: Returns some content if the parameter passed is a single character, and that character is a quote, such as &lt;code&gt;'&lt;/code&gt;.
:* Returns nothing for multiple characters, or if the character passed is not a quote.
; listToText {{Anchor|listToText}}
: Formats list elements analogously to [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.listToText|mw.text.listToText()]].
: The elements are separated by a comma and space ; the word "and" appears between the first and last.
: Unnamed parameters become the list items.
: Optional parameters for &lt;code&gt;#invoke&lt;/code&gt;:
:* &lt;code&gt;format&lt;/code&gt; – Every list element will first be formatted with this format string; see [[:mw:Extension:Scribunto/Lua_reference_manual#string.format|here]] for how to construct this string. The string must contain at least one &lt;code&gt;%s&lt;/code&gt; sequence.
:* &lt;code&gt;template=1&lt;/code&gt; – List elements should be taken from the calling template.
: Returns the resulting string.
; quote {{Anchor|quote}}
: Wrap the string in quotes; quotes can be chosen for a specific language.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; quoteUnquoted {{Anchor|quoteUnquoted}}
: 
: Wrap the string in quotes; quotes can be chosen for a specific language. Will not quote an empty string, and will not quote if there is a quote at the start or end of the (trimmed) string.
::; 1
::: Input text (will be automatically trimmed); may be empty.
::; 2
::: ''(optional)'' the [[ISO 639]] language code for the quote marks; should be one of the [[:de:Wikipedia:Lua/Modul/Text/Test#Anführungszeichen|supported languages]] {{in lang|de}}
::; 3
::: ''(optional)'' &lt;code&gt;2&lt;/code&gt; for second level quotes. This means the single quote marks in a statement such as: Jack said, “Jill said ‘fish’ last Tuesday.”
; removeDiacritics {{Anchor|removeDiacritics}}
: Removes all [[diacritical marks]] from the input.
::; 1
::;: Input text
; sentenceTerminated {{Anchor|sentenceTerminated}}
: Is this sentence terminated? Should work with CJK, and allows quotation marks to follow.
:* Returns nothing if the sentence is unterminated.
; ucfirstAll {{Anchor|ucfirstAll}}
: The first letter of every recognized word is converted to upper case. This contrasts with the parser function {{tlg|:mw:ucfirst|alttext=ucfirst:}} which changes only the first character of the whole string passed.
: A few common HTML entities are protected; the implementation of this may mean that numerical entities passed (e.g. &lt;code&gt;&amp;amp;#38;)&lt;/code&gt; are converted to &lt;code&gt;&amp;amp;amp;&lt;/code&gt; form
; uprightNonlatin {{Anchor|uprightNonlatin}}
: Takes a string. Italicized non-Latin characters are un-italicized, unless they are a single Greek letter.
; zip {{Anchor|zip}}
: Combines a tuple of lists by [[convolution (computer science)|convolution]]. This is easiest to explain by example: given two lists, list1 = "a,b,c" and list2 = "1,2,3", then&lt;br&gt;&lt;code&gt;zip(list1, list2, sep = ",", isep = "-", osep = "/")&lt;/code&gt;&lt;br&gt;outputs&lt;br&gt;&lt;code&gt;a-1/b-2/c-3&lt;/code&gt;
:* 1, 2, 3, … – Lists to be combined
:* &lt;code&gt;sep&lt;/code&gt; – A separator (in Lua [[regex]] form) used to split the lists. If empty, the lists are split into individual characters.
:* &lt;code&gt;sep1&lt;/code&gt;, &lt;code&gt;sep2&lt;/code&gt;, &lt;code&gt;sep3&lt;/code&gt;, … – Allows a different separator to be used for each list.
:* &lt;code&gt;isep&lt;/code&gt; – Output separator; placed between elements which were at the ''same'' index in their lists.
:* &lt;code&gt;osep&lt;/code&gt; – Output separator; placed between elements which had ''different'' original indices; i.e. between the groups joined with &lt;code&gt;isep&lt;/code&gt; 
; split {{Anchor|split}}
: Splits a string into chunks at the specified delimiter, and returns the first (or user-specified) chunk. This is non-Unicode-aware implementation of [[:mw:Lua_reference_manual#mw.text.split|mw.text.split]] which, for ASCII-only text, can be up to 60 times faster.
:* 1 (or &lt;code&gt;text&lt;/code&gt;) – the text to be split
:* 2 (or &lt;code&gt;pattern&lt;/code&gt;) – the pattern to use when splitting the text. By default, this can be a [[:mw:Extension:Scribunto/Lua_reference_manual#Patterns|string library pattern]].
:* 3 (or &lt;code&gt;plain&lt;/code&gt;) – if set to "true", &lt;code&gt;pattern&lt;/code&gt; will be interpreted as plain text, not a pattern.
:* 4 (or &lt;code&gt;index&lt;/code&gt;) – The chunk to return. If omitted, the first chunk will be returned. Can be set to a negative number to count from the end (e.g. &lt;code&gt;-1&lt;/code&gt; will return the last chunk).

===  Examples and test page ===
There are [[:de:Wikipedia:Lua/Modul/Text/Test|tests available]] {{in lang|de}} to illustrate this in practice.

== {{Anchor|Lua}} Use in another Lua module ==

All of the above functions can be called from other Lua modules. Use &lt;code&gt;require()&lt;/code&gt;; the below code checks for errors loading it:
&lt;syntaxhighlight lang="lua"&gt;
local lucky, Text = pcall( require, "Module:Text" )
if type( Text ) == "table" then
    Text = Text.Text()
else
    -- In the event of errors, Text is an error message.
    return "&lt;span class=\"error\"&gt;" .. Text .. "&lt;/span&gt;"
end
&lt;/syntaxhighlight&gt;
You may then call:
* &lt;code&gt;Text.char( apply, again, accept )&lt;/code&gt;
* &lt;code&gt;Text.concatParams( args, separator, format )&lt;/code&gt;
* &lt;code&gt;Text.containsCJK( s )&lt;/code&gt;
* &lt;code&gt;Text.removeDelimited( s )&lt;/code&gt;
* &lt;code&gt;Text.getPlain( s )&lt;/code&gt;
* &lt;code&gt;Text.isLatinRange( s )&lt;/code&gt;
* &lt;code&gt;Text.isQuote( c )&lt;/code&gt;
* &lt;code&gt;Text.listToText( table, format )&lt;/code&gt;
* &lt;code&gt;Text.quote( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.quoteUnquoted( s, lang, mode )&lt;/code&gt;
* &lt;code&gt;Text.removeDiacritics( s )&lt;/code&gt;
* &lt;code&gt;Text.sentenceTerminated( s )&lt;/code&gt;
* &lt;code&gt;Text.split( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.split|mw.text.split]]
* &lt;code&gt;Text.gsplit( text, pattern, plain )&lt;/code&gt; – non Unicode version of [[:mw:Extension:Scribunto/Lua_reference_manual#mw.text.gsplit|mw.text.gsplit]]
* &lt;code&gt;Text.ucfirstAll( s )&lt;/code&gt;
* &lt;code&gt;Text.uprightNonlatin( s )&lt;/code&gt;

== Usage ==
This is a general library; use it anywhere.

== Dependencies ==
* [[Module:Yesno]]
* [[Module:Text/data]] --- Lua patterns and information about quotes

== See also ==
{{String-handling templates}}
&lt;includeonly&gt;{{Sandbox other||
&lt;!-- Categories below this line; interwikis at Wikidata --&gt;
[[Category:Modules that manipulate strings]]
}}&lt;/includeonly&gt;</text>
      <sha1>b5m6h4fai5y74k611d2eyvenm6f0rhs</sha1>
    </revision>
  </page>
</mediawiki>
