<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Module:Type in location</title>
    <ns>828</ns>
    <id>10048</id>
    <revision>
      <id>995595</id>
      <timestamp>2018-05-18T12:46:43Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>new module</comment>
      <origin>995595</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="917" sha1="oqllkjamyhl1btiwfdfy7ncbvpee7d9" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Format location for use in short descriptions
function p.Format (text)
	text = text:gsub('&lt;br/?&gt;', ', ') --&lt;br&gt; or &lt;br/&gt; with commas
	text = plaintext(text) --plain textify to reduce strange stuff
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.General (frame)
	loc = frame.args[1]
	split = {}
	num = 0
	loc = p.Format(loc)
	loc = loc..','
	for k in loc:gmatch('([^,]-),') do --split by commmas
		split.insert(k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return loc[1]
	else
		return loc[num-1]..','..loc[num] --return last two entries seperated by commas
	end
end

return p</text>
      <sha1>oqllkjamyhl1btiwfdfy7ncbvpee7d9</sha1>
    </revision>
    <revision>
      <id>995596</id>
      <parentid>995595</parentid>
      <timestamp>2018-05-18T12:57:20Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>995596</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="930" sha1="3t5n8rqb8tcnvb7rf0suvtj24ij0yq7" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Format location for use in short descriptions
function p.Format (text)
	text = text:gsub('&lt;br/?&gt;', ', ') --&lt;br&gt; or &lt;br/&gt; with commas
	text = plaintext(text) --plain textify to reduce strange stuff
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.General (frame)
	loc = frame.args[1]
	split = {}
	num = 0
	loc = p.Format(loc)
	loc = loc..','
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

return p</text>
      <sha1>3t5n8rqb8tcnvb7rf0suvtj24ij0yq7</sha1>
    </revision>
    <revision>
      <id>995597</id>
      <parentid>995596</parentid>
      <timestamp>2018-05-18T14:54:54Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>remove things in brackets, other stuff</comment>
      <origin>995597</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1581" sha1="7onlohpoyqma21rdcqugiljt0cu3oey" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.preparceLoc (frame)
	return p_prepareLock (frame.args[1])
end

function p._prepareLoc (text)
	text = text:gsub('&lt;br/?&gt;', ', ') --&lt;br&gt; or &lt;br/&gt; with commas
	text = plaintext(text) --plain textify to reduce strange stuff
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[%s,](.-%d.-)[%s,]', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('%s%s', '') --remove possible double spaces from previous cleanup
			   :gsub('(,%s-),', '') --remove possible blank seperated commas from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._General (frame.args[1])
end

function p._generalLoc (loc)
	loc = p.prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..','
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

return p</text>
      <sha1>7onlohpoyqma21rdcqugiljt0cu3oey</sha1>
    </revision>
    <revision>
      <id>995598</id>
      <parentid>995597</parentid>
      <timestamp>2018-05-18T14:56:03Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>995598</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1582" sha1="585gflyvnao7hw2olsa8aza3k4nxhbo" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p_prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = text:gsub('&lt;br/?&gt;', ', ') --&lt;br&gt; or &lt;br/&gt; with commas
	text = plaintext(text) --plain textify to reduce strange stuff
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[%s,](.-%d.-)[%s,]', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('%s%s', '') --remove possible double spaces from previous cleanup
			   :gsub('(,%s-),', '') --remove possible blank seperated commas from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p.prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..','
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

return p</text>
      <sha1>585gflyvnao7hw2olsa8aza3k4nxhbo</sha1>
    </revision>
    <revision>
      <id>995599</id>
      <parentid>995598</parentid>
      <timestamp>2018-05-18T14:56:17Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fixx</comment>
      <origin>995599</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1583" sha1="e38ke43bhtib3zsvvx5hfgqdy2elouv" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p_prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = text:gsub('&lt;br/?&gt;', ', ') --&lt;br&gt; or &lt;br/&gt; with commas
	text = plaintext(text) --plain textify to reduce strange stuff
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[%s,](.-%d.-)[%s,]', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('%s%s', '') --remove possible double spaces from previous cleanup
			   :gsub('(,%s-),', '') --remove possible blank seperated commas from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..','
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

return p</text>
      <sha1>e38ke43bhtib3zsvvx5hfgqdy2elouv</sha1>
    </revision>
    <revision>
      <id>995600</id>
      <parentid>995599</parentid>
      <timestamp>2018-05-18T15:07:54Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>Galobtter moved page [[Module:Get location]] to [[Module:Type in location]] without leaving a redirect: better name</comment>
      <origin>995599</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1583" sha1="e38ke43bhtib3zsvvx5hfgqdy2elouv" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p_prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = text:gsub('&lt;br/?&gt;', ', ') --&lt;br&gt; or &lt;br/&gt; with commas
	text = plaintext(text) --plain textify to reduce strange stuff
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[%s,](.-%d.-)[%s,]', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('%s%s', '') --remove possible double spaces from previous cleanup
			   :gsub('(,%s-),', '') --remove possible blank seperated commas from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..','
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

return p</text>
      <sha1>e38ke43bhtib3zsvvx5hfgqdy2elouv</sha1>
    </revision>
    <revision>
      <id>995601</id>
      <parentid>995600</parentid>
      <timestamp>2018-05-18T15:34:44Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>add main function</comment>
      <origin>995601</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2127" sha1="lwmyfyifbcnyp6pvxeysgkrrlkq8703" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = text:gsub('&lt;br/?&gt;', ', ') --&lt;br&gt; or &lt;br/&gt; with commas
	text = plaintext(text) --plain textify to reduce strange stuff
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[%s,](.-%d.-)[%s,]', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('%s%s', '') --remove possible double spaces from previous cleanup
			   :gsub('(,%s-),', '') --remove possible blank seperated commas from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..','
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--generates type in location

function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	typ = plaintext(args[1])
	loc =  loc and (' in '..p._generalLoc(args[2])) or ''
	checkpattern = args.check-pattern
	if typ then
		if (not checkpattern) or typ:match(checkpattern) then --check checkpattern exists; if it doesn't, true; if it exists and matches, true
			frame:expandTemplate {'Short description', {typ..loc, 'noreplace'}}
		end
	end
end

return p</text>
      <sha1>lwmyfyifbcnyp6pvxeysgkrrlkq8703</sha1>
    </revision>
    <revision>
      <id>995602</id>
      <parentid>995601</parentid>
      <timestamp>2018-05-18T15:36:24Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>995602</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2130" sha1="o2f5ugfcbczuk6dolp7yf6azxq4ymtu" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = text:gsub('&lt;br/?&gt;', ', ') --&lt;br&gt; or &lt;br/&gt; with commas
	text = plaintext(text) --plain textify to reduce strange stuff
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[%s,](.-%d.-)[%s,]', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('%s%s', '') --remove possible double spaces from previous cleanup
			   :gsub('(,%s-),', '') --remove possible blank seperated commas from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..','
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--generates type in location

function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	typ = plaintext(args[1])
	loc =  loc and (' in '..p._generalLoc(args[2])) or ''
	checkpattern = args['check-pattern']
	if typ then
		if (not checkpattern) or typ:match(checkpattern) then --check checkpattern exists; if it doesn't, true; if it exists and matches, true
			frame:expandTemplate {'Short description', {typ..loc, 'noreplace'}}
		end
	end
end

return p</text>
      <sha1>o2f5ugfcbczuk6dolp7yf6azxq4ymtu</sha1>
    </revision>
    <revision>
      <id>995603</id>
      <parentid>995602</parentid>
      <timestamp>2018-05-18T15:37:12Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>995603</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2145" sha1="g1g6wd9nrt0j99rbzcobal1d60q6ti9" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = text:gsub('&lt;br/?&gt;', ', ') --&lt;br&gt; or &lt;br/&gt; with commas
	text = plaintext(text) --plain textify to reduce strange stuff
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[%s,](.-%d.-)[%s,]', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('%s%s', '') --remove possible double spaces from previous cleanup
			   :gsub('(,%s-),', '') --remove possible blank seperated commas from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..','
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--generates type in location

function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	typ = plaintext(args[1])
	loc =  loc and (' in '..p._generalLoc(args[2])) or ''
	checkpattern = args['check-pattern']
	if typ then
		if (not checkpattern) or typ:match(checkpattern) then --check checkpattern exists; if it doesn't, true; if it exists and matches, true
			frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}}
		end
	end
end

return p</text>
      <sha1>g1g6wd9nrt0j99rbzcobal1d60q6ti9</sha1>
    </revision>
    <revision>
      <id>995604</id>
      <parentid>995603</parentid>
      <timestamp>2018-05-18T15:37:37Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fixx</comment>
      <origin>995604</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2152" sha1="c8insjtxhw0g4qqb9467x0jsfk8r1do" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = text:gsub('&lt;br/?&gt;', ', ') --&lt;br&gt; or &lt;br/&gt; with commas
	text = plaintext(text) --plain textify to reduce strange stuff
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[%s,](.-%d.-)[%s,]', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('%s%s', '') --remove possible double spaces from previous cleanup
			   :gsub('(,%s-),', '') --remove possible blank seperated commas from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..','
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--generates type in location

function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	typ = plaintext(args[1])
	loc =  loc and (' in '..p._generalLoc(args[2])) or ''
	checkpattern = args['check-pattern']
	if typ then
		if (not checkpattern) or typ:match(checkpattern) then --check checkpattern exists; if it doesn't, true; if it exists and matches, true
			return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}}
		end
	end
end

return p</text>
      <sha1>c8insjtxhw0g4qqb9467x0jsfk8r1do</sha1>
    </revision>
    <revision>
      <id>995605</id>
      <parentid>995604</parentid>
      <timestamp>2018-05-19T04:07:42Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>995605</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2166" sha1="a3bxxn0excimb32lxmbfk8jqyz1glc1" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = text:gsub('&lt;br/?&gt;', ', ') --&lt;br&gt; or &lt;br/&gt; with commas
	text = plaintext(text) --plain textify to reduce strange stuff
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[%s,](.-%d.-)[%s,]', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('%s%s', '') --remove possible double spaces from previous cleanup
			   :gsub('(,%s-),', '') --remove possible blank seperated commas from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..','
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--generates type in location

function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	typ = plaintext(args[1])
	loc = args[2]
	loc = loc and (' in '..p._generalLoc(args[2])) or ''
	checkpattern = args['check-pattern']
	if typ then
		if (not checkpattern) or typ:match(checkpattern) then --check checkpattern exists; if it doesn't, true; if it exists and matches, true
			return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}}
		end
	end
end

return p</text>
      <sha1>a3bxxn0excimb32lxmbfk8jqyz1glc1</sha1>
    </revision>
    <revision>
      <id>995606</id>
      <parentid>995605</parentid>
      <timestamp>2018-05-19T04:14:32Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>995606</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2227" sha1="13h4y1n3c6zm2w8caxcxbl4ski6uzaz" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = text:gsub('&lt;br/?&gt;', ', ') --&lt;br&gt; or &lt;br/&gt; with commas
	text = plaintext(text) --plain textify to reduce strange stuff
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[%s,](.-%d.-)[%s,]', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('%s%s', '') --remove possible double spaces from previous cleanup
			   :gsub('(,%s-),', '') --remove possible blank seperated commas from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..','
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--generates type in location

function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	typ = plaintext(args[1])
	loc = args[2]
	loc = loc and (' in '..p._generalLoc(args[2])) or ''
	checkpattern = args['check-pattern']
	if typ then
		if (not checkpattern) or typ:match(checkpattern) then --check checkpattern exists; if it doesn't, true; if it exists and matches, true
			return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}}
		end
	end
end

return p</text>
      <sha1>13h4y1n3c6zm2w8caxcxbl4ski6uzaz</sha1>
    </revision>
    <revision>
      <id>995607</id>
      <parentid>995606</parentid>
      <timestamp>2018-05-19T04:15:06Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>+comment</comment>
      <origin>995607</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2283" sha1="nosuzmaw3hz3tet5ba0g68n81sokrxe" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = text:gsub('&lt;br/?&gt;', ', ') --&lt;br&gt; or &lt;br/&gt; with commas
	text = plaintext(text) --plain textify to reduce strange stuff
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[%s,](.-%d.-)[%s,]', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('%s%s', '') --remove possible double spaces from previous cleanup
			   :gsub('(,%s-),', '') --remove possible blank seperated commas from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--generates type in location

function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	typ = plaintext(args[1])
	loc = args[2]
	loc = loc and (' in '..p._generalLoc(args[2])) or ''
	checkpattern = args['check-pattern']
	if typ then
		if (not checkpattern) or typ:match(checkpattern) then --check checkpattern exists; if it doesn't, true; if it exists and matches, true
			return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}}
		end
	end
end

return p</text>
      <sha1>nosuzmaw3hz3tet5ba0g68n81sokrxe</sha1>
    </revision>
    <revision>
      <id>995608</id>
      <parentid>995607</parentid>
      <timestamp>2018-05-19T04:28:40Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fixes</comment>
      <origin>995608</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2277" sha1="i79844ah0oft9tb1ybu8ssi6h5nyhxh" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = text:gsub('&lt;br/?&gt;', ', ') --&lt;br&gt; or &lt;br/&gt; with commas
	text = plaintext(text) --plain textify to reduce strange stuff
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[^%s,]-%d[^%s,]-', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('(,%s-),', '%1') --fix possible blank seperated commas from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--generates type in location

function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	typ = plaintext(args[1])
	loc = args[2]
	loc = loc and (' in '..p._generalLoc(args[2])) or ''
	checkpattern = args['check-pattern']
	if typ then
		if (not checkpattern) or typ:match(checkpattern) then --check checkpattern exists; if it doesn't, true; if it exists and matches, true
			return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}}
		end
	end
end

return p</text>
      <sha1>i79844ah0oft9tb1ybu8ssi6h5nyhxh</sha1>
    </revision>
    <revision>
      <id>995609</id>
      <parentid>995608</parentid>
      <timestamp>2018-05-19T04:58:11Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>swap</comment>
      <origin>995609</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2277" sha1="fvv3wnyn3wipujvxfvswpbeamlivdil" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = text:gsub('&lt;br/?&gt;', ', ') --&lt;br&gt; or &lt;br/&gt; with commas
	text = plaintext(text) --plain textify to reduce strange stuff
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[^%s,]-%d[^%s,]-', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('(,%s-),', '%1') --fix possible blank seperated commas from previous cleanup
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--generates type in location

function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	typ = plaintext(args[1])
	loc = args[2]
	loc = loc and (' in '..p._generalLoc(args[2])) or ''
	checkpattern = args['check-pattern']
	if typ then
		if (not checkpattern) or typ:match(checkpattern) then --check checkpattern exists; if it doesn't, true; if it exists and matches, true
			return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}}
		end
	end
end

return p</text>
      <sha1>fvv3wnyn3wipujvxfvswpbeamlivdil</sha1>
    </revision>
    <revision>
      <id>995610</id>
      <parentid>995609</parentid>
      <timestamp>2018-05-19T05:03:54Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>995610</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2277" sha1="q1m2bra5jvvi8biiru9o3944vee5pbv" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = text:gsub('&lt;br/?&gt;', ', ') --&lt;br&gt; or &lt;br/&gt; with commas
	text = plaintext(text) --plain textify to reduce strange stuff
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('(,%s-),', '%1') --fix possible blank seperated commas from previous cleanup
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--generates type in location

function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	typ = plaintext(args[1])
	loc = args[2]
	loc = loc and (' in '..p._generalLoc(args[2])) or ''
	checkpattern = args['check-pattern']
	if typ then
		if (not checkpattern) or typ:match(checkpattern) then --check checkpattern exists; if it doesn't, true; if it exists and matches, true
			return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}}
		end
	end
end

return p</text>
      <sha1>q1m2bra5jvvi8biiru9o3944vee5pbv</sha1>
    </revision>
    <revision>
      <id>995611</id>
      <parentid>995610</parentid>
      <timestamp>2018-05-19T06:04:26Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>expand functionality</comment>
      <origin>995611</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2780" sha1="q4xx1o83qfceu9dlu20tv4mcmewwnoq" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text:gsub('&lt;br/?&gt;', ', ')) --&lt;br&gt; or &lt;br/&gt; with commas, plain textify to reduce strange stuff
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('(,%s-),', '%1') --fix possible blank seperated commas from previous cleanup
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	checkpatterns = args['check-patterns']..';'
	invalidadd = args.invalidadd
	if typ then
		if checkpatterns then
			for k in checkpatterns:gmatch('([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
				if typ:match(checkpattern) then return typ end
			end
			if not valid and invalidadd then --if invalid, add to make it valid
				return typ..invalidadd
			end
		end
		return typ
	end
end

--generates type in location
function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	if typ then typ = plaintext(args[1]:gsub('&lt;br/?&gt;', ', ')) else return end
	loc = args[2]
	sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between type and loc, if it has other value put that
	loc = loc and (sep..p._generalLoc(args[2])) or ''
	loc = p.validateTyp (typ, args)
	if typ then return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}} end
end

return p</text>
      <sha1>q4xx1o83qfceu9dlu20tv4mcmewwnoq</sha1>
    </revision>
    <revision>
      <id>995612</id>
      <parentid>995611</parentid>
      <timestamp>2018-05-19T06:36:40Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fixes</comment>
      <origin>995612</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2753" sha1="p7pv05gyajio77dya4wwi8m5aj47fm1" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text:gsub('&lt;br/?&gt;', ', ')) --&lt;br&gt; or &lt;br/&gt; with commas, plain textify to reduce strange stuff
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('(,%s-),', '%1') --fix possible blank seperated commas from previous cleanup
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	checkpatterns = args['check-patterns']..';'
	invalidadd = args.invalidadd
	if checkpatterns then
		for k in checkpatterns:gmatch('([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if typ:match(checkpattern) then return typ end
		end
		if not valid and invalidadd then --if invalid, add to make it valid
			return typ..invalidadd
		end
	end
	return typ
end

--generates type in location
function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	if typ then typ = plaintext(args[1]:gsub('&lt;br/?&gt;', ', ')) else return end
	loc = args[2]
	loc = loc and (sep..p._generalLoc(args[2])) or ''
	sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between type and loc, if it has other value put that
	typ = p.validateTyp (typ, args)
	if typ then return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}} end
end

return p</text>
      <sha1>p7pv05gyajio77dya4wwi8m5aj47fm1</sha1>
    </revision>
    <revision>
      <id>995613</id>
      <parentid>995612</parentid>
      <timestamp>2018-05-19T06:48:40Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>995613</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2760" sha1="3e5ws1qvyy8xj3cqy44uzbhy8okphhy" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text:gsub('&lt;br/?&gt;', ', ')) --&lt;br&gt; or &lt;br/&gt; with commas, plain textify to reduce strange stuff
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('(,%s-),', '%1') --fix possible blank seperated commas from previous cleanup
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	checkpatterns = args['check-patterns']..';'
	invalidadd = args.invalidadd
	if checkpatterns then
		for k in checkpatterns:gmatch('([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if typ:match(checkpattern) then return typ end
		end
		if not valid and invalidadd then --if invalid, add to make it valid
			return typ..invalidadd
		end
	else
		return typ
	end
end

--generates type in location
function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	if typ then typ = plaintext(args[1]:gsub('&lt;br/?&gt;', ', ')) else return end
	loc = args[2]
	loc = loc and (sep..p._generalLoc(args[2])) or ''
	sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between type and loc, if it has other value put that
	typ = p.validateTyp (typ, args)
	if typ then return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}} end
end

return p</text>
      <sha1>3e5ws1qvyy8xj3cqy44uzbhy8okphhy</sha1>
    </revision>
    <revision>
      <id>995614</id>
      <parentid>995613</parentid>
      <timestamp>2018-05-19T15:11:41Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fixes</comment>
      <origin>995614</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2809" sha1="e5mk7hi3ngkj2v8dqgtrzae6i7pewso" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text:gsub('&lt;br/?&gt;', ', ')) --&lt;br&gt; or &lt;br/&gt; with commas, plain textify to reduce strange stuff
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('(,%s-),', '%1') --fix possible blank seperated commas from previous cleanup
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	checkpatterns = args['check-patterns']
	invalidadd = args.invalidadd
	if checkpatterns then
		for k in (checkpatterns..';'):gmatch('([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if typ:match(k) then return typ end
		end
		if invalidadd then --if invalid, add to make it valid
			return typ..invalidadd
		end
	else
		return typ
	end
end

--generates type in location
function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	typ = args[1]
	if typ then typ = plaintext(args[1]:gsub('&lt;br/?&gt;', ', ')) end
	if not typ then return end --check after plaintexting if typ exists
	sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between type and loc, if it has other value put that
	loc = args[2]
	loc = loc and (sep..p._generalLoc(args[2])) or ''
	typ = p.validateTyp (typ, args)
	if typ then return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}} end
end

return p</text>
      <sha1>e5mk7hi3ngkj2v8dqgtrzae6i7pewso</sha1>
    </revision>
    <revision>
      <id>995615</id>
      <parentid>995614</parentid>
      <timestamp>2018-05-19T15:27:00Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>995615</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2814" sha1="1s70cj3wsc1tspireqhzcc7u730xljb" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text:gsub('&lt;br/?&gt;', ', ')) --&lt;br&gt; or &lt;br/&gt; with commas, plain textify to reduce strange stuff
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('(,%s-),', '%1') --fix possible blank seperated commas from previous cleanup
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	checkpatterns = args['check-patterns']
	invalidadd = args.invalidadd
	if checkpatterns then
		for k in (checkpatterns..';'):gmatch('([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if typ:match(k) then return typ end
		end
		if invalidadd then --if invalid, add to make it valid
			return typ..' '..invalidadd
		end
	else
		return typ
	end
end

--generates type in location
function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	typ = args[1]
	if typ then typ = plaintext(args[1]:gsub('&lt;br/?&gt;', ', ')) end
	if not typ then return end --check after plaintexting if typ exists
	sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between type and loc, if it has other value put that
	loc = args[2]
	loc = loc and (sep..p._generalLoc(args[2])) or ''
	typ = p.validateTyp (typ, args)
	if typ then return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}} end
end

return p</text>
      <sha1>1s70cj3wsc1tspireqhzcc7u730xljb</sha1>
    </revision>
    <revision>
      <id>995616</id>
      <parentid>995615</parentid>
      <timestamp>2018-05-23T16:32:28Z</timestamp>
      <contributor>
        <username>TonyBallioni</username>
        <id>470</id>
      </contributor>
      <minor/>
      <comment>Protected "[[Module:Type in location]]": [[WP:High-risk templates|Highly visible template]] ([Edit=Require template editor access] (indefinite) [Move=Require template editor access] (indefinite))</comment>
      <origin>995615</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2814" sha1="1s70cj3wsc1tspireqhzcc7u730xljb" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text:gsub('&lt;br/?&gt;', ', ')) --&lt;br&gt; or &lt;br/&gt; with commas, plain textify to reduce strange stuff
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('(,%s-),', '%1') --fix possible blank seperated commas from previous cleanup
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	checkpatterns = args['check-patterns']
	invalidadd = args.invalidadd
	if checkpatterns then
		for k in (checkpatterns..';'):gmatch('([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if typ:match(k) then return typ end
		end
		if invalidadd then --if invalid, add to make it valid
			return typ..' '..invalidadd
		end
	else
		return typ
	end
end

--generates type in location
function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	typ = args[1]
	if typ then typ = plaintext(args[1]:gsub('&lt;br/?&gt;', ', ')) end
	if not typ then return end --check after plaintexting if typ exists
	sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between type and loc, if it has other value put that
	loc = args[2]
	loc = loc and (sep..p._generalLoc(args[2])) or ''
	typ = p.validateTyp (typ, args)
	if typ then return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}} end
end

return p</text>
      <sha1>1s70cj3wsc1tspireqhzcc7u730xljb</sha1>
    </revision>
    <revision>
      <id>995617</id>
      <parentid>995616</parentid>
      <timestamp>2018-05-23T16:39:06Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>995617</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2818" sha1="hjp7b19avh4u5j2969ti2h7t8lclbbq" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text:gsub('&lt;br ?/?&gt;', ', ')) --&lt;br&gt; or &lt;br/&gt; with commas, plain textify to reduce strange stuff
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('(,%s-),', '%1') --fix possible blank seperated commas from previous cleanup
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	checkpatterns = args['check-patterns']
	invalidadd = args.invalidadd
	if checkpatterns then
		for k in (checkpatterns..';'):gmatch('([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if typ:match(k) then return typ end
		end
		if invalidadd then --if invalid, add to make it valid
			return typ..' '..invalidadd
		end
	else
		return typ
	end
end

--generates type in location
function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	typ = args[1]
	if typ then typ = plaintext(args[1]:gsub('&lt;br ?/?&gt;', ', ')) end
	if not typ then return end --check after plaintexting if typ exists
	sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between type and loc, if it has other value put that
	loc = args[2]
	loc = loc and (sep..p._generalLoc(args[2])) or ''
	typ = p.validateTyp (typ, args)
	if typ then return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}} end
end

return p</text>
      <sha1>hjp7b19avh4u5j2969ti2h7t8lclbbq</sha1>
    </revision>
    <revision>
      <id>995618</id>
      <parentid>995617</parentid>
      <timestamp>2018-05-24T13:04:52Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>unecessary</comment>
      <origin>995618</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2705" sha1="8yydtp88h6halobc0yzebkettln3akx" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text)
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('(,%s-),', '%1') --fix possible blank seperated commas from previous cleanup
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	checkpatterns = args['check-patterns']
	invalidadd = args.invalidadd
	if checkpatterns then
		for k in (checkpatterns..';'):gmatch('([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if typ:match(k) then return typ end
		end
		if invalidadd then --if invalid, add to make it valid
			return typ..' '..invalidadd
		end
	else
		return typ
	end
end

--generates type in location
function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	typ = args[1]
	if typ then typ = plaintext(args[1]) end
	if not typ then return end --check after plaintexting if typ exists
	sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between type and loc, if it has other value put that
	loc = args[2]
	loc = loc and (sep..p._generalLoc(args[2])) or ''
	typ = p.validateTyp (typ, args)
	if typ then return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}} end
end

return p</text>
      <sha1>8yydtp88h6halobc0yzebkettln3akx</sha1>
    </revision>
    <revision>
      <id>995619</id>
      <parentid>995618</parentid>
      <timestamp>2018-05-24T14:06:19Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>+option full-loc</comment>
      <origin>995619</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2777" sha1="9hg1rzcuj6o9yjubi4izskg996iaums" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text)
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('(,%s-),', '%1') --fix possible blank seperated commas from previous cleanup
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	checkpatterns = args['check-patterns']
	invalidadd = args.invalidadd
	if checkpatterns then
		for k in (checkpatterns..';'):gmatch('([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if typ:match(k) then return typ end
		end
		if invalidadd then --if invalid, add to make it valid
			return typ..' '..invalidadd
		end
	else
		return typ
	end
end

--generates type in location
function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	typ = args[1]
	if typ then typ = plaintext(args[1]) end
	if not typ then return end --check after plaintexting if typ exists
	sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between type and loc, if it has other value put that
	loc = args[2]
	if args['full-loc'] then func = '_prepareLoc' else func =  '_generalLoc' end
	loc = loc and (sep..p[func](args[2])) or ''
	typ = p.validateTyp (typ, args)
	if typ then return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}} end
end

return p</text>
      <sha1>9hg1rzcuj6o9yjubi4izskg996iaums</sha1>
    </revision>
    <revision>
      <id>995620</id>
      <parentid>995619</parentid>
      <timestamp>2019-01-21T08:57:44Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>rm redundancy</comment>
      <origin>995620</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2885" sha1="oivqo9s34vpqvfapfr5oiyla8u553uy" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text)
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('(,%s-),', '%1') --fix possible blank seperated commas from previous cleanup
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	checkpatterns = args['check-patterns']
	invalidadd = args.invalidadd
	if checkpatterns then
		for k in (checkpatterns..';'):gmatch('([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if typ:match(k) then return typ end
		end
		if invalidadd then --if invalid, add to make it valid
			return typ..' '..invalidadd
		end
	else
		return typ
	end
end

--generates type in location
function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	cleanupLoc = require('Module:Settlement short description').cleanupLoc
	typ = args[1]
	if typ then typ = plaintext(args[1]) end
	if not typ then return end --check after plaintexting if typ exists
	sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between type and loc, if it has other value put that
	loc = args[2]
	if args['full-loc'] then func = '_prepareLoc' else func =  '_generalLoc' end
	if loc then
		loc = p[func](loc)
		loc = cleanupLoc (loc)
		loc = sep..loc
	end
	typ = p.validateTyp (typ, args)
	if typ then return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}} end
end

return p</text>
      <sha1>oivqo9s34vpqvfapfr5oiyla8u553uy</sha1>
    </revision>
    <revision>
      <id>995621</id>
      <parentid>995620</parentid>
      <timestamp>2019-01-21T16:58:13Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>995621</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2902" sha1="18i03r9c2ng37nu17qqkfibvszy8nbi" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text)
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('(,%s-),', '%1') --fix possible blank seperated commas from previous cleanup
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	checkpatterns = args['check-patterns']
	invalidadd = args.invalidadd
	if checkpatterns then
		for k in (checkpatterns..';'):gmatch('([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if typ:match(k) then return typ end
		end
		if invalidadd then --if invalid, add to make it valid
			return typ..' '..invalidadd
		end
	else
		return typ
	end
end

--generates type in location
function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	cleanupLoc = require('Module:Settlement short description').cleanupLoc
	typ = args[1]
	if typ then typ = plaintext(args[1]) end
	if not typ then return end --check after plaintexting if typ exists
	sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between type and loc, if it has other value put that
	loc = args[2]
	if args['full-loc'] then func = '_prepareLoc' else func =  '_generalLoc' end
	if loc then
		loc = p[func](loc)
		loc = cleanupLoc (loc)
		loc = sep..loc
	else
		loc = ""
	end
	typ = p.validateTyp (typ, args)
	if typ then return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}} end
end

return p</text>
      <sha1>18i03r9c2ng37nu17qqkfibvszy8nbi</sha1>
    </revision>
    <revision>
      <id>995622</id>
      <parentid>995621</parentid>
      <timestamp>2019-01-21T17:01:49Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>995622</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2939" sha1="mdsr3kgxsu4nud01rxaainjabb95y1l" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text)
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unecessary postal codes/road numbers etc
			   :gsub('(,%s-),', '%1') --fix possible blank seperated commas from previous cleanup
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commmas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries seperated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	checkpatterns = args['check-patterns']
	invalidadd = args.invalidadd
	if checkpatterns then
		for k in (checkpatterns..';'):gmatch('([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if typ:match(k) then return typ end
		end
		if invalidadd then --if invalid, add to make it valid
			return typ..' '..invalidadd
		end
	else
		return typ
	end
end

--generates type in location
function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	cleanupLoc = require('Module:Settlement short description').cleanupLoc
	typ = args[1]
	if typ then typ = plaintext(args[1]) end
	if not typ then return end --check after plaintexting if typ exists
	sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between type and loc, if it has other value put that
	local loc = args[2]
	if args['full-loc'] then func = '_prepareLoc' else func =  '_generalLoc' end
	if loc then
		loc = p[func](loc)
		loc = cleanupLoc (loc)
		if loc then loc =  sep..loc else loc = "" end
	else
		loc = ""
	end
	typ = p.validateTyp (typ, args)
	if typ then return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}} end
end

return p</text>
      <sha1>mdsr3kgxsu4nud01rxaainjabb95y1l</sha1>
    </revision>
    <revision>
      <id>995623</id>
      <parentid>995622</parentid>
      <timestamp>2020-03-23T13:32:33Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Jonesey95</username>
        <id>0</id>
      </contributor>
      <comment>fix typos in comments</comment>
      <origin>995623</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2937" sha1="f13rbxmsjzbgahnjeaj3z9qxh6q901m" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text)
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as extraneous information
			   :gsub('[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unnecessary postal codes/road numbers etc
			   :gsub('(,%s-),', '%1') --fix possible blank separated commas from previous cleanup
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries separated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	checkpatterns = args['check-patterns']
	invalidadd = args.invalidadd
	if checkpatterns then
		for k in (checkpatterns..';'):gmatch('([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if typ:match(k) then return typ end
		end
		if invalidadd then --if invalid, add to make it valid
			return typ..' '..invalidadd
		end
	else
		return typ
	end
end

--generates type in location
function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

function p._main (args, frame)
	cleanupLoc = require('Module:Settlement short description').cleanupLoc
	typ = args[1]
	if typ then typ = plaintext(args[1]) end
	if not typ then return end --check after plaintexting if typ exists
	sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between typ and loc, if it has other value put that
	local loc = args[2]
	if args['full-loc'] then func = '_prepareLoc' else func =  '_generalLoc' end
	if loc then
		loc = p[func](loc)
		loc = cleanupLoc (loc)
		if loc then loc =  sep..loc else loc = "" end
	else
		loc = ""
	end
	typ = p.validateTyp (typ, args)
	if typ then return frame:expandTemplate {title = 'Short description', args = {typ..loc, 'noreplace'}} end
end

return p</text>
      <sha1>f13rbxmsjzbgahnjeaj3z9qxh6q901m</sha1>
    </revision>
    <revision>
      <id>995624</id>
      <parentid>995623</parentid>
      <timestamp>2022-01-31T22:29:20Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Jonesey95</username>
        <id>0</id>
      </contributor>
      <comment>upper-case the first character of the short description per [[WP:SDFORMAT]]. Some code copied from [[:Module:Settlement short description]]</comment>
      <origin>995624</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3132" sha1="dcgk80otpng5nipxoiw4yl6fw9kem70" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main
local language = mw.language.getContentLanguage()

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text)
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as extraneous information
			   :gsub('[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unnecessary postal codes/road numbers etc
			   :gsub('(,%s-),', '%1') --fix possible blank separated commas from previous cleanup
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	split = {}
	num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries separated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	checkpatterns = args['check-patterns']
	invalidadd = args.invalidadd
	if checkpatterns then
		for k in (checkpatterns..';'):gmatch('([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if typ:match(k) then return typ end
		end
		if invalidadd then --if invalid, add to make it valid
			return typ..' '..invalidadd
		end
	else
		return typ
	end
end

--generates type in location
function p.main(frame)
	args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p._main (args, frame)
	cleanupLoc = require('Module:Settlement short description').cleanupLoc
	typ = args[1]
	if typ then typ = plaintext(args[1]) end
	if not typ then return end --check after plaintexting if typ exists
	sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between typ and loc, if it has other value put that
	local loc = args[2]
	if args['full-loc'] then func = '_prepareLoc' else func =  '_generalLoc' end
	if loc then
		loc = p[func](loc)
		loc = cleanupLoc (loc)
		if loc then loc =  sep..loc else loc = "" end
	else
		loc = ""
	end
	typ = p.validateTyp (typ, args)
	if typ then return p.shortdesc(language:ucfirst(typ..loc), frame) end
end

return p</text>
      <sha1>dcgk80otpng5nipxoiw4yl6fw9kem70</sha1>
    </revision>
    <revision>
      <id>995625</id>
      <parentid>995624</parentid>
      <timestamp>2023-02-10T20:53:29Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izno</username>
        <id>0</id>
      </contributor>
      <comment>fix per VPT</comment>
      <origin>995625</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3192" sha1="gbvf5lt85bfhlzcnm3z70qa8vw4b0nf" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main
local language = mw.language.getContentLanguage()

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text)
	text = text..',' --comma at the end makes things convenient
	text = text:gsub('%b()', ', ') --remove things in brackets as extraneous information
			   :gsub('[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unnecessary postal codes/road numbers etc
			   :gsub('(,%s-),', '%1') --fix possible blank separated commas from previous cleanup
			   :gsub('%s%s', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	local split = {}
	local num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in loc:gmatch('([^,]*),') do --split by commas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries separated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	local checkpatterns = args['check-patterns']
	local invalidadd = args.invalidadd
	if checkpatterns then
		for k in (checkpatterns..';'):gmatch('([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if typ:match(k) then return typ end
		end
		if invalidadd then --if invalid, add to make it valid
			return typ..' '..invalidadd
		end
	else
		return typ
	end
end

--generates type in location
function p.main(frame)
	local args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame)
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p._main (args, frame)
	local cleanupLoc = require('Module:Settlement short description').cleanupLoc
	local typ = args[1]
	if typ then typ = plaintext(args[1]) end
	if not typ then return end --check after plaintexting if typ exists
	local sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between typ and loc, if it has other value put that
	local loc = args[2]
	local func
	if args['full-loc'] then func = '_prepareLoc' else func =  '_generalLoc' end
	if loc then
		loc = p[func](loc)
		loc = cleanupLoc (loc)
		if loc then loc =  sep..loc else loc = "" end
	else
		loc = ""
	end
	typ = p.validateTyp (typ, args)
	if typ then return p.shortdesc(language:ucfirst(typ..loc), frame) end
end

return p</text>
      <sha1>gbvf5lt85bfhlzcnm3z70qa8vw4b0nf</sha1>
    </revision>
    <revision>
      <id>995626</id>
      <parentid>995625</parentid>
      <timestamp>2023-04-27T14:32:40Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>New test entry point, handle unicode, allow lc patterns, Lua cleanup, fix whitespace bug. Created test cases: passed</comment>
      <origin>995626</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3659" sha1="luimxv6oilpf8xl008xuhd5ux8s7crk" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main
local language = mw.language.getContentLanguage()

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text)
	text = text..',' --comma at the end makes things convenient
	text = mw.ustring.gsub(text,'%b()', ', ') --remove things in brackets as extraneous information
	text = mw.ustring.gsub(text,'[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unnecessary postal codes/road numbers etc
	text = mw.ustring.gsub(text,'(,%s-),', '%1') --fix possible blank separated commas from previous cleanup
	text = mw.ustring.gsub(text,'%s%s', ' ') --fix possible extra spaces from previous cleanup
	text = mw.ustring.gsub(text,'^[%s,]*', '') --trim commas and spaces from beginning
	text = mw.ustring.gsub(text,'[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	local split = {}
	local num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in mw.ustring.gmatch(loc,'([^,]*),') do --split by commas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries separated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	args = args or {}
	local checkpatterns = args['check-patterns']
	local invalidadd = args.invalidadd
	if checkpatterns then
		for k in mw.ustring.gmatch(checkpatterns..';','([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if mw.ustring.match(mw.ustring.lower(typ),k) then 
				return typ
			end
		end
		if invalidadd then --if invalid, add to make it valid
			return typ..' '..invalidadd
		end
	else
		return typ
	end
end

function p._generateDesc (args)
	local cleanupLoc = require('Module:Settlement short description').cleanupLoc
	local typ = args[1]
	if typ then typ = plaintext(args[1]) end
	if not typ then return end --check after plaintexting if typ exists
	local sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between typ and loc, if it has other value put that
	local loc = args[2]
	local func
	if args['full-loc'] then func = '_prepareLoc' else func =  '_generalLoc' end
	if loc then
		loc = p[func](loc)
		loc = mw.text.trim(cleanupLoc(loc))
		loc = loc and sep..loc or ""
	else
		loc = ""
	end
	typ = p.validateTyp (typ, args)
	return typ and language:ucfirst(typ..loc)
end

function p.generateDesc(frame)
	local args = require('Module:Arguments').getArgs(frame)
	return p._generateDesc(args) or ""
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	frame = frame or mw.getCurrentFrame()
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p._main(args, frame)
	frame = frame or mw.getCurrentFrame()
	local desc = p._generateDesc(args)
	return desc and p.shortdesc(desc, frame)
end

--generates type in location
function p.main(frame)
	local args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame) or ""
end

return p</text>
      <sha1>luimxv6oilpf8xl008xuhd5ux8s7crk</sha1>
    </revision>
    <revision>
      <id>995627</id>
      <parentid>995626</parentid>
      <timestamp>2023-04-27T17:50:19Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>handle case where location has ubl (e.g.)</comment>
      <origin>995627</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3696" sha1="0pkndcc5j6focfxjndfv4j733dad4xf" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main
local language = mw.language.getContentLanguage()

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text)
	text = text..',' --comma at the end makes things convenient
	text = mw.ustring.gsub(text,'%b()', ', ') --remove things in brackets as extraneous information
	text = mw.ustring.gsub(text,'[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unnecessary postal codes/road numbers etc
	text = mw.ustring.gsub(text,'(,%s-),', '%1') --fix possible blank separated commas from previous cleanup
	text = mw.ustring.gsub(text,'%s%s', ' ') --fix possible extra spaces from previous cleanup
	text = mw.ustring.gsub(text,'^[%s,]*', '') --trim commas and spaces from beginning
	text = mw.ustring.gsub(text,'[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	local split = {}
	local num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in mw.ustring.gmatch(loc,'([^,]*),') do --split by commas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries separated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	args = args or {}
	local checkpatterns = args['check-patterns']
	local invalidadd = args.invalidadd
	if checkpatterns then
		for k in mw.ustring.gmatch(checkpatterns..';','([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if mw.ustring.match(mw.ustring.lower(typ),k) then 
				return typ
			end
		end
		if invalidadd then --if invalid, add to make it valid
			return typ..' '..invalidadd
		end
	else
		return typ
	end
end

function p._generateDesc (args)
	local cleanupLoc = require('Module:Settlement short description').cleanupLoc
	local typ = args[1]
	if typ then typ = plaintext(args[1]) end
	if not typ then return end --check after plaintexting if typ exists
	local sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between typ and loc, if it has other value put that
	local loc = args[2]
	local func
	if args['full-loc'] then func = '_prepareLoc' else func =  '_generalLoc' end
	if loc then
		loc = p[func](loc) or ""
		loc = cleanupLoc(loc) or ""
		loc = mw.text.trim(loc)
		loc = loc and #loc &gt; 0 and sep..loc or ""
	else
		loc = ""
	end
	typ = p.validateTyp (typ, args)
	return typ and language:ucfirst(typ..loc)
end

function p.generateDesc(frame)
	local args = require('Module:Arguments').getArgs(frame)
	return p._generateDesc(args) or ""
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	frame = frame or mw.getCurrentFrame()
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p._main(args, frame)
	frame = frame or mw.getCurrentFrame()
	local desc = p._generateDesc(args)
	return desc and p.shortdesc(desc, frame)
end

--generates type in location
function p.main(frame)
	local args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame) or ""
end

return p</text>
      <sha1>0pkndcc5j6focfxjndfv4j733dad4xf</sha1>
    </revision>
    <revision>
      <id>995593</id>
      <timestamp>2023-04-27T18:16:32Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>simplify code</comment>
      <origin>995593</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3650" sha1="6iht21q45jbptsbuduln8bs0tja9bfr" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main
local language = mw.language.getContentLanguage()

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text)
	text = text..',' --comma at the end makes things convenient
	text = mw.ustring.gsub(text,'%b()', ', ') --remove things in brackets as extraneous information
	text = mw.ustring.gsub(text,'[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unnecessary postal codes/road numbers etc
	text = mw.ustring.gsub(text,'(,%s-),', '%1') --fix possible blank separated commas from previous cleanup
	text = mw.ustring.gsub(text,'%s%s', ' ') --fix possible extra spaces from previous cleanup
	text = mw.ustring.gsub(text,'^[%s,]*', '') --trim commas and spaces from beginning
	text = mw.ustring.gsub(text,'[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	local split = {}
	local num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in mw.ustring.gmatch(loc,'([^,]*),') do --split by commas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries separated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	args = args or {}
	local checkpatterns = args['check-patterns']
	local invalidadd = args.invalidadd
	if checkpatterns then
		for k in mw.ustring.gmatch(checkpatterns..';','([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if mw.ustring.match(mw.ustring.lower(typ),k) then 
				return typ
			end
		end
		if invalidadd then --if invalid, add to make it valid
			return typ..' '..invalidadd
		end
	else
		return typ
	end
end

function p._generateDesc (args)
	local cleanupLoc = require('Module:Settlement short description').cleanupLoc
	local typ = args[1] or ""
	typ = plaintext(args[1]) or ""
	if #typ == 0 then return end  -- bail out if type is empty
	local sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between typ and loc, if it has other value put that
	local loc = args[2] or ""
	local func
	if args['full-loc'] then func = '_prepareLoc' else func =  '_generalLoc' end
	loc = p[func](loc) or ""
	loc = cleanupLoc(loc) or ""
	loc = mw.text.trim(loc)
	loc = loc and #loc &gt; 0 and sep..loc or ""
	typ = p.validateTyp (typ, args)
	return typ and language:ucfirst(typ..loc)
end

function p.generateDesc(frame)
	local args = require('Module:Arguments').getArgs(frame)
	return p._generateDesc(args) or ""
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	frame = frame or mw.getCurrentFrame()
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p._main(args, frame)
	frame = frame or mw.getCurrentFrame()
	local desc = p._generateDesc(args)
	return desc and p.shortdesc(desc, frame)
end

--generates type in location
function p.main(frame)
	local args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame) or ""
end

return p</text>
      <sha1>6iht21q45jbptsbuduln8bs0tja9bfr</sha1>
    </revision>
    <revision>
      <id>995594</id>
      <parentid>995593</parentid>
      <timestamp>2025-01-23T17:56:35Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported from [[:wikipedia:Module:Type_in_location]]</comment>
      <origin>995593</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3650" sha1="6iht21q45jbptsbuduln8bs0tja9bfr" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main
local language = mw.language.getContentLanguage()

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text)
	text = text..',' --comma at the end makes things convenient
	text = mw.ustring.gsub(text,'%b()', ', ') --remove things in brackets as extraneous information
	text = mw.ustring.gsub(text,'[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unnecessary postal codes/road numbers etc
	text = mw.ustring.gsub(text,'(,%s-),', '%1') --fix possible blank separated commas from previous cleanup
	text = mw.ustring.gsub(text,'%s%s', ' ') --fix possible extra spaces from previous cleanup
	text = mw.ustring.gsub(text,'^[%s,]*', '') --trim commas and spaces from beginning
	text = mw.ustring.gsub(text,'[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	local split = {}
	local num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in mw.ustring.gmatch(loc,'([^,]*),') do --split by commas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries separated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	args = args or {}
	local checkpatterns = args['check-patterns']
	local invalidadd = args.invalidadd
	if checkpatterns then
		for k in mw.ustring.gmatch(checkpatterns..';','([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if mw.ustring.match(mw.ustring.lower(typ),k) then 
				return typ
			end
		end
		if invalidadd then --if invalid, add to make it valid
			return typ..' '..invalidadd
		end
	else
		return typ
	end
end

function p._generateDesc (args)
	local cleanupLoc = require('Module:Settlement short description').cleanupLoc
	local typ = args[1] or ""
	typ = plaintext(args[1]) or ""
	if #typ == 0 then return end  -- bail out if type is empty
	local sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between typ and loc, if it has other value put that
	local loc = args[2] or ""
	local func
	if args['full-loc'] then func = '_prepareLoc' else func =  '_generalLoc' end
	loc = p[func](loc) or ""
	loc = cleanupLoc(loc) or ""
	loc = mw.text.trim(loc)
	loc = loc and #loc &gt; 0 and sep..loc or ""
	typ = p.validateTyp (typ, args)
	return typ and language:ucfirst(typ..loc)
end

function p.generateDesc(frame)
	local args = require('Module:Arguments').getArgs(frame)
	return p._generateDesc(args) or ""
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	frame = frame or mw.getCurrentFrame()
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p._main(args, frame)
	frame = frame or mw.getCurrentFrame()
	local desc = p._generateDesc(args)
	return desc and p.shortdesc(desc, frame)
end

--generates type in location
function p.main(frame)
	local args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame) or ""
end

return p</text>
      <sha1>6iht21q45jbptsbuduln8bs0tja9bfr</sha1>
    </revision>
    <revision>
      <id>10049</id>
      <timestamp>2025-12-08T02:08:39Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was 33 revisions imported from [[:wikipedia:Module:Type_in_location]]</comment>
      <origin>10049</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3650" sha1="6iht21q45jbptsbuduln8bs0tja9bfr" xml:space="preserve">local p = {}
local plaintext = require("Module:Plain text")._main
local language = mw.language.getContentLanguage()

--Cleanup/format location for use in short descriptions
function p.prepareLoc (frame)
	return p._prepareLoc (frame.args[1])
end

function p._prepareLoc (text)
	text = plaintext(text)
	text = text..',' --comma at the end makes things convenient
	text = mw.ustring.gsub(text,'%b()', ', ') --remove things in brackets as extraneous information
	text = mw.ustring.gsub(text,'[^%s,]*%d[^%s,]*', '') --remove things with digits as generally being unnecessary postal codes/road numbers etc
	text = mw.ustring.gsub(text,'(,%s-),', '%1') --fix possible blank separated commas from previous cleanup
	text = mw.ustring.gsub(text,'%s%s', ' ') --fix possible extra spaces from previous cleanup
	text = mw.ustring.gsub(text,'^[%s,]*', '') --trim commas and spaces from beginning
	text = mw.ustring.gsub(text,'[%s,]*$', '') --trim commas and spaces from end
	return text
end

--Gets general location from more specific one for short descriptions
--i.e if a location is specified to be "P. Sherman 42 Wallaby Way Sydney, Australia", return "Sydney, Australia"
--splits by commas and returns last two entries

function p.generalLoc (frame)
	return p._generalLoc (frame.args[1])
end

function p._generalLoc (loc)
	loc = p._prepareLoc(loc)
	local split = {}
	local num = 0
	loc = loc..',' --comma at the end for convenient splitting with gmatch
	for k in mw.ustring.gmatch(loc,'([^,]*),') do --split by commas
		table.insert(split, k)
		num = num + 1
	end
	if num == 1 then --if only comma was the one at the end return the whole thing
		return split[1]
	else
		return split[num-1]..','..split[num] --return last two entries separated by commas
	end
end

--validate type parameter
function p.validateTyp (typ, args)
	args = args or {}
	local checkpatterns = args['check-patterns']
	local invalidadd = args.invalidadd
	if checkpatterns then
		for k in mw.ustring.gmatch(checkpatterns..';','([^;]*);') do --split checkpatterns by ;, check if one of the patterns is in type
			if mw.ustring.match(mw.ustring.lower(typ),k) then 
				return typ
			end
		end
		if invalidadd then --if invalid, add to make it valid
			return typ..' '..invalidadd
		end
	else
		return typ
	end
end

function p._generateDesc (args)
	local cleanupLoc = require('Module:Settlement short description').cleanupLoc
	local typ = args[1] or ""
	typ = plaintext(args[1]) or ""
	if #typ == 0 then return end  -- bail out if type is empty
	local sep = ((args.sep == 'no') and '') or args.sep or ' in ' --if args.sep set to no, nothing between typ and loc, if it has other value put that
	local loc = args[2] or ""
	local func
	if args['full-loc'] then func = '_prepareLoc' else func =  '_generalLoc' end
	loc = p[func](loc) or ""
	loc = cleanupLoc(loc) or ""
	loc = mw.text.trim(loc)
	loc = loc and #loc &gt; 0 and sep..loc or ""
	typ = p.validateTyp (typ, args)
	return typ and language:ucfirst(typ..loc)
end

function p.generateDesc(frame)
	local args = require('Module:Arguments').getArgs(frame)
	return p._generateDesc(args) or ""
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	frame = frame or mw.getCurrentFrame()
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p._main(args, frame)
	frame = frame or mw.getCurrentFrame()
	local desc = p._generateDesc(args)
	return desc and p.shortdesc(desc, frame)
end

--generates type in location
function p.main(frame)
	local args = require('Module:Arguments').getArgs (frame, {frameOnly = true})
	return p._main(args, frame) or ""
end

return p</text>
      <sha1>6iht21q45jbptsbuduln8bs0tja9bfr</sha1>
    </revision>
  </page>
</mediawiki>
