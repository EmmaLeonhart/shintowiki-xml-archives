<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Module:Separated entries</title>
    <ns>828</ns>
    <id>39</id>
    <revision>
      <id>432548</id>
      <timestamp>2015-04-17T23:50:49Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izkala</username>
        <id>0</id>
      </contributor>
      <comment>Generic implementation of [[Module:Br separated entries]]</comment>
      <origin>432548</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="528" sha1="b0u9flervatdecz43nbnmzbbc93kgjx" xml:space="preserve">local getArgs = require('Module:Arguments').getArgs
local p = {}
 
function p.main(frame)
	local args = getArgs(frame, {
		trim = true,
		removeBlanks = true
	})
	return p._main(args)
end
 
function p._main(origArgs)
	local separator = origArgs.separator or ''
	local conjunction = origArgs.conjunction or separator

	args = {}
	for k, v in pairs(origArgs) do
		-- Discard named parameters.
		if type(k) == 'number' then
			table.insert(args, v)
		end
	end
	return mw.text.listToText(args, separator, conjunction)
end
 
return p</text>
      <sha1>b0u9flervatdecz43nbnmzbbc93kgjx</sha1>
    </revision>
    <revision>
      <id>432549</id>
      <parentid>432548</parentid>
      <timestamp>2015-04-18T11:34:57Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izkala</username>
        <id>0</id>
      </contributor>
      <comment>don't trim named parameters</comment>
      <origin>432549</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="543" sha1="0nlz8nlb78t0m3soi9lh8gx5rwlmtjj" xml:space="preserve">local getArgs = require('Module:Arguments').getArgs
local p = {}
 
function p.main(frame)
	local args = getArgs(frame, {
		removeBlanks = true,
		trim = false
	})
	return p._main(args)
end
 
function p._main(origArgs)
	local separator = origArgs.separator or ''
	local conjunction = origArgs.conjunction or separator

	args = {}
	for k, v in pairs(origArgs) do
		-- Discard named parameters.
		if type(k) == 'number' then
			table.insert(args, mw.text.trim(v))
		end
	end
	return mw.text.listToText(args, separator, conjunction)
end
 
return p</text>
      <sha1>0nlz8nlb78t0m3soi9lh8gx5rwlmtjj</sha1>
    </revision>
    <revision>
      <id>432550</id>
      <parentid>432549</parentid>
      <timestamp>2015-04-25T14:30:23Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Jc86035</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>Testing, does not affect anything using main</comment>
      <origin>432550</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1000" sha1="f1nlo6z87ca55824kjpesp4l7n87lpi" xml:space="preserve">local getArgs = require('Module:Arguments').getArgs
local p = {}
 
function p.main(frame)
	local args = getArgs(frame, {
		removeBlanks = true,
		trim = false
	})
	return p._main(args)
end
 
function p._main(origArgs)
	local separator = origArgs.separator or ''
	local conjunction = origArgs.conjunction or separator

	args = {}
	for k, v in pairs(origArgs) do
		-- Discard named parameters.
		if type(k) == 'number' then
			table.insert(args, mw.text.trim(v))
		end
	end
	return mw.text.listToText(args, separator, conjunction)
end

function p.notrim(frame)
	local args = getArgs(frame, {
		removeBlanks = true,
		trim = false
	})
	return p._notrim(args)
end

function p._notrim(origArgs)
	local separator = origArgs.separator or ''
	local conjunction = origArgs.conjunction or separator

	args = {}
	for k, v in pairs(origArgs) do
		-- Discard named parameters.
		if type(k) == 'number' then
			table.insert(args, v)
		end
	end
	return mw.text.listToText(args, separator, conjunction)
end

return p</text>
      <sha1>f1nlo6z87ca55824kjpesp4l7n87lpi</sha1>
    </revision>
    <revision>
      <id>432551</id>
      <parentid>432550</parentid>
      <timestamp>2015-05-01T11:32:57Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izkala</username>
        <id>0</id>
      </contributor>
      <comment>Reverted 1 edit by [[Special:Contributions/Jc86035|Jc86035]] ([[User talk:Jc86035|talk]]): Rv test - named parameters are automatically trimmed. ([[WP:TW|TW]])</comment>
      <origin>432551</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="543" sha1="0nlz8nlb78t0m3soi9lh8gx5rwlmtjj" xml:space="preserve">local getArgs = require('Module:Arguments').getArgs
local p = {}
 
function p.main(frame)
	local args = getArgs(frame, {
		removeBlanks = true,
		trim = false
	})
	return p._main(args)
end
 
function p._main(origArgs)
	local separator = origArgs.separator or ''
	local conjunction = origArgs.conjunction or separator

	args = {}
	for k, v in pairs(origArgs) do
		-- Discard named parameters.
		if type(k) == 'number' then
			table.insert(args, mw.text.trim(v))
		end
	end
	return mw.text.listToText(args, separator, conjunction)
end
 
return p</text>
      <sha1>0nlz8nlb78t0m3soi9lh8gx5rwlmtjj</sha1>
    </revision>
    <revision>
      <id>432552</id>
      <parentid>432551</parentid>
      <timestamp>2015-05-01T11:34:55Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izkala</username>
        <id>0</id>
      </contributor>
      <comment>Reverted to revision 656969384 by [[Special:Contributions/Alakzi|Alakzi]] ([[User talk:Alakzi|talk]]): Restore original. ([[WP:TW|TW]])</comment>
      <origin>432552</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="528" sha1="b0u9flervatdecz43nbnmzbbc93kgjx" xml:space="preserve">local getArgs = require('Module:Arguments').getArgs
local p = {}
 
function p.main(frame)
	local args = getArgs(frame, {
		trim = true,
		removeBlanks = true
	})
	return p._main(args)
end
 
function p._main(origArgs)
	local separator = origArgs.separator or ''
	local conjunction = origArgs.conjunction or separator

	args = {}
	for k, v in pairs(origArgs) do
		-- Discard named parameters.
		if type(k) == 'number' then
			table.insert(args, v)
		end
	end
	return mw.text.listToText(args, separator, conjunction)
end
 
return p</text>
      <sha1>b0u9flervatdecz43nbnmzbbc93kgjx</sha1>
    </revision>
    <revision>
      <id>432553</id>
      <parentid>432552</parentid>
      <timestamp>2015-05-04T01:30:45Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izkala</username>
        <id>0</id>
      </contributor>
      <comment>Discard named parameters more robustly; accidental global</comment>
      <origin>432553</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="527" sha1="7rerpw031n8km0xgwwuo03dfqzbf12h" xml:space="preserve">local getArgs = require('Module:Arguments').getArgs
local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}
 
function p.main(frame)
	local args = getArgs(frame, {
		trim = true,
		removeBlanks = true
	})
	return p._main(args)
end
 
function p._main(args)
	local separator = args.separator or ''
	local conjunction = args.conjunction or separator
	-- Discard named parameters.
	local values = compressSparseArray(args)

	return mw.text.listToText(values, separator, conjunction)
end
 
return p</text>
      <sha1>7rerpw031n8km0xgwwuo03dfqzbf12h</sha1>
    </revision>
    <revision>
      <id>432554</id>
      <parentid>432553</parentid>
      <timestamp>2015-05-04T13:38:02Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izkala</username>
        <id>0</id>
      </contributor>
      <comment>Decode HTML escape sequences in separator and conjunction</comment>
      <origin>432554</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="684" sha1="fjqtkeuevdfxlvhya0zn5elubosbyw0" xml:space="preserve">local getArgs = require('Module:Arguments').getArgs
local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}
 
function p.main(frame)
	local args = getArgs(frame, {
		trim = true,
		removeBlanks = true
	})
	return p._main(args)
end
 
function p._main(args)
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard named parameters.
	local values = compressSparseArray(args)

	return mw.text.listToText(values, separator, conjunction)
end
 
return p</text>
      <sha1>fjqtkeuevdfxlvhya0zn5elubosbyw0</sha1>
    </revision>
    <revision>
      <id>432555</id>
      <parentid>432554</parentid>
      <timestamp>2015-05-12T11:18:55Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Kelapstick</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>Protected Module:Separated entries: [[WP:High-risk templates|Highly visible template]] ([Edit=Allow only template editors and admins] (indefinite) [Move=Allow only template editors and admins] (indefinite))</comment>
      <origin>432554</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="684" sha1="fjqtkeuevdfxlvhya0zn5elubosbyw0" xml:space="preserve">local getArgs = require('Module:Arguments').getArgs
local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}
 
function p.main(frame)
	local args = getArgs(frame, {
		trim = true,
		removeBlanks = true
	})
	return p._main(args)
end
 
function p._main(args)
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard named parameters.
	local values = compressSparseArray(args)

	return mw.text.listToText(values, separator, conjunction)
end
 
return p</text>
      <sha1>fjqtkeuevdfxlvhya0zn5elubosbyw0</sha1>
    </revision>
    <revision>
      <id>432556</id>
      <parentid>432555</parentid>
      <timestamp>2015-05-26T18:01:57Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izkala</username>
        <id>0</id>
      </contributor>
      <comment>Update from sandbox, with a couple of changes</comment>
      <origin>432556</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1165" sha1="llc3d3c78nhgnxk4nekr2o428a8k5qt" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux".

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard named parameters.
	local values = compressSparseArray(args)
	return mw.text.listToText(values, separator, conjunction)
end

local function makeInvokeFunction(separator, conjunction)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>llc3d3c78nhgnxk4nekr2o428a8k5qt</sha1>
    </revision>
    <revision>
      <id>432557</id>
      <parentid>432556</parentid>
      <timestamp>2015-05-26T18:09:55Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izkala</username>
        <id>0</id>
      </contributor>
      <comment>wrappers=</comment>
      <origin>432557</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1303" sha1="ihqyo24tzsrbymxn1vub5c7uzj4ts6h" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux".

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard named parameters.
	local values = compressSparseArray(args)
	return mw.text.listToText(values, separator, conjunction)
end

local function makeInvokeFunction(separator, conjunction)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {
			wrappers = {
				'Template:Separated entries',
				'Template:Br separated entries',
				'Tempkate:Comma separated entries'
			}
		})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>ihqyo24tzsrbymxn1vub5c7uzj4ts6h</sha1>
    </revision>
    <revision>
      <id>432558</id>
      <parentid>432557</parentid>
      <timestamp>2015-05-26T22:20:50Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Izkala</username>
        <id>0</id>
      </contributor>
      <comment>rv, that broke something</comment>
      <origin>432558</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1165" sha1="llc3d3c78nhgnxk4nekr2o428a8k5qt" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux".

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard named parameters.
	local values = compressSparseArray(args)
	return mw.text.listToText(values, separator, conjunction)
end

local function makeInvokeFunction(separator, conjunction)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>llc3d3c78nhgnxk4nekr2o428a8k5qt</sha1>
    </revision>
    <revision>
      <id>432559</id>
      <parentid>432558</parentid>
      <timestamp>2018-06-26T03:05:37Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Jc86035</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>allow the first positional parameter to be specified (useful in some templates)</comment>
      <origin>432559</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1403" sha1="r3n53qz4zycs3vxyo5ms9qjo1jm32bp" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	return mw.text.listToText(values, separator, conjunction)
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>r3n53qz4zycs3vxyo5ms9qjo1jm32bp</sha1>
    </revision>
    <revision>
      <id>432542</id>
      <timestamp>2018-12-17T20:54:33Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Amorymeltzer</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>Changed protection level for "[[Module:Separated entries]]": [[WP:High-risk templates|High-risk Lua module]]: Over 2M transclusions ([Edit=Require administrator access] (indefinite) [Move=Require administrator access] (indefinite))</comment>
      <origin>432542</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1403" sha1="r3n53qz4zycs3vxyo5ms9qjo1jm32bp" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	return mw.text.listToText(values, separator, conjunction)
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>r3n53qz4zycs3vxyo5ms9qjo1jm32bp</sha1>
    </revision>
    <revision>
      <id>432546</id>
      <parentid>432542</parentid>
      <timestamp>2023-03-03T11:17:41Z</timestamp>
      <contributor>
        <username>ImprovedWikiImprovment</username>
        <id>24</id>
      </contributor>
      <minor/>
      <comment>pp</comment>
      <origin>432546</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1423" sha1="lh2kx0tkv7kfa4msgqkxsn655yu3ryd" xml:space="preserve">-- {{pp|small=yes}}
-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	return mw.text.listToText(values, separator, conjunction)
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>lh2kx0tkv7kfa4msgqkxsn655yu3ryd</sha1>
    </revision>
    <revision>
      <id>432543</id>
      <parentid>432542</parentid>
      <timestamp>2024-06-25T10:18:06Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported from [[:wikipedia:Module:Separated_entries]]: importing for the list. This is a canonical list of Shrines to work with</comment>
      <origin>432542</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1403" sha1="r3n53qz4zycs3vxyo5ms9qjo1jm32bp" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	return mw.text.listToText(values, separator, conjunction)
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>r3n53qz4zycs3vxyo5ms9qjo1jm32bp</sha1>
    </revision>
    <revision>
      <id>432544</id>
      <parentid>432543</parentid>
      <timestamp>2024-11-09T16:07:13Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432545</id>
      <parentid>432544</parentid>
      <timestamp>2024-12-10T12:11:41Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported from [[:wikipedia:Module:Separated_entries]]</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432547</id>
      <parentid>432545</parentid>
      <timestamp>2025-01-09T00:01:04Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: importing templates</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432560</id>
      <parentid>432547</parentid>
      <timestamp>2025-02-17T06:17:04Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>12 revisions imported from [[:wikipedia:Module:Separated_entries]]</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432561</id>
      <parentid>432560</parentid>
      <timestamp>2025-02-27T21:12:41Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432562</id>
      <parentid>432561</parentid>
      <timestamp>2025-02-27T21:15:26Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432563</id>
      <parentid>432562</parentid>
      <timestamp>2025-03-11T09:00:56Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432564</id>
      <parentid>432563</parentid>
      <timestamp>2025-03-15T19:25:36Z</timestamp>
      <contributor>
        <username>en&gt;Unknown user</username>
        <id>0</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432565</id>
      <parentid>432564</parentid>
      <timestamp>2025-04-06T00:14:17Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432566</id>
      <parentid>432565</parentid>
      <timestamp>2025-04-06T00:14:18Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: reimporting to fix errors</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432567</id>
      <parentid>432566</parentid>
      <timestamp>2025-04-06T23:25:58Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432568</id>
      <parentid>432567</parentid>
      <timestamp>2025-04-06T23:25:59Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: attempting to fix the nihongo</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432569</id>
      <parentid>432568</parentid>
      <timestamp>2025-04-27T22:11:02Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432571</id>
      <parentid>432569</parentid>
      <timestamp>2025-04-27T22:12:28Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432572</id>
      <parentid>432571</parentid>
      <timestamp>2025-04-27T22:13:21Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432573</id>
      <parentid>432572</parentid>
      <timestamp>2025-04-27T22:13:22Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: chat is this real</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432574</id>
      <parentid>432573</parentid>
      <timestamp>2025-04-27T22:17:03Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432575</id>
      <parentid>432574</parentid>
      <timestamp>2025-05-04T10:12:33Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432576</id>
      <parentid>432575</parentid>
      <timestamp>2025-05-05T21:16:13Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432577</id>
      <parentid>432576</parentid>
      <timestamp>2025-05-05T21:16:15Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: lets hope this fixes it since the qq=draft was showing up</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432578</id>
      <parentid>432577</parentid>
      <timestamp>2025-07-07T21:44:05Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432579</id>
      <parentid>432578</parentid>
      <timestamp>2025-07-07T22:04:26Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Copy from sandbox per request</comment>
      <origin>432544</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1728" sha1="0tp4y1kozxhpcw1ykqbo8e2gq604ky9" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame)
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>0tp4y1kozxhpcw1ykqbo8e2gq604ky9</sha1>
    </revision>
    <revision>
      <id>432584</id>
      <parentid>432579</parentid>
      <timestamp>2025-10-13T16:33:18Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>432584</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>432580</id>
      <parentid>432579</parentid>
      <timestamp>2025-11-21T03:31:51Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>432580</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>432581</id>
      <parentid>432580</parentid>
      <timestamp>2025-11-21T03:31:52Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting this page since it for some reason started to get issues</comment>
      <origin>432580</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>432582</id>
      <parentid>432581</parentid>
      <timestamp>2025-11-21T05:35:58Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: Reverting edits from past 90 minutes</comment>
      <origin>432582</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="528" sha1="b0u9flervatdecz43nbnmzbbc93kgjx" xml:space="preserve">local getArgs = require('Module:Arguments').getArgs
local p = {}
 
function p.main(frame)
	local args = getArgs(frame, {
		trim = true,
		removeBlanks = true
	})
	return p._main(args)
end
 
function p._main(origArgs)
	local separator = origArgs.separator or ''
	local conjunction = origArgs.conjunction or separator

	args = {}
	for k, v in pairs(origArgs) do
		-- Discard named parameters.
		if type(k) == 'number' then
			table.insert(args, v)
		end
	end
	return mw.text.listToText(args, separator, conjunction)
end
 
return p</text>
      <sha1>b0u9flervatdecz43nbnmzbbc93kgjx</sha1>
    </revision>
    <revision>
      <id>432583</id>
      <parentid>432582</parentid>
      <timestamp>2025-11-21T05:49:48Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>432583</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>40</id>
      <timestamp>2025-12-08T02:07:45Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was 1 revision imported from [[:wikipedia:Module:Separated_entries]]</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>219187</id>
      <parentid>40</parentid>
      <timestamp>2026-02-16T18:53:50Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>219189</id>
      <parentid>219187</parentid>
      <timestamp>2026-02-16T18:53:50Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting this template since it seems broken in how it treats unknown parameters in [[Category:Wikidata generated shikinaisha pages]]</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3303541</id>
      <parentid>219189</parentid>
      <timestamp>2026-02-26T20:10:15Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3303542</id>
      <parentid>3303541</parentid>
      <timestamp>2026-02-26T20:10:15Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: reimporting these to fix older errors</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3304509</id>
      <parentid>3303542</parentid>
      <timestamp>2026-02-26T20:18:54Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3304510</id>
      <parentid>3304509</parentid>
      <timestamp>2026-02-26T20:18:54Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: importing this file, and also doing a full refresh import of all dependencies, because I am concerned this is one with a lot of broken dependencies. Intended end result is the template [[Template:Talk page header]] which is gonna be highly customized to our usage later on</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3365617</id>
      <parentid>3304510</parentid>
      <timestamp>2026-03-02T16:22:07Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3365618</id>
      <parentid>3365617</parentid>
      <timestamp>2026-03-02T16:22:07Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: import overwrite</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3368035</id>
      <parentid>3365618</parentid>
      <timestamp>2026-03-02T18:53:59Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3368036</id>
      <parentid>3368035</parentid>
      <timestamp>2026-03-02T18:53:59Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3368509</id>
      <parentid>3368036</parentid>
      <timestamp>2026-03-02T18:56:22Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3368510</id>
      <parentid>3368509</parentid>
      <timestamp>2026-03-02T18:56:22Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3368983</id>
      <parentid>3368510</parentid>
      <timestamp>2026-03-02T19:02:44Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3368984</id>
      <parentid>3368983</parentid>
      <timestamp>2026-03-02T19:02:45Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3404989</id>
      <parentid>3368984</parentid>
      <timestamp>2026-03-12T18:09:35Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3404990</id>
      <parentid>3404989</parentid>
      <timestamp>2026-03-12T18:09:35Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3408129</id>
      <parentid>3404990</parentid>
      <timestamp>2026-03-12T20:30:37Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3408130</id>
      <parentid>3408129</parentid>
      <timestamp>2026-03-12T20:30:37Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3408714</id>
      <parentid>3408130</parentid>
      <timestamp>2026-03-12T20:38:45Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3408715</id>
      <parentid>3408714</parentid>
      <timestamp>2026-03-12T20:38:45Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3412120</id>
      <parentid>3408715</parentid>
      <timestamp>2026-03-13T00:15:59Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3412121</id>
      <parentid>3412120</parentid>
      <timestamp>2026-03-13T00:16:00Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: importing</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3412489</id>
      <parentid>3412121</parentid>
      <timestamp>2026-03-13T00:17:04Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3412490</id>
      <parentid>3412489</parentid>
      <timestamp>2026-03-13T00:17:04Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3516851</id>
      <parentid>3412490</parentid>
      <timestamp>2026-03-17T17:43:32Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3516852</id>
      <parentid>3516851</parentid>
      <timestamp>2026-03-17T17:43:32Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23208182735|commit triggered pipeline]]</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3516889</id>
      <parentid>3516852</parentid>
      <timestamp>2026-03-17T17:43:47Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3516890</id>
      <parentid>3516889</parentid>
      <timestamp>2026-03-17T17:43:47Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Bot: reimport from enwiki to fix erroneous transclusions [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23208182735|commit triggered pipeline]]</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3554001</id>
      <parentid>3516890</parentid>
      <timestamp>2026-03-20T18:19:51Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3554002</id>
      <parentid>3554001</parentid>
      <timestamp>2026-03-20T18:19:51Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3554215</id>
      <parentid>3554002</parentid>
      <timestamp>2026-03-20T18:20:52Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3554216</id>
      <parentid>3554215</parentid>
      <timestamp>2026-03-20T18:20:52Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3554429</id>
      <parentid>3554216</parentid>
      <timestamp>2026-03-20T18:22:20Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3554430</id>
      <parentid>3554429</parentid>
      <timestamp>2026-03-20T18:22:21Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3554643</id>
      <parentid>3554430</parentid>
      <timestamp>2026-03-20T18:24:21Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3554644</id>
      <parentid>3554643</parentid>
      <timestamp>2026-03-20T18:24:21Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3554857</id>
      <parentid>3554644</parentid>
      <timestamp>2026-03-20T18:26:51Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3554858</id>
      <parentid>3554857</parentid>
      <timestamp>2026-03-20T18:26:51Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3555071</id>
      <parentid>3554858</parentid>
      <timestamp>2026-03-20T18:29:53Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3555072</id>
      <parentid>3555071</parentid>
      <timestamp>2026-03-20T18:29:53Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3555285</id>
      <parentid>3555072</parentid>
      <timestamp>2026-03-20T18:33:23Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3555286</id>
      <parentid>3555285</parentid>
      <timestamp>2026-03-20T18:33:24Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3555499</id>
      <parentid>3555286</parentid>
      <timestamp>2026-03-20T18:37:27Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3555500</id>
      <parentid>3555499</parentid>
      <timestamp>2026-03-20T18:37:28Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3555713</id>
      <parentid>3555500</parentid>
      <timestamp>2026-03-20T18:41:57Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3555714</id>
      <parentid>3555713</parentid>
      <timestamp>2026-03-20T18:41:57Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3555927</id>
      <parentid>3555714</parentid>
      <timestamp>2026-03-20T18:47:00Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3555928</id>
      <parentid>3555927</parentid>
      <timestamp>2026-03-20T18:47:00Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3556141</id>
      <parentid>3555928</parentid>
      <timestamp>2026-03-20T18:52:30Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3556142</id>
      <parentid>3556141</parentid>
      <timestamp>2026-03-20T18:52:30Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3556355</id>
      <parentid>3556142</parentid>
      <timestamp>2026-03-20T18:58:34Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3556356</id>
      <parentid>3556355</parentid>
      <timestamp>2026-03-20T18:58:34Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3556569</id>
      <parentid>3556356</parentid>
      <timestamp>2026-03-20T19:05:04Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3556570</id>
      <parentid>3556569</parentid>
      <timestamp>2026-03-20T19:05:04Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3556783</id>
      <parentid>3556570</parentid>
      <timestamp>2026-03-20T19:12:06Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3556784</id>
      <parentid>3556783</parentid>
      <timestamp>2026-03-20T19:12:06Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3557041</id>
      <parentid>3556784</parentid>
      <timestamp>2026-03-20T19:37:48Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3557042</id>
      <parentid>3557041</parentid>
      <timestamp>2026-03-20T19:37:48Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3557363</id>
      <parentid>3557042</parentid>
      <timestamp>2026-03-20T19:41:44Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3557364</id>
      <parentid>3557363</parentid>
      <timestamp>2026-03-20T19:41:44Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3557876</id>
      <parentid>3557364</parentid>
      <timestamp>2026-03-20T19:46:18Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3557877</id>
      <parentid>3557876</parentid>
      <timestamp>2026-03-20T19:46:18Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3558748</id>
      <parentid>3557877</parentid>
      <timestamp>2026-03-20T19:55:19Z</timestamp>
      <contributor>
        <username>en&gt;Ahecht</username>
        <id>0</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3558749</id>
      <parentid>3558748</parentid>
      <timestamp>2026-03-20T19:55:19Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3562427</id>
      <parentid>3558749</parentid>
      <timestamp>2026-03-22T18:48:14Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3562428</id>
      <parentid>3562427</parentid>
      <timestamp>2026-03-22T18:48:15Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3562658</id>
      <parentid>3562428</parentid>
      <timestamp>2026-03-22T19:01:24Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3562659</id>
      <parentid>3562658</parentid>
      <timestamp>2026-03-22T19:01:25Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3607792</id>
      <parentid>3562659</parentid>
      <timestamp>2026-04-18T21:22:11Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3607793</id>
      <parentid>3607792</parentid>
      <timestamp>2026-04-18T21:22:11Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting to try to fix the lower alpha issue</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3607886</id>
      <parentid>3607793</parentid>
      <timestamp>2026-04-18T21:22:47Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3607887</id>
      <parentid>3607886</parentid>
      <timestamp>2026-04-18T21:22:47Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting to try to fix the lower alpha issue</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3646823</id>
      <parentid>3607887</parentid>
      <timestamp>2026-04-24T16:33:41Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3646824</id>
      <parentid>3646823</parentid>
      <timestamp>2026-04-24T16:33:41Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Template:Infobox saint </comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3646956</id>
      <parentid>3646824</parentid>
      <timestamp>2026-04-24T16:34:08Z</timestamp>
      <contributor>
        <username>Ahecht</username>
        <id>54</id>
      </contributor>
      <comment>{frameOnly = (frame.args.frameOnly or '') ~= ''}</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
    <revision>
      <id>3646957</id>
      <parentid>3646956</parentid>
      <timestamp>2026-04-24T16:34:08Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: en</comment>
      <origin>40</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1778" sha1="d3q6xup28j3xpu77wa8xwjjm6j0xizq" xml:space="preserve">-- This module takes positional parameters as input and concatenates them with
-- an optional separator. The final separator (the "conjunction") can be
-- specified independently, enabling natural-language lists like
-- "foo, bar, baz and qux". The starting parameter can also be specified.

local compressSparseArray = require('Module:TableTools').compressSparseArray
local p = {}

function p._main(args)
	local dataPlural = args.dataPlural
	local separator = args.separator
		-- Decode (convert to Unicode) HTML escape sequences, such as "&amp;#32;" for space.
		and mw.text.decode(args.separator) or ''
	local conjunction = args.conjunction and mw.text.decode(args.conjunction) or separator
	-- Discard values before the starting parameter.
	local start = tonumber(args.start)
	if start then
		for i = 1, start - 1 do args[i] = nil end
	end
	-- Discard named parameters.
	local values = compressSparseArray(args)
	local result = mw.text.listToText(values, separator, conjunction)
	if dataPlural and #values &gt; 0 and (#separator &gt; 0 or #conjunction &gt; 0) then
		-- flag to [[Module:Detect singular]] whether this is a list with &gt;1 element
		result = result..'&lt;span style="display:none" data-plural="'..(#values &gt; 1 and '1' or '0')..'"&gt;&lt;/span&gt;'
	end
	return result
end

local function makeInvokeFunction(separator, conjunction, first)
	return function (frame)
		local args = require('Module:Arguments').getArgs(frame, {frameOnly = (frame.args.frameOnly or '') ~= ''})
		args.separator = separator or args.separator
		args.conjunction = conjunction or args.conjunction
		args.first = first or args.first
		return p._main(args)
	end
end

p.main = makeInvokeFunction()
p.br = makeInvokeFunction('&lt;br /&gt;')
p.comma = makeInvokeFunction(mw.message.new('comma-separator'):plain())

return p</text>
      <sha1>d3q6xup28j3xpu77wa8xwjjm6j0xizq</sha1>
    </revision>
  </page>
</mediawiki>
