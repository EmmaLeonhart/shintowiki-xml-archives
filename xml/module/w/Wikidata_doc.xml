<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Module:Wikidata/doc</title>
    <ns>828</ns>
    <id>8281</id>
    <revision>
      <id>916606</id>
      <timestamp>2023-05-03T10:07:13Z</timestamp>
      <contributor>
        <username>en&gt;Gonnym</username>
        <id>0</id>
      </contributor>
      <comment>consistency</comment>
      <origin>916606</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="14615" sha1="963f882rkoxtjxcoycs5phr64zj7xoe" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' rank; or from the first image with 'normal' rank if no image has preferred rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[Bill Clinton]] you should get:
* -
* - [[Hillary Clinton]]
* - [[Hillary Rodham Clinton]]


In [[Barack Obama]] you should get:
* -
* - [[Michelle Obama]]
* - [[Hillary Rodham Clinton]]


In [[Richard Burton]] you should get:
* -
* - [[Elizabeth Taylor]], [[Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[Elizabeth Taylor]]
* - [[Hillary Rodham Clinton]]


In [[Franz Kafka]] you should get:
* -
* -
* - [[Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>963f882rkoxtjxcoycs5phr64zj7xoe</sha1>
    </revision>
    <revision>
      <id>916607</id>
      <parentid>916606</parentid>
      <timestamp>2025-01-10T09:34:49Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: IMPORTING ALL WANTED MODULES</comment>
      <origin>916606</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="14615" sha1="963f882rkoxtjxcoycs5phr64zj7xoe" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' rank; or from the first image with 'normal' rank if no image has preferred rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[Bill Clinton]] you should get:
* -
* - [[Hillary Clinton]]
* - [[Hillary Rodham Clinton]]


In [[Barack Obama]] you should get:
* -
* - [[Michelle Obama]]
* - [[Hillary Rodham Clinton]]


In [[Richard Burton]] you should get:
* -
* - [[Elizabeth Taylor]], [[Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[Elizabeth Taylor]]
* - [[Hillary Rodham Clinton]]


In [[Franz Kafka]] you should get:
* -
* -
* - [[Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>963f882rkoxtjxcoycs5phr64zj7xoe</sha1>
    </revision>
    <revision>
      <id>916608</id>
      <parentid>916607</parentid>
      <timestamp>2025-04-23T04:31:24Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "rank" to "Rank"</comment>
      <origin>916608</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="14615" sha1="d5be7gt5efykzob84n9kanxq8i5e2qz" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred Rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' Rank; or from the first image with 'normal' Rank if no image has preferred Rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[Bill Clinton]] you should get:
* -
* - [[Hillary Clinton]]
* - [[Hillary Rodham Clinton]]


In [[Barack Obama]] you should get:
* -
* - [[Michelle Obama]]
* - [[Hillary Rodham Clinton]]


In [[Richard Burton]] you should get:
* -
* - [[Elizabeth Taylor]], [[Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[Elizabeth Taylor]]
* - [[Hillary Rodham Clinton]]


In [[Franz Kafka]] you should get:
* -
* -
* - [[Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>d5be7gt5efykzob84n9kanxq8i5e2qz</sha1>
    </revision>
    <revision>
      <id>916609</id>
      <parentid>916608</parentid>
      <timestamp>2025-05-19T18:08:28Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>916609</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="14632" sha1="2h3c2pzzglontzicq7xnw7gv2xul9ci" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred Rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' Rank; or from the first image with 'normal' Rank if no image has preferred Rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[Bill Clinton]] you should get:
* -
* - [[Hillary Clinton]]
* - [[Hillary Rodham Clinton]]


In [[:en:Barack Obama|Barack Obama]] you should get:
* -
* - [[Michelle Obama]]
* - [[Hillary Rodham Clinton]]


In [[Richard Burton]] you should get:
* -
* - [[Elizabeth Taylor]], [[Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[Elizabeth Taylor]]
* - [[Hillary Rodham Clinton]]


In [[Franz Kafka]] you should get:
* -
* -
* - [[Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>2h3c2pzzglontzicq7xnw7gv2xul9ci</sha1>
    </revision>
    <revision>
      <id>916610</id>
      <parentid>916609</parentid>
      <timestamp>2025-05-19T19:57:57Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>916610</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="14674" sha1="ofstnaxsev9h4mzz3l4l2uz2zugc5v5" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred Rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' Rank; or from the first image with 'normal' Rank if no image has preferred Rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[Bill Clinton]] you should get:
* -
* - [[Hillary Clinton]]
* - [[Hillary Rodham Clinton]]


In [[:en:Barack Obama|Barack Obama]] you should get:
* -
* - [[Michelle Obama]]
* - [[Hillary Rodham Clinton]]


In [[Richard Burton]] you should get:
* -
* - [[:en:Elizabeth Taylor|Elizabeth Taylor]], [[Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[:en:Elizabeth Taylor|Elizabeth Taylor]]
* - [[Hillary Rodham Clinton]]


In [[Franz Kafka]] you should get:
* -
* -
* - [[Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>ofstnaxsev9h4mzz3l4l2uz2zugc5v5</sha1>
    </revision>
    <revision>
      <id>916611</id>
      <parentid>916610</parentid>
      <timestamp>2025-05-19T19:59:52Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>916611</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="14678" sha1="8uoh4affrabzehmhoh3w5gufcy00t8q" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[:en:Deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred Rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' Rank; or from the first image with 'normal' Rank if no image has preferred Rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[Bill Clinton]] you should get:
* -
* - [[Hillary Clinton]]
* - [[Hillary Rodham Clinton]]


In [[:en:Barack Obama|Barack Obama]] you should get:
* -
* - [[Michelle Obama]]
* - [[Hillary Rodham Clinton]]


In [[Richard Burton]] you should get:
* -
* - [[:en:Elizabeth Taylor|Elizabeth Taylor]], [[Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[:en:Elizabeth Taylor|Elizabeth Taylor]]
* - [[Hillary Rodham Clinton]]


In [[Franz Kafka]] you should get:
* -
* -
* - [[Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>8uoh4affrabzehmhoh3w5gufcy00t8q</sha1>
    </revision>
    <revision>
      <id>916612</id>
      <parentid>916611</parentid>
      <timestamp>2025-05-19T20:19:52Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>916612</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="14729" sha1="hujzzuk3qesomlfd2nvch4phm18a8yf" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[:en:Deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred Rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' Rank; or from the first image with 'normal' Rank if no image has preferred Rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[:en:Bill Clinton|Bill Clinton]] you should get:
* -
* - [[Hillary Clinton]]
* - [[Hillary Rodham Clinton]]


In [[:en:Barack Obama|Barack Obama]] you should get:
* -
* - [[Michelle Obama]]
* - [[Hillary Rodham Clinton]]


In [[Richard Burton]] you should get:
* -
* - [[:en:Elizabeth Taylor|Elizabeth Taylor]], [[Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[:en:Elizabeth Taylor|Elizabeth Taylor]]
* - [[Hillary Rodham Clinton]]


In [[Franz Kafka]] you should get:
* -
* -
* - [[Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>hujzzuk3qesomlfd2nvch4phm18a8yf</sha1>
    </revision>
    <revision>
      <id>916613</id>
      <parentid>916612</parentid>
      <timestamp>2025-05-19T20:41:54Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>916613</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="14745" sha1="fpvk5dkzifegsoefzvivho2v6v3kxi7" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[:en:Deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred Rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' Rank; or from the first image with 'normal' Rank if no image has preferred Rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[:en:Bill Clinton|Bill Clinton]] you should get:
* -
* - [[Hillary Clinton]]
* - [[Hillary Rodham Clinton]]


In [[:en:Barack Obama|Barack Obama]] you should get:
* -
* - [[Michelle Obama]]
* - [[Hillary Rodham Clinton]]


In [[Richard Burton]] you should get:
* -
* - [[:en:Elizabeth Taylor|Elizabeth Taylor]], [[Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[:en:Elizabeth Taylor|Elizabeth Taylor]]
* - [[Hillary Rodham Clinton]]


In [[:en:Franz Kafka|Franz Kafka]] you should get:
* -
* -
* - [[Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>fpvk5dkzifegsoefzvivho2v6v3kxi7</sha1>
    </revision>
    <revision>
      <id>916614</id>
      <parentid>916613</parentid>
      <timestamp>2025-05-21T03:01:04Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>916614</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="14785" sha1="2xe1h2ictd2gb06s6um789wwehswzvh" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[:en:Deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred Rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' Rank; or from the first image with 'normal' Rank if no image has preferred Rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hillary Clinton|Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[:en:Bill Clinton|Bill Clinton]] you should get:
* -
* - [[:en:Hillary Clinton|Hillary Clinton]]
* - [[Hillary Rodham Clinton]]


In [[:en:Barack Obama|Barack Obama]] you should get:
* -
* - [[Michelle Obama]]
* - [[Hillary Rodham Clinton]]


In [[Richard Burton]] you should get:
* -
* - [[:en:Elizabeth Taylor|Elizabeth Taylor]], [[Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[:en:Elizabeth Taylor|Elizabeth Taylor]]
* - [[Hillary Rodham Clinton]]


In [[:en:Franz Kafka|Franz Kafka]] you should get:
* -
* -
* - [[Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>2xe1h2ictd2gb06s6um789wwehswzvh</sha1>
    </revision>
    <revision>
      <id>916615</id>
      <parentid>916614</parentid>
      <timestamp>2025-05-21T03:01:11Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>916615</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="14974" sha1="2hdl2rojzi6es8yfu2ugpla3tekyjmh" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[:en:Deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred Rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' Rank; or from the first image with 'normal' Rank if no image has preferred Rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hillary Clinton|Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[:en:Bill Clinton|Bill Clinton]] you should get:
* -
* - [[:en:Hillary Clinton|Hillary Clinton]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Barack Obama|Barack Obama]] you should get:
* -
* - [[Michelle Obama]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[Richard Burton]] you should get:
* -
* - [[:en:Elizabeth Taylor|Elizabeth Taylor]], [[Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[:en:Elizabeth Taylor|Elizabeth Taylor]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Franz Kafka|Franz Kafka]] you should get:
* -
* -
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>2hdl2rojzi6es8yfu2ugpla3tekyjmh</sha1>
    </revision>
    <revision>
      <id>916616</id>
      <parentid>916615</parentid>
      <timestamp>2025-05-21T07:13:35Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>916616</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="15001" sha1="c8bnkgp2k0p71d9d0c0iefj1defeze1" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[:en:Deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred Rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' Rank; or from the first image with 'normal' Rank if no image has preferred Rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hillary Clinton|Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hope, Arkansas|Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[:en:Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[:en:Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[:en:Bill Clinton|Bill Clinton]] you should get:
* -
* - [[:en:Hillary Clinton|Hillary Clinton]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Barack Obama|Barack Obama]] you should get:
* -
* - [[Michelle Obama]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[Richard Burton]] you should get:
* -
* - [[:en:Elizabeth Taylor|Elizabeth Taylor]], [[Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[:en:Elizabeth Taylor|Elizabeth Taylor]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Franz Kafka|Franz Kafka]] you should get:
* -
* -
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>c8bnkgp2k0p71d9d0c0iefj1defeze1</sha1>
    </revision>
    <revision>
      <id>916617</id>
      <parentid>916616</parentid>
      <timestamp>2025-05-23T04:14:47Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>916617</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="15020" sha1="jx28gr2spu4srgshn6ftcplwzaklx1g" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[:en:Deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred Rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' Rank; or from the first image with 'normal' Rank if no image has preferred Rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hillary Clinton|Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hope, Arkansas|Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[:en:Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[:en:Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[:en:Bill Clinton|Bill Clinton]] you should get:
* -
* - [[:en:Hillary Clinton|Hillary Clinton]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Barack Obama|Barack Obama]] you should get:
* -
* - [[:en:Michelle Obama|Michelle Obama]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[Richard Burton]] you should get:
* -
* - [[:en:Elizabeth Taylor|Elizabeth Taylor]], [[Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[:en:Elizabeth Taylor|Elizabeth Taylor]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Franz Kafka|Franz Kafka]] you should get:
* -
* -
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>jx28gr2spu4srgshn6ftcplwzaklx1g</sha1>
    </revision>
    <revision>
      <id>916618</id>
      <parentid>916617</parentid>
      <timestamp>2025-05-24T20:50:02Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>916618</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="15037" sha1="64v8qumx7gpgwvrbkjhx5k76eosgw1m" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[:en:Deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred Rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' Rank; or from the first image with 'normal' Rank if no image has preferred Rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hillary Clinton|Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hope, Arkansas|Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[:en:Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[:en:Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[:en:Bill Clinton|Bill Clinton]] you should get:
* -
* - [[:en:Hillary Clinton|Hillary Clinton]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Barack Obama|Barack Obama]] you should get:
* -
* - [[:en:Michelle Obama|Michelle Obama]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[Richard Burton]] you should get:
* -
* - [[:en:Elizabeth Taylor|Elizabeth Taylor]], [[:en:Sally Burton|Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[:en:Elizabeth Taylor|Elizabeth Taylor]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Franz Kafka|Franz Kafka]] you should get:
* -
* -
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>64v8qumx7gpgwvrbkjhx5k76eosgw1m</sha1>
    </revision>
    <revision>
      <id>916619</id>
      <parentid>916618</parentid>
      <timestamp>2025-05-24T20:54:15Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>916619</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="15056" sha1="lhojrria5an5a1d2jw3tjfihvw9yyc9" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[:en:Deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred Rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' Rank; or from the first image with 'normal' Rank if no image has preferred Rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hillary Clinton|Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hope, Arkansas|Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[:en:Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[:en:Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[:en:Bill Clinton|Bill Clinton]] you should get:
* -
* - [[:en:Hillary Clinton|Hillary Clinton]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Barack Obama|Barack Obama]] you should get:
* -
* - [[:en:Michelle Obama|Michelle Obama]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Richard Burton|Richard Burton]] you should get:
* -
* - [[:en:Elizabeth Taylor|Elizabeth Taylor]], [[:en:Sally Burton|Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[:en:Elizabeth Taylor|Elizabeth Taylor]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Franz Kafka|Franz Kafka]] you should get:
* -
* -
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>lhojrria5an5a1d2jw3tjfihvw9yyc9</sha1>
    </revision>
    <revision>
      <id>916620</id>
      <parentid>916619</parentid>
      <timestamp>2025-05-24T22:13:11Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>916620</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="15075" sha1="ozc4hyyqujieoz8zd5actwqnsas83xy" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[:en:Deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred Rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' Rank; or from the first image with 'normal' Rank if no image has preferred Rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hillary Clinton|Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hope, Arkansas|Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[:en:Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[:en:Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[:en:Bill Clinton|Bill Clinton]] you should get:
* -
* - [[:en:Hillary Clinton|Hillary Clinton]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Barack Obama|Barack Obama]] you should get:
* -
* - [[:en:Michelle Obama|Michelle Obama]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Richard Burton|Richard Burton]] you should get:
* -
* - [[:en:Elizabeth Taylor|Elizabeth Taylor]], [[:en:Sally Burton|Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[:en:Elizabeth Taylor|Elizabeth Taylor]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Franz Kafka|Franz Kafka]] you should get:
* -
* -
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[:en:William Ellery|William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>ozc4hyyqujieoz8zd5actwqnsas83xy</sha1>
    </revision>
    <revision>
      <id>916621</id>
      <parentid>916620</parentid>
      <timestamp>2025-05-25T05:41:48Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>916621</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="15091" sha1="h0ocyu6sd151ztsxlfri88jfatbt93w" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[:en:Deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred Rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' Rank; or from the first image with 'normal' Rank if no image has preferred Rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hillary Clinton|Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hope, Arkansas|Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[:en:Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[:en:Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[:en:Bill Clinton|Bill Clinton]] you should get:
* -
* - [[:en:Hillary Clinton|Hillary Clinton]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Barack Obama|Barack Obama]] you should get:
* -
* - [[:en:Michelle Obama|Michelle Obama]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Richard Burton|Richard Burton]] you should get:
* -
* - [[:en:Elizabeth Taylor|Elizabeth Taylor]], [[:en:Sally Burton|Sally Burton]], [[Sybil Christopher]], [[:en:Suzy Miller|Suzy Miller]], [[:en:Elizabeth Taylor|Elizabeth Taylor]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Franz Kafka|Franz Kafka]] you should get:
* -
* -
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[:en:William Ellery|William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>h0ocyu6sd151ztsxlfri88jfatbt93w</sha1>
    </revision>
    <revision>
      <id>916622</id>
      <parentid>916621</parentid>
      <timestamp>2025-05-25T06:02:07Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>916622</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="15113" sha1="7lvkgdctn1riyh1ssto6ryjx4so8hz8" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[:en:Deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred Rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' Rank; or from the first image with 'normal' Rank if no image has preferred Rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hillary Clinton|Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hope, Arkansas|Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[:en:Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[:en:Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[:en:Bill Clinton|Bill Clinton]] you should get:
* -
* - [[:en:Hillary Clinton|Hillary Clinton]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Barack Obama|Barack Obama]] you should get:
* -
* - [[:en:Michelle Obama|Michelle Obama]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Richard Burton|Richard Burton]] you should get:
* -
* - [[:en:Elizabeth Taylor|Elizabeth Taylor]], [[:en:Sally Burton|Sally Burton]], [[:en:Sybil Christopher|Sybil Christopher]], [[:en:Suzy Miller|Suzy Miller]], [[:en:Elizabeth Taylor|Elizabeth Taylor]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]


In [[:en:Franz Kafka|Franz Kafka]] you should get:
* -
* -
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[:en:William Ellery|William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>7lvkgdctn1riyh1ssto6ryjx4so8hz8</sha1>
    </revision>
    <revision>
      <id>8282</id>
      <timestamp>2025-11-22T14:38:08Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was Text replacement - "↵↵↵" to "
"</comment>
      <origin>8282</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="15105" sha1="6arhfqgta3ktzqhhgetb8ve24qhtamx" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[:en:Deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred Rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' Rank; or from the first image with 'normal' Rank if no image has preferred Rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hillary Clinton|Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[:en:Bill Clinton|Bill Clinton]], it returns [[:en:Hope, Arkansas|Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[:en:Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[:en:Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;
== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[:en:Bill Clinton|Bill Clinton]] you should get:
* -
* - [[:en:Hillary Clinton|Hillary Clinton]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]
In [[:en:Barack Obama|Barack Obama]] you should get:
* -
* - [[:en:Michelle Obama|Michelle Obama]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]
In [[:en:Richard Burton|Richard Burton]] you should get:
* -
* - [[:en:Elizabeth Taylor|Elizabeth Taylor]], [[:en:Sally Burton|Sally Burton]], [[:en:Sybil Christopher|Sybil Christopher]], [[:en:Suzy Miller|Suzy Miller]], [[:en:Elizabeth Taylor|Elizabeth Taylor]]
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]
In [[:en:Franz Kafka|Franz Kafka]] you should get:
* -
* -
* - [[:en:Hillary Rodham Clinton|Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[:en:William Ellery|William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>6arhfqgta3ktzqhhgetb8ve24qhtamx</sha1>
    </revision>
    <revision>
      <id>3412308</id>
      <parentid>8282</parentid>
      <timestamp>2026-03-13T00:16:23Z</timestamp>
      <contributor>
        <username>en&gt;Gonnym</username>
        <id>0</id>
      </contributor>
      <comment>consistency</comment>
      <origin>3412308</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="14615" sha1="963f882rkoxtjxcoycs5phr64zj7xoe" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' rank; or from the first image with 'normal' rank if no image has preferred rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[Bill Clinton]] you should get:
* -
* - [[Hillary Clinton]]
* - [[Hillary Rodham Clinton]]


In [[Barack Obama]] you should get:
* -
* - [[Michelle Obama]]
* - [[Hillary Rodham Clinton]]


In [[Richard Burton]] you should get:
* -
* - [[Elizabeth Taylor]], [[Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[Elizabeth Taylor]]
* - [[Hillary Rodham Clinton]]


In [[Franz Kafka]] you should get:
* -
* -
* - [[Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>963f882rkoxtjxcoycs5phr64zj7xoe</sha1>
    </revision>
    <revision>
      <id>3412309</id>
      <parentid>3412308</parentid>
      <timestamp>2026-03-13T00:16:23Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: importing</comment>
      <origin>3412308</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="14615" sha1="963f882rkoxtjxcoycs5phr64zj7xoe" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' rank; or from the first image with 'normal' rank if no image has preferred rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[Bill Clinton]] you should get:
* -
* - [[Hillary Clinton]]
* - [[Hillary Rodham Clinton]]


In [[Barack Obama]] you should get:
* -
* - [[Michelle Obama]]
* - [[Hillary Rodham Clinton]]


In [[Richard Burton]] you should get:
* -
* - [[Elizabeth Taylor]], [[Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[Elizabeth Taylor]]
* - [[Hillary Rodham Clinton]]


In [[Franz Kafka]] you should get:
* -
* -
* - [[Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>963f882rkoxtjxcoycs5phr64zj7xoe</sha1>
    </revision>
    <revision>
      <id>3412677</id>
      <parentid>3412309</parentid>
      <timestamp>2026-03-13T00:17:26Z</timestamp>
      <contributor>
        <username>en&gt;Gonnym</username>
        <id>0</id>
      </contributor>
      <comment>consistency</comment>
      <origin>3412308</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="14615" sha1="963f882rkoxtjxcoycs5phr64zj7xoe" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' rank; or from the first image with 'normal' rank if no image has preferred rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[Bill Clinton]] you should get:
* -
* - [[Hillary Clinton]]
* - [[Hillary Rodham Clinton]]


In [[Barack Obama]] you should get:
* -
* - [[Michelle Obama]]
* - [[Hillary Rodham Clinton]]


In [[Richard Burton]] you should get:
* -
* - [[Elizabeth Taylor]], [[Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[Elizabeth Taylor]]
* - [[Hillary Rodham Clinton]]


In [[Franz Kafka]] you should get:
* -
* -
* - [[Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>963f882rkoxtjxcoycs5phr64zj7xoe</sha1>
    </revision>
    <revision>
      <id>3412678</id>
      <parentid>3412677</parentid>
      <timestamp>2026-03-13T00:17:26Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>3412308</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="14615" sha1="963f882rkoxtjxcoycs5phr64zj7xoe" xml:space="preserve">{{Ombox|image=[[File:Stop hand nuvola.svg|40px]]|type=delete|text=[[Module:Wikidata]] is [[deprecation|deprecated]]. Please use [[Module:WikidataIB]] or [[Module:Wd]] instead.}}
{{module rating|protected}}
{{High-use}}

== Methods ==
The module "{{BASEPAGENAME}}" contains the following methods, that allow the calling script to retrieve the value for any property from Wikidata by supplying the property ID as the first parameter:
* &lt;code&gt;getValue&lt;/code&gt;: Returns wiki-linked values, if applicable. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, except that if values with preferred rank exist, then only they are returned.  Unlike &lt;code&gt;formatStatements&lt;/code&gt; from [[:w:ru:Модуль:Wikidata]], &lt;code&gt;getValue&lt;/code&gt; does not yet pick up any references (see [[:w:en:Module talk:Wikidata/Archive 1#Why the references from Wikidata get dropped?]]).
* &lt;code&gt;getRawValue&lt;/code&gt;: Returns non-linked property values and numbers with the thousand separator. It also returns the plain numeric value of a property which is a quantity. All other values will be output similar to &lt;code&gt;&lt;nowiki&gt;{{#property:}}&lt;/nowiki&gt;&lt;/code&gt;, including normal values, even if preferred values exist.
* &lt;code&gt;getDateValue&lt;/code&gt;: Special method to return formatted dates. The default date format is [day month year]; an optional third parameter can be added for other date formats: for [month day, year], add "mdy"; for [month year], add "my"; for year only, add "y". A further optional parameter, which must be fourth, such as "BCE", may be added to override the default "BC" that is suffixed for dates BC.
* &lt;code&gt;getImages&lt;/code&gt;: Returns all of the images of a particular property, e.g. image (P18), Gene Atlas Image (P692), etc. Parameters are &lt;code&gt;| propertyID | value / FETCH_WIKIDATA / nil | separator (default=space) | size (default=220px)&lt;/code&gt;. The returned text will be in the format &lt;nowiki&gt;[[File:Filename | size]]&lt;/nowiki&gt; for each image with a selectable size and separator (which may be html). If a property is supplied that is not of type "commonsMedia", it will return empty text.
* &lt;code&gt;getUnits&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the name of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".
* &lt;code&gt;getUnitID&lt;/code&gt; This takes a propertyID which is a quantity as the first parameter, and returns the QID of the units that the property is using on the current page. It returns empty if the property is not a quantity type or if the value is not set. The second parameter may be used to override fetching from Wikidata, if it is anything other than "FETCH_WIKIDATA".

Wikidata qualifier values (if present) can be retrieved by the following methods:
* &lt;code&gt;getQualifierValue&lt;/code&gt;: returns only wiki-linked values, if applicable.
* &lt;code&gt;getRawQualifierValue&lt;/code&gt;: returns non-linked values
* &lt;code&gt;getQualifierDateValue&lt;/code&gt;: returns formatted dates

If the property is not defined in Wikidata for the article that invokes this code, then an empty string ("") is returned.

Other methods:
* &lt;code&gt;pageId&lt;/code&gt;: returns the Wikidata id (Q…) of the current page or nothing if the page is not connected to Wikidata. To get the Wikidata id of another page, use [[Module:ResolveEntityId|&lt;nowiki&gt;{{#invoke:ResolveEntityId|&lt;/nowiki&gt;''page''&lt;nowiki&gt;}}&lt;/nowiki&gt;]].
* &lt;code&gt;getTAValue&lt;/code&gt;: gets the TA98 (Terminologia Anatomica first edition 1998) values for property P1323. It takes no parameters and constructs its output as a list, with each value linked to an external resource. This is an exemplar for writing calls that need to return external links.
* &lt;code&gt;ViewSomething&lt;/code&gt;: gets anything in the structured data, including labels, descriptions, references and interwiki links. See [https://en.wikipedia.org/w/index.php?title=Module_talk%3AWikidata&amp;diff=652812891&amp;oldid=650633541#Is_it_possible_to_get_value.28s.29_in_structured_tree_by_this_Module.3F]
* &lt;code&gt;getSiteLink&lt;/code&gt;: gets name of a page in given in argument wiki (like "enwikiquote" for English Wikiquote, "arwiki" for the Arabic Wikipedia).
* &lt;code&gt;Dump&lt;/code&gt;: &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|Dump|claims}}&lt;/nowiki&gt;&lt;/code&gt; spies the structured data. It uses the same arguments as ViewSomething. Try this with preview only to see results. That helps you a lot in developing Lua scripts that access the data. If used without arguments, it dumps everything including labels, descriptions, references and interwiki links. There is the wrapper template, see {{tl|Dump}}.
* &lt;code&gt;getImageLegend&lt;/code&gt;: returns an image legend (image is property P18; image legend is property P2096).
*: Call as &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata |getImageLegend | &lt;PARAMETER&gt; | lang=&lt;ISO-639code&gt; |id=&lt;QID&gt;}}&lt;/nowiki&gt;&lt;/code&gt;
*: Returns PARAMETER, unless it is equal to "FETCH_WIKIDATA", from Item QID (expensive call). If QID is omitted or blank, the current article is used (not an expensive call). If lang is omitted, it uses the local wiki language, otherwise it uses the provided ISO-639 language code.
*: The label is returned from the first image with 'preferred' rank; or from the first image with 'normal' rank if no image has preferred rank.
* &lt;code&gt;getValueShortName&lt;/code&gt;: returns the same data as getValue, but utilizes the property [[d:Property:P1813|short name]] as the label, if available. This allows for piped links to use a shorter label where preferred. If short name is not set on the item, the normal label is used.

=== Arbitrary Access ===
As of 16 September 2015, it is now possible to fetch data from other articles by using their QID.
The following call:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|&lt;QID&gt;|&lt;Property&gt;|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will do the same as getValue, but takes an extra parameter, which is the QID of the Wikidata item that you want to get the property value from.
For example:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValueFromID|{{Get QID|Richard Burton (actor) }}|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;
will fetch a list of the linked values for 'spouse' (P26) from redirect page title [[Richard Burton (actor) ]] (corresponding to Wikidata label {{Q|{{Get QID|Richard Burton (actor) }}}}) from anywhere in the English Wikipedia.

This means that testing environments may be set up in user space, but remember that these calls are classed as ''expensive'', so please use them as sparingly as possible.

== Parameters ==
* For the generalized case (getValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P19 for birthplace or P26 for spouse). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized unlinked case (getRawValue), two unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P21 for gender). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value.
* For the generalized date case (getDateValue), three unnamed parameters are supplied. The first is the ID of the property that is to be retrieved (e.g. P569 for date of birth). The second may be null, "FETCH_WIKIDATA", or any other string, which becomes the returned value. The third is the format that the date should be returned in, either dmy, mdy, my, or y.
Please note that lower-case parameters are no longer supported by the wikibase call: &lt;s&gt;p123&lt;/s&gt;, so please check that upper-case, like P123, is used if problems should arise.

== Usage ==
=== Example: spouse (P26) ===
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of spouse in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P26 (spouse) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hillary Clinton]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hillary Rodham Clinton]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example in Infobox template ===
Inside an infobox definition, it may be called like this:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|FETCH_WIKIDATA}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)

Optionally, it could be called as:
* &lt;code&gt;&lt;nowiki&gt;| data55 = {{#invoke:Wikidata|getValue|P26|{{{spouse|}}} }}&lt;/nowiki&gt;&lt;/code&gt;
which causes the infobox to:
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is set to be blank (as &lt;code&gt;"| spouse ="&lt;/code&gt;}
# not display spouse if the infobox parameter &lt;code&gt;|spouse&lt;/code&gt; is not supplied
# display the local infobox parameter &lt;code&gt;|spouse&lt;/code&gt; if it is supplied (e.g. &lt;code&gt;"|spouse = Hillary Rodham Clinton"&lt;/code&gt;)
# display the linked value(s) from Wikidata if the infobox parameter is locally set to FETCH_WIKIDATA

See [[Module:WikidataIB]] for a modification that allows fields, on a per article basis, to be blacklisted so that they never display. It also allows the editor to specify, on a per article basis, which fields may be automatically fetched from Wikidata when local parameter is supplied; the default is none, allowing an infobox to be modified to accept Wikidata without any change in the articles using the infobox until the functionality is enabled in the article.

=== Example:birth place ===
This works in just the same way as the calls above:
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|}}&lt;/nowiki&gt;&lt;/code&gt; = returns nothing, so suppresses the display of birth place in an infobox
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt; = returns the linked value(s) of property P19 (place of birth) stored in wikidata for the corresponding article (e.g. for article [[Bill Clinton]], it returns [[Hope, Arkansas]])
* &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getValue|P19|[[Hope, Arkansas|Hope]]}}&lt;/nowiki&gt;&lt;/code&gt; = returns [[Hope, Arkansas|Hope]], allowing an infobox to use a local value rather than the value stored in Wikidata.

=== Example:gender ===
We don't want the returned value linked, so use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example:date of birth ===
If we want the date of birth in dmy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want the date of birth in mdy format, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth, we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}&lt;/nowiki&gt;&lt;/code&gt;

If we want a year of birth that may be BC, but should read "BCE", we use:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to Wikidata item ===
Use the following code to just retrieve the Q-ID:
* - &lt;code&gt;&lt;nowiki&gt;{{#invoke:Wikidata|pageId}}&lt;/nowiki&gt;&lt;/code&gt;

Linking to Wikidata used the usual Wiki markup:
* - &lt;code&gt;&lt;nowiki&gt;[[d:{{#invoke:Wikidata|pageId}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;

=== Example: Linking to another wiki page ===
Use code like this to link to another wiki. English Wikivoyage in the example:
* - &lt;code&gt;&lt;nowiki&gt;[[voy:{{#invoke:Wikidata|getSiteLink|enwikivoyage}}|Name of Link]]&lt;/nowiki&gt;&lt;/code&gt;


== Testing ==
=== Testing spouse ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P26|}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|[[Hillary Rodham Clinton]]}}
&lt;/syntaxhighlight&gt;

In [[Bill Clinton]] you should get:
* -
* - [[Hillary Clinton]]
* - [[Hillary Rodham Clinton]]


In [[Barack Obama]] you should get:
* -
* - [[Michelle Obama]]
* - [[Hillary Rodham Clinton]]


In [[Richard Burton]] you should get:
* -
* - [[Elizabeth Taylor]], [[Sally Burton]], [[Sybil Christopher]], [[Suzy Miller]], [[Elizabeth Taylor]]
* - [[Hillary Rodham Clinton]]


In [[Franz Kafka]] you should get:
* -
* -
* - [[Hillary Rodham Clinton]]

=== Testing birthplace ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|}}
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P19|[[Newport]]}}
&lt;/syntaxhighlight&gt;
Try [[William Ellery]] and check that the Wikidata call correctly disambiguates.

=== Testing getValue, getRawValue and getDateValue ===
Copy and paste the following into any article and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getValue|P19|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P26|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P27|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getValue|P140|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getRawValue|P21|FETCH_WIKIDATA}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|dmy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|mdy}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
&lt;/syntaxhighlight&gt;
This should return the Wikidata values for birthplace, spouse, citizenship, religion, gender, date of birth (twice) and year of birth, if they exist. The dob is first in dmy format and then in mdy.

=== Testing dates BC/BCE ===
Copy and paste the following into a short section of article such as [[Horace #See also]] and preview it (please don't save!):
&lt;syntaxhighlight lang="wikitext"&gt;
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y}}
 * - {{#invoke:Wikidata|getDateValue|P569|FETCH_WIKIDATA|y|BCE}}
&lt;/syntaxhighlight&gt;
This should return 65 BC and 65 BCE, respectively.

==See also==
* [[:Module:WikidataIB]], for handling Wikidata in infoboxes

&lt;includeonly&gt;{{#ifeq:{{SUBPAGENAME}}|sandbox||
[[Category:Wikidata modules]]
[[Category:Modules using data from Wikidata]]
}}&lt;/includeonly&gt;</text>
      <sha1>963f882rkoxtjxcoycs5phr64zj7xoe</sha1>
    </revision>
  </page>
</mediawiki>
