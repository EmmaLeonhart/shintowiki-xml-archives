<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Module:CountryData</title>
    <ns>828</ns>
    <id>14244</id>
    <revision>
      <id>1155616</id>
      <timestamp>2015-08-21T09:25:59Z</timestamp>
      <contributor>
        <username>wikipedia&gt;SiBr4</username>
        <id>0</id>
      </contributor>
      <comment>Moved from [[Module:Sandbox/SiBr4|my sandbox]]</comment>
      <origin>1155616</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1507" sha1="fbavu5z1vuuppm4ikzlaew244y1mog3" xml:space="preserve">function gettable(frame,country,params)
--Returns the parameters of a country data template as a Lua table
  --If not a valid data template, return empty table
  if string.find(frame:preprocess("{{Country data "..country.."}}"),"^%{%{ *%{%{%{1")
  then
    --Params to be passed into data template
    params = params or {}
    --Transclude template without first parameter for string list of params
    local s = frame:expandTemplate({title="Country data "..country;args=params})
    --Replace parameter delimiters with arbitrary control characters
    --to avoid clashes if param values contain equals/pipe signs
    s = string.gsub(s,"|([^|=]-)=","\1\1%1\2")
    s = string.gsub(s,"}}%s*$","\1")
    --Loop over string and add params to table
    local part = {}
    for par in string.gmatch(s,"\1[^\1\2]-\2[^\1\2]-\1") do
      local k = string.match(par,"\1%s*(.-)%s*\2")
      local v = string.match(par,"\2%s*(.-)%s*\1")
      part[k] = v
    end
    return part
  else
  	return {}
  end
end

function p.getalias(frame)
--Returns a single parameter value from a data template
  local part = gettable(frame,frame.args[1])
  return tostring(part[frame.args[2]])
end

function p.gettemplate(frame)
--For testing, recreates the country data from the created Lua table
  --Get data table
  local data = gettable(frame,frame.args[1])
  --Concatenate fields into a template-like string
  local out = "{{ {{{1}}}"
  for k,v in pairs(data) do
    out = out.."\n| "..k.." = "..v
  end
  return out.."\n}}"
end</text>
      <sha1>fbavu5z1vuuppm4ikzlaew244y1mog3</sha1>
    </revision>
    <revision>
      <id>1155617</id>
      <parentid>1155616</parentid>
      <timestamp>2015-08-21T09:28:10Z</timestamp>
      <contributor>
        <username>wikipedia&gt;SiBr4</username>
        <id>0</id>
      </contributor>
      <origin>1155617</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1531" sha1="qi2tozn17y0sc4j0aknbyu0j89pl06l" xml:space="preserve">local p = {}

function gettable(frame,country,params)
--Returns the parameters of a country data template as a Lua table
  --If not a valid data template, return empty table
  if string.find(frame:preprocess("{{Country data "..country.."}}"),"^%{%{ *%{%{%{1")
  then
    --Params to be passed into data template
    params = params or {}
    --Transclude template without first parameter for string list of params
    local s = frame:expandTemplate({title="Country data "..country;args=params})
    --Replace parameter delimiters with arbitrary control characters
    --to avoid clashes if param values contain equals/pipe signs
    s = string.gsub(s,"|([^|=]-)=","\1\1%1\2")
    s = string.gsub(s,"}}%s*$","\1")
    --Loop over string and add params to table
    local part = {}
    for par in string.gmatch(s,"\1[^\1\2]-\2[^\1\2]-\1") do
      local k = string.match(par,"\1%s*(.-)%s*\2")
      local v = string.match(par,"\2%s*(.-)%s*\1")
      part[k] = v
    end
    return part
  else
  	return {}
  end
end

function p.getalias(frame)
--Returns a single parameter value from a data template
  local part = gettable(frame,frame.args[1])
  return tostring(part[frame.args[2]])
end

function p.gettemplate(frame)
--For testing, recreates the country data from the created Lua table
  --Get data table
  local data = gettable(frame,frame.args[1])
  --Concatenate fields into a template-like string
  local out = "{{ {{{1}}}"
  for k,v in pairs(data) do
    out = out.."\n| "..k.." = "..v
  end
  return out.."\n}}"
end

return p</text>
      <sha1>qi2tozn17y0sc4j0aknbyu0j89pl06l</sha1>
    </revision>
    <revision>
      <id>1155618</id>
      <parentid>1155617</parentid>
      <timestamp>2015-08-21T10:02:53Z</timestamp>
      <contributor>
        <username>wikipedia&gt;SiBr4</username>
        <id>0</id>
      </contributor>
      <minor/>
      <origin>1155618</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1537" sha1="53wn1ficp6cckel0lgqbsn6eatkdzni" xml:space="preserve">local p = {}

function p.gettable(frame,country,params)
--Returns the parameters of a country data template as a Lua table
  --If not a valid data template, return empty table
  if string.find(frame:preprocess("{{Country data "..country.."}}"),"^%{%{ *%{%{%{1")
  then
    --Params to be passed into data template
    params = params or {}
    --Transclude template without first parameter for string list of params
    local s = frame:expandTemplate({title="Country data "..country;args=params})
    --Replace parameter delimiters with arbitrary control characters
    --to avoid clashes if param values contain equals/pipe signs
    s = string.gsub(s,"|([^|=]-)=","\1\1%1\2")
    s = string.gsub(s,"}}%s*$","\1")
    --Loop over string and add params to table
    local part = {}
    for par in string.gmatch(s,"\1[^\1\2]-\2[^\1\2]-\1") do
      local k = string.match(par,"\1%s*(.-)%s*\2")
      local v = string.match(par,"\2%s*(.-)%s*\1")
      part[k] = v
    end
    return part
  else
  	return {}
  end
end

function p.getalias(frame)
--Returns a single parameter value from a data template
  local part = p.gettable(frame,frame.args[1])
  return tostring(part[frame.args[2]])
end

function p.gettemplate(frame)
--For testing, recreates the country data from the created Lua table
  --Get data table
  local data = p.gettable(frame,frame.args[1])
  --Concatenate fields into a template-like string
  local out = "{{ {{{1}}}"
  for k,v in pairs(data) do
    out = out.."\n| "..k.." = "..v
  end
  return out.."\n}}"
end

return p</text>
      <sha1>53wn1ficp6cckel0lgqbsn6eatkdzni</sha1>
    </revision>
    <revision>
      <id>1155619</id>
      <parentid>1155618</parentid>
      <timestamp>2015-09-02T13:55:47Z</timestamp>
      <contributor>
        <username>wikipedia&gt;SiBr4</username>
        <id>0</id>
      </contributor>
      <comment>Adding parameters</comment>
      <origin>1155619</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1729" sha1="j5wfboqv45oaebg709ghes8iesja8ec" xml:space="preserve">local p = {}

function p.gettable(frame,country,params)
--Returns the parameters of a country data template as a Lua table
  --If not a valid data template, return empty table
  if string.find(frame:preprocess("{{Country data "..country.."}}"),"^%{%{ *%{%{%{1")
  then
    --Params to be passed into data template
    params = params or {}
    --Transclude template without first parameter for string list of params
    local s = frame:expandTemplate({title="Country data "..country;args=params})
    --Replace parameter delimiters with arbitrary control characters
    --to avoid clashes if param values contain equals/pipe signs
    s = string.gsub(s,"|([^|=]-)=","\1\1%1\2")
    s = string.gsub(s,"}}%s*$","\1")
    --Loop over string and add params to table
    local part = {}
    for par in string.gmatch(s,"\1[^\1\2]-\2[^\1\2]-\1") do
      local k = string.match(par,"\1%s*(.-)%s*\2")
      local v = string.match(par,"\2%s*(.-)%s*\1")
      part[k] = v
    end
    return part
  else
  	return {}
  end
end

function p.getalias(frame)
--Returns a single parameter value from a data template
  local part = p.gettable(frame,frame.args[1])
  if frame.args.variant
    then return tostring(part[frame.args[2].."-"..frame.args.variant]
                         or part[frame.args[2]] or frame.args.def)
    else return tostring(part[frame.args[2]] or frame.args.def)
  end
end

function p.gettemplate(frame)
--For testing, recreates the country data from the created Lua table
  --Get data table
  local data = p.gettable(frame,frame.args[1])
  --Concatenate fields into a template-like string
  local out = "{{ {{{1}}}"
  for k,v in pairs(data) do
    out = out.."\n| "..k.." = "..v
  end
  return out.."\n}}"
end

return p</text>
      <sha1>j5wfboqv45oaebg709ghes8iesja8ec</sha1>
    </revision>
    <revision>
      <id>1155620</id>
      <parentid>1155619</parentid>
      <timestamp>2016-01-27T18:18:41Z</timestamp>
      <contributor>
        <username>wikipedia&gt;SiBr4</username>
        <id>0</id>
      </contributor>
      <comment>Don't pass through empty flag alias</comment>
      <origin>1155620</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1806" sha1="q8i3nzkw39throf30s78t84zmsee9kc" xml:space="preserve">local p = {}

function p.gettable(frame,country,params)
--Returns the parameters of a country data template as a Lua table
  --If not a valid data template, return empty table
  if string.find(frame:preprocess("{{Country data "..country.."}}"),"^%{%{ *%{%{%{1")
  then
    --Params to be passed into data template
    params = params or {}
    --Transclude template without first parameter for string list of params
    local s = frame:expandTemplate({title="Country data "..country;args=params})
    --Replace parameter delimiters with arbitrary control characters
    --to avoid clashes if param values contain equals/pipe signs
    s = string.gsub(s,"|([^|=]-)=","\1\1%1\2")
    s = string.gsub(s,"}}%s*$","\1")
    --Loop over string and add params to table
    local part = {}
    for par in string.gmatch(s,"\1[^\1\2]-\2[^\1\2]-\1") do
      local k = string.match(par,"\1%s*(.-)%s*\2")
      local v = string.match(par,"\2%s*(.-)%s*\1")
      if v and not (v=="" and string.find(k,"^flag alias")) then
        part[k] = v
      end
    end
    return part
  else
  	return {}
  end
end

function p.getalias(frame)
--Returns a single parameter value from a data template
  local part = p.gettable(frame,frame.args[1])
  if frame.args.variant
    then return tostring(part[frame.args[2].."-"..frame.args.variant]
                         or part[frame.args[2]] or frame.args.def)
    else return tostring(part[frame.args[2]] or frame.args.def)
  end
end

function p.gettemplate(frame)
--For testing, recreates the country data from the created Lua table
  --Get data table
  local data = p.gettable(frame,frame.args[1])
  --Concatenate fields into a template-like string
  local out = "{{ {{{1}}}"
  for k,v in pairs(data) do
    out = out.."\n| "..k.." = "..v
  end
  return out.."\n}}"
end

return p</text>
      <sha1>q8i3nzkw39throf30s78t84zmsee9kc</sha1>
    </revision>
    <revision>
      <id>1155621</id>
      <parentid>1155620</parentid>
      <timestamp>2016-03-28T16:56:12Z</timestamp>
      <contributor>
        <username>wikipedia&gt;SiBr4</username>
        <id>0</id>
      </contributor>
      <comment>Use pcall to have to load the data template only once instead of twice</comment>
      <origin>1155621</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1726" sha1="1gswcab4weafii5kextiyg227xrlnqg" xml:space="preserve">local p = {}

function getcontents(frame,country,params)
  return frame:expandTemplate({title="Country data "..country;args=params})
end

function p.gettable(frame,country,params)
--Returns the parameters of a country data template as a Lua table
  --If not a valid data template, return empty table
  local bool, s = pcall(getcontents,frame,country,params or {})
  if bool and string.find(s,"^%{%{ *%{%{%{1")
  then
    --Replace parameter delimiters with arbitrary control characters
    --to avoid clashes if param values contain equals/pipe signs
    s = string.gsub(s,"|([^|=]-)=","\1\1%1\2")
    s = string.gsub(s,"}}%s*$","\1")
    --Loop over string and add params to table
    local part = {}
    for par in string.gmatch(s,"\1[^\1\2]-\2[^\1\2]-\1") do
      local k = string.match(par,"\1%s*(.-)%s*\2")
      local v = string.match(par,"\2%s*(.-)%s*\1")
      if v and not (v=="" and string.find(k,"^flag alias")) then
        part[k] = v
      end
    end
    return part
  else
  	return {}
  end
end

function p.getalias(frame)
--Returns a single parameter value from a data template
  local part = p.gettable(frame,frame.args[1])
  if frame.args.variant
    then return tostring(part[frame.args[2].."-"..frame.args.variant]
                         or part[frame.args[2]] or frame.args.def)
    else return tostring(part[frame.args[2]] or frame.args.def)
  end
end

function p.gettemplate(frame)
--For testing, recreates the country data from the created Lua table
  --Get data table
  local data = p.gettable(frame,frame.args[1])
  --Concatenate fields into a template-like string
  local out = "{{ {{{1}}}"
  for k,v in pairs(data) do
    out = out.."\n| "..k.." = "..v
  end
  return out.."\n}}"
end

return p</text>
      <sha1>1gswcab4weafii5kextiyg227xrlnqg</sha1>
    </revision>
    <revision>
      <id>1155622</id>
      <parentid>1155621</parentid>
      <timestamp>2017-03-14T19:57:17Z</timestamp>
      <contributor>
        <username>wikipedia&gt;SiBr4</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>Global to local function (see my talk)</comment>
      <origin>1155622</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1732" sha1="eo183jfek1xef56bcgmwc64t822nstp" xml:space="preserve">local p = {}

local function getcontents(frame,country,params)
  return frame:expandTemplate({title="Country data "..country;args=params})
end

function p.gettable(frame,country,params)
--Returns the parameters of a country data template as a Lua table
  --If not a valid data template, return empty table
  local bool, s = pcall(getcontents,frame,country,params or {})
  if bool and string.find(s,"^%{%{ *%{%{%{1")
  then
    --Replace parameter delimiters with arbitrary control characters
    --to avoid clashes if param values contain equals/pipe signs
    s = string.gsub(s,"|([^|=]-)=","\1\1%1\2")
    s = string.gsub(s,"}}%s*$","\1")
    --Loop over string and add params to table
    local part = {}
    for par in string.gmatch(s,"\1[^\1\2]-\2[^\1\2]-\1") do
      local k = string.match(par,"\1%s*(.-)%s*\2")
      local v = string.match(par,"\2%s*(.-)%s*\1")
      if v and not (v=="" and string.find(k,"^flag alias")) then
        part[k] = v
      end
    end
    return part
  else
  	return {}
  end
end

function p.getalias(frame)
--Returns a single parameter value from a data template
  local part = p.gettable(frame,frame.args[1])
  if frame.args.variant
    then return tostring(part[frame.args[2].."-"..frame.args.variant]
                         or part[frame.args[2]] or frame.args.def)
    else return tostring(part[frame.args[2]] or frame.args.def)
  end
end

function p.gettemplate(frame)
--For testing, recreates the country data from the created Lua table
  --Get data table
  local data = p.gettable(frame,frame.args[1])
  --Concatenate fields into a template-like string
  local out = "{{ {{{1}}}"
  for k,v in pairs(data) do
    out = out.."\n| "..k.." = "..v
  end
  return out.."\n}}"
end

return p</text>
      <sha1>eo183jfek1xef56bcgmwc64t822nstp</sha1>
    </revision>
    <revision>
      <id>1155623</id>
      <parentid>1155622</parentid>
      <timestamp>2017-03-21T16:42:07Z</timestamp>
      <contributor>
        <username>Ferret</username>
        <id>153</id>
      </contributor>
      <minor/>
      <comment>Protected "[[Module:CountryData]]": [[WP:High-risk templates|Highly visible template]] ([Edit=Require template editor access] (indefinite) [Move=Require template editor access] (indefinite))</comment>
      <origin>1155622</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1732" sha1="eo183jfek1xef56bcgmwc64t822nstp" xml:space="preserve">local p = {}

local function getcontents(frame,country,params)
  return frame:expandTemplate({title="Country data "..country;args=params})
end

function p.gettable(frame,country,params)
--Returns the parameters of a country data template as a Lua table
  --If not a valid data template, return empty table
  local bool, s = pcall(getcontents,frame,country,params or {})
  if bool and string.find(s,"^%{%{ *%{%{%{1")
  then
    --Replace parameter delimiters with arbitrary control characters
    --to avoid clashes if param values contain equals/pipe signs
    s = string.gsub(s,"|([^|=]-)=","\1\1%1\2")
    s = string.gsub(s,"}}%s*$","\1")
    --Loop over string and add params to table
    local part = {}
    for par in string.gmatch(s,"\1[^\1\2]-\2[^\1\2]-\1") do
      local k = string.match(par,"\1%s*(.-)%s*\2")
      local v = string.match(par,"\2%s*(.-)%s*\1")
      if v and not (v=="" and string.find(k,"^flag alias")) then
        part[k] = v
      end
    end
    return part
  else
  	return {}
  end
end

function p.getalias(frame)
--Returns a single parameter value from a data template
  local part = p.gettable(frame,frame.args[1])
  if frame.args.variant
    then return tostring(part[frame.args[2].."-"..frame.args.variant]
                         or part[frame.args[2]] or frame.args.def)
    else return tostring(part[frame.args[2]] or frame.args.def)
  end
end

function p.gettemplate(frame)
--For testing, recreates the country data from the created Lua table
  --Get data table
  local data = p.gettable(frame,frame.args[1])
  --Concatenate fields into a template-like string
  local out = "{{ {{{1}}}"
  for k,v in pairs(data) do
    out = out.."\n| "..k.." = "..v
  end
  return out.."\n}}"
end

return p</text>
      <sha1>eo183jfek1xef56bcgmwc64t822nstp</sha1>
    </revision>
    <revision>
      <id>1155624</id>
      <parentid>1155623</parentid>
      <timestamp>2020-05-05T18:28:16Z</timestamp>
      <contributor>
        <username>wikipedia&gt;SiBr4</username>
        <id>0</id>
      </contributor>
      <comment>Fixing for data templates with safesubst:</comment>
      <origin>1155624</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1779" sha1="mwc9d6kypou82ahccibbeww5bldlva8" xml:space="preserve">local p = {}

local function getcontents(frame,country,params)
  return frame:expandTemplate({title="Country data "..country;args=params})
end

function p.gettable(frame,country,params)
--Returns the parameters of a country data template as a Lua table
  --If not a valid data template, return empty table
  local bool, s = pcall(getcontents,frame,country,params or {})
  if bool and (string.find(s,"^%{%{ *%{%{%{1") or string.find(s,"^%{%{safesubst: *%{%{%{1"))
  then
    --Replace parameter delimiters with arbitrary control characters
    --to avoid clashes if param values contain equals/pipe signs
    s = string.gsub(s,"|([^|=]-)=","\1\1%1\2")
    s = string.gsub(s,"}}%s*$","\1")
    --Loop over string and add params to table
    local part = {}
    for par in string.gmatch(s,"\1[^\1\2]-\2[^\1\2]-\1") do
      local k = string.match(par,"\1%s*(.-)%s*\2")
      local v = string.match(par,"\2%s*(.-)%s*\1")
      if v and not (v=="" and string.find(k,"^flag alias")) then
        part[k] = v
      end
    end
    return part
  else
  	return {}
  end
end

function p.getalias(frame)
--Returns a single parameter value from a data template
  local part = p.gettable(frame,frame.args[1])
  if frame.args.variant
    then return tostring(part[frame.args[2].."-"..frame.args.variant]
                         or part[frame.args[2]] or frame.args.def)
    else return tostring(part[frame.args[2]] or frame.args.def)
  end
end

function p.gettemplate(frame)
--For testing, recreates the country data from the created Lua table
  --Get data table
  local data = p.gettable(frame,frame.args[1])
  --Concatenate fields into a template-like string
  local out = "{{ {{{1}}}"
  for k,v in pairs(data) do
    out = out.."\n| "..k.." = "..v
  end
  return out.."\n}}"
end

return p</text>
      <sha1>mwc9d6kypou82ahccibbeww5bldlva8</sha1>
    </revision>
    <revision>
      <id>1155612</id>
      <timestamp>2021-05-05T20:40:05Z</timestamp>
      <contributor>
        <username>wikipedia&gt;GKFX</username>
        <id>0</id>
      </contributor>
      <comment>Add optional caching layer for most-used templates via loadData. Reduces rendering time as tested on [[List_of_twin_towns_and_sister_cities_in_Germany]]</comment>
      <origin>1155612</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2330" sha1="cceq9gf8ovsa0t1xy826rbloh46yv3o" xml:space="preserve">local p = {}
local mostUsed = mw.loadData('Module:CountryData/summary')

local function getcontents(frame,country,params)
  return frame:expandTemplate({title="Country data "..country;args=params})
end

function p.getcachedtable(frame, country, params)
	country = mostUsed.redirects[country] or country
	if params and next(params) then return p.gettable(frame, country, params) end
	-- Uses mw.loadData to cache data for the most-used templates
	if mostUsed.pages[country] then
		local cache = mw.loadData('Module:CountryData/cache' .. mostUsed.pages[country])
		if cache.data[country] then return cache.data[country] end
	end
	-- if not in cache
	return p.gettable(frame, country, params)
end

function p.gettable(frame,country,params)
--Returns the parameters of a country data template as a Lua table
  --If not a valid data template, return empty table
  local bool, s = pcall(getcontents,frame,country,params or {})
  if bool and (string.find(s,"^%{%{ *%{%{%{1") or string.find(s,"^%{%{safesubst: *%{%{%{1"))
  then
    --Replace parameter delimiters with arbitrary control characters
    --to avoid clashes if param values contain equals/pipe signs
    s = string.gsub(s,"|([^|=]-)=","\1\1%1\2")
    s = string.gsub(s,"}}%s*$","\1")
    --Loop over string and add params to table
    local part = {}
    for par in string.gmatch(s,"\1[^\1\2]-\2[^\1\2]-\1") do
      local k = string.match(par,"\1%s*(.-)%s*\2")
      local v = string.match(par,"\2%s*(.-)%s*\1")
      if v and not (v=="" and string.find(k,"^flag alias")) then
        part[k] = v
      end
    end
    return part
  else
  	return {}
  end
end

function p.getalias(frame)
--Returns a single parameter value from a data template
  local part = p.gettable(frame,frame.args[1])
  if frame.args.variant
    then return tostring(part[frame.args[2].."-"..frame.args.variant]
                         or part[frame.args[2]] or frame.args.def)
    else return tostring(part[frame.args[2]] or frame.args.def)
  end
end

function p.gettemplate(frame)
--For testing, recreates the country data from the created Lua table
  --Get data table
  local data = p.gettable(frame,frame.args[1])
  --Concatenate fields into a template-like string
  local out = "{{ {{{1}}}"
  for k,v in pairs(data) do
    out = out.."\n| "..k.." = "..v
  end
  return out.."\n}}"
end

return p</text>
      <sha1>cceq9gf8ovsa0t1xy826rbloh46yv3o</sha1>
    </revision>
    <revision>
      <id>1155613</id>
      <parentid>1155612</parentid>
      <timestamp>2025-04-11T15:57:32Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported from [[:wikipedia:Module:CountryData]]: Mass importing wikipedia</comment>
      <origin>1155612</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2330" sha1="cceq9gf8ovsa0t1xy826rbloh46yv3o" xml:space="preserve">local p = {}
local mostUsed = mw.loadData('Module:CountryData/summary')

local function getcontents(frame,country,params)
  return frame:expandTemplate({title="Country data "..country;args=params})
end

function p.getcachedtable(frame, country, params)
	country = mostUsed.redirects[country] or country
	if params and next(params) then return p.gettable(frame, country, params) end
	-- Uses mw.loadData to cache data for the most-used templates
	if mostUsed.pages[country] then
		local cache = mw.loadData('Module:CountryData/cache' .. mostUsed.pages[country])
		if cache.data[country] then return cache.data[country] end
	end
	-- if not in cache
	return p.gettable(frame, country, params)
end

function p.gettable(frame,country,params)
--Returns the parameters of a country data template as a Lua table
  --If not a valid data template, return empty table
  local bool, s = pcall(getcontents,frame,country,params or {})
  if bool and (string.find(s,"^%{%{ *%{%{%{1") or string.find(s,"^%{%{safesubst: *%{%{%{1"))
  then
    --Replace parameter delimiters with arbitrary control characters
    --to avoid clashes if param values contain equals/pipe signs
    s = string.gsub(s,"|([^|=]-)=","\1\1%1\2")
    s = string.gsub(s,"}}%s*$","\1")
    --Loop over string and add params to table
    local part = {}
    for par in string.gmatch(s,"\1[^\1\2]-\2[^\1\2]-\1") do
      local k = string.match(par,"\1%s*(.-)%s*\2")
      local v = string.match(par,"\2%s*(.-)%s*\1")
      if v and not (v=="" and string.find(k,"^flag alias")) then
        part[k] = v
      end
    end
    return part
  else
  	return {}
  end
end

function p.getalias(frame)
--Returns a single parameter value from a data template
  local part = p.gettable(frame,frame.args[1])
  if frame.args.variant
    then return tostring(part[frame.args[2].."-"..frame.args.variant]
                         or part[frame.args[2]] or frame.args.def)
    else return tostring(part[frame.args[2]] or frame.args.def)
  end
end

function p.gettemplate(frame)
--For testing, recreates the country data from the created Lua table
  --Get data table
  local data = p.gettable(frame,frame.args[1])
  --Concatenate fields into a template-like string
  local out = "{{ {{{1}}}"
  for k,v in pairs(data) do
    out = out.."\n| "..k.." = "..v
  end
  return out.."\n}}"
end

return p</text>
      <sha1>cceq9gf8ovsa0t1xy826rbloh46yv3o</sha1>
    </revision>
    <revision>
      <id>1155614</id>
      <parentid>1155613</parentid>
      <timestamp>2025-04-25T22:09:12Z</timestamp>
      <contributor>
        <username>en&gt;GKFX</username>
        <id>0</id>
      </contributor>
      <comment>Add optional caching layer for most-used templates via loadData. Reduces rendering time as tested on [[List_of_twin_towns_and_sister_cities_in_Germany]]</comment>
      <origin>1155612</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2330" sha1="cceq9gf8ovsa0t1xy826rbloh46yv3o" xml:space="preserve">local p = {}
local mostUsed = mw.loadData('Module:CountryData/summary')

local function getcontents(frame,country,params)
  return frame:expandTemplate({title="Country data "..country;args=params})
end

function p.getcachedtable(frame, country, params)
	country = mostUsed.redirects[country] or country
	if params and next(params) then return p.gettable(frame, country, params) end
	-- Uses mw.loadData to cache data for the most-used templates
	if mostUsed.pages[country] then
		local cache = mw.loadData('Module:CountryData/cache' .. mostUsed.pages[country])
		if cache.data[country] then return cache.data[country] end
	end
	-- if not in cache
	return p.gettable(frame, country, params)
end

function p.gettable(frame,country,params)
--Returns the parameters of a country data template as a Lua table
  --If not a valid data template, return empty table
  local bool, s = pcall(getcontents,frame,country,params or {})
  if bool and (string.find(s,"^%{%{ *%{%{%{1") or string.find(s,"^%{%{safesubst: *%{%{%{1"))
  then
    --Replace parameter delimiters with arbitrary control characters
    --to avoid clashes if param values contain equals/pipe signs
    s = string.gsub(s,"|([^|=]-)=","\1\1%1\2")
    s = string.gsub(s,"}}%s*$","\1")
    --Loop over string and add params to table
    local part = {}
    for par in string.gmatch(s,"\1[^\1\2]-\2[^\1\2]-\1") do
      local k = string.match(par,"\1%s*(.-)%s*\2")
      local v = string.match(par,"\2%s*(.-)%s*\1")
      if v and not (v=="" and string.find(k,"^flag alias")) then
        part[k] = v
      end
    end
    return part
  else
  	return {}
  end
end

function p.getalias(frame)
--Returns a single parameter value from a data template
  local part = p.gettable(frame,frame.args[1])
  if frame.args.variant
    then return tostring(part[frame.args[2].."-"..frame.args.variant]
                         or part[frame.args[2]] or frame.args.def)
    else return tostring(part[frame.args[2]] or frame.args.def)
  end
end

function p.gettemplate(frame)
--For testing, recreates the country data from the created Lua table
  --Get data table
  local data = p.gettable(frame,frame.args[1])
  --Concatenate fields into a template-like string
  local out = "{{ {{{1}}}"
  for k,v in pairs(data) do
    out = out.."\n| "..k.." = "..v
  end
  return out.."\n}}"
end

return p</text>
      <sha1>cceq9gf8ovsa0t1xy826rbloh46yv3o</sha1>
    </revision>
    <revision>
      <id>1155615</id>
      <parentid>1155614</parentid>
      <timestamp>2025-04-25T22:09:13Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: fixing this template</comment>
      <origin>1155612</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2330" sha1="cceq9gf8ovsa0t1xy826rbloh46yv3o" xml:space="preserve">local p = {}
local mostUsed = mw.loadData('Module:CountryData/summary')

local function getcontents(frame,country,params)
  return frame:expandTemplate({title="Country data "..country;args=params})
end

function p.getcachedtable(frame, country, params)
	country = mostUsed.redirects[country] or country
	if params and next(params) then return p.gettable(frame, country, params) end
	-- Uses mw.loadData to cache data for the most-used templates
	if mostUsed.pages[country] then
		local cache = mw.loadData('Module:CountryData/cache' .. mostUsed.pages[country])
		if cache.data[country] then return cache.data[country] end
	end
	-- if not in cache
	return p.gettable(frame, country, params)
end

function p.gettable(frame,country,params)
--Returns the parameters of a country data template as a Lua table
  --If not a valid data template, return empty table
  local bool, s = pcall(getcontents,frame,country,params or {})
  if bool and (string.find(s,"^%{%{ *%{%{%{1") or string.find(s,"^%{%{safesubst: *%{%{%{1"))
  then
    --Replace parameter delimiters with arbitrary control characters
    --to avoid clashes if param values contain equals/pipe signs
    s = string.gsub(s,"|([^|=]-)=","\1\1%1\2")
    s = string.gsub(s,"}}%s*$","\1")
    --Loop over string and add params to table
    local part = {}
    for par in string.gmatch(s,"\1[^\1\2]-\2[^\1\2]-\1") do
      local k = string.match(par,"\1%s*(.-)%s*\2")
      local v = string.match(par,"\2%s*(.-)%s*\1")
      if v and not (v=="" and string.find(k,"^flag alias")) then
        part[k] = v
      end
    end
    return part
  else
  	return {}
  end
end

function p.getalias(frame)
--Returns a single parameter value from a data template
  local part = p.gettable(frame,frame.args[1])
  if frame.args.variant
    then return tostring(part[frame.args[2].."-"..frame.args.variant]
                         or part[frame.args[2]] or frame.args.def)
    else return tostring(part[frame.args[2]] or frame.args.def)
  end
end

function p.gettemplate(frame)
--For testing, recreates the country data from the created Lua table
  --Get data table
  local data = p.gettable(frame,frame.args[1])
  --Concatenate fields into a template-like string
  local out = "{{ {{{1}}}"
  for k,v in pairs(data) do
    out = out.."\n| "..k.." = "..v
  end
  return out.."\n}}"
end

return p</text>
      <sha1>cceq9gf8ovsa0t1xy826rbloh46yv3o</sha1>
    </revision>
    <revision>
      <id>14245</id>
      <timestamp>2025-05-15T00:29:58Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was 9 revisions imported from [[:wikipedia:Module:CountryData]]</comment>
      <origin>14245</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2330" sha1="cceq9gf8ovsa0t1xy826rbloh46yv3o" xml:space="preserve">local p = {}
local mostUsed = mw.loadData('Module:CountryData/summary')

local function getcontents(frame,country,params)
  return frame:expandTemplate({title="Country data "..country;args=params})
end

function p.getcachedtable(frame, country, params)
	country = mostUsed.redirects[country] or country
	if params and next(params) then return p.gettable(frame, country, params) end
	-- Uses mw.loadData to cache data for the most-used templates
	if mostUsed.pages[country] then
		local cache = mw.loadData('Module:CountryData/cache' .. mostUsed.pages[country])
		if cache.data[country] then return cache.data[country] end
	end
	-- if not in cache
	return p.gettable(frame, country, params)
end

function p.gettable(frame,country,params)
--Returns the parameters of a country data template as a Lua table
  --If not a valid data template, return empty table
  local bool, s = pcall(getcontents,frame,country,params or {})
  if bool and (string.find(s,"^%{%{ *%{%{%{1") or string.find(s,"^%{%{safesubst: *%{%{%{1"))
  then
    --Replace parameter delimiters with arbitrary control characters
    --to avoid clashes if param values contain equals/pipe signs
    s = string.gsub(s,"|([^|=]-)=","\1\1%1\2")
    s = string.gsub(s,"}}%s*$","\1")
    --Loop over string and add params to table
    local part = {}
    for par in string.gmatch(s,"\1[^\1\2]-\2[^\1\2]-\1") do
      local k = string.match(par,"\1%s*(.-)%s*\2")
      local v = string.match(par,"\2%s*(.-)%s*\1")
      if v and not (v=="" and string.find(k,"^flag alias")) then
        part[k] = v
      end
    end
    return part
  else
  	return {}
  end
end

function p.getalias(frame)
--Returns a single parameter value from a data template
  local part = p.gettable(frame,frame.args[1])
  if frame.args.variant
    then return tostring(part[frame.args[2].."-"..frame.args.variant]
                         or part[frame.args[2]] or frame.args.def)
    else return tostring(part[frame.args[2]] or frame.args.def)
  end
end

function p.gettemplate(frame)
--For testing, recreates the country data from the created Lua table
  --Get data table
  local data = p.gettable(frame,frame.args[1])
  --Concatenate fields into a template-like string
  local out = "{{ {{{1}}}"
  for k,v in pairs(data) do
    out = out.."\n| "..k.." = "..v
  end
  return out.."\n}}"
end

return p</text>
      <sha1>cceq9gf8ovsa0t1xy826rbloh46yv3o</sha1>
    </revision>
  </page>
</mediawiki>
