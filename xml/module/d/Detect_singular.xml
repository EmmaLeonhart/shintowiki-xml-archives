<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Module:Detect singular</title>
    <ns>828</ns>
    <id>2423</id>
    <revision>
      <id>656984</id>
      <timestamp>2022-01-08T05:27:41Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>implement current state of [[Template:Detect singular]]</comment>
      <origin>656984</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1016" sha1="75glinnftxegjqwg2gewmnnpzdylzzc" xml:space="preserve">local p = {}
local getArgs = require('Module:GetArguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, plain)
end

local function plainCount(s, sub)
	local _, count = mw.ustring.gsub(s, sub, '')
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.nocomma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and plainFind(s, ',')
	local hasList = plainFind(s,'list')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and plainCount(s,'*') &gt; 1
	return not (hasComma or hasList or hasBreak or hasBullets)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>75glinnftxegjqwg2gewmnnpzdylzzc</sha1>
    </revision>
    <revision>
      <id>656985</id>
      <parentid>656984</parentid>
      <timestamp>2022-01-08T05:28:33Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>wrong name</comment>
      <origin>656985</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1013" sha1="4gz2az8blppkt9ni5uci4oetx7usdf9" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, plain)
end

local function plainCount(s, sub)
	local _, count = mw.ustring.gsub(s, sub, '')
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.nocomma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and plainFind(s, ',')
	local hasList = plainFind(s,'list')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and plainCount(s,'*') &gt; 1
	return not (hasComma or hasList or hasBreak or hasBullets)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>4gz2az8blppkt9ni5uci4oetx7usdf9</sha1>
    </revision>
    <revision>
      <id>656986</id>
      <parentid>656985</parentid>
      <timestamp>2022-01-08T05:30:04Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>handle empty input</comment>
      <origin>656986</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1090" sha1="kl9tnrm8giwiarq2zyxmg3mutxe5wcx" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, plain)
end

local function plainCount(s, sub)
	local _, count = mw.ustring.gsub(s, sub, '')
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.nocomma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and plainFind(s, ',')
	local hasList = plainFind(s,'list')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and plainCount(s,'*') &gt; 1
	return not (hasComma or hasList or hasBreak or hasBullets)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>kl9tnrm8giwiarq2zyxmg3mutxe5wcx</sha1>
    </revision>
    <revision>
      <id>656987</id>
      <parentid>656986</parentid>
      <timestamp>2022-01-08T05:37:46Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>oops</comment>
      <origin>656987</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1089" sha1="ov90k80biu1cbdinyqg0ukvb9phc4id" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function plainCount(s, sub)
	local _, count = mw.ustring.gsub(s, sub, '')
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.nocomma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and plainFind(s, ',')
	local hasList = plainFind(s,'list')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and plainCount(s,'*') &gt; 1
	return not (hasComma or hasList or hasBreak or hasBullets)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>ov90k80biu1cbdinyqg0ukvb9phc4id</sha1>
    </revision>
    <revision>
      <id>656988</id>
      <parentid>656987</parentid>
      <timestamp>2022-01-08T05:48:13Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>wrong name</comment>
      <origin>656988</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1090" sha1="pc4u1c0h6vukbeiwjmyz8hqd6lo6mpw" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function plainCount(s, sub)
	local _, count = mw.ustring.gsub(s, sub, '')
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and plainFind(s, ',')
	local hasList = plainFind(s,'list')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and plainCount(s,'*') &gt; 1
	return not (hasComma or hasList or hasBreak or hasBullets)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>pc4u1c0h6vukbeiwjmyz8hqd6lo6mpw</sha1>
    </revision>
    <revision>
      <id>656989</id>
      <parentid>656988</parentid>
      <timestamp>2022-01-08T05:51:58Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>check for isolated "list" and "and"</comment>
      <origin>656989</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1155" sha1="o4e1r4s5cszevlgez5vj22ol82xk666" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function plainCount(s, sub)
	local _, count = mw.ustring.gsub(s, sub, '')
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and plainFind(s, ',')
	local hasList = mw.ustring.find(s,'%Alist%A')
	local hasAnd = mw.ustring.find(s,'%Aand%A')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and plainCount(s,'*') &gt; 1
	return not (hasComma or hasList or hasAnd or hasBreak or hasBullets)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>o4e1r4s5cszevlgez5vj22ol82xk666</sha1>
    </revision>
    <revision>
      <id>656990</id>
      <parentid>656989</parentid>
      <timestamp>2022-01-08T05:55:42Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>comma should trigger when following a letter and before a non-letter</comment>
      <origin>656990</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1166" sha1="60ljc3t64x907r3l0l94af6c1lnf1hl" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function plainCount(s, sub)
	local _, count = mw.ustring.gsub(s, sub, '')
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and mw.ustring.find(s, '%a,%A') 
	local hasList = mw.ustring.find(s,'%Alist%A')
	local hasAnd = mw.ustring.find(s,'%Aand%A')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and plainCount(s,'*') &gt; 1
	return not (hasComma or hasList or hasAnd or hasBreak or hasBullets)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>60ljc3t64x907r3l0l94af6c1lnf1hl</sha1>
    </revision>
    <revision>
      <id>656991</id>
      <parentid>656990</parentid>
      <timestamp>2022-01-08T06:01:02Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>"list" should be either hlist, or an explicit &lt;li&gt; element</comment>
      <origin>656991</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1188" sha1="j9dpv6uildi4g2x7kzodv4njyly3pe8" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function plainCount(s, sub)
	local _, count = mw.ustring.gsub(s, sub, '')
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and mw.ustring.find(s, '%a,%A') 
	local hasList = plainFind(s,'hlist') or mw.ustring.find(s,'&lt;%s*li')
	local hasAnd = mw.ustring.find(s,'%Aand%A')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and plainCount(s,'*') &gt; 1
	return not (hasComma or hasList or hasAnd or hasBreak or hasBullets)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>j9dpv6uildi4g2x7kzodv4njyly3pe8</sha1>
    </revision>
    <revision>
      <id>656992</id>
      <parentid>656991</parentid>
      <timestamp>2022-01-08T06:03:15Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>treat all wikilinks as blank (i.e., singular)</comment>
      <origin>656992</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1248" sha1="23ynah6kq03d9eut3j8dkeija2zjcac" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function plainCount(s, sub)
	local _, count = mw.ustring.gsub(s, sub, '')
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.ustring.gsub(s,'%b[[]]','') -- delete all wikilinks
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and mw.ustring.find(s, '%a,%A') 
	local hasList = plainFind(s,'hlist') or mw.ustring.find(s,'&lt;%s*li')
	local hasAnd = mw.ustring.find(s,'%Aand%A')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and plainCount(s,'*') &gt; 1
	return not (hasComma or hasList or hasAnd or hasBreak or hasBullets)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>23ynah6kq03d9eut3j8dkeija2zjcac</sha1>
    </revision>
    <revision>
      <id>656993</id>
      <parentid>656992</parentid>
      <timestamp>2022-01-08T06:03:55Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>just use balanced bracket (html links will also get mashed out)</comment>
      <origin>656993</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1246" sha1="e4v7tlvbv6y1dhspt740de7xq6i13l4" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function plainCount(s, sub)
	local _, count = mw.ustring.gsub(s, sub, '')
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.ustring.gsub(s,'%b[]','') -- delete all wikilinks
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and mw.ustring.find(s, '%a,%A') 
	local hasList = plainFind(s,'hlist') or mw.ustring.find(s,'&lt;%s*li')
	local hasAnd = mw.ustring.find(s,'%Aand%A')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and plainCount(s,'*') &gt; 1
	return not (hasComma or hasList or hasAnd or hasBreak or hasBullets)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>e4v7tlvbv6y1dhspt740de7xq6i13l4</sha1>
    </revision>
    <revision>
      <id>656994</id>
      <parentid>656993</parentid>
      <timestamp>2022-01-08T06:05:37Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>more than one asterisk in a row should only count for one</comment>
      <origin>656994</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1252" sha1="swyp8x10s1by6m7bdu2s3290qcizuh2" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function plainCount(s, sub)
	local _, count = mw.ustring.gsub(s, sub, '')
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.ustring.gsub(s,'%b[]','') -- delete all wikilinks
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and mw.ustring.find(s, '%a,%A') 
	local hasList = plainFind(s,'hlist') or mw.ustring.find(s,'&lt;%s*li')
	local hasAnd = mw.ustring.find(s,'%Aand%A')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and mw.ustring.gsub(s,'*+') &gt; 1
	return not (hasComma or hasList or hasAnd or hasBreak or hasBullets)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>swyp8x10s1by6m7bdu2s3290qcizuh2</sha1>
    </revision>
    <revision>
      <id>656995</id>
      <parentid>656994</parentid>
      <timestamp>2022-01-08T06:08:03Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>fix gsub</comment>
      <origin>656995</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1156" sha1="sus71yt7ppuh9ev4dqlgmx7nv9u1ap8" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.ustring.gsub(s,'%b[]','') -- delete all wikilinks
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and mw.ustring.find(s, '%a,%A') 
	local hasList = plainFind(s,'hlist') or mw.ustring.find(s,'&lt;%s*li')
	local hasAnd = mw.ustring.find(s,'%Aand%A')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and mw.ustring.gsub(s,'*+','') &gt; 1
	return not (hasComma or hasList or hasAnd or hasBreak or hasBullets)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>sus71yt7ppuh9ev4dqlgmx7nv9u1ap8</sha1>
    </revision>
    <revision>
      <id>656996</id>
      <parentid>656995</parentid>
      <timestamp>2022-01-08T06:10:21Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>656996</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1270" sha1="966tribszftilkq6w5dj7e8c3ubd9po" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local count = 0
	_, count = mw.ustring.gsub(s, pattern, '')
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.ustring.gsub(s,'%b[]','') -- delete all wikilinks
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and mw.ustring.find(s, '%a,%A') 
	local hasList = plainFind(s,'hlist') or mw.ustring.find(s,'&lt;%s*li')
	local hasAnd = mw.ustring.find(s,'%Aand%A')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'*+') &gt; 1
	return not (hasComma or hasList or hasAnd or hasBreak or hasBullets)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>966tribszftilkq6w5dj7e8c3ubd9po</sha1>
    </revision>
    <revision>
      <id>656997</id>
      <parentid>656996</parentid>
      <timestamp>2022-01-08T06:14:53Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>escape asterisk</comment>
      <origin>656997</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1271" sha1="7ep3ew65ldwd4a71iryyess8yetlog8" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local count = 0
	_, count = mw.ustring.gsub(s, pattern, '')
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.ustring.gsub(s,'%b[]','') -- delete all wikilinks
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and mw.ustring.find(s, '%a,%A') 
	local hasList = plainFind(s,'hlist') or mw.ustring.find(s,'&lt;%s*li')
	local hasAnd = mw.ustring.find(s,'%Aand%A')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	return not (hasComma or hasList or hasAnd or hasBreak or hasBullets)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>7ep3ew65ldwd4a71iryyess8yetlog8</sha1>
    </revision>
    <revision>
      <id>656998</id>
      <parentid>656997</parentid>
      <timestamp>2022-01-08T06:19:10Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>check for multiple QIDs</comment>
      <origin>656998</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1372" sha1="7f828pjku8aluzpy9zfl3jugrn05l0e" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local dbg, count = mw.ustring.gsub(s, pattern, 'ASTERISK')
	mw.log(dbg)
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.ustring.gsub(s,'%b[]','') -- delete all wikilinks
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and mw.ustring.find(s, '%a,%A') 
	local hasList = plainFind(s,'hlist') or mw.ustring.find(s,'&lt;%s*li')
	local hasAnd = mw.ustring.find(s,'%Aand%A')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasList or hasAnd or hasBreak or hasBullets)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>7f828pjku8aluzpy9zfl3jugrn05l0e</sha1>
    </revision>
    <revision>
      <id>656999</id>
      <parentid>656998</parentid>
      <timestamp>2022-01-08T06:22:45Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>oops, forgot to return</comment>
      <origin>656999</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1388" sha1="h9n65t9p5jm6i1np28pyr4cwxitfabn" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local dbg, count = mw.ustring.gsub(s, pattern, 'ASTERISK')
	mw.log(dbg)
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.ustring.gsub(s,'%b[]','') -- delete all wikilinks
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and mw.ustring.find(s, '%a,%A') 
	local hasList = plainFind(s,'hlist') or mw.ustring.find(s,'&lt;%s*li')
	local hasAnd = mw.ustring.find(s,'%Aand%A')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasList or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>h9n65t9p5jm6i1np28pyr4cwxitfabn</sha1>
    </revision>
    <revision>
      <id>657000</id>
      <parentid>656999</parentid>
      <timestamp>2022-01-08T06:39:13Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>don't blank the wikilink, replace with string of uppercase</comment>
      <origin>657000</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1409" sha1="57rcorqkh2ocn142qfn24a3hvxkxr9p" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local dbg, count = mw.ustring.gsub(s, pattern, 'ASTERISK')
	mw.log(dbg)
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.ustring.gsub(s,'%b[]','WIKILINK') -- replace all wikilinks with string
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and mw.ustring.find(s, '%a,%A') 
	local hasList = plainFind(s,'hlist') or mw.ustring.find(s,'&lt;%s*li')
	local hasAnd = mw.ustring.find(s,'%Aand%A')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasList or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>57rcorqkh2ocn142qfn24a3hvxkxr9p</sha1>
    </revision>
    <revision>
      <id>657001</id>
      <parentid>657000</parentid>
      <timestamp>2022-01-08T06:43:17Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>change comma logic</comment>
      <origin>657001</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1409" sha1="5rre5b869x907dmeef1urqlhcmii0e4" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local dbg, count = mw.ustring.gsub(s, pattern, 'ASTERISK')
	mw.log(dbg)
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.ustring.gsub(s,'%b[]','WIKILINK') -- replace all wikilinks with string
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and mw.ustring.find(s, '%a,%s') 
	local hasList = plainFind(s,'hlist') or mw.ustring.find(s,'&lt;%s*li')
	local hasAnd = mw.ustring.find(s,'%Aand%A')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasList or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>5rre5b869x907dmeef1urqlhcmii0e4</sha1>
    </revision>
    <revision>
      <id>657002</id>
      <parentid>657001</parentid>
      <timestamp>2022-01-08T06:54:25Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>count the number of list elements</comment>
      <origin>657002</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1418" sha1="n60b0okttpvkzegmga0m1unfrjqadbf" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local dbg, count = mw.ustring.gsub(s, pattern, 'ASTERISK')
	mw.log(dbg)
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.ustring.gsub(s,'%b[]','WIKILINK') -- replace all wikilinks with string
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	local hasComma = checkComma and mw.ustring.find(s, '%a,%s') 
	local hasMultipleListElements = countMatches(s,'&lt;%s*li') &gt; 1
	local hasAnd = mw.ustring.find(s,'%Aand%A')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasMultipleListElements or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>n60b0okttpvkzegmga0m1unfrjqadbf</sha1>
    </revision>
    <revision>
      <id>657003</id>
      <parentid>657002</parentid>
      <timestamp>2022-01-08T07:07:52Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>an entry in a list should always be singular, also</comment>
      <origin>657003</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1545" sha1="fz32b3rp4reyvu74pr8eqis7579cnll" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local dbg, count = mw.ustring.gsub(s, pattern, 'ASTERISK')
	mw.log(dbg)
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- replace all wikilinks with fixed string
	s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	-- replace all list items with a fixed string
	s, numListItems = mw.ustring.gsub(s,'&lt;%s*li%s*&gt;.-&lt;%s*/li%s*&gt;','&lt;li&gt;LISTITEM&lt;/li&gt;')
	local hasComma = checkComma and mw.ustring.find(s, '%a,%s') 
	local hasMultipleListElements = numListItems &gt; 1
	local hasAnd = mw.ustring.find(s,'%Aand%A')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasMultipleListElements or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>fz32b3rp4reyvu74pr8eqis7579cnll</sha1>
    </revision>
    <revision>
      <id>657004</id>
      <parentid>657003</parentid>
      <timestamp>2022-01-08T07:13:47Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>simplify logic a bit</comment>
      <origin>657004</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1572" sha1="6k6w4d2a6p5f75er8if909jg1zi6lub" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local dbg, count = mw.ustring.gsub(s, pattern, 'ASTERISK')
	mw.log(dbg)
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	local hasComma = checkComma and mw.ustring.find(s, '%a,%s') 
	local hasAnd = mw.ustring.find(s,'%Aand%A')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>6k6w4d2a6p5f75er8if909jg1zi6lub</sha1>
    </revision>
    <revision>
      <id>657005</id>
      <parentid>657004</parentid>
      <timestamp>2022-01-08T07:23:14Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>more specific match on and</comment>
      <origin>657005</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1575" sha1="dyuftml7ydfefb9iizobf9fq56b53ju" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local dbg, count = mw.ustring.gsub(s, pattern, 'ASTERISK')
	mw.log(dbg)
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	local hasComma = checkComma and mw.ustring.find(s, '%a,%s') 
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>dyuftml7ydfefb9iizobf9fq56b53ju</sha1>
    </revision>
    <revision>
      <id>657006</id>
      <parentid>657005</parentid>
      <timestamp>2022-01-08T07:26:40Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>isolated commas can trigger plural also</comment>
      <origin>657006</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1579" sha1="1txtyl68ack8rxt39jlt17sehc028m8" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local dbg, count = mw.ustring.gsub(s, pattern, 'ASTERISK')
	mw.log(dbg)
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	local hasComma = checkComma and mw.ustring.find(s, '[%a%s],%s') 
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>1txtyl68ack8rxt39jlt17sehc028m8</sha1>
    </revision>
    <revision>
      <id>657007</id>
      <parentid>657006</parentid>
      <timestamp>2022-01-08T07:32:48Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>get rid of debugging</comment>
      <origin>657007</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1556" sha1="qanxjun5a5080yj0yckfpehbrykz0av" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	local hasComma = checkComma and mw.ustring.find(s, '[%a%s],%s') 
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>qanxjun5a5080yj0yckfpehbrykz0av</sha1>
    </revision>
    <revision>
      <id>657008</id>
      <parentid>657007</parentid>
      <timestamp>2022-01-08T08:01:14Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>allow tightly packed comma-separated lists</comment>
      <origin>657008</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1560" sha1="rbwvcpzzyf5j5ustuh01okd5znzc2ph" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	local hasComma = checkComma and mw.ustring.find(s, '[%a%s],[%a%s]') 
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>rbwvcpzzyf5j5ustuh01okd5znzc2ph</sha1>
    </revision>
    <revision>
      <id>657009</id>
      <parentid>657008</parentid>
      <timestamp>2022-01-08T08:56:39Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>args.bullets defaults to true</comment>
      <origin>657009</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1616" sha1="2bog4jdv86kt3zhx05efinfenb3b5h9" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

function p._main(args)
	local checkComma = not yesNo(args.no_comma,true)
	if args.bullets == nil then
		args.bullets = true
	end
	local checkBullets = yesNo(args.bullets,true)
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	local hasComma = checkComma and mw.ustring.find(s, '[%a%s],[%a%s]') 
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>2bog4jdv86kt3zhx05efinfenb3b5h9</sha1>
    </revision>
    <revision>
      <id>657010</id>
      <parentid>657009</parentid>
      <timestamp>2022-01-08T20:43:56Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>make it an option to rewrite the links</comment>
      <origin>657010</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1848" sha1="7ookjlza5960hz883j707m207ax2651" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

function p._main(origArgs)
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,bullets=true,ignore_links=true}) do
		if origArgs[key] then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkBullets = args.bullets
	local rewriteLinks = args.ignore_links
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	local hasComma = checkComma and mw.ustring.find(s, '[%a%s],[%a%s]') 
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>7ookjlza5960hz883j707m207ax2651</sha1>
    </revision>
    <revision>
      <id>657011</id>
      <parentid>657010</parentid>
      <timestamp>2022-01-08T20:46:55Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>oops</comment>
      <origin>657011</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1855" sha1="2o7vlx4701olf0lb5egs3zxh546d07r" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

function p._main(origArgs)
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,bullets=true,ignore_links=true}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkBullets = args.bullets
	local rewriteLinks = args.ignore_links
	local s = args[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	local hasComma = checkComma and mw.ustring.find(s, '[%a%s],[%a%s]') 
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>2o7vlx4701olf0lb5egs3zxh546d07r</sha1>
    </revision>
    <revision>
      <id>657012</id>
      <parentid>657011</parentid>
      <timestamp>2022-01-08T20:48:33Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>oops</comment>
      <origin>657012</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1859" sha1="3w09c64soqlamo77yp1alool2s2px7s" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

function p._main(origArgs)
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,bullets=true,ignore_links=true}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkBullets = args.bullets
	local rewriteLinks = args.ignore_links
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	local hasComma = checkComma and mw.ustring.find(s, '[%a%s],[%a%s]') 
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

return p</text>
      <sha1>3w09c64soqlamo77yp1alool2s2px7s</sha1>
    </revision>
    <revision>
      <id>657013</id>
      <parentid>657012</parentid>
      <timestamp>2022-01-08T21:09:15Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>implement wrapper</comment>
      <origin>657013</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2055" sha1="gpjzytk09hkjtpgcakhvnnzwm61b10h" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

function p._main(origArgs)
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,bullets=true,ignore_links=true}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkBullets = args.bullets
	local rewriteLinks = args.ignore_links
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	local hasComma = checkComma and mw.ustring.find(s, '[%a%s],[%a%s]') 
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	return args[2] and args[4] or p._main(args) and args[3] or args[4]
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args) or ""
end

return p</text>
      <sha1>gpjzytk09hkjtpgcakhvnnzwm61b10h</sha1>
    </revision>
    <revision>
      <id>657014</id>
      <parentid>657013</parentid>
      <timestamp>2022-01-08T21:28:18Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>oops, mishandled nil</comment>
      <origin>657014</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2130" sha1="sdmp7tn1wwjefy92wqhwv741sfddtnx" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

function p._main(origArgs)
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,bullets=true,ignore_links=true}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkBullets = args.bullets
	local rewriteLinks = args.ignore_links
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	local hasComma = checkComma and mw.ustring.find(s, '[%a%s],[%a%s]') 
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	local singularForm = args[3] or ""
	local pluralForm = args[4] or ""
	return args[2] and pluralForm or p._main(args) and singularForm or pluralForm
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>sdmp7tn1wwjefy92wqhwv741sfddtnx</sha1>
    </revision>
    <revision>
      <id>657015</id>
      <parentid>657014</parentid>
      <timestamp>2022-01-08T21:50:39Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>accept link argument</comment>
      <origin>657015</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2260" sha1="4xehyv6p3ockmc75hchar7vhsxnyn03" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

function p._main(origArgs)
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,bullets=true,ignore_links=true}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkBullets = args.bullets
	local rewriteLinks = args.ignore_links
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	local hasComma = checkComma and mw.ustring.find(s, '[%a%s],[%a%s]') 
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	local singularForm = args[3] or ""
	local pluralForm = args[4] or ""
	if args[5] then
		singularForm = '[['..args[5]..'|'..singularForm..']]'
		pluralForm = '[['..args[5]..'|'..pluralForm..']]'
	end
	return args[2] and pluralForm or p._main(args) and singularForm or pluralForm
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>4xehyv6p3ockmc75hchar7vhsxnyn03</sha1>
    </revision>
    <revision>
      <id>657016</id>
      <parentid>657015</parentid>
      <timestamp>2022-01-08T22:10:41Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>more accepting of commas</comment>
      <origin>657016</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2256" sha1="oexw60hyz1qm06ryh0ocqg3mhg5jt0k" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

function p._main(origArgs)
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,bullets=true,ignore_links=true}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkBullets = args.bullets
	local rewriteLinks = args.ignore_links
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	local hasComma = checkComma and mw.ustring.find(s, '[%D],[%D]') 
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	local singularForm = args[3] or ""
	local pluralForm = args[4] or ""
	if args[5] then
		singularForm = '[['..args[5]..'|'..singularForm..']]'
		pluralForm = '[['..args[5]..'|'..pluralForm..']]'
	end
	return args[2] and pluralForm or p._main(args) and singularForm or pluralForm
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>oexw60hyz1qm06ryh0ocqg3mhg5jt0k</sha1>
    </revision>
    <revision>
      <id>657017</id>
      <parentid>657016</parentid>
      <timestamp>2022-01-08T22:11:02Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>no need for sets</comment>
      <origin>657017</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2252" sha1="e6rm7p5avux9514yr6fl6avhdbffxa1" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

function p._main(origArgs)
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,bullets=true,ignore_links=true}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkBullets = args.bullets
	local rewriteLinks = args.ignore_links
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	local hasComma = checkComma and mw.ustring.find(s, '%D,%D') 
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	local singularForm = args[3] or ""
	local pluralForm = args[4] or ""
	if args[5] then
		singularForm = '[['..args[5]..'|'..singularForm..']]'
		pluralForm = '[['..args[5]..'|'..pluralForm..']]'
	end
	return args[2] and pluralForm or p._main(args) and singularForm or pluralForm
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>e6rm7p5avux9514yr6fl6avhdbffxa1</sha1>
    </revision>
    <revision>
      <id>657018</id>
      <parentid>657017</parentid>
      <timestamp>2022-01-08T22:26:09Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>make semi-colon work</comment>
      <origin>657018</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2285" sha1="gf0zlya2y2t4k8cdaw5312mjkw8yz2p" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

function p._main(origArgs)
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,bullets=true,ignore_links=true}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkBullets = args.bullets
	local rewriteLinks = args.ignore_links
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	local hasComma = checkComma and mw.ustring.find(s, '%D[,;]%D') -- semi-colon similar to comma
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = checkBullets and countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	local singularForm = args[3] or ""
	local pluralForm = args[4] or ""
	if args[5] then
		singularForm = '[['..args[5]..'|'..singularForm..']]'
		pluralForm = '[['..args[5]..'|'..pluralForm..']]'
	end
	return args[2] and pluralForm or p._main(args) and singularForm or pluralForm
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>gf0zlya2y2t4k8cdaw5312mjkw8yz2p</sha1>
    </revision>
    <revision>
      <id>657019</id>
      <parentid>657018</parentid>
      <timestamp>2022-01-08T23:39:50Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>always count bullets</comment>
      <origin>657019</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2220" sha1="ocle2g5qrg7jhu603xd4mnl7pi1qpyi" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

function p._main(origArgs)
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,ignore_links=true}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local rewriteLinks = args.ignore_links
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	local hasComma = checkComma and mw.ustring.find(s, '%D[,;]%D') -- semi-colon similar to comma
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	local singularForm = args[3] or ""
	local pluralForm = args[4] or ""
	if args[5] then
		singularForm = '[['..args[5]..'|'..singularForm..']]'
		pluralForm = '[['..args[5]..'|'..pluralForm..']]'
	end
	return args[2] and pluralForm or p._main(args) and singularForm or pluralForm
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>ocle2g5qrg7jhu603xd4mnl7pi1qpyi</sha1>
    </revision>
    <revision>
      <id>657020</id>
      <parentid>657019</parentid>
      <timestamp>2022-01-08T23:48:36Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>protect against bad wlink</comment>
      <origin>657020</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2241" sha1="1ged81xfdzpy8crgh65mzyumnq2ahm2" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

function p._main(origArgs)
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,ignore_links=true}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local rewriteLinks = args.ignore_links
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	local hasComma = checkComma and mw.ustring.find(s, '%D[,;]%D') -- semi-colon similar to comma
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	local singularForm = args[3] or ""
	local pluralForm = args[4] or ""
	if args[5] then
		link = tostring(args[5])
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
	end
	return args[2] and pluralForm or p._main(args) and singularForm or pluralForm
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>1ged81xfdzpy8crgh65mzyumnq2ahm2</sha1>
    </revision>
    <revision>
      <id>657021</id>
      <parentid>657020</parentid>
      <timestamp>2022-01-09T00:07:05Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>flip argument meaning</comment>
      <origin>657021</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2244" sha1="a997jrgrlp0um39s3djnkhx08wtqb1v" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

function p._main(origArgs)
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local rewriteLinks = not args.parse_links
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	local hasComma = checkComma and mw.ustring.find(s, '%D[,;]%D') -- semi-colon similar to comma
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	local singularForm = args[3] or ""
	local pluralForm = args[4] or ""
	if args[5] then
		link = tostring(args[5])
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
	end
	return args[2] and pluralForm or p._main(args) and singularForm or pluralForm
end

function p.main(frame)
	local args = getArgs(frame)
	if p._main(args) then
		return 1
	end
	return ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>a997jrgrlp0um39s3djnkhx08wtqb1v</sha1>
    </revision>
    <revision>
      <id>657022</id>
      <parentid>657021</parentid>
      <timestamp>2022-01-09T05:54:34Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>add comments</comment>
      <origin>657022</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2843" sha1="hze8w1tdzjz2cqxa67867rloq6ng25c" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
function p._main(origArgs)
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local rewriteLinks = not args.parse_links
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a plural
	local hasComma = checkComma and mw.ustring.find(s, '%D[,;]%D') -- semi-colon similar to comma
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	-- return bool: is it singular?
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	local singularForm = args[3] or ""
	local pluralForm = args[4] or ""
	if args[5] then
		link = tostring(args[5])
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
	end
	return args[2] and pluralForm or p._main(args) and singularForm or pluralForm
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if not
	if p._main(args) then
		return 1
	end
	return ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>hze8w1tdzjz2cqxa67867rloq6ng25c</sha1>
    </revision>
    <revision>
      <id>657023</id>
      <parentid>657022</parentid>
      <timestamp>2022-01-09T17:59:56Z</timestamp>
      <contributor>
        <username>wikipedia&gt;MusikBot II</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>Protected "[[Module:Detect singular]]": [[Wikipedia:High-risk templates|High-risk template or module]]: 18344 transclusions ([[User:MusikBot II/TemplateProtector|more info]]) ([Edit=Require template editor access] (indefinite) [Move=Require template editor access] (indefinite))</comment>
      <origin>657022</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2843" sha1="hze8w1tdzjz2cqxa67867rloq6ng25c" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
function p._main(origArgs)
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local rewriteLinks = not args.parse_links
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a plural
	local hasComma = checkComma and mw.ustring.find(s, '%D[,;]%D') -- semi-colon similar to comma
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	-- return bool: is it singular?
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	local singularForm = args[3] or ""
	local pluralForm = args[4] or ""
	if args[5] then
		link = tostring(args[5])
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
	end
	return args[2] and pluralForm or p._main(args) and singularForm or pluralForm
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if not
	if p._main(args) then
		return 1
	end
	return ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>hze8w1tdzjz2cqxa67867rloq6ng25c</sha1>
    </revision>
    <revision>
      <id>657024</id>
      <parentid>657023</parentid>
      <timestamp>2022-01-10T01:06:53Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>remove wikimarkup before performing last 4 plural tests</comment>
      <origin>657024</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2978" sha1="q7wle68s8zmwohahkqk38zd5fwzq1nz" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
function p._main(origArgs)
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local rewriteLinks = not args.parse_links
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasComma = checkComma and mw.ustring.find(s, '%D[,;]%D') -- semi-colon similar to comma
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	-- return bool: is it singular?
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	local singularForm = args[3] or ""
	local pluralForm = args[4] or ""
	if args[5] then
		link = tostring(args[5])
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
	end
	return args[2] and pluralForm or p._main(args) and singularForm or pluralForm
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if not
	if p._main(args) then
		return 1
	end
	return ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>q7wle68s8zmwohahkqk38zd5fwzq1nz</sha1>
    </revision>
    <revision>
      <id>657025</id>
      <parentid>657024</parentid>
      <timestamp>2022-01-10T01:59:53Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>check for "list of" in wikilink, if so, then plural</comment>
      <origin>657025</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3123" sha1="0alksn0pl70l0dvws1h1sd69hqnaoc7" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
function p._main(origArgs)
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local rewriteLinks = not args.parse_links
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasComma = checkComma and mw.ustring.find(s, '%D[,;]%D') -- semi-colon similar to comma
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	-- return bool: is it singular?
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	local singularForm = args[3] or ""
	local pluralForm = args[4] or ""
	if args[5] then
		link = tostring(args[5])
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
	end
	return args[2] and pluralForm or p._main(args) and singularForm or pluralForm
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if not
	if p._main(args) then
		return 1
	end
	return ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>0alksn0pl70l0dvws1h1sd69hqnaoc7</sha1>
    </revision>
    <revision>
      <id>657026</id>
      <parentid>657025</parentid>
      <timestamp>2022-01-10T07:20:54Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>protect against bad data types</comment>
      <origin>657026</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3225" sha1="lg3eyzjb8sa5hkwrc6mkiw8q4u9qa3a" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local rewriteLinks = not args.parse_links
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasComma = checkComma and mw.ustring.find(s, '%D[,;]%D') -- semi-colon similar to comma
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	-- return bool: is it singular?
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or ""
	local pluralForm = args[4] or ""
	if args[5] then
		link = tostring(args[5])
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
	end
	return args[2] and pluralForm or p._main(args) and singularForm or pluralForm
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if not
	if p._main(args) then
		return 1
	end
	return ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>lg3eyzjb8sa5hkwrc6mkiw8q4u9qa3a</sha1>
    </revision>
    <revision>
      <id>657027</id>
      <parentid>657026</parentid>
      <timestamp>2022-01-14T08:26:40Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>if no inputs given, return blank</comment>
      <origin>657027</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3342" sha1="cl4gm35frb6qdnsoktb6mpv6bpuvf9k" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local rewriteLinks = not args.parse_links
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasComma = checkComma and mw.ustring.find(s, '%D[,;]%D') -- semi-colon similar to comma
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	-- return bool: is it singular?
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or ""
	local pluralForm = args[4] or ""
	if args[5] then
		link = tostring(args[5])
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	return detect and singularForm or pluralForm
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if not
	if p._main(args) then
		return 1
	end
	return ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>cl4gm35frb6qdnsoktb6mpv6bpuvf9k</sha1>
    </revision>
    <revision>
      <id>657028</id>
      <parentid>657027</parentid>
      <timestamp>2022-01-14T08:51:24Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>fix detect return for blank input, also</comment>
      <origin>657028</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3400" sha1="ipo5cr3rh1vilcrr80op0fyt65giizt" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local rewriteLinks = not args.parse_links
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasComma = checkComma and mw.ustring.find(s, '%D[,;]%D') -- semi-colon similar to comma
	local hasAnd = mw.ustring.find(s,'[,%s]and%s')
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	-- return bool: is it singular?
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or ""
	local pluralForm = args[4] or ""
	if args[5] then
		link = tostring(args[5])
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	return detect and singularForm or pluralForm
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>ipo5cr3rh1vilcrr80op0fyt65giizt</sha1>
    </revision>
    <revision>
      <id>657029</id>
      <parentid>657028</parentid>
      <timestamp>2022-01-15T06:25:32Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>add any_comma and no_and flags</comment>
      <origin>657029</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3568" sha1="pi720b1yy5g5dx14xuhtwhdtrh2m27j" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,no_and=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and mw.ustring.find(s,'[,%s]and%s')
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	-- return bool: is it singular?
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or ""
	local pluralForm = args[4] or ""
	if args[5] then
		link = tostring(args[5])
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	return detect and singularForm or pluralForm
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>pi720b1yy5g5dx14xuhtwhdtrh2m27j</sha1>
    </revision>
    <revision>
      <id>657030</id>
      <parentid>657029</parentid>
      <timestamp>2022-01-28T07:30:24Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>implement named arguments: singular, plural, link</comment>
      <origin>657030</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3629" sha1="lxxs8rssncqd9ur4b0nz7jlldvq1nej" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,no_and=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return true
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return false
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return true
	end
	if numListItems &gt; 1 then
		return false
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return false
	end
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and mw.ustring.find(s,'[,%s]and%s')
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	-- return bool: is it singular?
	return not (hasComma or hasAnd or hasBreak or hasBullets or multipleQids)
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	return detect and singularForm or pluralForm
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>lxxs8rssncqd9ur4b0nz7jlldvq1nej</sha1>
    </revision>
    <revision>
      <id>657031</id>
      <parentid>657030</parentid>
      <timestamp>2022-09-07T02:17:35Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>modify pluralize to accept "likely" parameter (for situations where plurality is likely, but not certain)</comment>
      <origin>657031</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4249" sha1="01bfyjh1h297s4dafe2odt4n3ac52wc" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,no_and=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
	-- fix for trailing br tags passed through [[template:marriage]]
	-- Frietjes fix: should we use it? comment out for now
	-- s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and mw.ustring.find(s,'[,%s]and%s')
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or args.likely or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>01bfyjh1h297s4dafe2odt4n3ac52wc</sha1>
    </revision>
    <revision>
      <id>657032</id>
      <parentid>657031</parentid>
      <timestamp>2022-09-07T02:18:43Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>mistake in argument handling</comment>
      <origin>657032</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4234" sha1="6ofxeobzjpek2gnqp0uwrillqkwwer3" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,no_and=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'forcedetectsingular') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'forcedetectplural') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
	-- fix for trailing br tags passed through [[template:marriage]]
	-- Frietjes fix: should we use it? comment out for now
	-- s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and mw.ustring.find(s,'[,%s]and%s')
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>6ofxeobzjpek2gnqp0uwrillqkwwer3</sha1>
    </revision>
    <revision>
      <id>657033</id>
      <parentid>657032</parentid>
      <timestamp>2022-09-07T05:03:32Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>update force singular/plural detection strings</comment>
      <origin>657033</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4228" sha1="r8gh0gd6ensgtmbhtjwelnhxnl0o6ut" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,no_and=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
	-- fix for trailing br tags passed through [[template:marriage]]
	-- Frietjes fix: should we use it? comment out for now
	-- s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and mw.ustring.find(s,'[,%s]and%s')
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>r8gh0gd6ensgtmbhtjwelnhxnl0o6ut</sha1>
    </revision>
    <revision>
      <id>657034</id>
      <parentid>657033</parentid>
      <timestamp>2022-09-08T04:24:04Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>ignore br elements immediately before &lt;/div&gt; (per Frietjes)</comment>
      <origin>657034</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4169" sha1="b0vveqb10f1ycghifmiss07wkxg6abe" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,no_and=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and mw.ustring.find(s,'[,%s]and%s')
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>b0vveqb10f1ycghifmiss07wkxg6abe</sha1>
    </revision>
    <revision>
      <id>656977</id>
      <timestamp>2023-01-02T22:05:13Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>decode HTML entities</comment>
      <origin>656977</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4255" sha1="chvl518dso4lfzkxto06sdqio7vynp5" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,no_and=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.text.decode(s,true)  --- replace HTML entities (to avoid spurious semicolons)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and mw.ustring.find(s,'[,%s]and%s')
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>chvl518dso4lfzkxto06sdqio7vynp5</sha1>
    </revision>
    <revision>
      <id>656978</id>
      <parentid>656977</parentid>
      <timestamp>2024-12-02T23:07:31Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported from [[:wikipedia:Module:Detect_singular]]</comment>
      <origin>656977</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4255" sha1="chvl518dso4lfzkxto06sdqio7vynp5" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,no_and=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.text.decode(s,true)  --- replace HTML entities (to avoid spurious semicolons)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and mw.ustring.find(s,'[,%s]and%s')
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>chvl518dso4lfzkxto06sdqio7vynp5</sha1>
    </revision>
    <revision>
      <id>656979</id>
      <parentid>656978</parentid>
      <timestamp>2024-12-20T01:54:25Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Adding [[Category:pages from wikipedia]][[en:{{subst:PAGENAME}}]][[Category:pages from wikipedia]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>656979</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4348" sha1="e1ava26dg9r6dim5h4vcykx1kes3po6" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,no_and=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.text.decode(s,true)  --- replace HTML entities (to avoid spurious semicolons)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and mw.ustring.find(s,'[,%s]and%s')
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p

[[Category:pages from wikipedia]][[en:{{subst:PAGENAME}}]][[Category:pages from wikipedia]]</text>
      <sha1>e1ava26dg9r6dim5h4vcykx1kes3po6</sha1>
    </revision>
    <revision>
      <id>656980</id>
      <parentid>656979</parentid>
      <timestamp>2024-12-22T01:54:25Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Fixing Terrible editing mistake</comment>
      <origin>656980</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4257" sha1="cn65axa529lbpf1mpmahhboyegdmtux" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,no_and=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.text.decode(s,true)  --- replace HTML entities (to avoid spurious semicolons)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and mw.ustring.find(s,'[,%s]and%s')
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p

</text>
      <sha1>cn65axa529lbpf1mpmahhboyegdmtux</sha1>
    </revision>
    <revision>
      <id>656981</id>
      <parentid>656980</parentid>
      <timestamp>2024-12-23T02:34:52Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Fixing my edit mistake that put this category on numerous templates it did not belong on</comment>
      <origin>656980</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4257" sha1="cn65axa529lbpf1mpmahhboyegdmtux" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,no_and=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.text.decode(s,true)  --- replace HTML entities (to avoid spurious semicolons)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and mw.ustring.find(s,'[,%s]and%s')
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p

</text>
      <sha1>cn65axa529lbpf1mpmahhboyegdmtux</sha1>
    </revision>
    <revision>
      <id>657035</id>
      <parentid>656981</parentid>
      <timestamp>2025-03-05T07:06:22Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>when flag is set, parse number and determine plurality from that</comment>
      <origin>657035</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4518" sha1="tbxusdks736s295jx6akmw7kbid80kf" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,
                               no_and=false,parse_number=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
    local parseNumber = args.parse_number
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.text.decode(s,true)  --- replace HTML entities (to avoid spurious semicolons)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
    if parseNumber then
        local m = tonumber(mw.ustring.match(s,"^%s*(%d+)"))
        if m then
            return m &gt; 1 and plural or singular
        end
    end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and mw.ustring.find(s,'[,%s]and%s')
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>tbxusdks736s295jx6akmw7kbid80kf</sha1>
    </revision>
    <revision>
      <id>656982</id>
      <parentid>656981</parentid>
      <timestamp>2025-03-15T19:25:54Z</timestamp>
      <contributor>
        <username>en&gt;Unknown user</username>
        <id>0</id>
      </contributor>
      <comment>when flag is set, parse number and determine plurality from that</comment>
      <origin>656982</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4518" sha1="tbxusdks736s295jx6akmw7kbid80kf" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,
                               no_and=false,parse_number=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
    local parseNumber = args.parse_number
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.text.decode(s,true)  --- replace HTML entities (to avoid spurious semicolons)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
    if parseNumber then
        local m = tonumber(mw.ustring.match(s,"^%s*(%d+)"))
        if m then
            return m &gt; 1 and plural or singular
        end
    end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and mw.ustring.find(s,'[,%s]and%s')
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>tbxusdks736s295jx6akmw7kbid80kf</sha1>
    </revision>
    <revision>
      <id>656983</id>
      <parentid>656982</parentid>
      <timestamp>2025-03-15T19:25:55Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: I hope this actually fixes things</comment>
      <origin>656982</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4518" sha1="tbxusdks736s295jx6akmw7kbid80kf" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,
                               no_and=false,parse_number=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
    local parseNumber = args.parse_number
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.text.decode(s,true)  --- replace HTML entities (to avoid spurious semicolons)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
    if parseNumber then
        local m = tonumber(mw.ustring.match(s,"^%s*(%d+)"))
        if m then
            return m &gt; 1 and plural or singular
        end
    end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and mw.ustring.find(s,'[,%s]and%s')
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>tbxusdks736s295jx6akmw7kbid80kf</sha1>
    </revision>
    <revision>
      <id>2424</id>
      <timestamp>2025-03-25T05:23:38Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was 52 revisions imported from [[:wikipedia:Module:Detect_singular]]</comment>
      <origin>2424</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4518" sha1="tbxusdks736s295jx6akmw7kbid80kf" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')
local getPlain = require('Module:Text').Text().getPlain

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,
                               no_and=false,parse_number=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
    local parseNumber = args.parse_number
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.text.decode(s,true)  --- replace HTML entities (to avoid spurious semicolons)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
    if parseNumber then
        local m = tonumber(mw.ustring.match(s,"^%s*(%d+)"))
        if m then
            return m &gt; 1 and plural or singular
        end
    end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and mw.ustring.find(s,'[,%s]and%s')
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>tbxusdks736s295jx6akmw7kbid80kf</sha1>
    </revision>
    <revision>
      <id>3407334</id>
      <parentid>2424</parentid>
      <timestamp>2025-10-16T06:01:56Z</timestamp>
      <contributor>
        <username>en&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>handle ampersand the same as "and"</comment>
      <origin>3407334</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4874" sha1="ks2mlneszbgw2vr1nl2rhac5ypdp5y2" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
--   origArgs.any_comma: if true, allow any comma to cause likely plural, including ones between digits (default false)
--   origArgs.no_and: if false, use existence of "and" to detect plural (default false)
--   origArgs.parse_number: if true, parse number in argument: if 1, singular; if &gt;1, plural (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,
                               no_and=false,parse_number=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
    local parseNumber = args.parse_number
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.text.decode(s,true)  --- replace HTML entities (to avoid spurious semicolons)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
    if parseNumber then
        local m = tonumber(mw.ustring.match(s,"^%s*(%d+)"))
        if m then
            return m &gt; 1 and plural or singular
        end
    end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
    local getPlain = require('Module:Text').Text().getPlain
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and (mw.ustring.find(s,'[,%s]and%s') or mw.ustring.find(s,'&amp;',1,true))
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>ks2mlneszbgw2vr1nl2rhac5ypdp5y2</sha1>
    </revision>
    <revision>
      <id>219269</id>
      <parentid>2424</parentid>
      <timestamp>2026-02-16T18:54:02Z</timestamp>
      <contributor>
        <username>en&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>handle ampersand the same as "and"</comment>
      <origin>219269</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4874" sha1="ks2mlneszbgw2vr1nl2rhac5ypdp5y2" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
--   origArgs.any_comma: if true, allow any comma to cause likely plural, including ones between digits (default false)
--   origArgs.no_and: if false, use existence of "and" to detect plural (default false)
--   origArgs.parse_number: if true, parse number in argument: if 1, singular; if &gt;1, plural (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,
                               no_and=false,parse_number=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
    local parseNumber = args.parse_number
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.text.decode(s,true)  --- replace HTML entities (to avoid spurious semicolons)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
    if parseNumber then
        local m = tonumber(mw.ustring.match(s,"^%s*(%d+)"))
        if m then
            return m &gt; 1 and plural or singular
        end
    end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
    local getPlain = require('Module:Text').Text().getPlain
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and (mw.ustring.find(s,'[,%s]and%s') or mw.ustring.find(s,'&amp;',1,true))
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>ks2mlneszbgw2vr1nl2rhac5ypdp5y2</sha1>
    </revision>
    <revision>
      <id>219270</id>
      <parentid>219269</parentid>
      <timestamp>2026-02-16T18:54:02Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting this template since it seems broken in how it treats unknown parameters in [[Category:Wikidata generated shikinaisha pages]]</comment>
      <origin>219269</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4874" sha1="ks2mlneszbgw2vr1nl2rhac5ypdp5y2" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
--   origArgs.any_comma: if true, allow any comma to cause likely plural, including ones between digits (default false)
--   origArgs.no_and: if false, use existence of "and" to detect plural (default false)
--   origArgs.parse_number: if true, parse number in argument: if 1, singular; if &gt;1, plural (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,
                               no_and=false,parse_number=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
    local parseNumber = args.parse_number
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.text.decode(s,true)  --- replace HTML entities (to avoid spurious semicolons)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
    if parseNumber then
        local m = tonumber(mw.ustring.match(s,"^%s*(%d+)"))
        if m then
            return m &gt; 1 and plural or singular
        end
    end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
    local getPlain = require('Module:Text').Text().getPlain
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and (mw.ustring.find(s,'[,%s]and%s') or mw.ustring.find(s,'&amp;',1,true))
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>ks2mlneszbgw2vr1nl2rhac5ypdp5y2</sha1>
    </revision>
    <revision>
      <id>3407335</id>
      <parentid>219270</parentid>
      <timestamp>2026-03-12T20:21:24Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>219269</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4874" sha1="ks2mlneszbgw2vr1nl2rhac5ypdp5y2" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
--   origArgs.any_comma: if true, allow any comma to cause likely plural, including ones between digits (default false)
--   origArgs.no_and: if false, use existence of "and" to detect plural (default false)
--   origArgs.parse_number: if true, parse number in argument: if 1, singular; if &gt;1, plural (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,
                               no_and=false,parse_number=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
    local parseNumber = args.parse_number
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.text.decode(s,true)  --- replace HTML entities (to avoid spurious semicolons)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
    if parseNumber then
        local m = tonumber(mw.ustring.match(s,"^%s*(%d+)"))
        if m then
            return m &gt; 1 and plural or singular
        end
    end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
    local getPlain = require('Module:Text').Text().getPlain
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and (mw.ustring.find(s,'[,%s]and%s') or mw.ustring.find(s,'&amp;',1,true))
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>ks2mlneszbgw2vr1nl2rhac5ypdp5y2</sha1>
    </revision>
    <revision>
      <id>3408179</id>
      <parentid>3407335</parentid>
      <timestamp>2026-03-12T20:30:43Z</timestamp>
      <contributor>
        <username>en&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>handle ampersand the same as "and"</comment>
      <origin>219269</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4874" sha1="ks2mlneszbgw2vr1nl2rhac5ypdp5y2" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
--   origArgs.any_comma: if true, allow any comma to cause likely plural, including ones between digits (default false)
--   origArgs.no_and: if false, use existence of "and" to detect plural (default false)
--   origArgs.parse_number: if true, parse number in argument: if 1, singular; if &gt;1, plural (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,
                               no_and=false,parse_number=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
    local parseNumber = args.parse_number
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.text.decode(s,true)  --- replace HTML entities (to avoid spurious semicolons)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
    if parseNumber then
        local m = tonumber(mw.ustring.match(s,"^%s*(%d+)"))
        if m then
            return m &gt; 1 and plural or singular
        end
    end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
    local getPlain = require('Module:Text').Text().getPlain
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and (mw.ustring.find(s,'[,%s]and%s') or mw.ustring.find(s,'&amp;',1,true))
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>ks2mlneszbgw2vr1nl2rhac5ypdp5y2</sha1>
    </revision>
    <revision>
      <id>3408180</id>
      <parentid>3408179</parentid>
      <timestamp>2026-03-12T20:30:44Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>219269</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4874" sha1="ks2mlneszbgw2vr1nl2rhac5ypdp5y2" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
--   origArgs.any_comma: if true, allow any comma to cause likely plural, including ones between digits (default false)
--   origArgs.no_and: if false, use existence of "and" to detect plural (default false)
--   origArgs.parse_number: if true, parse number in argument: if 1, singular; if &gt;1, plural (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,
                               no_and=false,parse_number=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
    local parseNumber = args.parse_number
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.text.decode(s,true)  --- replace HTML entities (to avoid spurious semicolons)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
    if parseNumber then
        local m = tonumber(mw.ustring.match(s,"^%s*(%d+)"))
        if m then
            return m &gt; 1 and plural or singular
        end
    end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
    local getPlain = require('Module:Text').Text().getPlain
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and (mw.ustring.find(s,'[,%s]and%s') or mw.ustring.find(s,'&amp;',1,true))
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>ks2mlneszbgw2vr1nl2rhac5ypdp5y2</sha1>
    </revision>
    <revision>
      <id>3647007</id>
      <parentid>3408180</parentid>
      <timestamp>2026-04-24T16:34:15Z</timestamp>
      <contributor>
        <username>en&gt;Hike395</username>
        <id>0</id>
      </contributor>
      <comment>handle ampersand the same as "and"</comment>
      <origin>219269</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4874" sha1="ks2mlneszbgw2vr1nl2rhac5ypdp5y2" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
--   origArgs.any_comma: if true, allow any comma to cause likely plural, including ones between digits (default false)
--   origArgs.no_and: if false, use existence of "and" to detect plural (default false)
--   origArgs.parse_number: if true, parse number in argument: if 1, singular; if &gt;1, plural (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,
                               no_and=false,parse_number=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
    local parseNumber = args.parse_number
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.text.decode(s,true)  --- replace HTML entities (to avoid spurious semicolons)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
    if parseNumber then
        local m = tonumber(mw.ustring.match(s,"^%s*(%d+)"))
        if m then
            return m &gt; 1 and plural or singular
        end
    end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
    local getPlain = require('Module:Text').Text().getPlain
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and (mw.ustring.find(s,'[,%s]and%s') or mw.ustring.find(s,'&amp;',1,true))
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>ks2mlneszbgw2vr1nl2rhac5ypdp5y2</sha1>
    </revision>
    <revision>
      <id>3647008</id>
      <parentid>3647007</parentid>
      <timestamp>2026-04-24T16:34:15Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: en</comment>
      <origin>219269</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4874" sha1="ks2mlneszbgw2vr1nl2rhac5ypdp5y2" xml:space="preserve">local p = {}
local getArgs = require('Module:Arguments').getArgs
local yesNo = require('Module:Yesno')

-- function to determine whether "sub" occurs in "s"
local function plainFind(s, sub)
	return mw.ustring.find(s, sub, 1, true)
end

-- function to count the number of times "pattern" (a regex) occurs in "s"
local function countMatches(s, pattern)
	local _, count = mw.ustring.gsub(s, pattern, '')
	return count
end

local singular = 1
local likelyPlural = 2
local plural = 3

-- Determine whether a string is singular or plural (i.e., it represents one
-- item or many)
-- Arguments:
--   origArgs[1]: string to process
--   origArgs.no_comma:  if false, use commas to detect plural (default false)
--   origArgs.parse_links: if false, treat wikilinks as opaque singular objects (default false)
--   origArgs.any_comma: if true, allow any comma to cause likely plural, including ones between digits (default false)
--   origArgs.no_and: if false, use existence of "and" to detect plural (default false)
--   origArgs.parse_number: if true, parse number in argument: if 1, singular; if &gt;1, plural (default false)
-- Returns:
--   singular, likelyPlural, or plural (see constants above), or nil for completely unknown
function p._main(origArgs)
	origArgs = type(origArgs) == 'table' and origArgs or {}
	local args = {}
	-- canonicalize boolean arguments
	for key, default in pairs({no_comma=false,parse_links=false,any_comma=false,
                               no_and=false,parse_number=false}) do
		if origArgs[key] == nil then
			args[key] = default
		else
			args[key] = yesNo(origArgs[key],default)
		end
	end
	local checkComma = not args.no_comma
	local checkAnd = not args.no_and
	local rewriteLinks = not args.parse_links
	local anyComma = args.any_comma
    local parseNumber = args.parse_number
	local s = origArgs[1]  -- the input string
	if not s then
		return nil -- empty input returns nil
	end
	s = tostring(s)
	s = mw.text.decode(s,true)  --- replace HTML entities (to avoid spurious semicolons)
	if plainFind(s,'data-plural="0"') then -- magic data string to return true
		return singular
	end
	if plainFind(s,'data-plural="1"') then -- magic data string to return false
		return plural
	end
	-- count number of list items
	local numListItems = countMatches(s,'&lt;%s*li')
	-- if exactly one, then singular, if more than one, then plural
	if numListItems == 1 then
		return singular
	end
	if numListItems &gt; 1 then
		return plural
	end
	-- if "list of" occurs inside of wlink, then it's plural
	if mw.ustring.find(s:lower(), '%[%[[^%]]*list of[^%]]+%]%]') then
		return plural
	end
    if parseNumber then
        local m = tonumber(mw.ustring.match(s,"^%s*(%d+)"))
        if m then
            return m &gt; 1 and plural or singular
        end
    end
	-- fix for trailing br tags passed through [[template:marriage]]
	s = mw.ustring.gsub(s, '&lt;%s*br[^&gt;]*&gt;%s*(&lt;/div&gt;)', '%1')
	-- replace all wikilinks with fixed string
	if rewriteLinks then
		s = mw.ustring.gsub(s,'%b[]','WIKILINK') 
	end
	-- Five conditions: any one of them can make the string a likely plural or plural
	local hasBreak = mw.ustring.find(s,'&lt;%s*br')
	-- For the last 4, evaluate on string stripped of wikimarkup
    local getPlain = require('Module:Text').Text().getPlain
	s = getPlain(s)
	local hasBullets = countMatches(s,'%*+') &gt; 1
	local multipleQids = mw.ustring.find(s,'Q%d+[%p%s]+Q%d+') -- has multiple QIDs in a row
	if hasBullets or multipleQids then
		return plural
	end
	local commaPattern = anyComma and '[,;]' or '%D[,;]%D'  -- semi-colon similar to comma
	local hasComma = checkComma and mw.ustring.find(s, commaPattern)
	local hasAnd = checkAnd and (mw.ustring.find(s,'[,%s]and%s') or mw.ustring.find(s,'&amp;',1,true))
	if hasBreak or hasComma or hasAnd then
		return likelyPlural
	end
	return singular
end

function p._pluralize(args)
	args = type(args) == 'table' and args or {}
	local singularForm = args[3] or args.singular or ""
	local pluralForm = args[4] or args.plural or ""
	local likelyForm = args.likely or pluralForm
	local link = args[5] or args.link
	if link then
		link = tostring(link)
		singularForm = '[['..link..'|'..singularForm..']]'
		pluralForm = '[['..link..'|'..pluralForm..']]'
		likelyForm = '[['..link..'|'..likelyForm..']]'
	end
	if args[2] then
		return pluralForm
	end
	local detect = p._main(args)
	if detect == nil then
		return ""   -- return blank on complete failure
	end
	if detect == singular then
		return singularForm
	elseif detect == likelyPlural then
		return likelyForm
	else
		return pluralForm
	end
end

function p.main(frame)
	local args = getArgs(frame)
	-- For template, return 1 if singular, blank if plural or empty
	local result = p._main(args)
	if result == nil then
		return 1
	end
	return result == singular and 1 or ""
end

function p.pluralize(frame)
	local args = getArgs(frame)
	return p._pluralize(args)
end

return p</text>
      <sha1>ks2mlneszbgw2vr1nl2rhac5ypdp5y2</sha1>
    </revision>
  </page>
</mediawiki>
