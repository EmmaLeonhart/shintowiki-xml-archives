<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Module:Exponential search</title>
    <ns>828</ns>
    <id>54</id>
    <revision>
      <id>436071</id>
      <timestamp>2015-02-25T03:41:26Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>create a generic binary search module, modified from [[Module:Highest archive number]]</comment>
      <origin>436071</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="845" sha1="r9rwxv4vj0v6v05pkj6xyjaqdzs93e8" xml:space="preserve">-- This module provides a generic binary search algorithm.

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function binarySearch(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return binarySearch(testFunc, i, lower, upper)
	else
		upper = i
		lower = lower or 0
		i = midPoint(lower, upper)
		return binarySearch(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Binary search', 1, testFunc, 'function')
	checkType('Binary search', 2, initNum, 'number', true)
	init = init or 1
	if not testFunc(1) then
		return nil
	end
	return binarySearch(testFunc, init)
end</text>
      <sha1>r9rwxv4vj0v6v05pkj6xyjaqdzs93e8</sha1>
    </revision>
    <revision>
      <id>436072</id>
      <parentid>436071</parentid>
      <timestamp>2015-02-25T03:46:33Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>more robust init check</comment>
      <origin>436072</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1049" sha1="bzip3sc1trk47epqumq3uqsdnxhkyru" xml:space="preserve">-- This module provides a generic binary search algorithm.

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function binarySearch(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return binarySearch(testFunc, i, lower, upper)
	else
		upper = i
		lower = lower or 0
		i = midPoint(lower, upper)
		return binarySearch(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Binary search', 1, testFunc, 'function')
	checkType('Binary search', 2, initNum, 'number', true)
	if init then
		if init &lt; 1 or init ~= floor(init) or init == math.huge or tostring(init) == '-nan' then
			error("if an init is specified to 'Binary search', it must be a positive integer", 2)
		end
	else
		init = 1
	end
	if not testFunc(1) then
		return nil
	end
	return binarySearch(testFunc, init)
end</text>
      <sha1>bzip3sc1trk47epqumq3uqsdnxhkyru</sha1>
    </revision>
    <revision>
      <id>436073</id>
      <parentid>436072</parentid>
      <timestamp>2015-02-25T07:10:52Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>remove the NaN test (it will fail "init ~= floor(init)" anyway) and make the error message more useful</comment>
      <origin>436073</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1095" sha1="jhpqkoxs80i1zeg7adsbonycrgk79v8" xml:space="preserve">-- This module provides a generic binary search algorithm.

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function binarySearch(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return binarySearch(testFunc, i, lower, upper)
	else
		upper = i
		lower = lower or 0
		i = midPoint(lower, upper)
		return binarySearch(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Binary search', 1, testFunc, 'function')
	checkType('Binary search', 2, initNum, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Binary search' (init values must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 1
	if not testFunc(1) then
		return nil
	end
	return binarySearch(testFunc, init)
end</text>
      <sha1>jhpqkoxs80i1zeg7adsbonycrgk79v8</sha1>
    </revision>
    <revision>
      <id>436074</id>
      <parentid>436073</parentid>
      <timestamp>2015-02-25T13:30:26Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>Mr. Stradivarius moved page [[Module:Binary search]] to [[Module:Exponential search]] without leaving a redirect: turns out [[binary search]]es are technically for bounded arrays - [[exponential search]] is a better name</comment>
      <origin>436073</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1095" sha1="jhpqkoxs80i1zeg7adsbonycrgk79v8" xml:space="preserve">-- This module provides a generic binary search algorithm.

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function binarySearch(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return binarySearch(testFunc, i, lower, upper)
	else
		upper = i
		lower = lower or 0
		i = midPoint(lower, upper)
		return binarySearch(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Binary search', 1, testFunc, 'function')
	checkType('Binary search', 2, initNum, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Binary search' (init values must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 1
	if not testFunc(1) then
		return nil
	end
	return binarySearch(testFunc, init)
end</text>
      <sha1>jhpqkoxs80i1zeg7adsbonycrgk79v8</sha1>
    </revision>
    <revision>
      <id>436075</id>
      <parentid>436074</parentid>
      <timestamp>2015-02-25T13:33:49Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>change names after page move</comment>
      <origin>436075</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1091" sha1="ts0p875fyl5b5niol3fo3hpu7d42opt" xml:space="preserve">-- This module provides a generic exponential search algorithm.

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		lower = lower or 0
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, initNum, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init values must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 1
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init)
end</text>
      <sha1>ts0p875fyl5b5niol3fo3hpu7d42opt</sha1>
    </revision>
    <revision>
      <id>436076</id>
      <parentid>436075</parentid>
      <timestamp>2015-02-25T13:36:58Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>specify a lower bound of 0 initially so that we don't have to do it on every recursion</comment>
      <origin>436076</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1078" sha1="6parurwmga1kz4bompuhs0a0a052dub" xml:space="preserve">-- This module provides a generic exponential search algorithm.

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, initNum, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init values must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 1
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 0, nil)
end</text>
      <sha1>6parurwmga1kz4bompuhs0a0a052dub</sha1>
    </revision>
    <revision>
      <id>436077</id>
      <parentid>436076</parentid>
      <timestamp>2015-02-26T12:48:51Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>don't check testFunc(1) twice every time</comment>
      <origin>436077</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1078" sha1="3ib7ry76py9aimifb9vrj2fj9zzyh3a" xml:space="preserve">-- This module provides a generic exponential search algorithm.

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, initNum, 'number', true)
	if not testFunc(1) then
		return nil
	end
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init values must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>3ib7ry76py9aimifb9vrj2fj9zzyh3a</sha1>
    </revision>
    <revision>
      <id>436078</id>
      <parentid>436077</parentid>
      <timestamp>2015-02-26T13:12:13Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>move the init checkType a little later so that we don't execute it for empty arrays</comment>
      <origin>436078</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1078" sha1="qqi1weazbbhaiqqi1xrqx3eyiz0bxtk" xml:space="preserve">-- This module provides a generic exponential search algorithm.

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	if not testFunc(1) then
		return nil
	end
	checkType('Exponential search', 2, initNum, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init values must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>qqi1weazbbhaiqqi1xrqx3eyiz0bxtk</sha1>
    </revision>
    <revision>
      <id>436079</id>
      <parentid>436078</parentid>
      <timestamp>2015-02-27T10:29:41Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>fix bad variable name</comment>
      <origin>436079</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1075" sha1="bsvwd70x9rb42zjb25pruehairvdevg" xml:space="preserve">-- This module provides a generic exponential search algorithm.

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	if not testFunc(1) then
		return nil
	end
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init values must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>bsvwd70x9rb42zjb25pruehairvdevg</sha1>
    </revision>
    <revision>
      <id>436080</id>
      <parentid>436079</parentid>
      <timestamp>2015-02-27T11:59:45Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <comment>do init checks at the start after all, so that we raise an error even for empty arrays</comment>
      <origin>436080</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1075" sha1="eu7ozuuisicj302lnftwx4u0b8ozyi6" xml:space="preserve">-- This module provides a generic exponential search algorithm.

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init values must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>eu7ozuuisicj302lnftwx4u0b8ozyi6</sha1>
    </revision>
    <revision>
      <id>436081</id>
      <parentid>436080</parentid>
      <timestamp>2015-02-27T13:37:05Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>fix error message tense</comment>
      <origin>436081</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1074" sha1="ndcjiubm1yvxboo7en7zf00no0kec02" xml:space="preserve">-- This module provides a generic exponential search algorithm.

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>ndcjiubm1yvxboo7en7zf00no0kec02</sha1>
    </revision>
    <revision>
      <id>436082</id>
      <parentid>436081</parentid>
      <timestamp>2019-09-29T15:39:52Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Return 0 instead of nil when testFunc returns false, per comments at [[Module talk:TableTools]]</comment>
      <origin>436082</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1072" sha1="2tqykimg9vdmli7e0nkrj10df7ohsgj" xml:space="preserve">-- This module provides a generic exponential search algorithm.

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return 0
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>2tqykimg9vdmli7e0nkrj10df7ohsgj</sha1>
    </revision>
    <revision>
      <id>436083</id>
      <parentid>436082</parentid>
      <timestamp>2019-09-29T20:59:01Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Undid revision 918619241 by [[Special:Contributions/Pppery|Pppery]] ([[User talk:Pppery|talk]])</comment>
      <origin>436083</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1074" sha1="ndcjiubm1yvxboo7en7zf00no0kec02" xml:space="preserve">-- This module provides a generic exponential search algorithm.

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>ndcjiubm1yvxboo7en7zf00no0kec02</sha1>
    </revision>
    <revision>
      <id>436084</id>
      <parentid>436083</parentid>
      <timestamp>2019-10-08T16:14:11Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Mr. Stradivarius</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>Protected "[[Module:Exponential search]]": [[WP:High-risk templates|High-risk Lua module]]: used in [[Module:Highest archive number]] ([Edit=Require template editor access] (indefinite) [Move=Require template editor access] (indefinite))</comment>
      <origin>436083</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1074" sha1="ndcjiubm1yvxboo7en7zf00no0kec02" xml:space="preserve">-- This module provides a generic exponential search algorithm.

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>ndcjiubm1yvxboo7en7zf00no0kec02</sha1>
    </revision>
    <revision>
      <id>436064</id>
      <timestamp>2023-02-10T20:10:53Z</timestamp>
      <contributor>
        <username>Uzume</username>
        <id>96</id>
      </contributor>
      <comment>strict</comment>
      <origin>436064</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>436066</id>
      <parentid>436064</parentid>
      <timestamp>2024-03-22T11:36:43Z</timestamp>
      <contributor>
        <username>simple&gt;Djsasso</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>1 revision imported from [[:en:Module:Exponential_search]]</comment>
      <origin>436064</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>436065</id>
      <parentid>436064</parentid>
      <timestamp>2024-06-25T10:18:12Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported from [[:wikipedia:Module:Exponential_search]]: importing for the list. This is a canonical list of Shrines to work with</comment>
      <origin>436064</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>436067</id>
      <parentid>436065</parentid>
      <timestamp>2025-01-09T00:01:17Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: importing templates</comment>
      <origin>436064</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>436068</id>
      <parentid>436067</parentid>
      <timestamp>2025-03-11T09:01:14Z</timestamp>
      <contributor>
        <username>Uzume</username>
        <id>96</id>
      </contributor>
      <comment>strict</comment>
      <origin>436064</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>436069</id>
      <parentid>436068</parentid>
      <timestamp>2025-03-15T19:25:42Z</timestamp>
      <contributor>
        <username>en&gt;Unknown user</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>436064</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>436070</id>
      <parentid>436069</parentid>
      <timestamp>2025-03-15T19:25:43Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: I hope this actually fixes things</comment>
      <origin>436064</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>436085</id>
      <parentid>436070</parentid>
      <timestamp>2025-03-18T09:18:51Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>14 revisions imported from [[:wikipedia:Module:Exponential_search]]: Import just to get the source</comment>
      <origin>436064</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>436086</id>
      <parentid>436085</parentid>
      <timestamp>2025-04-06T23:26:05Z</timestamp>
      <contributor>
        <username>Uzume</username>
        <id>96</id>
      </contributor>
      <comment>strict</comment>
      <origin>436064</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>436087</id>
      <parentid>436086</parentid>
      <timestamp>2025-04-27T22:11:09Z</timestamp>
      <contributor>
        <username>Uzume</username>
        <id>96</id>
      </contributor>
      <comment>strict</comment>
      <origin>436064</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>436088</id>
      <parentid>436087</parentid>
      <timestamp>2025-04-27T22:12:34Z</timestamp>
      <contributor>
        <username>Uzume</username>
        <id>96</id>
      </contributor>
      <comment>strict</comment>
      <origin>436064</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>436089</id>
      <parentid>436088</parentid>
      <timestamp>2025-04-27T22:12:35Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: chat is this real</comment>
      <origin>436064</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>436090</id>
      <parentid>436089</parentid>
      <timestamp>2025-04-27T22:13:28Z</timestamp>
      <contributor>
        <username>Uzume</username>
        <id>96</id>
      </contributor>
      <comment>strict</comment>
      <origin>436064</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>436091</id>
      <parentid>436090</parentid>
      <timestamp>2025-04-27T22:13:29Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: chat is this real</comment>
      <origin>436064</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>436092</id>
      <parentid>436091</parentid>
      <timestamp>2025-04-27T22:17:10Z</timestamp>
      <contributor>
        <username>Uzume</username>
        <id>96</id>
      </contributor>
      <comment>strict</comment>
      <origin>436064</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>436093</id>
      <parentid>436092</parentid>
      <timestamp>2025-11-21T03:31:57Z</timestamp>
      <contributor>
        <username>Uzume</username>
        <id>96</id>
      </contributor>
      <comment>strict</comment>
      <origin>436064</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>436094</id>
      <parentid>436093</parentid>
      <timestamp>2025-11-21T03:31:58Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting this page since it for some reason started to get issues</comment>
      <origin>436064</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>436095</id>
      <parentid>436094</parentid>
      <timestamp>2025-11-21T05:35:11Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: Reverting edits from past 90 minutes</comment>
      <origin>436095</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="845" sha1="r9rwxv4vj0v6v05pkj6xyjaqdzs93e8" xml:space="preserve">-- This module provides a generic binary search algorithm.

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function binarySearch(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return binarySearch(testFunc, i, lower, upper)
	else
		upper = i
		lower = lower or 0
		i = midPoint(lower, upper)
		return binarySearch(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Binary search', 1, testFunc, 'function')
	checkType('Binary search', 2, initNum, 'number', true)
	init = init or 1
	if not testFunc(1) then
		return nil
	end
	return binarySearch(testFunc, init)
end</text>
      <sha1>r9rwxv4vj0v6v05pkj6xyjaqdzs93e8</sha1>
    </revision>
    <revision>
      <id>55</id>
      <timestamp>2025-11-21T05:49:54Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was 1 revision imported: re-importing and overwriting due to the weird script failure </comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3367911</id>
      <parentid>55</parentid>
      <timestamp>2026-03-02T18:44:23Z</timestamp>
      <contributor>
        <username>Uzume</username>
        <id>96</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3367912</id>
      <parentid>3367911</parentid>
      <timestamp>2026-03-02T18:44:23Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3554035</id>
      <parentid>3367912</parentid>
      <timestamp>2026-03-20T18:19:58Z</timestamp>
      <contributor>
        <username>en&gt;Uzume</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3554036</id>
      <parentid>3554035</parentid>
      <timestamp>2026-03-20T18:19:58Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3554249</id>
      <parentid>3554036</parentid>
      <timestamp>2026-03-20T18:20:58Z</timestamp>
      <contributor>
        <username>en&gt;Uzume</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3554250</id>
      <parentid>3554249</parentid>
      <timestamp>2026-03-20T18:20:59Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3554463</id>
      <parentid>3554250</parentid>
      <timestamp>2026-03-20T18:22:26Z</timestamp>
      <contributor>
        <username>en&gt;Uzume</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3554464</id>
      <parentid>3554463</parentid>
      <timestamp>2026-03-20T18:22:27Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3554677</id>
      <parentid>3554464</parentid>
      <timestamp>2026-03-20T18:24:27Z</timestamp>
      <contributor>
        <username>en&gt;Uzume</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3554678</id>
      <parentid>3554677</parentid>
      <timestamp>2026-03-20T18:24:27Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3554891</id>
      <parentid>3554678</parentid>
      <timestamp>2026-03-20T18:26:57Z</timestamp>
      <contributor>
        <username>en&gt;Uzume</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3554892</id>
      <parentid>3554891</parentid>
      <timestamp>2026-03-20T18:26:57Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3555105</id>
      <parentid>3554892</parentid>
      <timestamp>2026-03-20T18:29:59Z</timestamp>
      <contributor>
        <username>en&gt;Uzume</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3555106</id>
      <parentid>3555105</parentid>
      <timestamp>2026-03-20T18:30:00Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3555319</id>
      <parentid>3555106</parentid>
      <timestamp>2026-03-20T18:33:30Z</timestamp>
      <contributor>
        <username>en&gt;Uzume</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3555320</id>
      <parentid>3555319</parentid>
      <timestamp>2026-03-20T18:33:30Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3555533</id>
      <parentid>3555320</parentid>
      <timestamp>2026-03-20T18:37:34Z</timestamp>
      <contributor>
        <username>en&gt;Uzume</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3555534</id>
      <parentid>3555533</parentid>
      <timestamp>2026-03-20T18:37:34Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3555747</id>
      <parentid>3555534</parentid>
      <timestamp>2026-03-20T18:42:03Z</timestamp>
      <contributor>
        <username>en&gt;Uzume</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3555748</id>
      <parentid>3555747</parentid>
      <timestamp>2026-03-20T18:42:03Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3555961</id>
      <parentid>3555748</parentid>
      <timestamp>2026-03-20T18:47:06Z</timestamp>
      <contributor>
        <username>en&gt;Uzume</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3555962</id>
      <parentid>3555961</parentid>
      <timestamp>2026-03-20T18:47:06Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3556175</id>
      <parentid>3555962</parentid>
      <timestamp>2026-03-20T18:52:36Z</timestamp>
      <contributor>
        <username>en&gt;Uzume</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3556176</id>
      <parentid>3556175</parentid>
      <timestamp>2026-03-20T18:52:36Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3556389</id>
      <parentid>3556176</parentid>
      <timestamp>2026-03-20T18:58:41Z</timestamp>
      <contributor>
        <username>en&gt;Uzume</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3556390</id>
      <parentid>3556389</parentid>
      <timestamp>2026-03-20T18:58:41Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3556603</id>
      <parentid>3556390</parentid>
      <timestamp>2026-03-20T19:05:11Z</timestamp>
      <contributor>
        <username>en&gt;Uzume</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3556604</id>
      <parentid>3556603</parentid>
      <timestamp>2026-03-20T19:05:11Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3556817</id>
      <parentid>3556604</parentid>
      <timestamp>2026-03-20T19:12:13Z</timestamp>
      <contributor>
        <username>en&gt;Uzume</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3556818</id>
      <parentid>3556817</parentid>
      <timestamp>2026-03-20T19:12:13Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3557077</id>
      <parentid>3556818</parentid>
      <timestamp>2026-03-20T19:37:54Z</timestamp>
      <contributor>
        <username>en&gt;Uzume</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3557078</id>
      <parentid>3557077</parentid>
      <timestamp>2026-03-20T19:37:54Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3557400</id>
      <parentid>3557078</parentid>
      <timestamp>2026-03-20T19:41:51Z</timestamp>
      <contributor>
        <username>Uzume</username>
        <id>96</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3557401</id>
      <parentid>3557400</parentid>
      <timestamp>2026-03-20T19:41:51Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3557916</id>
      <parentid>3557401</parentid>
      <timestamp>2026-03-20T19:46:24Z</timestamp>
      <contributor>
        <username>en&gt;Uzume</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3557917</id>
      <parentid>3557916</parentid>
      <timestamp>2026-03-20T19:46:24Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3558782</id>
      <parentid>3557917</parentid>
      <timestamp>2026-03-20T19:55:26Z</timestamp>
      <contributor>
        <username>en&gt;Uzume</username>
        <id>0</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3558783</id>
      <parentid>3558782</parentid>
      <timestamp>2026-03-20T19:55:26Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: Manual reimport from enwiki to fix erroneous transclusions</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3646492</id>
      <parentid>3558783</parentid>
      <timestamp>2026-04-24T16:31:27Z</timestamp>
      <contributor>
        <username>Uzume</username>
        <id>96</id>
      </contributor>
      <comment>strict</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
    <revision>
      <id>3646493</id>
      <parentid>3646492</parentid>
      <timestamp>2026-04-24T16:31:27Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: trying to do a last fix of these, they will be fixed and not bloat the site</comment>
      <origin>55</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1092" sha1="jqqi8l27tb73lglksbukg2g3bzt3fmv" xml:space="preserve">-- This module provides a generic exponential search algorithm.
require[[strict]]

local checkType = require('libraryUtil').checkType
local floor = math.floor

local function midPoint(lower, upper)
	return floor(lower + (upper - lower) / 2)
end

local function search(testFunc, i, lower, upper)
	if testFunc(i) then
		if i + 1 == upper then
			return i
		end
		lower = i
		if upper then
			i = midPoint(lower, upper)
		else
			i = i * 2
		end
		return search(testFunc, i, lower, upper)
	else
		upper = i
		i = midPoint(lower, upper)
		return search(testFunc, i, lower, upper)
	end
end

return function (testFunc, init)
	checkType('Exponential search', 1, testFunc, 'function')
	checkType('Exponential search', 2, init, 'number', true)
	if init and (init &lt; 1 or init ~= floor(init) or init == math.huge) then
		error(string.format(
			"invalid init value '%s' detected in argument #2 to " ..
			"'Exponential search' (init value must be a positive integer)",
			tostring(init)
		), 2)
	end
	init = init or 2
	if not testFunc(1) then
		return nil
	end
	return search(testFunc, init, 1, nil)
end</text>
      <sha1>jqqi8l27tb73lglksbukg2g3bzt3fmv</sha1>
    </revision>
  </page>
</mediawiki>
