<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Help:Cascading Style Sheets</title>
    <ns>12</ns>
    <id>3316</id>
    <revision>
      <id>721105</id>
      <timestamp>2024-10-05T09:41:21Z</timestamp>
      <contributor>
        <username>en&gt;Sirocco745</username>
        <id>0</id>
      </contributor>
      <comment>Restored revision 1244555035 by [[Special:Contributions/Redrose64|Redrose64]] ([[User talk:Redrose64|talk]]): Reverted unexplained section blanking</comment>
      <origin>721105</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="13755" sha1="1ha9ifnvsldts6451btg8hewjp2hml5" xml:space="preserve">{{Information page|H:CSS|WP:CSS}}
[[Cascading Style Sheets]] allows for flexible formatting of a page. They should be used instead of [[Help:Table|tables]] for non-tabular content whenever possible, because they can be [[Help:User style|manipulated by the reader]] or overridden by an author if your CSS is embedded in another page via a [[Help:Template|template]].

== Levels of CSS settings ==
Style may be chosen specifically for a piece of content, see e.g., [[Help:Table#Color; scope of parameters|color; scope of parameters]]

Alternatively, style is specified for CSS selectors, expressed in terms of elements, classes, and ID's. This is done on various levels:

Author style sheets, in this order:

''Note: See [[WP:CLASS]] for a list of all the style sheets loaded.''

===MediaWiki core style-sheets===

Per skin: [[mw:Gallery of user styles|MediaWiki Manual:Gallery of user styles]] etc. 
Typically loaded style sheets:
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/shared.css common/shared.css]
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/commonPrint.css common/commonPrint.css]

;Skin-specific main file:
e.g., [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/main.css monobook/main.css] (normal skin for PC's), [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/chick/main.css chick/main.css] (normal skin for handhelds)

;Browser-specific fixes (also skin-specific): 
Examples for Monobook:
*For Firefox: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/FF2Fixes.css monobook/FF2Fixes.css]
*For Internet Explorer: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE60Fixes.css monobook/IE60Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE70Fixes.css monobook/IE70Fixes.css]
*For Opera: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera6Fixes.css monobook/Opera6Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera7Fixes.css monobook/Opera7Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera9Fixes.css monobook/Opera9Fixes.css]

===Site-wide style-sheets===

* Site-wide for all skins (desktop view only): [[MediaWiki:Common.css]]
* Site-wide for all skins (mobile view only): [[MediaWiki:Mobile.css]]
* Site-wide per skin: [[MediaWiki:Vector.css]], [[MediaWiki:Monobook.css]], etc.
* Site-wide for user groups: [[MediaWiki:Group-user.css]], [[MediaWiki:Group-autoconfirmed.css]], etc.
* Site-wide for specialist purposes: [[MediaWiki:Print.css]], [[MediaWiki:Noscript.css]], [[MediaWiki:Filepage.css]]
* Site-wide if gadgets loaded: see [[Wikipedia:Gadget]] for more information

Note: MediaWiki sites other than English Wikipedia may use &lt;code&gt;MediaWiki:Gadget-site.css&lt;/code&gt; instead of MediaWiki:Common.css.

===Page-specific style-sheets===

Page-specific style-sheets may be introduced through [[Wikipedia:TemplateStyles|TemplateStyles]]. Pages, particularly templates, may also have inline CSS.

Some pages have their own CSS, e.g., [[MediaWiki:FileUploadWizard.css]]

===User-specific style-sheets===

* Global user-specific for all skins: [[meta:Special:MyPage/global.css]]
* User-specific for all skins: [[Special:MyPage/common.css]]
* User-specific per skin: e.g., [[Special:MyPage/vector.css]], or [[Special:MyPage/skin.css]] for your current skin 
* User-specific CSS loaded through JavaScript, e.g., loaded at [[Special:MyPage/common.js]]
* User-specific web-wide browser settings: local file referred to in the browser settings, or directly set in the browser

Note that in CSS terminology, the user-specific style sheets are ''not'' [[Help:User style|user style sheets]].

===Usage===

An HTML element may be just taken from the wikitext (see [[Help:HTML in wikitext|HTML in wikitext]]), e.g., span, or the result of translating wikitext, e.g., the &lt;code&gt;&lt;nowiki&gt;'''...'''&lt;/nowiki&gt;&lt;/code&gt; code is changed into {{tag|b}}, or part of the code for the skin.

A class may be produced by the software, e.g., ns-''namespace number'' for the HTML-element "body", and extiw for an interwiki link in the page body, or taken from the wikitext.

Similarly, an ID may be produced by the software, e.g., bodyContent, or taken from the wikitext.

In the case of conflicting style settings for a piece of content, the resulting setting depends primarily on the indication "!important". Secondarily, if both are important, the user wins, if neither is, the author wins. Tertiarily it depends on specificity. Only lastly, it depends on order between and within style sheets: the last wins. Thus, a User:''username''/monobook.css does not win from MediaWiki:Monobook.css (both author, not user) if the specificity of the latter is greater. See also [http://www.w3.org/TR/REC-CSS2/cascade.html cascade].

== Supported elements ==
MediaWiki supports most CSS, with such exceptions as the url() attribute. There were some bugs in CSS support in earlier versions.

{{Further2|the [http://www.w3.org/TR/2011/REC-CSS2-20110607/ Cascading Style Sheets Level 2 Revision 1 (CSS 2.1) Specification]}}

== CSS in wikitext ==

You can use CSS styling in HTML elements in your code (see [[Help:HTML in wikitext]] for a list of elements supported by MediaWiki) like you would in normal HTML markup.

For example, a {{tag|div}} element with a green border and its contents floated to the right would be created with
&lt;syntaxhighlight lang="html"&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
&lt;/syntaxhighlight&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
which would produce the box to the right.
Some wikitext elements allow you to insert CSS styling directly into them. An example is the table syntax:
{{-}}
&lt;syntaxhighlight lang="wikitext"&gt;
{| style="your style here"
|-
|your table stuff
|}
&lt;/syntaxhighlight&gt;
{{Further2|[[Help:Tables]]}}

== MediaWiki existing styles ==

You may wish to use a style type that is already predefined by MediaWiki, or the site that you are visiting. You can also create a style that is unique to your page.

Vector is the default style, you can view it at: [[MediaWiki:Vector.css]]

You will give your CSS tag an existing "class"

Please put a list of existing classes here.

== Tips and tricks ==

=== Non-display ===
In an embedded page, one can hide comments in one version, and show them in another view.
One extreme "style" for a text is not displaying it, with

&lt;syntaxhighlight lang="css"&gt;
.''classname'' {display: none}
#''id'' {display: none}
&lt;/syntaxhighlight&gt;

etc.

Non-displayed links do not work (as opposed to links in a very small font).

It cannot be used to remove text in expressions for template names, parameter names, parameter values, page names in links, etc.

To view hidden text, download the Web Developer Toolbar for Firefox [https://addons.mozilla.org/en-US/firefox/addon/60 here], then choose Misc. &amp;rarr; show hidden elements in that toolbar. It will make all hidden elements appear.

=== Non-print ===

One can exclude content from being printed by declaring the content to be of the "noprint" class:
&lt;syntaxhighlight lang="html"&gt;
&lt;div class="someclass noprint"&gt;This will not appear in the print version.&lt;/div&gt;
&lt;/syntaxhighlight&gt;

== Major style blocks ==
[[File:Wikimedia major style blocks example.svg|right|thumb|An annotated desktop screenshot of [[Wikipedia:Example|an example article]], showcasing the style blocks]]
*''column-content'' – overall space within the margins of which the content exists.
* ''firstHeading'' – the class of the heading tag at the top of every page
*''contentSub'' – the name of the wiki immediately underneath the main heading, but above the body text
* ''content'' – the white background, thin bordered box which contains the main page content.
* ''bodyContent'' – the main page content within the content box

The portlet class is the style used by all the div blocks around the main content.  Identified blocks using that class:
*''p-cactions'' – id for the list of page-related tabs above the main content (page, talk, edit, etc.), top.
*''p-personal'' – id for the list of user-related links above the main content (username, talk, etc.), top.
*''p-logo'' – id for the block that contains the logo, top left.
*''p-navigation'' – id for the block that contains the navigation links on the left of the page
*''p-search'' – the block that contains the search buttons
*''p-tb'' – the block that contains the toolbox links
*''p-lang'' – the block that contains interlanguage links

The footer at the bottom of the page includes blocks with the following ids
*''footer'' – overall footer container block
*''f-poweredbyico'' – the powered by MediaWiki image that normally resides to the right of the page
*''f-list'' – id for the list that contains all the bits of text at the bottom of the page

== Style depending on a parameter or variable ==
=== Variable class or id ===

A class or id can depend on the result produced by a template or on a template parameter, e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1|def}}}"&lt;/syntaxhighlight&gt;. For one or more of the possible class names the style of that class can be defined. If the class is undefined it is ignored, so the standard style is used.

In the simplest case we have e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1}}}"&lt;/syntaxhighlight&gt; and define class abcdef. If the parameter value is "def" it applies.

If a page for general use only makes sense when styles are defined for certain classes, then these have to be specified in the page [[MediaWiki:Common.css]], which applies for all users and all skins, as far as not overridden.

=== Variable style parameter value ===

Wikitext that reads
&lt;syntaxhighlight lang="html"&gt;&lt;span style="display:{{{3|none}}}"&gt;Wed&lt;/span&gt;&lt;/syntaxhighlight&gt;
will display "Wed" if parameter 3 is defined, but its value is not "none", and displays nothing if parameter 3 is undefined or "none". If the value of parameter 3 is a display style other than "none", that style is applied.

==Wiki headings==
Wiki headings use the following default CSS:
&lt;section begin="HTML_section" /&gt;
{| class="wikitable"
|-
! Wikimarkup !! [[HTML]] !! Style
|-
| {{nowrap|1=&lt;code&gt;= Heading =&lt;/code&gt;}} || {{tag|h1|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.8em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;== Heading ==&lt;/code&gt;}} || {{tag|h2|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.5em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;=== Heading ===&lt;/code&gt;}} || {{tag|h3|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.2em; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;==== Heading ====&lt;/code&gt;}} || {{tag|h4|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;===== Heading =====&lt;/code&gt;}} || {{tag|h5|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;====== Heading ======&lt;/code&gt;}} || {{tag|h6|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
|}&lt;section end="HTML_section" /&gt;

== See also ==
*[[Span and div]]
*[[Cascading Style Sheets]] – article on CSS
*[[Wikipedia:Customisation]] – also covers user names, preferences settings, skins, user scripting, etc.
*[[Help:User style]] – modifying style for accessibility or for additional feature testing.
*[[Wikipedia:TemplateStyles]] – modifying style for advanced visual appearance that can be applied with template.
*[[Wikipedia:Catalogue of CSS classes]] – list of classes globally defined across the site
*[[Wikipedia:WikiProject Microformats/classes]] – list of classes used in microformats employed on Wikipedia
*[[Help:User CSS for a monospaced coding font]] – both for the editing window and for display of monospaced elements like {{tag|code|o}}
*[[meta:Help:Cascading_style_sheets]]
*[[mw:Manual:CSS]] and [[mw:Manual:Interface/Stylesheets]]
*[[mw:Gallery of user styles]]
*[[m:Customization:Explaining skins]]
*[[mw:Skin projects]]

{{Wikipedia technical help|collapsed}}
{{CSS and JS MediaWiki messages|state=collapsed|style=}}

[[Category:Wikipedia features]]
[[Category:Wikipedia help]]</text>
      <sha1>1ha9ifnvsldts6451btg8hewjp2hml5</sha1>
    </revision>
    <revision>
      <id>721106</id>
      <parentid>721105</parentid>
      <timestamp>2024-12-08T07:14:48Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: importing wikipedia</comment>
      <origin>721105</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="13755" sha1="1ha9ifnvsldts6451btg8hewjp2hml5" xml:space="preserve">{{Information page|H:CSS|WP:CSS}}
[[Cascading Style Sheets]] allows for flexible formatting of a page. They should be used instead of [[Help:Table|tables]] for non-tabular content whenever possible, because they can be [[Help:User style|manipulated by the reader]] or overridden by an author if your CSS is embedded in another page via a [[Help:Template|template]].

== Levels of CSS settings ==
Style may be chosen specifically for a piece of content, see e.g., [[Help:Table#Color; scope of parameters|color; scope of parameters]]

Alternatively, style is specified for CSS selectors, expressed in terms of elements, classes, and ID's. This is done on various levels:

Author style sheets, in this order:

''Note: See [[WP:CLASS]] for a list of all the style sheets loaded.''

===MediaWiki core style-sheets===

Per skin: [[mw:Gallery of user styles|MediaWiki Manual:Gallery of user styles]] etc. 
Typically loaded style sheets:
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/shared.css common/shared.css]
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/commonPrint.css common/commonPrint.css]

;Skin-specific main file:
e.g., [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/main.css monobook/main.css] (normal skin for PC's), [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/chick/main.css chick/main.css] (normal skin for handhelds)

;Browser-specific fixes (also skin-specific): 
Examples for Monobook:
*For Firefox: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/FF2Fixes.css monobook/FF2Fixes.css]
*For Internet Explorer: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE60Fixes.css monobook/IE60Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE70Fixes.css monobook/IE70Fixes.css]
*For Opera: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera6Fixes.css monobook/Opera6Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera7Fixes.css monobook/Opera7Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera9Fixes.css monobook/Opera9Fixes.css]

===Site-wide style-sheets===

* Site-wide for all skins (desktop view only): [[MediaWiki:Common.css]]
* Site-wide for all skins (mobile view only): [[MediaWiki:Mobile.css]]
* Site-wide per skin: [[MediaWiki:Vector.css]], [[MediaWiki:Monobook.css]], etc.
* Site-wide for user groups: [[MediaWiki:Group-user.css]], [[MediaWiki:Group-autoconfirmed.css]], etc.
* Site-wide for specialist purposes: [[MediaWiki:Print.css]], [[MediaWiki:Noscript.css]], [[MediaWiki:Filepage.css]]
* Site-wide if gadgets loaded: see [[Wikipedia:Gadget]] for more information

Note: MediaWiki sites other than English Wikipedia may use &lt;code&gt;MediaWiki:Gadget-site.css&lt;/code&gt; instead of MediaWiki:Common.css.

===Page-specific style-sheets===

Page-specific style-sheets may be introduced through [[Wikipedia:TemplateStyles|TemplateStyles]]. Pages, particularly templates, may also have inline CSS.

Some pages have their own CSS, e.g., [[MediaWiki:FileUploadWizard.css]]

===User-specific style-sheets===

* Global user-specific for all skins: [[meta:Special:MyPage/global.css]]
* User-specific for all skins: [[Special:MyPage/common.css]]
* User-specific per skin: e.g., [[Special:MyPage/vector.css]], or [[Special:MyPage/skin.css]] for your current skin 
* User-specific CSS loaded through JavaScript, e.g., loaded at [[Special:MyPage/common.js]]
* User-specific web-wide browser settings: local file referred to in the browser settings, or directly set in the browser

Note that in CSS terminology, the user-specific style sheets are ''not'' [[Help:User style|user style sheets]].

===Usage===

An HTML element may be just taken from the wikitext (see [[Help:HTML in wikitext|HTML in wikitext]]), e.g., span, or the result of translating wikitext, e.g., the &lt;code&gt;&lt;nowiki&gt;'''...'''&lt;/nowiki&gt;&lt;/code&gt; code is changed into {{tag|b}}, or part of the code for the skin.

A class may be produced by the software, e.g., ns-''namespace number'' for the HTML-element "body", and extiw for an interwiki link in the page body, or taken from the wikitext.

Similarly, an ID may be produced by the software, e.g., bodyContent, or taken from the wikitext.

In the case of conflicting style settings for a piece of content, the resulting setting depends primarily on the indication "!important". Secondarily, if both are important, the user wins, if neither is, the author wins. Tertiarily it depends on specificity. Only lastly, it depends on order between and within style sheets: the last wins. Thus, a User:''username''/monobook.css does not win from MediaWiki:Monobook.css (both author, not user) if the specificity of the latter is greater. See also [http://www.w3.org/TR/REC-CSS2/cascade.html cascade].

== Supported elements ==
MediaWiki supports most CSS, with such exceptions as the url() attribute. There were some bugs in CSS support in earlier versions.

{{Further2|the [http://www.w3.org/TR/2011/REC-CSS2-20110607/ Cascading Style Sheets Level 2 Revision 1 (CSS 2.1) Specification]}}

== CSS in wikitext ==

You can use CSS styling in HTML elements in your code (see [[Help:HTML in wikitext]] for a list of elements supported by MediaWiki) like you would in normal HTML markup.

For example, a {{tag|div}} element with a green border and its contents floated to the right would be created with
&lt;syntaxhighlight lang="html"&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
&lt;/syntaxhighlight&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
which would produce the box to the right.
Some wikitext elements allow you to insert CSS styling directly into them. An example is the table syntax:
{{-}}
&lt;syntaxhighlight lang="wikitext"&gt;
{| style="your style here"
|-
|your table stuff
|}
&lt;/syntaxhighlight&gt;
{{Further2|[[Help:Tables]]}}

== MediaWiki existing styles ==

You may wish to use a style type that is already predefined by MediaWiki, or the site that you are visiting. You can also create a style that is unique to your page.

Vector is the default style, you can view it at: [[MediaWiki:Vector.css]]

You will give your CSS tag an existing "class"

Please put a list of existing classes here.

== Tips and tricks ==

=== Non-display ===
In an embedded page, one can hide comments in one version, and show them in another view.
One extreme "style" for a text is not displaying it, with

&lt;syntaxhighlight lang="css"&gt;
.''classname'' {display: none}
#''id'' {display: none}
&lt;/syntaxhighlight&gt;

etc.

Non-displayed links do not work (as opposed to links in a very small font).

It cannot be used to remove text in expressions for template names, parameter names, parameter values, page names in links, etc.

To view hidden text, download the Web Developer Toolbar for Firefox [https://addons.mozilla.org/en-US/firefox/addon/60 here], then choose Misc. &amp;rarr; show hidden elements in that toolbar. It will make all hidden elements appear.

=== Non-print ===

One can exclude content from being printed by declaring the content to be of the "noprint" class:
&lt;syntaxhighlight lang="html"&gt;
&lt;div class="someclass noprint"&gt;This will not appear in the print version.&lt;/div&gt;
&lt;/syntaxhighlight&gt;

== Major style blocks ==
[[File:Wikimedia major style blocks example.svg|right|thumb|An annotated desktop screenshot of [[Wikipedia:Example|an example article]], showcasing the style blocks]]
*''column-content'' – overall space within the margins of which the content exists.
* ''firstHeading'' – the class of the heading tag at the top of every page
*''contentSub'' – the name of the wiki immediately underneath the main heading, but above the body text
* ''content'' – the white background, thin bordered box which contains the main page content.
* ''bodyContent'' – the main page content within the content box

The portlet class is the style used by all the div blocks around the main content.  Identified blocks using that class:
*''p-cactions'' – id for the list of page-related tabs above the main content (page, talk, edit, etc.), top.
*''p-personal'' – id for the list of user-related links above the main content (username, talk, etc.), top.
*''p-logo'' – id for the block that contains the logo, top left.
*''p-navigation'' – id for the block that contains the navigation links on the left of the page
*''p-search'' – the block that contains the search buttons
*''p-tb'' – the block that contains the toolbox links
*''p-lang'' – the block that contains interlanguage links

The footer at the bottom of the page includes blocks with the following ids
*''footer'' – overall footer container block
*''f-poweredbyico'' – the powered by MediaWiki image that normally resides to the right of the page
*''f-list'' – id for the list that contains all the bits of text at the bottom of the page

== Style depending on a parameter or variable ==
=== Variable class or id ===

A class or id can depend on the result produced by a template or on a template parameter, e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1|def}}}"&lt;/syntaxhighlight&gt;. For one or more of the possible class names the style of that class can be defined. If the class is undefined it is ignored, so the standard style is used.

In the simplest case we have e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1}}}"&lt;/syntaxhighlight&gt; and define class abcdef. If the parameter value is "def" it applies.

If a page for general use only makes sense when styles are defined for certain classes, then these have to be specified in the page [[MediaWiki:Common.css]], which applies for all users and all skins, as far as not overridden.

=== Variable style parameter value ===

Wikitext that reads
&lt;syntaxhighlight lang="html"&gt;&lt;span style="display:{{{3|none}}}"&gt;Wed&lt;/span&gt;&lt;/syntaxhighlight&gt;
will display "Wed" if parameter 3 is defined, but its value is not "none", and displays nothing if parameter 3 is undefined or "none". If the value of parameter 3 is a display style other than "none", that style is applied.

==Wiki headings==
Wiki headings use the following default CSS:
&lt;section begin="HTML_section" /&gt;
{| class="wikitable"
|-
! Wikimarkup !! [[HTML]] !! Style
|-
| {{nowrap|1=&lt;code&gt;= Heading =&lt;/code&gt;}} || {{tag|h1|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.8em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;== Heading ==&lt;/code&gt;}} || {{tag|h2|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.5em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;=== Heading ===&lt;/code&gt;}} || {{tag|h3|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.2em; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;==== Heading ====&lt;/code&gt;}} || {{tag|h4|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;===== Heading =====&lt;/code&gt;}} || {{tag|h5|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;====== Heading ======&lt;/code&gt;}} || {{tag|h6|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
|}&lt;section end="HTML_section" /&gt;

== See also ==
*[[Span and div]]
*[[Cascading Style Sheets]] – article on CSS
*[[Wikipedia:Customisation]] – also covers user names, preferences settings, skins, user scripting, etc.
*[[Help:User style]] – modifying style for accessibility or for additional feature testing.
*[[Wikipedia:TemplateStyles]] – modifying style for advanced visual appearance that can be applied with template.
*[[Wikipedia:Catalogue of CSS classes]] – list of classes globally defined across the site
*[[Wikipedia:WikiProject Microformats/classes]] – list of classes used in microformats employed on Wikipedia
*[[Help:User CSS for a monospaced coding font]] – both for the editing window and for display of monospaced elements like {{tag|code|o}}
*[[meta:Help:Cascading_style_sheets]]
*[[mw:Manual:CSS]] and [[mw:Manual:Interface/Stylesheets]]
*[[mw:Gallery of user styles]]
*[[m:Customization:Explaining skins]]
*[[mw:Skin projects]]

{{Wikipedia technical help|collapsed}}
{{CSS and JS MediaWiki messages|state=collapsed|style=}}

[[Category:Wikipedia features]]
[[Category:Wikipedia help]]</text>
      <sha1>1ha9ifnvsldts6451btg8hewjp2hml5</sha1>
    </revision>
    <revision>
      <id>721107</id>
      <parentid>721106</parentid>
      <timestamp>2024-12-20T02:02:54Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Adding [[Category:pages from wikipedia]][[en:{{subst:PAGENAME}}]][[Category:pages from wikipedia]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>721107</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="13851" sha1="lrys9rf1itx3yhvbmmw6lruai95ml83" xml:space="preserve">{{Information page|H:CSS|WP:CSS}}
[[Cascading Style Sheets]] allows for flexible formatting of a page. They should be used instead of [[Help:Table|tables]] for non-tabular content whenever possible, because they can be [[Help:User style|manipulated by the reader]] or overridden by an author if your CSS is embedded in another page via a [[Help:Template|template]].

== Levels of CSS settings ==
Style may be chosen specifically for a piece of content, see e.g., [[Help:Table#Color; scope of parameters|color; scope of parameters]]

Alternatively, style is specified for CSS selectors, expressed in terms of elements, classes, and ID's. This is done on various levels:

Author style sheets, in this order:

''Note: See [[WP:CLASS]] for a list of all the style sheets loaded.''

===MediaWiki core style-sheets===

Per skin: [[mw:Gallery of user styles|MediaWiki Manual:Gallery of user styles]] etc. 
Typically loaded style sheets:
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/shared.css common/shared.css]
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/commonPrint.css common/commonPrint.css]

;Skin-specific main file:
e.g., [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/main.css monobook/main.css] (normal skin for PC's), [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/chick/main.css chick/main.css] (normal skin for handhelds)

;Browser-specific fixes (also skin-specific): 
Examples for Monobook:
*For Firefox: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/FF2Fixes.css monobook/FF2Fixes.css]
*For Internet Explorer: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE60Fixes.css monobook/IE60Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE70Fixes.css monobook/IE70Fixes.css]
*For Opera: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera6Fixes.css monobook/Opera6Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera7Fixes.css monobook/Opera7Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera9Fixes.css monobook/Opera9Fixes.css]

===Site-wide style-sheets===

* Site-wide for all skins (desktop view only): [[MediaWiki:Common.css]]
* Site-wide for all skins (mobile view only): [[MediaWiki:Mobile.css]]
* Site-wide per skin: [[MediaWiki:Vector.css]], [[MediaWiki:Monobook.css]], etc.
* Site-wide for user groups: [[MediaWiki:Group-user.css]], [[MediaWiki:Group-autoconfirmed.css]], etc.
* Site-wide for specialist purposes: [[MediaWiki:Print.css]], [[MediaWiki:Noscript.css]], [[MediaWiki:Filepage.css]]
* Site-wide if gadgets loaded: see [[Wikipedia:Gadget]] for more information

Note: MediaWiki sites other than English Wikipedia may use &lt;code&gt;MediaWiki:Gadget-site.css&lt;/code&gt; instead of MediaWiki:Common.css.

===Page-specific style-sheets===

Page-specific style-sheets may be introduced through [[Wikipedia:TemplateStyles|TemplateStyles]]. Pages, particularly templates, may also have inline CSS.

Some pages have their own CSS, e.g., [[MediaWiki:FileUploadWizard.css]]

===User-specific style-sheets===

* Global user-specific for all skins: [[meta:Special:MyPage/global.css]]
* User-specific for all skins: [[Special:MyPage/common.css]]
* User-specific per skin: e.g., [[Special:MyPage/vector.css]], or [[Special:MyPage/skin.css]] for your current skin 
* User-specific CSS loaded through JavaScript, e.g., loaded at [[Special:MyPage/common.js]]
* User-specific web-wide browser settings: local file referred to in the browser settings, or directly set in the browser

Note that in CSS terminology, the user-specific style sheets are ''not'' [[Help:User style|user style sheets]].

===Usage===

An HTML element may be just taken from the wikitext (see [[Help:HTML in wikitext|HTML in wikitext]]), e.g., span, or the result of translating wikitext, e.g., the &lt;code&gt;&lt;nowiki&gt;'''...'''&lt;/nowiki&gt;&lt;/code&gt; code is changed into {{tag|b}}, or part of the code for the skin.

A class may be produced by the software, e.g., ns-''namespace number'' for the HTML-element "body", and extiw for an interwiki link in the page body, or taken from the wikitext.

Similarly, an ID may be produced by the software, e.g., bodyContent, or taken from the wikitext.

In the case of conflicting style settings for a piece of content, the resulting setting depends primarily on the indication "!important". Secondarily, if both are important, the user wins, if neither is, the author wins. Tertiarily it depends on specificity. Only lastly, it depends on order between and within style sheets: the last wins. Thus, a User:''username''/monobook.css does not win from MediaWiki:Monobook.css (both author, not user) if the specificity of the latter is greater. See also [http://www.w3.org/TR/REC-CSS2/cascade.html cascade].

== Supported elements ==
MediaWiki supports most CSS, with such exceptions as the url() attribute. There were some bugs in CSS support in earlier versions.

{{Further2|the [http://www.w3.org/TR/2011/REC-CSS2-20110607/ Cascading Style Sheets Level 2 Revision 1 (CSS 2.1) Specification]}}

== CSS in wikitext ==

You can use CSS styling in HTML elements in your code (see [[Help:HTML in wikitext]] for a list of elements supported by MediaWiki) like you would in normal HTML markup.

For example, a {{tag|div}} element with a green border and its contents floated to the right would be created with
&lt;syntaxhighlight lang="html"&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
&lt;/syntaxhighlight&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
which would produce the box to the right.
Some wikitext elements allow you to insert CSS styling directly into them. An example is the table syntax:
{{-}}
&lt;syntaxhighlight lang="wikitext"&gt;
{| style="your style here"
|-
|your table stuff
|}
&lt;/syntaxhighlight&gt;
{{Further2|[[Help:Tables]]}}

== MediaWiki existing styles ==

You may wish to use a style type that is already predefined by MediaWiki, or the site that you are visiting. You can also create a style that is unique to your page.

Vector is the default style, you can view it at: [[MediaWiki:Vector.css]]

You will give your CSS tag an existing "class"

Please put a list of existing classes here.

== Tips and tricks ==

=== Non-display ===
In an embedded page, one can hide comments in one version, and show them in another view.
One extreme "style" for a text is not displaying it, with

&lt;syntaxhighlight lang="css"&gt;
.''classname'' {display: none}
#''id'' {display: none}
&lt;/syntaxhighlight&gt;

etc.

Non-displayed links do not work (as opposed to links in a very small font).

It cannot be used to remove text in expressions for template names, parameter names, parameter values, page names in links, etc.

To view hidden text, download the Web Developer Toolbar for Firefox [https://addons.mozilla.org/en-US/firefox/addon/60 here], then choose Misc. &amp;rarr; show hidden elements in that toolbar. It will make all hidden elements appear.

=== Non-print ===

One can exclude content from being printed by declaring the content to be of the "noprint" class:
&lt;syntaxhighlight lang="html"&gt;
&lt;div class="someclass noprint"&gt;This will not appear in the print version.&lt;/div&gt;
&lt;/syntaxhighlight&gt;

== Major style blocks ==
[[File:Wikimedia major style blocks example.svg|right|thumb|An annotated desktop screenshot of [[Wikipedia:Example|an example article]], showcasing the style blocks]]
*''column-content'' – overall space within the margins of which the content exists.
* ''firstHeading'' – the class of the heading tag at the top of every page
*''contentSub'' – the name of the wiki immediately underneath the main heading, but above the body text
* ''content'' – the white background, thin bordered box which contains the main page content.
* ''bodyContent'' – the main page content within the content box

The portlet class is the style used by all the div blocks around the main content.  Identified blocks using that class:
*''p-cactions'' – id for the list of page-related tabs above the main content (page, talk, edit, etc.), top.
*''p-personal'' – id for the list of user-related links above the main content (username, talk, etc.), top.
*''p-logo'' – id for the block that contains the logo, top left.
*''p-navigation'' – id for the block that contains the navigation links on the left of the page
*''p-search'' – the block that contains the search buttons
*''p-tb'' – the block that contains the toolbox links
*''p-lang'' – the block that contains interlanguage links

The footer at the bottom of the page includes blocks with the following ids
*''footer'' – overall footer container block
*''f-poweredbyico'' – the powered by MediaWiki image that normally resides to the right of the page
*''f-list'' – id for the list that contains all the bits of text at the bottom of the page

== Style depending on a parameter or variable ==
=== Variable class or id ===

A class or id can depend on the result produced by a template or on a template parameter, e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1|def}}}"&lt;/syntaxhighlight&gt;. For one or more of the possible class names the style of that class can be defined. If the class is undefined it is ignored, so the standard style is used.

In the simplest case we have e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1}}}"&lt;/syntaxhighlight&gt; and define class abcdef. If the parameter value is "def" it applies.

If a page for general use only makes sense when styles are defined for certain classes, then these have to be specified in the page [[MediaWiki:Common.css]], which applies for all users and all skins, as far as not overridden.

=== Variable style parameter value ===

Wikitext that reads
&lt;syntaxhighlight lang="html"&gt;&lt;span style="display:{{{3|none}}}"&gt;Wed&lt;/span&gt;&lt;/syntaxhighlight&gt;
will display "Wed" if parameter 3 is defined, but its value is not "none", and displays nothing if parameter 3 is undefined or "none". If the value of parameter 3 is a display style other than "none", that style is applied.

==Wiki headings==
Wiki headings use the following default CSS:
&lt;section begin="HTML_section" /&gt;
{| class="wikitable"
|-
! Wikimarkup !! [[HTML]] !! Style
|-
| {{nowrap|1=&lt;code&gt;= Heading =&lt;/code&gt;}} || {{tag|h1|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.8em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;== Heading ==&lt;/code&gt;}} || {{tag|h2|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.5em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;=== Heading ===&lt;/code&gt;}} || {{tag|h3|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.2em; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;==== Heading ====&lt;/code&gt;}} || {{tag|h4|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;===== Heading =====&lt;/code&gt;}} || {{tag|h5|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;====== Heading ======&lt;/code&gt;}} || {{tag|h6|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
|}&lt;section end="HTML_section" /&gt;

== See also ==
*[[Span and div]]
*[[Cascading Style Sheets]] – article on CSS
*[[Wikipedia:Customisation]] – also covers user names, preferences settings, skins, user scripting, etc.
*[[Help:User style]] – modifying style for accessibility or for additional feature testing.
*[[Wikipedia:TemplateStyles]] – modifying style for advanced visual appearance that can be applied with template.
*[[Wikipedia:Catalogue of CSS classes]] – list of classes globally defined across the site
*[[Wikipedia:WikiProject Microformats/classes]] – list of classes used in microformats employed on Wikipedia
*[[Help:User CSS for a monospaced coding font]] – both for the editing window and for display of monospaced elements like {{tag|code|o}}
*[[meta:Help:Cascading_style_sheets]]
*[[mw:Manual:CSS]] and [[mw:Manual:Interface/Stylesheets]]
*[[mw:Gallery of user styles]]
*[[m:Customization:Explaining skins]]
*[[mw:Skin projects]]

{{Wikipedia technical help|collapsed}}
{{CSS and JS MediaWiki messages|state=collapsed|style=}}

[[Category:Wikipedia features]]
[[Category:Wikipedia help]]
[[Category:pages from wikipedia]][[en:Cascading Style Sheets]][[Category:pages from wikipedia]]</text>
      <sha1>lrys9rf1itx3yhvbmmw6lruai95ml83</sha1>
    </revision>
    <revision>
      <id>721108</id>
      <parentid>721107</parentid>
      <timestamp>2024-12-20T03:11:01Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Removing from [[Category:Pages from wikipedia]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>721108</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="13785" sha1="iaiy633z90zyxkj578csfjsosknaz5c" xml:space="preserve">{{Information page|H:CSS|WP:CSS}}
[[Cascading Style Sheets]] allows for flexible formatting of a page. They should be used instead of [[Help:Table|tables]] for non-tabular content whenever possible, because they can be [[Help:User style|manipulated by the reader]] or overridden by an author if your CSS is embedded in another page via a [[Help:Template|template]].

== Levels of CSS settings ==
Style may be chosen specifically for a piece of content, see e.g., [[Help:Table#Color; scope of parameters|color; scope of parameters]]

Alternatively, style is specified for CSS selectors, expressed in terms of elements, classes, and ID's. This is done on various levels:

Author style sheets, in this order:

''Note: See [[WP:CLASS]] for a list of all the style sheets loaded.''

===MediaWiki core style-sheets===

Per skin: [[mw:Gallery of user styles|MediaWiki Manual:Gallery of user styles]] etc. 
Typically loaded style sheets:
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/shared.css common/shared.css]
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/commonPrint.css common/commonPrint.css]

;Skin-specific main file:
e.g., [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/main.css monobook/main.css] (normal skin for PC's), [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/chick/main.css chick/main.css] (normal skin for handhelds)

;Browser-specific fixes (also skin-specific): 
Examples for Monobook:
*For Firefox: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/FF2Fixes.css monobook/FF2Fixes.css]
*For Internet Explorer: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE60Fixes.css monobook/IE60Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE70Fixes.css monobook/IE70Fixes.css]
*For Opera: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera6Fixes.css monobook/Opera6Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera7Fixes.css monobook/Opera7Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera9Fixes.css monobook/Opera9Fixes.css]

===Site-wide style-sheets===

* Site-wide for all skins (desktop view only): [[MediaWiki:Common.css]]
* Site-wide for all skins (mobile view only): [[MediaWiki:Mobile.css]]
* Site-wide per skin: [[MediaWiki:Vector.css]], [[MediaWiki:Monobook.css]], etc.
* Site-wide for user groups: [[MediaWiki:Group-user.css]], [[MediaWiki:Group-autoconfirmed.css]], etc.
* Site-wide for specialist purposes: [[MediaWiki:Print.css]], [[MediaWiki:Noscript.css]], [[MediaWiki:Filepage.css]]
* Site-wide if gadgets loaded: see [[Wikipedia:Gadget]] for more information

Note: MediaWiki sites other than English Wikipedia may use &lt;code&gt;MediaWiki:Gadget-site.css&lt;/code&gt; instead of MediaWiki:Common.css.

===Page-specific style-sheets===

Page-specific style-sheets may be introduced through [[Wikipedia:TemplateStyles|TemplateStyles]]. Pages, particularly templates, may also have inline CSS.

Some pages have their own CSS, e.g., [[MediaWiki:FileUploadWizard.css]]

===User-specific style-sheets===

* Global user-specific for all skins: [[meta:Special:MyPage/global.css]]
* User-specific for all skins: [[Special:MyPage/common.css]]
* User-specific per skin: e.g., [[Special:MyPage/vector.css]], or [[Special:MyPage/skin.css]] for your current skin 
* User-specific CSS loaded through JavaScript, e.g., loaded at [[Special:MyPage/common.js]]
* User-specific web-wide browser settings: local file referred to in the browser settings, or directly set in the browser

Note that in CSS terminology, the user-specific style sheets are ''not'' [[Help:User style|user style sheets]].

===Usage===

An HTML element may be just taken from the wikitext (see [[Help:HTML in wikitext|HTML in wikitext]]), e.g., span, or the result of translating wikitext, e.g., the &lt;code&gt;&lt;nowiki&gt;'''...'''&lt;/nowiki&gt;&lt;/code&gt; code is changed into {{tag|b}}, or part of the code for the skin.

A class may be produced by the software, e.g., ns-''namespace number'' for the HTML-element "body", and extiw for an interwiki link in the page body, or taken from the wikitext.

Similarly, an ID may be produced by the software, e.g., bodyContent, or taken from the wikitext.

In the case of conflicting style settings for a piece of content, the resulting setting depends primarily on the indication "!important". Secondarily, if both are important, the user wins, if neither is, the author wins. Tertiarily it depends on specificity. Only lastly, it depends on order between and within style sheets: the last wins. Thus, a User:''username''/monobook.css does not win from MediaWiki:Monobook.css (both author, not user) if the specificity of the latter is greater. See also [http://www.w3.org/TR/REC-CSS2/cascade.html cascade].

== Supported elements ==
MediaWiki supports most CSS, with such exceptions as the url() attribute. There were some bugs in CSS support in earlier versions.

{{Further2|the [http://www.w3.org/TR/2011/REC-CSS2-20110607/ Cascading Style Sheets Level 2 Revision 1 (CSS 2.1) Specification]}}

== CSS in wikitext ==

You can use CSS styling in HTML elements in your code (see [[Help:HTML in wikitext]] for a list of elements supported by MediaWiki) like you would in normal HTML markup.

For example, a {{tag|div}} element with a green border and its contents floated to the right would be created with
&lt;syntaxhighlight lang="html"&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
&lt;/syntaxhighlight&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
which would produce the box to the right.
Some wikitext elements allow you to insert CSS styling directly into them. An example is the table syntax:
{{-}}
&lt;syntaxhighlight lang="wikitext"&gt;
{| style="your style here"
|-
|your table stuff
|}
&lt;/syntaxhighlight&gt;
{{Further2|[[Help:Tables]]}}

== MediaWiki existing styles ==

You may wish to use a style type that is already predefined by MediaWiki, or the site that you are visiting. You can also create a style that is unique to your page.

Vector is the default style, you can view it at: [[MediaWiki:Vector.css]]

You will give your CSS tag an existing "class"

Please put a list of existing classes here.

== Tips and tricks ==

=== Non-display ===
In an embedded page, one can hide comments in one version, and show them in another view.
One extreme "style" for a text is not displaying it, with

&lt;syntaxhighlight lang="css"&gt;
.''classname'' {display: none}
#''id'' {display: none}
&lt;/syntaxhighlight&gt;

etc.

Non-displayed links do not work (as opposed to links in a very small font).

It cannot be used to remove text in expressions for template names, parameter names, parameter values, page names in links, etc.

To view hidden text, download the Web Developer Toolbar for Firefox [https://addons.mozilla.org/en-US/firefox/addon/60 here], then choose Misc. &amp;rarr; show hidden elements in that toolbar. It will make all hidden elements appear.

=== Non-print ===

One can exclude content from being printed by declaring the content to be of the "noprint" class:
&lt;syntaxhighlight lang="html"&gt;
&lt;div class="someclass noprint"&gt;This will not appear in the print version.&lt;/div&gt;
&lt;/syntaxhighlight&gt;

== Major style blocks ==
[[File:Wikimedia major style blocks example.svg|right|thumb|An annotated desktop screenshot of [[Wikipedia:Example|an example article]], showcasing the style blocks]]
*''column-content'' – overall space within the margins of which the content exists.
* ''firstHeading'' – the class of the heading tag at the top of every page
*''contentSub'' – the name of the wiki immediately underneath the main heading, but above the body text
* ''content'' – the white background, thin bordered box which contains the main page content.
* ''bodyContent'' – the main page content within the content box

The portlet class is the style used by all the div blocks around the main content.  Identified blocks using that class:
*''p-cactions'' – id for the list of page-related tabs above the main content (page, talk, edit, etc.), top.
*''p-personal'' – id for the list of user-related links above the main content (username, talk, etc.), top.
*''p-logo'' – id for the block that contains the logo, top left.
*''p-navigation'' – id for the block that contains the navigation links on the left of the page
*''p-search'' – the block that contains the search buttons
*''p-tb'' – the block that contains the toolbox links
*''p-lang'' – the block that contains interlanguage links

The footer at the bottom of the page includes blocks with the following ids
*''footer'' – overall footer container block
*''f-poweredbyico'' – the powered by MediaWiki image that normally resides to the right of the page
*''f-list'' – id for the list that contains all the bits of text at the bottom of the page

== Style depending on a parameter or variable ==
=== Variable class or id ===

A class or id can depend on the result produced by a template or on a template parameter, e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1|def}}}"&lt;/syntaxhighlight&gt;. For one or more of the possible class names the style of that class can be defined. If the class is undefined it is ignored, so the standard style is used.

In the simplest case we have e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1}}}"&lt;/syntaxhighlight&gt; and define class abcdef. If the parameter value is "def" it applies.

If a page for general use only makes sense when styles are defined for certain classes, then these have to be specified in the page [[MediaWiki:Common.css]], which applies for all users and all skins, as far as not overridden.

=== Variable style parameter value ===

Wikitext that reads
&lt;syntaxhighlight lang="html"&gt;&lt;span style="display:{{{3|none}}}"&gt;Wed&lt;/span&gt;&lt;/syntaxhighlight&gt;
will display "Wed" if parameter 3 is defined, but its value is not "none", and displays nothing if parameter 3 is undefined or "none". If the value of parameter 3 is a display style other than "none", that style is applied.

==Wiki headings==
Wiki headings use the following default CSS:
&lt;section begin="HTML_section" /&gt;
{| class="wikitable"
|-
! Wikimarkup !! [[HTML]] !! Style
|-
| {{nowrap|1=&lt;code&gt;= Heading =&lt;/code&gt;}} || {{tag|h1|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.8em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;== Heading ==&lt;/code&gt;}} || {{tag|h2|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.5em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;=== Heading ===&lt;/code&gt;}} || {{tag|h3|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.2em; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;==== Heading ====&lt;/code&gt;}} || {{tag|h4|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;===== Heading =====&lt;/code&gt;}} || {{tag|h5|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;====== Heading ======&lt;/code&gt;}} || {{tag|h6|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
|}&lt;section end="HTML_section" /&gt;

== See also ==
*[[Span and div]]
*[[Cascading Style Sheets]] – article on CSS
*[[Wikipedia:Customisation]] – also covers user names, preferences settings, skins, user scripting, etc.
*[[Help:User style]] – modifying style for accessibility or for additional feature testing.
*[[Wikipedia:TemplateStyles]] – modifying style for advanced visual appearance that can be applied with template.
*[[Wikipedia:Catalogue of CSS classes]] – list of classes globally defined across the site
*[[Wikipedia:WikiProject Microformats/classes]] – list of classes used in microformats employed on Wikipedia
*[[Help:User CSS for a monospaced coding font]] – both for the editing window and for display of monospaced elements like {{tag|code|o}}
*[[meta:Help:Cascading_style_sheets]]
*[[mw:Manual:CSS]] and [[mw:Manual:Interface/Stylesheets]]
*[[mw:Gallery of user styles]]
*[[m:Customization:Explaining skins]]
*[[mw:Skin projects]]

{{Wikipedia technical help|collapsed}}
{{CSS and JS MediaWiki messages|state=collapsed|style=}}

[[Category:Wikipedia features]]
[[Category:Wikipedia help]]
[[en:Cascading Style Sheets]]</text>
      <sha1>iaiy633z90zyxkj578csfjsosknaz5c</sha1>
    </revision>
    <revision>
      <id>721109</id>
      <parentid>721108</parentid>
      <timestamp>2025-03-11T20:37:33Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:List of Shikinaisha]] to [[Category:qq]][[sn:{{subst:PAGENAME}}]][[Category:qq]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>721109</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="13845" sha1="ghceh5ek735qcve3ze7ouo6ca0y0tmz" xml:space="preserve">{{Information page|H:CSS|WP:CSS}}
[[Cascading Style Sheets]] allows for flexible formatting of a page. They should be used instead of [[Help:Table|tables]] for non-tabular content whenever possible, because they can be [[Help:User style|manipulated by the reader]] or overridden by an author if your CSS is embedded in another page via a [[Help:Template|template]].

== Levels of CSS settings ==
Style may be chosen specifically for a piece of content, see e.g., [[Help:Table#Color; scope of parameters|color; scope of parameters]]

Alternatively, style is specified for CSS selectors, expressed in terms of elements, classes, and ID's. This is done on various levels:

Author style sheets, in this order:

''Note: See [[WP:CLASS]] for a list of all the style sheets loaded.''

===MediaWiki core style-sheets===

Per skin: [[mw:Gallery of user styles|MediaWiki Manual:Gallery of user styles]] etc. 
Typically loaded style sheets:
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/shared.css common/shared.css]
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/commonPrint.css common/commonPrint.css]

;Skin-specific main file:
e.g., [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/main.css monobook/main.css] (normal skin for PC's), [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/chick/main.css chick/main.css] (normal skin for handhelds)

;Browser-specific fixes (also skin-specific): 
Examples for Monobook:
*For Firefox: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/FF2Fixes.css monobook/FF2Fixes.css]
*For Internet Explorer: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE60Fixes.css monobook/IE60Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE70Fixes.css monobook/IE70Fixes.css]
*For Opera: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera6Fixes.css monobook/Opera6Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera7Fixes.css monobook/Opera7Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera9Fixes.css monobook/Opera9Fixes.css]

===Site-wide style-sheets===

* Site-wide for all skins (desktop view only): [[MediaWiki:Common.css]]
* Site-wide for all skins (mobile view only): [[MediaWiki:Mobile.css]]
* Site-wide per skin: [[MediaWiki:Vector.css]], [[MediaWiki:Monobook.css]], etc.
* Site-wide for user groups: [[MediaWiki:Group-user.css]], [[MediaWiki:Group-autoconfirmed.css]], etc.
* Site-wide for specialist purposes: [[MediaWiki:Print.css]], [[MediaWiki:Noscript.css]], [[MediaWiki:Filepage.css]]
* Site-wide if gadgets loaded: see [[Wikipedia:Gadget]] for more information

Note: MediaWiki sites other than English Wikipedia may use &lt;code&gt;MediaWiki:Gadget-site.css&lt;/code&gt; instead of MediaWiki:Common.css.

===Page-specific style-sheets===

Page-specific style-sheets may be introduced through [[Wikipedia:TemplateStyles|TemplateStyles]]. Pages, particularly templates, may also have inline CSS.

Some pages have their own CSS, e.g., [[MediaWiki:FileUploadWizard.css]]

===User-specific style-sheets===

* Global user-specific for all skins: [[meta:Special:MyPage/global.css]]
* User-specific for all skins: [[Special:MyPage/common.css]]
* User-specific per skin: e.g., [[Special:MyPage/vector.css]], or [[Special:MyPage/skin.css]] for your current skin 
* User-specific CSS loaded through JavaScript, e.g., loaded at [[Special:MyPage/common.js]]
* User-specific web-wide browser settings: local file referred to in the browser settings, or directly set in the browser

Note that in CSS terminology, the user-specific style sheets are ''not'' [[Help:User style|user style sheets]].

===Usage===

An HTML element may be just taken from the wikitext (see [[Help:HTML in wikitext|HTML in wikitext]]), e.g., span, or the result of translating wikitext, e.g., the &lt;code&gt;&lt;nowiki&gt;'''...'''&lt;/nowiki&gt;&lt;/code&gt; code is changed into {{tag|b}}, or part of the code for the skin.

A class may be produced by the software, e.g., ns-''namespace number'' for the HTML-element "body", and extiw for an interwiki link in the page body, or taken from the wikitext.

Similarly, an ID may be produced by the software, e.g., bodyContent, or taken from the wikitext.

In the case of conflicting style settings for a piece of content, the resulting setting depends primarily on the indication "!important". Secondarily, if both are important, the user wins, if neither is, the author wins. Tertiarily it depends on specificity. Only lastly, it depends on order between and within style sheets: the last wins. Thus, a User:''username''/monobook.css does not win from MediaWiki:Monobook.css (both author, not user) if the specificity of the latter is greater. See also [http://www.w3.org/TR/REC-CSS2/cascade.html cascade].

== Supported elements ==
MediaWiki supports most CSS, with such exceptions as the url() attribute. There were some bugs in CSS support in earlier versions.

{{Further2|the [http://www.w3.org/TR/2011/REC-CSS2-20110607/ Cascading Style Sheets Level 2 Revision 1 (CSS 2.1) Specification]}}

== CSS in wikitext ==

You can use CSS styling in HTML elements in your code (see [[Help:HTML in wikitext]] for a list of elements supported by MediaWiki) like you would in normal HTML markup.

For example, a {{tag|div}} element with a green border and its contents floated to the right would be created with
&lt;syntaxhighlight lang="html"&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
&lt;/syntaxhighlight&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
which would produce the box to the right.
Some wikitext elements allow you to insert CSS styling directly into them. An example is the table syntax:
{{-}}
&lt;syntaxhighlight lang="wikitext"&gt;
{| style="your style here"
|-
|your table stuff
|}
&lt;/syntaxhighlight&gt;
{{Further2|[[Help:Tables]]}}

== MediaWiki existing styles ==

You may wish to use a style type that is already predefined by MediaWiki, or the site that you are visiting. You can also create a style that is unique to your page.

Vector is the default style, you can view it at: [[MediaWiki:Vector.css]]

You will give your CSS tag an existing "class"

Please put a list of existing classes here.

== Tips and tricks ==

=== Non-display ===
In an embedded page, one can hide comments in one version, and show them in another view.
One extreme "style" for a text is not displaying it, with

&lt;syntaxhighlight lang="css"&gt;
.''classname'' {display: none}
#''id'' {display: none}
&lt;/syntaxhighlight&gt;

etc.

Non-displayed links do not work (as opposed to links in a very small font).

It cannot be used to remove text in expressions for template names, parameter names, parameter values, page names in links, etc.

To view hidden text, download the Web Developer Toolbar for Firefox [https://addons.mozilla.org/en-US/firefox/addon/60 here], then choose Misc. &amp;rarr; show hidden elements in that toolbar. It will make all hidden elements appear.

=== Non-print ===

One can exclude content from being printed by declaring the content to be of the "noprint" class:
&lt;syntaxhighlight lang="html"&gt;
&lt;div class="someclass noprint"&gt;This will not appear in the print version.&lt;/div&gt;
&lt;/syntaxhighlight&gt;

== Major style blocks ==
[[File:Wikimedia major style blocks example.svg|right|thumb|An annotated desktop screenshot of [[Wikipedia:Example|an example article]], showcasing the style blocks]]
*''column-content'' – overall space within the margins of which the content exists.
* ''firstHeading'' – the class of the heading tag at the top of every page
*''contentSub'' – the name of the wiki immediately underneath the main heading, but above the body text
* ''content'' – the white background, thin bordered box which contains the main page content.
* ''bodyContent'' – the main page content within the content box

The portlet class is the style used by all the div blocks around the main content.  Identified blocks using that class:
*''p-cactions'' – id for the list of page-related tabs above the main content (page, talk, edit, etc.), top.
*''p-personal'' – id for the list of user-related links above the main content (username, talk, etc.), top.
*''p-logo'' – id for the block that contains the logo, top left.
*''p-navigation'' – id for the block that contains the navigation links on the left of the page
*''p-search'' – the block that contains the search buttons
*''p-tb'' – the block that contains the toolbox links
*''p-lang'' – the block that contains interlanguage links

The footer at the bottom of the page includes blocks with the following ids
*''footer'' – overall footer container block
*''f-poweredbyico'' – the powered by MediaWiki image that normally resides to the right of the page
*''f-list'' – id for the list that contains all the bits of text at the bottom of the page

== Style depending on a parameter or variable ==
=== Variable class or id ===

A class or id can depend on the result produced by a template or on a template parameter, e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1|def}}}"&lt;/syntaxhighlight&gt;. For one or more of the possible class names the style of that class can be defined. If the class is undefined it is ignored, so the standard style is used.

In the simplest case we have e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1}}}"&lt;/syntaxhighlight&gt; and define class abcdef. If the parameter value is "def" it applies.

If a page for general use only makes sense when styles are defined for certain classes, then these have to be specified in the page [[MediaWiki:Common.css]], which applies for all users and all skins, as far as not overridden.

=== Variable style parameter value ===

Wikitext that reads
&lt;syntaxhighlight lang="html"&gt;&lt;span style="display:{{{3|none}}}"&gt;Wed&lt;/span&gt;&lt;/syntaxhighlight&gt;
will display "Wed" if parameter 3 is defined, but its value is not "none", and displays nothing if parameter 3 is undefined or "none". If the value of parameter 3 is a display style other than "none", that style is applied.

==Wiki headings==
Wiki headings use the following default CSS:
&lt;section begin="HTML_section" /&gt;
{| class="wikitable"
|-
! Wikimarkup !! [[HTML]] !! Style
|-
| {{nowrap|1=&lt;code&gt;= Heading =&lt;/code&gt;}} || {{tag|h1|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.8em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;== Heading ==&lt;/code&gt;}} || {{tag|h2|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.5em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;=== Heading ===&lt;/code&gt;}} || {{tag|h3|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.2em; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;==== Heading ====&lt;/code&gt;}} || {{tag|h4|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;===== Heading =====&lt;/code&gt;}} || {{tag|h5|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;====== Heading ======&lt;/code&gt;}} || {{tag|h6|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
|}&lt;section end="HTML_section" /&gt;

== See also ==
*[[Span and div]]
*[[Cascading Style Sheets]] – article on CSS
*[[Wikipedia:Customisation]] – also covers user names, preferences settings, skins, user scripting, etc.
*[[Help:User style]] – modifying style for accessibility or for additional feature testing.
*[[Wikipedia:TemplateStyles]] – modifying style for advanced visual appearance that can be applied with template.
*[[Wikipedia:Catalogue of CSS classes]] – list of classes globally defined across the site
*[[Wikipedia:WikiProject Microformats/classes]] – list of classes used in microformats employed on Wikipedia
*[[Help:User CSS for a monospaced coding font]] – both for the editing window and for display of monospaced elements like {{tag|code|o}}
*[[meta:Help:Cascading_style_sheets]]
*[[mw:Manual:CSS]] and [[mw:Manual:Interface/Stylesheets]]
*[[mw:Gallery of user styles]]
*[[m:Customization:Explaining skins]]
*[[mw:Skin projects]]

{{Wikipedia technical help|collapsed}}
{{CSS and JS MediaWiki messages|state=collapsed|style=}}

[[Category:Wikipedia features]]
[[Category:Wikipedia help]]
[[Category:qq]][[sn:Cascading Style Sheets]][[Category:qq]]
[[en:Cascading Style Sheets]]</text>
      <sha1>ghceh5ek735qcve3ze7ouo6ca0y0tmz</sha1>
    </revision>
    <revision>
      <id>721110</id>
      <parentid>721109</parentid>
      <timestamp>2025-03-19T20:47:50Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "qq" to "11"</comment>
      <origin>721110</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="13845" sha1="nlmy4xhp2zodko1466mdcs31kokoei6" xml:space="preserve">{{Information page|H:CSS|WP:CSS}}
[[Cascading Style Sheets]] allows for flexible formatting of a page. They should be used instead of [[Help:Table|tables]] for non-tabular content whenever possible, because they can be [[Help:User style|manipulated by the reader]] or overridden by an author if your CSS is embedded in another page via a [[Help:Template|template]].

== Levels of CSS settings ==
Style may be chosen specifically for a piece of content, see e.g., [[Help:Table#Color; scope of parameters|color; scope of parameters]]

Alternatively, style is specified for CSS selectors, expressed in terms of elements, classes, and ID's. This is done on various levels:

Author style sheets, in this order:

''Note: See [[WP:CLASS]] for a list of all the style sheets loaded.''

===MediaWiki core style-sheets===

Per skin: [[mw:Gallery of user styles|MediaWiki Manual:Gallery of user styles]] etc. 
Typically loaded style sheets:
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/shared.css common/shared.css]
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/commonPrint.css common/commonPrint.css]

;Skin-specific main file:
e.g., [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/main.css monobook/main.css] (normal skin for PC's), [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/chick/main.css chick/main.css] (normal skin for handhelds)

;Browser-specific fixes (also skin-specific): 
Examples for Monobook:
*For Firefox: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/FF2Fixes.css monobook/FF2Fixes.css]
*For Internet Explorer: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE60Fixes.css monobook/IE60Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE70Fixes.css monobook/IE70Fixes.css]
*For Opera: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera6Fixes.css monobook/Opera6Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera7Fixes.css monobook/Opera7Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera9Fixes.css monobook/Opera9Fixes.css]

===Site-wide style-sheets===

* Site-wide for all skins (desktop view only): [[MediaWiki:Common.css]]
* Site-wide for all skins (mobile view only): [[MediaWiki:Mobile.css]]
* Site-wide per skin: [[MediaWiki:Vector.css]], [[MediaWiki:Monobook.css]], etc.
* Site-wide for user groups: [[MediaWiki:Group-user.css]], [[MediaWiki:Group-autoconfirmed.css]], etc.
* Site-wide for specialist purposes: [[MediaWiki:Print.css]], [[MediaWiki:Noscript.css]], [[MediaWiki:Filepage.css]]
* Site-wide if gadgets loaded: see [[Wikipedia:Gadget]] for more information

Note: MediaWiki sites other than English Wikipedia may use &lt;code&gt;MediaWiki:Gadget-site.css&lt;/code&gt; instead of MediaWiki:Common.css.

===Page-specific style-sheets===

Page-specific style-sheets may be introduced through [[Wikipedia:TemplateStyles|TemplateStyles]]. Pages, particularly templates, may also have inline CSS.

Some pages have their own CSS, e.g., [[MediaWiki:FileUploadWizard.css]]

===User-specific style-sheets===

* Global user-specific for all skins: [[meta:Special:MyPage/global.css]]
* User-specific for all skins: [[Special:MyPage/common.css]]
* User-specific per skin: e.g., [[Special:MyPage/vector.css]], or [[Special:MyPage/skin.css]] for your current skin 
* User-specific CSS loaded through JavaScript, e.g., loaded at [[Special:MyPage/common.js]]
* User-specific web-wide browser settings: local file referred to in the browser settings, or directly set in the browser

Note that in CSS terminology, the user-specific style sheets are ''not'' [[Help:User style|user style sheets]].

===Usage===

An HTML element may be just taken from the wikitext (see [[Help:HTML in wikitext|HTML in wikitext]]), e.g., span, or the result of translating wikitext, e.g., the &lt;code&gt;&lt;nowiki&gt;'''...'''&lt;/nowiki&gt;&lt;/code&gt; code is changed into {{tag|b}}, or part of the code for the skin.

A class may be produced by the software, e.g., ns-''namespace number'' for the HTML-element "body", and extiw for an interwiki link in the page body, or taken from the wikitext.

Similarly, an ID may be produced by the software, e.g., bodyContent, or taken from the wikitext.

In the case of conflicting style settings for a piece of content, the resulting setting depends primarily on the indication "!important". Secondarily, if both are important, the user wins, if neither is, the author wins. Tertiarily it depends on specificity. Only lastly, it depends on order between and within style sheets: the last wins. Thus, a User:''username''/monobook.css does not win from MediaWiki:Monobook.css (both author, not user) if the specificity of the latter is greater. See also [http://www.w3.org/TR/REC-CSS2/cascade.html cascade].

== Supported elements ==
MediaWiki supports most CSS, with such exceptions as the url() attribute. There were some bugs in CSS support in earlier versions.

{{Further2|the [http://www.w3.org/TR/2011/REC-CSS2-20110607/ Cascading Style Sheets Level 2 Revision 1 (CSS 2.1) Specification]}}

== CSS in wikitext ==

You can use CSS styling in HTML elements in your code (see [[Help:HTML in wikitext]] for a list of elements supported by MediaWiki) like you would in normal HTML markup.

For example, a {{tag|div}} element with a green border and its contents floated to the right would be created with
&lt;syntaxhighlight lang="html"&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
&lt;/syntaxhighlight&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
which would produce the box to the right.
Some wikitext elements allow you to insert CSS styling directly into them. An example is the table syntax:
{{-}}
&lt;syntaxhighlight lang="wikitext"&gt;
{| style="your style here"
|-
|your table stuff
|}
&lt;/syntaxhighlight&gt;
{{Further2|[[Help:Tables]]}}

== MediaWiki existing styles ==

You may wish to use a style type that is already predefined by MediaWiki, or the site that you are visiting. You can also create a style that is unique to your page.

Vector is the default style, you can view it at: [[MediaWiki:Vector.css]]

You will give your CSS tag an existing "class"

Please put a list of existing classes here.

== Tips and tricks ==

=== Non-display ===
In an embedded page, one can hide comments in one version, and show them in another view.
One extreme "style" for a text is not displaying it, with

&lt;syntaxhighlight lang="css"&gt;
.''classname'' {display: none}
#''id'' {display: none}
&lt;/syntaxhighlight&gt;

etc.

Non-displayed links do not work (as opposed to links in a very small font).

It cannot be used to remove text in expressions for template names, parameter names, parameter values, page names in links, etc.

To view hidden text, download the Web Developer Toolbar for Firefox [https://addons.mozilla.org/en-US/firefox/addon/60 here], then choose Misc. &amp;rarr; show hidden elements in that toolbar. It will make all hidden elements appear.

=== Non-print ===

One can exclude content from being printed by declaring the content to be of the "noprint" class:
&lt;syntaxhighlight lang="html"&gt;
&lt;div class="someclass noprint"&gt;This will not appear in the print version.&lt;/div&gt;
&lt;/syntaxhighlight&gt;

== Major style blocks ==
[[File:Wikimedia major style blocks example.svg|right|thumb|An annotated desktop screenshot of [[Wikipedia:Example|an example article]], showcasing the style blocks]]
*''column-content'' – overall space within the margins of which the content exists.
* ''firstHeading'' – the class of the heading tag at the top of every page
*''contentSub'' – the name of the wiki immediately underneath the main heading, but above the body text
* ''content'' – the white background, thin bordered box which contains the main page content.
* ''bodyContent'' – the main page content within the content box

The portlet class is the style used by all the div blocks around the main content.  Identified blocks using that class:
*''p-cactions'' – id for the list of page-related tabs above the main content (page, talk, edit, etc.), top.
*''p-personal'' – id for the list of user-related links above the main content (username, talk, etc.), top.
*''p-logo'' – id for the block that contains the logo, top left.
*''p-navigation'' – id for the block that contains the navigation links on the left of the page
*''p-search'' – the block that contains the search buttons
*''p-tb'' – the block that contains the toolbox links
*''p-lang'' – the block that contains interlanguage links

The footer at the bottom of the page includes blocks with the following ids
*''footer'' – overall footer container block
*''f-poweredbyico'' – the powered by MediaWiki image that normally resides to the right of the page
*''f-list'' – id for the list that contains all the bits of text at the bottom of the page

== Style depending on a parameter or variable ==
=== Variable class or id ===

A class or id can depend on the result produced by a template or on a template parameter, e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1|def}}}"&lt;/syntaxhighlight&gt;. For one or more of the possible class names the style of that class can be defined. If the class is undefined it is ignored, so the standard style is used.

In the simplest case we have e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1}}}"&lt;/syntaxhighlight&gt; and define class abcdef. If the parameter value is "def" it applies.

If a page for general use only makes sense when styles are defined for certain classes, then these have to be specified in the page [[MediaWiki:Common.css]], which applies for all users and all skins, as far as not overridden.

=== Variable style parameter value ===

Wikitext that reads
&lt;syntaxhighlight lang="html"&gt;&lt;span style="display:{{{3|none}}}"&gt;Wed&lt;/span&gt;&lt;/syntaxhighlight&gt;
will display "Wed" if parameter 3 is defined, but its value is not "none", and displays nothing if parameter 3 is undefined or "none". If the value of parameter 3 is a display style other than "none", that style is applied.

==Wiki headings==
Wiki headings use the following default CSS:
&lt;section begin="HTML_section" /&gt;
{| class="wikitable"
|-
! Wikimarkup !! [[HTML]] !! Style
|-
| {{nowrap|1=&lt;code&gt;= Heading =&lt;/code&gt;}} || {{tag|h1|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.8em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;== Heading ==&lt;/code&gt;}} || {{tag|h2|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.5em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;=== Heading ===&lt;/code&gt;}} || {{tag|h3|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.2em; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;==== Heading ====&lt;/code&gt;}} || {{tag|h4|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;===== Heading =====&lt;/code&gt;}} || {{tag|h5|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;====== Heading ======&lt;/code&gt;}} || {{tag|h6|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
|}&lt;section end="HTML_section" /&gt;

== See also ==
*[[Span and div]]
*[[Cascading Style Sheets]] – article on CSS
*[[Wikipedia:Customisation]] – also covers user names, preferences settings, skins, user scripting, etc.
*[[Help:User style]] – modifying style for accessibility or for additional feature testing.
*[[Wikipedia:TemplateStyles]] – modifying style for advanced visual appearance that can be applied with template.
*[[Wikipedia:Catalogue of CSS classes]] – list of classes globally defined across the site
*[[Wikipedia:WikiProject Microformats/classes]] – list of classes used in microformats employed on Wikipedia
*[[Help:User CSS for a monospaced coding font]] – both for the editing window and for display of monospaced elements like {{tag|code|o}}
*[[meta:Help:Cascading_style_sheets]]
*[[mw:Manual:CSS]] and [[mw:Manual:Interface/Stylesheets]]
*[[mw:Gallery of user styles]]
*[[m:Customization:Explaining skins]]
*[[mw:Skin projects]]

{{Wikipedia technical help|collapsed}}
{{CSS and JS MediaWiki messages|state=collapsed|style=}}

[[Category:Wikipedia features]]
[[Category:Wikipedia help]]
[[Category:11]][[sn:Cascading Style Sheets]][[Category:11]]
[[en:Cascading Style Sheets]]</text>
      <sha1>nlmy4xhp2zodko1466mdcs31kokoei6</sha1>
    </revision>
    <revision>
      <id>721111</id>
      <parentid>721110</parentid>
      <timestamp>2025-03-22T06:43:31Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>721111</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="13862" sha1="bfqq66miw63p4kbtltjpfplxtya9aoq" xml:space="preserve">{{Information page|H:CSS|WP:CSS}}
[[Cascading Style Sheets]] allows for flexible formatting of a page. They should be used instead of [[Help:Table|tables]] for non-tabular content whenever possible, because they can be [[Help:User style|manipulated by the reader]] or overridden by an author if your CSS is embedded in another page via a [[Help:Template|template]].

== Levels of CSS settings ==
Style may be chosen specifically for a piece of content, see e.g., [[Help:Table#Color; scope of parameters|color; scope of parameters]]

Alternatively, style is specified for CSS selectors, expressed in terms of elements, classes, and ID's. This is done on various levels:

Author style sheets, in this order:

''Note: See [[WP:CLASS]] for a list of all the style sheets loaded.''

===MediaWiki core style-sheets===

Per skin: [[mw:Gallery of user styles|MediaWiki Manual:Gallery of user styles]] etc. 
Typically loaded style sheets:
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/shared.css common/shared.css]
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/commonPrint.css common/commonPrint.css]

;Skin-specific main file:
e.g., [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/main.css monobook/main.css] (normal skin for PC's), [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/chick/main.css chick/main.css] (normal skin for handhelds)

;Browser-specific fixes (also skin-specific): 
Examples for Monobook:
*For Firefox: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/FF2Fixes.css monobook/FF2Fixes.css]
*For Internet Explorer: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE60Fixes.css monobook/IE60Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE70Fixes.css monobook/IE70Fixes.css]
*For Opera: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera6Fixes.css monobook/Opera6Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera7Fixes.css monobook/Opera7Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera9Fixes.css monobook/Opera9Fixes.css]

===Site-wide style-sheets===

* Site-wide for all skins (desktop view only): [[MediaWiki:Common.css]]
* Site-wide for all skins (mobile view only): [[MediaWiki:Mobile.css]]
* Site-wide per skin: [[MediaWiki:Vector.css]], [[MediaWiki:Monobook.css]], etc.
* Site-wide for user groups: [[MediaWiki:Group-user.css]], [[MediaWiki:Group-autoconfirmed.css]], etc.
* Site-wide for specialist purposes: [[MediaWiki:Print.css]], [[MediaWiki:Noscript.css]], [[MediaWiki:Filepage.css]]
* Site-wide if gadgets loaded: see [[Wikipedia:Gadget]] for more information

Note: MediaWiki sites other than English Wikipedia may use &lt;code&gt;MediaWiki:Gadget-site.css&lt;/code&gt; instead of MediaWiki:Common.css.

===Page-specific style-sheets===

Page-specific style-sheets may be introduced through [[Wikipedia:TemplateStyles|TemplateStyles]]. Pages, particularly templates, may also have inline CSS.

Some pages have their own CSS, e.g., [[MediaWiki:FileUploadWizard.css]]

===User-specific style-sheets===

* Global user-specific for all skins: [[meta:Special:MyPage/global.css]]
* User-specific for all skins: [[Special:MyPage/common.css]]
* User-specific per skin: e.g., [[Special:MyPage/vector.css]], or [[Special:MyPage/skin.css]] for your current skin 
* User-specific CSS loaded through JavaScript, e.g., loaded at [[Special:MyPage/common.js]]
* User-specific web-wide browser settings: local file referred to in the browser settings, or directly set in the browser

Note that in CSS terminology, the user-specific style sheets are ''not'' [[Help:User style|user style sheets]].

===Usage===

An HTML element may be just taken from the wikitext (see [[Help:HTML in wikitext|HTML in wikitext]]), e.g., span, or the result of translating wikitext, e.g., the &lt;code&gt;&lt;nowiki&gt;'''...'''&lt;/nowiki&gt;&lt;/code&gt; code is changed into {{tag|b}}, or part of the code for the skin.

A class may be produced by the software, e.g., ns-''namespace number'' for the HTML-element "body", and extiw for an interwiki link in the page body, or taken from the wikitext.

Similarly, an ID may be produced by the software, e.g., bodyContent, or taken from the wikitext.

In the case of conflicting style settings for a piece of content, the resulting setting depends primarily on the indication "!important". Secondarily, if both are important, the user wins, if neither is, the author wins. Tertiarily it depends on specificity. Only lastly, it depends on order between and within style sheets: the last wins. Thus, a User:''username''/monobook.css does not win from MediaWiki:Monobook.css (both author, not user) if the specificity of the latter is greater. See also [http://www.w3.org/TR/REC-CSS2/cascade.html cascade].

== Supported elements ==
MediaWiki supports most CSS, with such exceptions as the url() attribute. There were some bugs in CSS support in earlier versions.

{{Further2|the [http://www.w3.org/TR/2011/REC-CSS2-20110607/ Cascading Style Sheets Level 2 Revision 1 (CSS 2.1) Specification]}}

== CSS in wikitext ==

You can use CSS styling in HTML elements in your code (see [[Help:HTML in wikitext]] for a list of elements supported by MediaWiki) like you would in normal HTML markup.

For example, a {{tag|div}} element with a green border and its contents floated to the right would be created with
&lt;syntaxhighlight lang="html"&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
&lt;/syntaxhighlight&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
which would produce the box to the right.
Some wikitext elements allow you to insert CSS styling directly into them. An example is the table syntax:
{{-}}
&lt;syntaxhighlight lang="wikitext"&gt;
{| style="your style here"
|-
|your table stuff
|}
&lt;/syntaxhighlight&gt;
{{Further2|[[Help:Tables]]}}

== MediaWiki existing styles ==

You may wish to use a style type that is already predefined by MediaWiki, or the site that you are visiting. You can also create a style that is unique to your page.

Vector is the default style, you can view it at: [[MediaWiki:Vector.css]]

You will give your CSS tag an existing "class"

Please put a list of existing classes here.

== Tips and tricks ==

=== Non-display ===
In an embedded page, one can hide comments in one version, and show them in another view.
One extreme "style" for a text is not displaying it, with

&lt;syntaxhighlight lang="css"&gt;
.''classname'' {display: none}
#''id'' {display: none}
&lt;/syntaxhighlight&gt;

etc.

Non-displayed links do not work (as opposed to links in a very small font).

It cannot be used to remove text in expressions for template names, parameter names, parameter values, page names in links, etc.

To view hidden text, download the Web Developer Toolbar for Firefox [https://addons.mozilla.org/en-US/firefox/addon/60 here], then choose Misc. &amp;rarr; show hidden elements in that toolbar. It will make all hidden elements appear.

=== Non-print ===

One can exclude content from being printed by declaring the content to be of the "noprint" class:
&lt;syntaxhighlight lang="html"&gt;
&lt;div class="someclass noprint"&gt;This will not appear in the print version.&lt;/div&gt;
&lt;/syntaxhighlight&gt;

== Major style blocks ==
[[File:Wikimedia major style blocks example.svg|right|thumb|An annotated desktop screenshot of [[Wikipedia:Example|an example article]], showcasing the style blocks]]
*''column-content'' – overall space within the margins of which the content exists.
* ''firstHeading'' – the class of the heading tag at the top of every page
*''contentSub'' – the name of the wiki immediately underneath the main heading, but above the body text
* ''content'' – the white background, thin bordered box which contains the main page content.
* ''bodyContent'' – the main page content within the content box

The portlet class is the style used by all the div blocks around the main content.  Identified blocks using that class:
*''p-cactions'' – id for the list of page-related tabs above the main content (page, talk, edit, etc.), top.
*''p-personal'' – id for the list of user-related links above the main content (username, talk, etc.), top.
*''p-logo'' – id for the block that contains the logo, top left.
*''p-navigation'' – id for the block that contains the navigation links on the left of the page
*''p-search'' – the block that contains the search buttons
*''p-tb'' – the block that contains the toolbox links
*''p-lang'' – the block that contains interlanguage links

The footer at the bottom of the page includes blocks with the following ids
*''footer'' – overall footer container block
*''f-poweredbyico'' – the powered by MediaWiki image that normally resides to the right of the page
*''f-list'' – id for the list that contains all the bits of text at the bottom of the page

== Style depending on a parameter or variable ==
=== Variable class or id ===

A class or id can depend on the result produced by a template or on a template parameter, e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1|def}}}"&lt;/syntaxhighlight&gt;. For one or more of the possible class names the style of that class can be defined. If the class is undefined it is ignored, so the standard style is used.

In the simplest case we have e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1}}}"&lt;/syntaxhighlight&gt; and define class abcdef. If the parameter value is "def" it applies.

If a page for general use only makes sense when styles are defined for certain classes, then these have to be specified in the page [[MediaWiki:Common.css]], which applies for all users and all skins, as far as not overridden.

=== Variable style parameter value ===

Wikitext that reads
&lt;syntaxhighlight lang="html"&gt;&lt;span style="display:{{{3|none}}}"&gt;Wed&lt;/span&gt;&lt;/syntaxhighlight&gt;
will display "Wed" if parameter 3 is defined, but its value is not "none", and displays nothing if parameter 3 is undefined or "none". If the value of parameter 3 is a display style other than "none", that style is applied.

==Wiki headings==
Wiki headings use the following default CSS:
&lt;section begin="HTML_section" /&gt;
{| class="wikitable"
|-
! Wikimarkup !! [[HTML]] !! Style
|-
| {{nowrap|1=&lt;code&gt;= Heading =&lt;/code&gt;}} || {{tag|h1|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.8em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;== Heading ==&lt;/code&gt;}} || {{tag|h2|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.5em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;=== Heading ===&lt;/code&gt;}} || {{tag|h3|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.2em; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;==== Heading ====&lt;/code&gt;}} || {{tag|h4|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;===== Heading =====&lt;/code&gt;}} || {{tag|h5|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;====== Heading ======&lt;/code&gt;}} || {{tag|h6|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
|}&lt;section end="HTML_section" /&gt;

== See also ==
*[[Span and div]]
*[[Cascading Style Sheets]] – article on CSS
*[[Wikipedia:Customisation]] – also covers user names, preferences settings, skins, user scripting, etc.
*[[Help:User style]] – modifying style for accessibility or for additional feature testing.
*[[Wikipedia:TemplateStyles]] – modifying style for advanced visual appearance that can be applied with template.
*[[Wikipedia:Catalogue of CSS classes]] – list of classes globally defined across the site
*[[Wikipedia:WikiProject Microformats/classes]] – list of classes used in microformats employed on Wikipedia
*[[Help:User CSS for a monospaced coding font]] – both for the editing window and for display of monospaced elements like {{tag|code|o}}
*[[meta:Help:Cascading_style_sheets]]
*[[mw:Manual:CSS]] and [[mw:Manual:Interface/Stylesheets]]
*[[mw:Gallery of user styles]]
*[[m:Customization:Explaining skins]]
*[[mw:Skin projects]]

{{Wikipedia technical help|collapsed}}
{{CSS and JS MediaWiki messages|state=collapsed|style=}}

[[Category:Wikipedia features]]
[[Category:Wikipedia help]]
[[Category:11]][[sn:Cascading Style Sheets]][[Category:11]]
[[en:Cascading Style Sheets]]
[[Category:New]]</text>
      <sha1>bfqq66miw63p4kbtltjpfplxtya9aoq</sha1>
    </revision>
    <revision>
      <id>721112</id>
      <parentid>721111</parentid>
      <timestamp>2025-03-27T10:14:02Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Removing from [[Category:New]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>721112</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="13845" sha1="nlmy4xhp2zodko1466mdcs31kokoei6" xml:space="preserve">{{Information page|H:CSS|WP:CSS}}
[[Cascading Style Sheets]] allows for flexible formatting of a page. They should be used instead of [[Help:Table|tables]] for non-tabular content whenever possible, because they can be [[Help:User style|manipulated by the reader]] or overridden by an author if your CSS is embedded in another page via a [[Help:Template|template]].

== Levels of CSS settings ==
Style may be chosen specifically for a piece of content, see e.g., [[Help:Table#Color; scope of parameters|color; scope of parameters]]

Alternatively, style is specified for CSS selectors, expressed in terms of elements, classes, and ID's. This is done on various levels:

Author style sheets, in this order:

''Note: See [[WP:CLASS]] for a list of all the style sheets loaded.''

===MediaWiki core style-sheets===

Per skin: [[mw:Gallery of user styles|MediaWiki Manual:Gallery of user styles]] etc. 
Typically loaded style sheets:
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/shared.css common/shared.css]
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/commonPrint.css common/commonPrint.css]

;Skin-specific main file:
e.g., [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/main.css monobook/main.css] (normal skin for PC's), [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/chick/main.css chick/main.css] (normal skin for handhelds)

;Browser-specific fixes (also skin-specific): 
Examples for Monobook:
*For Firefox: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/FF2Fixes.css monobook/FF2Fixes.css]
*For Internet Explorer: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE60Fixes.css monobook/IE60Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE70Fixes.css monobook/IE70Fixes.css]
*For Opera: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera6Fixes.css monobook/Opera6Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera7Fixes.css monobook/Opera7Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera9Fixes.css monobook/Opera9Fixes.css]

===Site-wide style-sheets===

* Site-wide for all skins (desktop view only): [[MediaWiki:Common.css]]
* Site-wide for all skins (mobile view only): [[MediaWiki:Mobile.css]]
* Site-wide per skin: [[MediaWiki:Vector.css]], [[MediaWiki:Monobook.css]], etc.
* Site-wide for user groups: [[MediaWiki:Group-user.css]], [[MediaWiki:Group-autoconfirmed.css]], etc.
* Site-wide for specialist purposes: [[MediaWiki:Print.css]], [[MediaWiki:Noscript.css]], [[MediaWiki:Filepage.css]]
* Site-wide if gadgets loaded: see [[Wikipedia:Gadget]] for more information

Note: MediaWiki sites other than English Wikipedia may use &lt;code&gt;MediaWiki:Gadget-site.css&lt;/code&gt; instead of MediaWiki:Common.css.

===Page-specific style-sheets===

Page-specific style-sheets may be introduced through [[Wikipedia:TemplateStyles|TemplateStyles]]. Pages, particularly templates, may also have inline CSS.

Some pages have their own CSS, e.g., [[MediaWiki:FileUploadWizard.css]]

===User-specific style-sheets===

* Global user-specific for all skins: [[meta:Special:MyPage/global.css]]
* User-specific for all skins: [[Special:MyPage/common.css]]
* User-specific per skin: e.g., [[Special:MyPage/vector.css]], or [[Special:MyPage/skin.css]] for your current skin 
* User-specific CSS loaded through JavaScript, e.g., loaded at [[Special:MyPage/common.js]]
* User-specific web-wide browser settings: local file referred to in the browser settings, or directly set in the browser

Note that in CSS terminology, the user-specific style sheets are ''not'' [[Help:User style|user style sheets]].

===Usage===

An HTML element may be just taken from the wikitext (see [[Help:HTML in wikitext|HTML in wikitext]]), e.g., span, or the result of translating wikitext, e.g., the &lt;code&gt;&lt;nowiki&gt;'''...'''&lt;/nowiki&gt;&lt;/code&gt; code is changed into {{tag|b}}, or part of the code for the skin.

A class may be produced by the software, e.g., ns-''namespace number'' for the HTML-element "body", and extiw for an interwiki link in the page body, or taken from the wikitext.

Similarly, an ID may be produced by the software, e.g., bodyContent, or taken from the wikitext.

In the case of conflicting style settings for a piece of content, the resulting setting depends primarily on the indication "!important". Secondarily, if both are important, the user wins, if neither is, the author wins. Tertiarily it depends on specificity. Only lastly, it depends on order between and within style sheets: the last wins. Thus, a User:''username''/monobook.css does not win from MediaWiki:Monobook.css (both author, not user) if the specificity of the latter is greater. See also [http://www.w3.org/TR/REC-CSS2/cascade.html cascade].

== Supported elements ==
MediaWiki supports most CSS, with such exceptions as the url() attribute. There were some bugs in CSS support in earlier versions.

{{Further2|the [http://www.w3.org/TR/2011/REC-CSS2-20110607/ Cascading Style Sheets Level 2 Revision 1 (CSS 2.1) Specification]}}

== CSS in wikitext ==

You can use CSS styling in HTML elements in your code (see [[Help:HTML in wikitext]] for a list of elements supported by MediaWiki) like you would in normal HTML markup.

For example, a {{tag|div}} element with a green border and its contents floated to the right would be created with
&lt;syntaxhighlight lang="html"&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
&lt;/syntaxhighlight&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
which would produce the box to the right.
Some wikitext elements allow you to insert CSS styling directly into them. An example is the table syntax:
{{-}}
&lt;syntaxhighlight lang="wikitext"&gt;
{| style="your style here"
|-
|your table stuff
|}
&lt;/syntaxhighlight&gt;
{{Further2|[[Help:Tables]]}}

== MediaWiki existing styles ==

You may wish to use a style type that is already predefined by MediaWiki, or the site that you are visiting. You can also create a style that is unique to your page.

Vector is the default style, you can view it at: [[MediaWiki:Vector.css]]

You will give your CSS tag an existing "class"

Please put a list of existing classes here.

== Tips and tricks ==

=== Non-display ===
In an embedded page, one can hide comments in one version, and show them in another view.
One extreme "style" for a text is not displaying it, with

&lt;syntaxhighlight lang="css"&gt;
.''classname'' {display: none}
#''id'' {display: none}
&lt;/syntaxhighlight&gt;

etc.

Non-displayed links do not work (as opposed to links in a very small font).

It cannot be used to remove text in expressions for template names, parameter names, parameter values, page names in links, etc.

To view hidden text, download the Web Developer Toolbar for Firefox [https://addons.mozilla.org/en-US/firefox/addon/60 here], then choose Misc. &amp;rarr; show hidden elements in that toolbar. It will make all hidden elements appear.

=== Non-print ===

One can exclude content from being printed by declaring the content to be of the "noprint" class:
&lt;syntaxhighlight lang="html"&gt;
&lt;div class="someclass noprint"&gt;This will not appear in the print version.&lt;/div&gt;
&lt;/syntaxhighlight&gt;

== Major style blocks ==
[[File:Wikimedia major style blocks example.svg|right|thumb|An annotated desktop screenshot of [[Wikipedia:Example|an example article]], showcasing the style blocks]]
*''column-content'' – overall space within the margins of which the content exists.
* ''firstHeading'' – the class of the heading tag at the top of every page
*''contentSub'' – the name of the wiki immediately underneath the main heading, but above the body text
* ''content'' – the white background, thin bordered box which contains the main page content.
* ''bodyContent'' – the main page content within the content box

The portlet class is the style used by all the div blocks around the main content.  Identified blocks using that class:
*''p-cactions'' – id for the list of page-related tabs above the main content (page, talk, edit, etc.), top.
*''p-personal'' – id for the list of user-related links above the main content (username, talk, etc.), top.
*''p-logo'' – id for the block that contains the logo, top left.
*''p-navigation'' – id for the block that contains the navigation links on the left of the page
*''p-search'' – the block that contains the search buttons
*''p-tb'' – the block that contains the toolbox links
*''p-lang'' – the block that contains interlanguage links

The footer at the bottom of the page includes blocks with the following ids
*''footer'' – overall footer container block
*''f-poweredbyico'' – the powered by MediaWiki image that normally resides to the right of the page
*''f-list'' – id for the list that contains all the bits of text at the bottom of the page

== Style depending on a parameter or variable ==
=== Variable class or id ===

A class or id can depend on the result produced by a template or on a template parameter, e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1|def}}}"&lt;/syntaxhighlight&gt;. For one or more of the possible class names the style of that class can be defined. If the class is undefined it is ignored, so the standard style is used.

In the simplest case we have e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1}}}"&lt;/syntaxhighlight&gt; and define class abcdef. If the parameter value is "def" it applies.

If a page for general use only makes sense when styles are defined for certain classes, then these have to be specified in the page [[MediaWiki:Common.css]], which applies for all users and all skins, as far as not overridden.

=== Variable style parameter value ===

Wikitext that reads
&lt;syntaxhighlight lang="html"&gt;&lt;span style="display:{{{3|none}}}"&gt;Wed&lt;/span&gt;&lt;/syntaxhighlight&gt;
will display "Wed" if parameter 3 is defined, but its value is not "none", and displays nothing if parameter 3 is undefined or "none". If the value of parameter 3 is a display style other than "none", that style is applied.

==Wiki headings==
Wiki headings use the following default CSS:
&lt;section begin="HTML_section" /&gt;
{| class="wikitable"
|-
! Wikimarkup !! [[HTML]] !! Style
|-
| {{nowrap|1=&lt;code&gt;= Heading =&lt;/code&gt;}} || {{tag|h1|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.8em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;== Heading ==&lt;/code&gt;}} || {{tag|h2|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.5em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;=== Heading ===&lt;/code&gt;}} || {{tag|h3|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.2em; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;==== Heading ====&lt;/code&gt;}} || {{tag|h4|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;===== Heading =====&lt;/code&gt;}} || {{tag|h5|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;====== Heading ======&lt;/code&gt;}} || {{tag|h6|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
|}&lt;section end="HTML_section" /&gt;

== See also ==
*[[Span and div]]
*[[Cascading Style Sheets]] – article on CSS
*[[Wikipedia:Customisation]] – also covers user names, preferences settings, skins, user scripting, etc.
*[[Help:User style]] – modifying style for accessibility or for additional feature testing.
*[[Wikipedia:TemplateStyles]] – modifying style for advanced visual appearance that can be applied with template.
*[[Wikipedia:Catalogue of CSS classes]] – list of classes globally defined across the site
*[[Wikipedia:WikiProject Microformats/classes]] – list of classes used in microformats employed on Wikipedia
*[[Help:User CSS for a monospaced coding font]] – both for the editing window and for display of monospaced elements like {{tag|code|o}}
*[[meta:Help:Cascading_style_sheets]]
*[[mw:Manual:CSS]] and [[mw:Manual:Interface/Stylesheets]]
*[[mw:Gallery of user styles]]
*[[m:Customization:Explaining skins]]
*[[mw:Skin projects]]

{{Wikipedia technical help|collapsed}}
{{CSS and JS MediaWiki messages|state=collapsed|style=}}

[[Category:Wikipedia features]]
[[Category:Wikipedia help]]
[[Category:11]][[sn:Cascading Style Sheets]][[Category:11]]
[[en:Cascading Style Sheets]]</text>
      <sha1>nlmy4xhp2zodko1466mdcs31kokoei6</sha1>
    </revision>
    <revision>
      <id>721113</id>
      <parentid>721112</parentid>
      <timestamp>2025-04-13T00:57:29Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Removing from [[Category:11]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>721113</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="13814" sha1="o0zgladvscjdwrtzki33ahx70fo0gkx" xml:space="preserve">{{Information page|H:CSS|WP:CSS}}
[[Cascading Style Sheets]] allows for flexible formatting of a page. They should be used instead of [[Help:Table|tables]] for non-tabular content whenever possible, because they can be [[Help:User style|manipulated by the reader]] or overridden by an author if your CSS is embedded in another page via a [[Help:Template|template]].

== Levels of CSS settings ==
Style may be chosen specifically for a piece of content, see e.g., [[Help:Table#Color; scope of parameters|color; scope of parameters]]

Alternatively, style is specified for CSS selectors, expressed in terms of elements, classes, and ID's. This is done on various levels:

Author style sheets, in this order:

''Note: See [[WP:CLASS]] for a list of all the style sheets loaded.''

===MediaWiki core style-sheets===

Per skin: [[mw:Gallery of user styles|MediaWiki Manual:Gallery of user styles]] etc. 
Typically loaded style sheets:
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/shared.css common/shared.css]
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/commonPrint.css common/commonPrint.css]

;Skin-specific main file:
e.g., [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/main.css monobook/main.css] (normal skin for PC's), [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/chick/main.css chick/main.css] (normal skin for handhelds)

;Browser-specific fixes (also skin-specific): 
Examples for Monobook:
*For Firefox: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/FF2Fixes.css monobook/FF2Fixes.css]
*For Internet Explorer: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE60Fixes.css monobook/IE60Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE70Fixes.css monobook/IE70Fixes.css]
*For Opera: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera6Fixes.css monobook/Opera6Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera7Fixes.css monobook/Opera7Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera9Fixes.css monobook/Opera9Fixes.css]

===Site-wide style-sheets===

* Site-wide for all skins (desktop view only): [[MediaWiki:Common.css]]
* Site-wide for all skins (mobile view only): [[MediaWiki:Mobile.css]]
* Site-wide per skin: [[MediaWiki:Vector.css]], [[MediaWiki:Monobook.css]], etc.
* Site-wide for user groups: [[MediaWiki:Group-user.css]], [[MediaWiki:Group-autoconfirmed.css]], etc.
* Site-wide for specialist purposes: [[MediaWiki:Print.css]], [[MediaWiki:Noscript.css]], [[MediaWiki:Filepage.css]]
* Site-wide if gadgets loaded: see [[Wikipedia:Gadget]] for more information

Note: MediaWiki sites other than English Wikipedia may use &lt;code&gt;MediaWiki:Gadget-site.css&lt;/code&gt; instead of MediaWiki:Common.css.

===Page-specific style-sheets===

Page-specific style-sheets may be introduced through [[Wikipedia:TemplateStyles|TemplateStyles]]. Pages, particularly templates, may also have inline CSS.

Some pages have their own CSS, e.g., [[MediaWiki:FileUploadWizard.css]]

===User-specific style-sheets===

* Global user-specific for all skins: [[meta:Special:MyPage/global.css]]
* User-specific for all skins: [[Special:MyPage/common.css]]
* User-specific per skin: e.g., [[Special:MyPage/vector.css]], or [[Special:MyPage/skin.css]] for your current skin 
* User-specific CSS loaded through JavaScript, e.g., loaded at [[Special:MyPage/common.js]]
* User-specific web-wide browser settings: local file referred to in the browser settings, or directly set in the browser

Note that in CSS terminology, the user-specific style sheets are ''not'' [[Help:User style|user style sheets]].

===Usage===

An HTML element may be just taken from the wikitext (see [[Help:HTML in wikitext|HTML in wikitext]]), e.g., span, or the result of translating wikitext, e.g., the &lt;code&gt;&lt;nowiki&gt;'''...'''&lt;/nowiki&gt;&lt;/code&gt; code is changed into {{tag|b}}, or part of the code for the skin.

A class may be produced by the software, e.g., ns-''namespace number'' for the HTML-element "body", and extiw for an interwiki link in the page body, or taken from the wikitext.

Similarly, an ID may be produced by the software, e.g., bodyContent, or taken from the wikitext.

In the case of conflicting style settings for a piece of content, the resulting setting depends primarily on the indication "!important". Secondarily, if both are important, the user wins, if neither is, the author wins. Tertiarily it depends on specificity. Only lastly, it depends on order between and within style sheets: the last wins. Thus, a User:''username''/monobook.css does not win from MediaWiki:Monobook.css (both author, not user) if the specificity of the latter is greater. See also [http://www.w3.org/TR/REC-CSS2/cascade.html cascade].

== Supported elements ==
MediaWiki supports most CSS, with such exceptions as the url() attribute. There were some bugs in CSS support in earlier versions.

{{Further2|the [http://www.w3.org/TR/2011/REC-CSS2-20110607/ Cascading Style Sheets Level 2 Revision 1 (CSS 2.1) Specification]}}

== CSS in wikitext ==

You can use CSS styling in HTML elements in your code (see [[Help:HTML in wikitext]] for a list of elements supported by MediaWiki) like you would in normal HTML markup.

For example, a {{tag|div}} element with a green border and its contents floated to the right would be created with
&lt;syntaxhighlight lang="html"&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
&lt;/syntaxhighlight&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
which would produce the box to the right.
Some wikitext elements allow you to insert CSS styling directly into them. An example is the table syntax:
{{-}}
&lt;syntaxhighlight lang="wikitext"&gt;
{| style="your style here"
|-
|your table stuff
|}
&lt;/syntaxhighlight&gt;
{{Further2|[[Help:Tables]]}}

== MediaWiki existing styles ==

You may wish to use a style type that is already predefined by MediaWiki, or the site that you are visiting. You can also create a style that is unique to your page.

Vector is the default style, you can view it at: [[MediaWiki:Vector.css]]

You will give your CSS tag an existing "class"

Please put a list of existing classes here.

== Tips and tricks ==

=== Non-display ===
In an embedded page, one can hide comments in one version, and show them in another view.
One extreme "style" for a text is not displaying it, with

&lt;syntaxhighlight lang="css"&gt;
.''classname'' {display: none}
#''id'' {display: none}
&lt;/syntaxhighlight&gt;

etc.

Non-displayed links do not work (as opposed to links in a very small font).

It cannot be used to remove text in expressions for template names, parameter names, parameter values, page names in links, etc.

To view hidden text, download the Web Developer Toolbar for Firefox [https://addons.mozilla.org/en-US/firefox/addon/60 here], then choose Misc. &amp;rarr; show hidden elements in that toolbar. It will make all hidden elements appear.

=== Non-print ===

One can exclude content from being printed by declaring the content to be of the "noprint" class:
&lt;syntaxhighlight lang="html"&gt;
&lt;div class="someclass noprint"&gt;This will not appear in the print version.&lt;/div&gt;
&lt;/syntaxhighlight&gt;

== Major style blocks ==
[[File:Wikimedia major style blocks example.svg|right|thumb|An annotated desktop screenshot of [[Wikipedia:Example|an example article]], showcasing the style blocks]]
*''column-content'' – overall space within the margins of which the content exists.
* ''firstHeading'' – the class of the heading tag at the top of every page
*''contentSub'' – the name of the wiki immediately underneath the main heading, but above the body text
* ''content'' – the white background, thin bordered box which contains the main page content.
* ''bodyContent'' – the main page content within the content box

The portlet class is the style used by all the div blocks around the main content.  Identified blocks using that class:
*''p-cactions'' – id for the list of page-related tabs above the main content (page, talk, edit, etc.), top.
*''p-personal'' – id for the list of user-related links above the main content (username, talk, etc.), top.
*''p-logo'' – id for the block that contains the logo, top left.
*''p-navigation'' – id for the block that contains the navigation links on the left of the page
*''p-search'' – the block that contains the search buttons
*''p-tb'' – the block that contains the toolbox links
*''p-lang'' – the block that contains interlanguage links

The footer at the bottom of the page includes blocks with the following ids
*''footer'' – overall footer container block
*''f-poweredbyico'' – the powered by MediaWiki image that normally resides to the right of the page
*''f-list'' – id for the list that contains all the bits of text at the bottom of the page

== Style depending on a parameter or variable ==
=== Variable class or id ===

A class or id can depend on the result produced by a template or on a template parameter, e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1|def}}}"&lt;/syntaxhighlight&gt;. For one or more of the possible class names the style of that class can be defined. If the class is undefined it is ignored, so the standard style is used.

In the simplest case we have e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1}}}"&lt;/syntaxhighlight&gt; and define class abcdef. If the parameter value is "def" it applies.

If a page for general use only makes sense when styles are defined for certain classes, then these have to be specified in the page [[MediaWiki:Common.css]], which applies for all users and all skins, as far as not overridden.

=== Variable style parameter value ===

Wikitext that reads
&lt;syntaxhighlight lang="html"&gt;&lt;span style="display:{{{3|none}}}"&gt;Wed&lt;/span&gt;&lt;/syntaxhighlight&gt;
will display "Wed" if parameter 3 is defined, but its value is not "none", and displays nothing if parameter 3 is undefined or "none". If the value of parameter 3 is a display style other than "none", that style is applied.

==Wiki headings==
Wiki headings use the following default CSS:
&lt;section begin="HTML_section" /&gt;
{| class="wikitable"
|-
! Wikimarkup !! [[HTML]] !! Style
|-
| {{nowrap|1=&lt;code&gt;= Heading =&lt;/code&gt;}} || {{tag|h1|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.8em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;== Heading ==&lt;/code&gt;}} || {{tag|h2|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.5em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;=== Heading ===&lt;/code&gt;}} || {{tag|h3|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.2em; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;==== Heading ====&lt;/code&gt;}} || {{tag|h4|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;===== Heading =====&lt;/code&gt;}} || {{tag|h5|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;====== Heading ======&lt;/code&gt;}} || {{tag|h6|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
|}&lt;section end="HTML_section" /&gt;

== See also ==
*[[Span and div]]
*[[Cascading Style Sheets]] – article on CSS
*[[Wikipedia:Customisation]] – also covers user names, preferences settings, skins, user scripting, etc.
*[[Help:User style]] – modifying style for accessibility or for additional feature testing.
*[[Wikipedia:TemplateStyles]] – modifying style for advanced visual appearance that can be applied with template.
*[[Wikipedia:Catalogue of CSS classes]] – list of classes globally defined across the site
*[[Wikipedia:WikiProject Microformats/classes]] – list of classes used in microformats employed on Wikipedia
*[[Help:User CSS for a monospaced coding font]] – both for the editing window and for display of monospaced elements like {{tag|code|o}}
*[[meta:Help:Cascading_style_sheets]]
*[[mw:Manual:CSS]] and [[mw:Manual:Interface/Stylesheets]]
*[[mw:Gallery of user styles]]
*[[m:Customization:Explaining skins]]
*[[mw:Skin projects]]

{{Wikipedia technical help|collapsed}}
{{CSS and JS MediaWiki messages|state=collapsed|style=}}

[[Category:Wikipedia features]]
[[Category:Wikipedia help]]
[[sn:Cascading Style Sheets]][[en:Cascading Style Sheets]]</text>
      <sha1>o0zgladvscjdwrtzki33ahx70fo0gkx</sha1>
    </revision>
    <revision>
      <id>721114</id>
      <parentid>721113</parentid>
      <timestamp>2025-05-19T18:06:32Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>721114</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="13868" sha1="l8ai9rc93eqeufl474bs9bykhp164vb" xml:space="preserve">{{Information page|H:CSS|WP:CSS}}
[[:en:Cascading Style Sheets|Cascading Style Sheets]] allows for flexible formatting of a page. They should be used instead of [[Help:Table|tables]] for non-tabular content whenever possible, because they can be [[Help:User style|manipulated by the reader]] or overridden by an author if your CSS is embedded in another page via a [[Help:Template|template]].

== Levels of CSS settings ==
Style may be chosen specifically for a piece of content, see e.g., [[Help:Table#Color; scope of parameters|color; scope of parameters]]

Alternatively, style is specified for CSS selectors, expressed in terms of elements, classes, and ID's. This is done on various levels:

Author style sheets, in this order:

''Note: See [[WP:CLASS]] for a list of all the style sheets loaded.''

===MediaWiki core style-sheets===

Per skin: [[mw:Gallery of user styles|MediaWiki Manual:Gallery of user styles]] etc. 
Typically loaded style sheets:
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/shared.css common/shared.css]
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/commonPrint.css common/commonPrint.css]

;Skin-specific main file:
e.g., [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/main.css monobook/main.css] (normal skin for PC's), [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/chick/main.css chick/main.css] (normal skin for handhelds)

;Browser-specific fixes (also skin-specific): 
Examples for Monobook:
*For Firefox: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/FF2Fixes.css monobook/FF2Fixes.css]
*For Internet Explorer: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE60Fixes.css monobook/IE60Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE70Fixes.css monobook/IE70Fixes.css]
*For Opera: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera6Fixes.css monobook/Opera6Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera7Fixes.css monobook/Opera7Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera9Fixes.css monobook/Opera9Fixes.css]

===Site-wide style-sheets===

* Site-wide for all skins (desktop view only): [[MediaWiki:Common.css]]
* Site-wide for all skins (mobile view only): [[MediaWiki:Mobile.css]]
* Site-wide per skin: [[MediaWiki:Vector.css]], [[MediaWiki:Monobook.css]], etc.
* Site-wide for user groups: [[MediaWiki:Group-user.css]], [[MediaWiki:Group-autoconfirmed.css]], etc.
* Site-wide for specialist purposes: [[MediaWiki:Print.css]], [[MediaWiki:Noscript.css]], [[MediaWiki:Filepage.css]]
* Site-wide if gadgets loaded: see [[Wikipedia:Gadget]] for more information

Note: MediaWiki sites other than English Wikipedia may use &lt;code&gt;MediaWiki:Gadget-site.css&lt;/code&gt; instead of MediaWiki:Common.css.

===Page-specific style-sheets===

Page-specific style-sheets may be introduced through [[Wikipedia:TemplateStyles|TemplateStyles]]. Pages, particularly templates, may also have inline CSS.

Some pages have their own CSS, e.g., [[MediaWiki:FileUploadWizard.css]]

===User-specific style-sheets===

* Global user-specific for all skins: [[meta:Special:MyPage/global.css]]
* User-specific for all skins: [[Special:MyPage/common.css]]
* User-specific per skin: e.g., [[Special:MyPage/vector.css]], or [[Special:MyPage/skin.css]] for your current skin 
* User-specific CSS loaded through JavaScript, e.g., loaded at [[Special:MyPage/common.js]]
* User-specific web-wide browser settings: local file referred to in the browser settings, or directly set in the browser

Note that in CSS terminology, the user-specific style sheets are ''not'' [[Help:User style|user style sheets]].

===Usage===

An HTML element may be just taken from the wikitext (see [[Help:HTML in wikitext|HTML in wikitext]]), e.g., span, or the result of translating wikitext, e.g., the &lt;code&gt;&lt;nowiki&gt;'''...'''&lt;/nowiki&gt;&lt;/code&gt; code is changed into {{tag|b}}, or part of the code for the skin.

A class may be produced by the software, e.g., ns-''namespace number'' for the HTML-element "body", and extiw for an interwiki link in the page body, or taken from the wikitext.

Similarly, an ID may be produced by the software, e.g., bodyContent, or taken from the wikitext.

In the case of conflicting style settings for a piece of content, the resulting setting depends primarily on the indication "!important". Secondarily, if both are important, the user wins, if neither is, the author wins. Tertiarily it depends on specificity. Only lastly, it depends on order between and within style sheets: the last wins. Thus, a User:''username''/monobook.css does not win from MediaWiki:Monobook.css (both author, not user) if the specificity of the latter is greater. See also [http://www.w3.org/TR/REC-CSS2/cascade.html cascade].

== Supported elements ==
MediaWiki supports most CSS, with such exceptions as the url() attribute. There were some bugs in CSS support in earlier versions.

{{Further2|the [http://www.w3.org/TR/2011/REC-CSS2-20110607/ Cascading Style Sheets Level 2 Revision 1 (CSS 2.1) Specification]}}

== CSS in wikitext ==

You can use CSS styling in HTML elements in your code (see [[Help:HTML in wikitext]] for a list of elements supported by MediaWiki) like you would in normal HTML markup.

For example, a {{tag|div}} element with a green border and its contents floated to the right would be created with
&lt;syntaxhighlight lang="html"&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
&lt;/syntaxhighlight&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
which would produce the box to the right.
Some wikitext elements allow you to insert CSS styling directly into them. An example is the table syntax:
{{-}}
&lt;syntaxhighlight lang="wikitext"&gt;
{| style="your style here"
|-
|your table stuff
|}
&lt;/syntaxhighlight&gt;
{{Further2|[[Help:Tables]]}}

== MediaWiki existing styles ==

You may wish to use a style type that is already predefined by MediaWiki, or the site that you are visiting. You can also create a style that is unique to your page.

Vector is the default style, you can view it at: [[MediaWiki:Vector.css]]

You will give your CSS tag an existing "class"

Please put a list of existing classes here.

== Tips and tricks ==

=== Non-display ===
In an embedded page, one can hide comments in one version, and show them in another view.
One extreme "style" for a text is not displaying it, with

&lt;syntaxhighlight lang="css"&gt;
.''classname'' {display: none}
#''id'' {display: none}
&lt;/syntaxhighlight&gt;

etc.

Non-displayed links do not work (as opposed to links in a very small font).

It cannot be used to remove text in expressions for template names, parameter names, parameter values, page names in links, etc.

To view hidden text, download the Web Developer Toolbar for Firefox [https://addons.mozilla.org/en-US/firefox/addon/60 here], then choose Misc. &amp;rarr; show hidden elements in that toolbar. It will make all hidden elements appear.

=== Non-print ===

One can exclude content from being printed by declaring the content to be of the "noprint" class:
&lt;syntaxhighlight lang="html"&gt;
&lt;div class="someclass noprint"&gt;This will not appear in the print version.&lt;/div&gt;
&lt;/syntaxhighlight&gt;

== Major style blocks ==
[[File:Wikimedia major style blocks example.svg|right|thumb|An annotated desktop screenshot of [[Wikipedia:Example|an example article]], showcasing the style blocks]]
*''column-content'' – overall space within the margins of which the content exists.
* ''firstHeading'' – the class of the heading tag at the top of every page
*''contentSub'' – the name of the wiki immediately underneath the main heading, but above the body text
* ''content'' – the white background, thin bordered box which contains the main page content.
* ''bodyContent'' – the main page content within the content box

The portlet class is the style used by all the div blocks around the main content.  Identified blocks using that class:
*''p-cactions'' – id for the list of page-related tabs above the main content (page, talk, edit, etc.), top.
*''p-personal'' – id for the list of user-related links above the main content (username, talk, etc.), top.
*''p-logo'' – id for the block that contains the logo, top left.
*''p-navigation'' – id for the block that contains the navigation links on the left of the page
*''p-search'' – the block that contains the search buttons
*''p-tb'' – the block that contains the toolbox links
*''p-lang'' – the block that contains interlanguage links

The footer at the bottom of the page includes blocks with the following ids
*''footer'' – overall footer container block
*''f-poweredbyico'' – the powered by MediaWiki image that normally resides to the right of the page
*''f-list'' – id for the list that contains all the bits of text at the bottom of the page

== Style depending on a parameter or variable ==
=== Variable class or id ===

A class or id can depend on the result produced by a template or on a template parameter, e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1|def}}}"&lt;/syntaxhighlight&gt;. For one or more of the possible class names the style of that class can be defined. If the class is undefined it is ignored, so the standard style is used.

In the simplest case we have e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1}}}"&lt;/syntaxhighlight&gt; and define class abcdef. If the parameter value is "def" it applies.

If a page for general use only makes sense when styles are defined for certain classes, then these have to be specified in the page [[MediaWiki:Common.css]], which applies for all users and all skins, as far as not overridden.

=== Variable style parameter value ===

Wikitext that reads
&lt;syntaxhighlight lang="html"&gt;&lt;span style="display:{{{3|none}}}"&gt;Wed&lt;/span&gt;&lt;/syntaxhighlight&gt;
will display "Wed" if parameter 3 is defined, but its value is not "none", and displays nothing if parameter 3 is undefined or "none". If the value of parameter 3 is a display style other than "none", that style is applied.

==Wiki headings==
Wiki headings use the following default CSS:
&lt;section begin="HTML_section" /&gt;
{| class="wikitable"
|-
! Wikimarkup !! [[HTML]] !! Style
|-
| {{nowrap|1=&lt;code&gt;= Heading =&lt;/code&gt;}} || {{tag|h1|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.8em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;== Heading ==&lt;/code&gt;}} || {{tag|h2|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.5em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;=== Heading ===&lt;/code&gt;}} || {{tag|h3|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.2em; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;==== Heading ====&lt;/code&gt;}} || {{tag|h4|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;===== Heading =====&lt;/code&gt;}} || {{tag|h5|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;====== Heading ======&lt;/code&gt;}} || {{tag|h6|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
|}&lt;section end="HTML_section" /&gt;

== See also ==
*[[Span and div]]
*[[:en:Cascading Style Sheets|Cascading Style Sheets]] – article on CSS
*[[Wikipedia:Customisation]] – also covers user names, preferences settings, skins, user scripting, etc.
*[[Help:User style]] – modifying style for accessibility or for additional feature testing.
*[[Wikipedia:TemplateStyles]] – modifying style for advanced visual appearance that can be applied with template.
*[[Wikipedia:Catalogue of CSS classes]] – list of classes globally defined across the site
*[[Wikipedia:WikiProject Microformats/classes]] – list of classes used in microformats employed on Wikipedia
*[[Help:User CSS for a monospaced coding font]] – both for the editing window and for display of monospaced elements like {{tag|code|o}}
*[[meta:Help:Cascading_style_sheets]]
*[[mw:Manual:CSS]] and [[mw:Manual:Interface/Stylesheets]]
*[[mw:Gallery of user styles]]
*[[m:Customization:Explaining skins]]
*[[mw:Skin projects]]

{{Wikipedia technical help|collapsed}}
{{CSS and JS MediaWiki messages|state=collapsed|style=}}

[[Category:Wikipedia features]]
[[Category:Wikipedia help]]
[[sn:Cascading Style Sheets]][[en:Cascading Style Sheets]]</text>
      <sha1>l8ai9rc93eqeufl474bs9bykhp164vb</sha1>
    </revision>
    <revision>
      <id>721115</id>
      <parentid>721114</parentid>
      <timestamp>2025-05-20T19:30:27Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>721115</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="13877" sha1="diji63vd8uuf2mh9dk1fhrynelqrcev" xml:space="preserve">{{Information page|H:CSS|WP:CSS}}
[[:en:Cascading Style Sheets|Cascading Style Sheets]] allows for flexible formatting of a page. They should be used instead of [[Help:Table|tables]] for non-tabular content whenever possible, because they can be [[Help:User style|manipulated by the reader]] or overridden by an author if your CSS is embedded in another page via a [[Help:Template|template]].

== Levels of CSS settings ==
Style may be chosen specifically for a piece of content, see e.g., [[Help:Table#Color; scope of parameters|color; scope of parameters]]

Alternatively, style is specified for CSS selectors, expressed in terms of elements, classes, and ID's. This is done on various levels:

Author style sheets, in this order:

''Note: See [[WP:CLASS]] for a list of all the style sheets loaded.''

===MediaWiki core style-sheets===

Per skin: [[mw:Gallery of user styles|MediaWiki Manual:Gallery of user styles]] etc. 
Typically loaded style sheets:
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/shared.css common/shared.css]
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/commonPrint.css common/commonPrint.css]

;Skin-specific main file:
e.g., [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/main.css monobook/main.css] (normal skin for PC's), [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/chick/main.css chick/main.css] (normal skin for handhelds)

;Browser-specific fixes (also skin-specific): 
Examples for Monobook:
*For Firefox: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/FF2Fixes.css monobook/FF2Fixes.css]
*For Internet Explorer: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE60Fixes.css monobook/IE60Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE70Fixes.css monobook/IE70Fixes.css]
*For Opera: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera6Fixes.css monobook/Opera6Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera7Fixes.css monobook/Opera7Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera9Fixes.css monobook/Opera9Fixes.css]

===Site-wide style-sheets===

* Site-wide for all skins (desktop view only): [[MediaWiki:Common.css]]
* Site-wide for all skins (mobile view only): [[MediaWiki:Mobile.css]]
* Site-wide per skin: [[MediaWiki:Vector.css]], [[MediaWiki:Monobook.css]], etc.
* Site-wide for user groups: [[MediaWiki:Group-user.css]], [[MediaWiki:Group-autoconfirmed.css]], etc.
* Site-wide for specialist purposes: [[MediaWiki:Print.css]], [[MediaWiki:Noscript.css]], [[MediaWiki:Filepage.css]]
* Site-wide if gadgets loaded: see [[Wikipedia:Gadget]] for more information

Note: MediaWiki sites other than English Wikipedia may use &lt;code&gt;MediaWiki:Gadget-site.css&lt;/code&gt; instead of MediaWiki:Common.css.

===Page-specific style-sheets===

Page-specific style-sheets may be introduced through [[Wikipedia:TemplateStyles|TemplateStyles]]. Pages, particularly templates, may also have inline CSS.

Some pages have their own CSS, e.g., [[MediaWiki:FileUploadWizard.css]]

===User-specific style-sheets===

* Global user-specific for all skins: [[meta:Special:MyPage/global.css]]
* User-specific for all skins: [[Special:MyPage/common.css]]
* User-specific per skin: e.g., [[Special:MyPage/vector.css]], or [[Special:MyPage/skin.css]] for your current skin 
* User-specific CSS loaded through JavaScript, e.g., loaded at [[Special:MyPage/common.js]]
* User-specific web-wide browser settings: local file referred to in the browser settings, or directly set in the browser

Note that in CSS terminology, the user-specific style sheets are ''not'' [[Help:User style|user style sheets]].

===Usage===

An HTML element may be just taken from the wikitext (see [[Help:HTML in wikitext|HTML in wikitext]]), e.g., span, or the result of translating wikitext, e.g., the &lt;code&gt;&lt;nowiki&gt;'''...'''&lt;/nowiki&gt;&lt;/code&gt; code is changed into {{tag|b}}, or part of the code for the skin.

A class may be produced by the software, e.g., ns-''namespace number'' for the HTML-element "body", and extiw for an interwiki link in the page body, or taken from the wikitext.

Similarly, an ID may be produced by the software, e.g., bodyContent, or taken from the wikitext.

In the case of conflicting style settings for a piece of content, the resulting setting depends primarily on the indication "!important". Secondarily, if both are important, the user wins, if neither is, the author wins. Tertiarily it depends on specificity. Only lastly, it depends on order between and within style sheets: the last wins. Thus, a User:''username''/monobook.css does not win from MediaWiki:Monobook.css (both author, not user) if the specificity of the latter is greater. See also [http://www.w3.org/TR/REC-CSS2/cascade.html cascade].

== Supported elements ==
MediaWiki supports most CSS, with such exceptions as the url() attribute. There were some bugs in CSS support in earlier versions.

{{Further2|the [http://www.w3.org/TR/2011/REC-CSS2-20110607/ Cascading Style Sheets Level 2 Revision 1 (CSS 2.1) Specification]}}

== CSS in wikitext ==

You can use CSS styling in HTML elements in your code (see [[Help:HTML in wikitext]] for a list of elements supported by MediaWiki) like you would in normal HTML markup.

For example, a {{tag|div}} element with a green border and its contents floated to the right would be created with
&lt;syntaxhighlight lang="html"&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
&lt;/syntaxhighlight&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
which would produce the box to the right.
Some wikitext elements allow you to insert CSS styling directly into them. An example is the table syntax:
{{-}}
&lt;syntaxhighlight lang="wikitext"&gt;
{| style="your style here"
|-
|your table stuff
|}
&lt;/syntaxhighlight&gt;
{{Further2|[[Help:Tables]]}}

== MediaWiki existing styles ==

You may wish to use a style type that is already predefined by MediaWiki, or the site that you are visiting. You can also create a style that is unique to your page.

Vector is the default style, you can view it at: [[MediaWiki:Vector.css]]

You will give your CSS tag an existing "class"

Please put a list of existing classes here.

== Tips and tricks ==

=== Non-display ===
In an embedded page, one can hide comments in one version, and show them in another view.
One extreme "style" for a text is not displaying it, with

&lt;syntaxhighlight lang="css"&gt;
.''classname'' {display: none}
#''id'' {display: none}
&lt;/syntaxhighlight&gt;

etc.

Non-displayed links do not work (as opposed to links in a very small font).

It cannot be used to remove text in expressions for template names, parameter names, parameter values, page names in links, etc.

To view hidden text, download the Web Developer Toolbar for Firefox [https://addons.mozilla.org/en-US/firefox/addon/60 here], then choose Misc. &amp;rarr; show hidden elements in that toolbar. It will make all hidden elements appear.

=== Non-print ===

One can exclude content from being printed by declaring the content to be of the "noprint" class:
&lt;syntaxhighlight lang="html"&gt;
&lt;div class="someclass noprint"&gt;This will not appear in the print version.&lt;/div&gt;
&lt;/syntaxhighlight&gt;

== Major style blocks ==
[[File:Wikimedia major style blocks example.svg|right|thumb|An annotated desktop screenshot of [[Wikipedia:Example|an example article]], showcasing the style blocks]]
*''column-content'' – overall space within the margins of which the content exists.
* ''firstHeading'' – the class of the heading tag at the top of every page
*''contentSub'' – the name of the wiki immediately underneath the main heading, but above the body text
* ''content'' – the white background, thin bordered box which contains the main page content.
* ''bodyContent'' – the main page content within the content box

The portlet class is the style used by all the div blocks around the main content.  Identified blocks using that class:
*''p-cactions'' – id for the list of page-related tabs above the main content (page, talk, edit, etc.), top.
*''p-personal'' – id for the list of user-related links above the main content (username, talk, etc.), top.
*''p-logo'' – id for the block that contains the logo, top left.
*''p-navigation'' – id for the block that contains the navigation links on the left of the page
*''p-search'' – the block that contains the search buttons
*''p-tb'' – the block that contains the toolbox links
*''p-lang'' – the block that contains interlanguage links

The footer at the bottom of the page includes blocks with the following ids
*''footer'' – overall footer container block
*''f-poweredbyico'' – the powered by MediaWiki image that normally resides to the right of the page
*''f-list'' – id for the list that contains all the bits of text at the bottom of the page

== Style depending on a parameter or variable ==
=== Variable class or id ===

A class or id can depend on the result produced by a template or on a template parameter, e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1|def}}}"&lt;/syntaxhighlight&gt;. For one or more of the possible class names the style of that class can be defined. If the class is undefined it is ignored, so the standard style is used.

In the simplest case we have e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1}}}"&lt;/syntaxhighlight&gt; and define class abcdef. If the parameter value is "def" it applies.

If a page for general use only makes sense when styles are defined for certain classes, then these have to be specified in the page [[MediaWiki:Common.css]], which applies for all users and all skins, as far as not overridden.

=== Variable style parameter value ===

Wikitext that reads
&lt;syntaxhighlight lang="html"&gt;&lt;span style="display:{{{3|none}}}"&gt;Wed&lt;/span&gt;&lt;/syntaxhighlight&gt;
will display "Wed" if parameter 3 is defined, but its value is not "none", and displays nothing if parameter 3 is undefined or "none". If the value of parameter 3 is a display style other than "none", that style is applied.

==Wiki headings==
Wiki headings use the following default CSS:
&lt;section begin="HTML_section" /&gt;
{| class="wikitable"
|-
! Wikimarkup !! [[:en:HTML|HTML]] !! Style
|-
| {{nowrap|1=&lt;code&gt;= Heading =&lt;/code&gt;}} || {{tag|h1|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.8em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;== Heading ==&lt;/code&gt;}} || {{tag|h2|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.5em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;=== Heading ===&lt;/code&gt;}} || {{tag|h3|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.2em; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;==== Heading ====&lt;/code&gt;}} || {{tag|h4|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;===== Heading =====&lt;/code&gt;}} || {{tag|h5|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;====== Heading ======&lt;/code&gt;}} || {{tag|h6|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
|}&lt;section end="HTML_section" /&gt;

== See also ==
*[[Span and div]]
*[[:en:Cascading Style Sheets|Cascading Style Sheets]] – article on CSS
*[[Wikipedia:Customisation]] – also covers user names, preferences settings, skins, user scripting, etc.
*[[Help:User style]] – modifying style for accessibility or for additional feature testing.
*[[Wikipedia:TemplateStyles]] – modifying style for advanced visual appearance that can be applied with template.
*[[Wikipedia:Catalogue of CSS classes]] – list of classes globally defined across the site
*[[Wikipedia:WikiProject Microformats/classes]] – list of classes used in microformats employed on Wikipedia
*[[Help:User CSS for a monospaced coding font]] – both for the editing window and for display of monospaced elements like {{tag|code|o}}
*[[meta:Help:Cascading_style_sheets]]
*[[mw:Manual:CSS]] and [[mw:Manual:Interface/Stylesheets]]
*[[mw:Gallery of user styles]]
*[[m:Customization:Explaining skins]]
*[[mw:Skin projects]]

{{Wikipedia technical help|collapsed}}
{{CSS and JS MediaWiki messages|state=collapsed|style=}}

[[Category:Wikipedia features]]
[[Category:Wikipedia help]]
[[sn:Cascading Style Sheets]][[en:Cascading Style Sheets]]</text>
      <sha1>diji63vd8uuf2mh9dk1fhrynelqrcev</sha1>
    </revision>
    <revision>
      <id>3317</id>
      <timestamp>2025-05-25T03:26:49Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was Bot: convert local link → enwiki interwiki</comment>
      <origin>3317</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="13894" sha1="3kwj84hk6u6b9scjj9573lckaubx8dp" xml:space="preserve">{{Information page|H:CSS|WP:CSS}}
[[:en:Cascading Style Sheets|Cascading Style Sheets]] allows for flexible formatting of a page. They should be used instead of [[Help:Table|tables]] for non-tabular content whenever possible, because they can be [[Help:User style|manipulated by the reader]] or overridden by an author if your CSS is embedded in another page via a [[Help:Template|template]].

== Levels of CSS settings ==
Style may be chosen specifically for a piece of content, see e.g., [[Help:Table#Color; scope of parameters|color; scope of parameters]]

Alternatively, style is specified for CSS selectors, expressed in terms of elements, classes, and ID's. This is done on various levels:

Author style sheets, in this order:

''Note: See [[WP:CLASS]] for a list of all the style sheets loaded.''

===MediaWiki core style-sheets===

Per skin: [[mw:Gallery of user styles|MediaWiki Manual:Gallery of user styles]] etc. 
Typically loaded style sheets:
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/shared.css common/shared.css]
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/commonPrint.css common/commonPrint.css]

;Skin-specific main file:
e.g., [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/main.css monobook/main.css] (normal skin for PC's), [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/chick/main.css chick/main.css] (normal skin for handhelds)

;Browser-specific fixes (also skin-specific): 
Examples for Monobook:
*For Firefox: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/FF2Fixes.css monobook/FF2Fixes.css]
*For Internet Explorer: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE60Fixes.css monobook/IE60Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE70Fixes.css monobook/IE70Fixes.css]
*For Opera: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera6Fixes.css monobook/Opera6Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera7Fixes.css monobook/Opera7Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera9Fixes.css monobook/Opera9Fixes.css]

===Site-wide style-sheets===

* Site-wide for all skins (desktop view only): [[MediaWiki:Common.css]]
* Site-wide for all skins (mobile view only): [[MediaWiki:Mobile.css]]
* Site-wide per skin: [[MediaWiki:Vector.css]], [[MediaWiki:Monobook.css]], etc.
* Site-wide for user groups: [[MediaWiki:Group-user.css]], [[MediaWiki:Group-autoconfirmed.css]], etc.
* Site-wide for specialist purposes: [[MediaWiki:Print.css]], [[MediaWiki:Noscript.css]], [[MediaWiki:Filepage.css]]
* Site-wide if gadgets loaded: see [[Wikipedia:Gadget]] for more information

Note: MediaWiki sites other than English Wikipedia may use &lt;code&gt;MediaWiki:Gadget-site.css&lt;/code&gt; instead of MediaWiki:Common.css.

===Page-specific style-sheets===

Page-specific style-sheets may be introduced through [[Wikipedia:TemplateStyles|TemplateStyles]]. Pages, particularly templates, may also have inline CSS.

Some pages have their own CSS, e.g., [[MediaWiki:FileUploadWizard.css]]

===User-specific style-sheets===

* Global user-specific for all skins: [[meta:Special:MyPage/global.css]]
* User-specific for all skins: [[Special:MyPage/common.css]]
* User-specific per skin: e.g., [[Special:MyPage/vector.css]], or [[Special:MyPage/skin.css]] for your current skin 
* User-specific CSS loaded through JavaScript, e.g., loaded at [[Special:MyPage/common.js]]
* User-specific web-wide browser settings: local file referred to in the browser settings, or directly set in the browser

Note that in CSS terminology, the user-specific style sheets are ''not'' [[Help:User style|user style sheets]].

===Usage===

An HTML element may be just taken from the wikitext (see [[Help:HTML in wikitext|HTML in wikitext]]), e.g., span, or the result of translating wikitext, e.g., the &lt;code&gt;&lt;nowiki&gt;'''...'''&lt;/nowiki&gt;&lt;/code&gt; code is changed into {{tag|b}}, or part of the code for the skin.

A class may be produced by the software, e.g., ns-''namespace number'' for the HTML-element "body", and extiw for an interwiki link in the page body, or taken from the wikitext.

Similarly, an ID may be produced by the software, e.g., bodyContent, or taken from the wikitext.

In the case of conflicting style settings for a piece of content, the resulting setting depends primarily on the indication "!important". Secondarily, if both are important, the user wins, if neither is, the author wins. Tertiarily it depends on specificity. Only lastly, it depends on order between and within style sheets: the last wins. Thus, a User:''username''/monobook.css does not win from MediaWiki:Monobook.css (both author, not user) if the specificity of the latter is greater. See also [http://www.w3.org/TR/REC-CSS2/cascade.html cascade].

== Supported elements ==
MediaWiki supports most CSS, with such exceptions as the url() attribute. There were some bugs in CSS support in earlier versions.

{{Further2|the [http://www.w3.org/TR/2011/REC-CSS2-20110607/ Cascading Style Sheets Level 2 Revision 1 (CSS 2.1) Specification]}}

== CSS in wikitext ==

You can use CSS styling in HTML elements in your code (see [[Help:HTML in wikitext]] for a list of elements supported by MediaWiki) like you would in normal HTML markup.

For example, a {{tag|div}} element with a green border and its contents floated to the right would be created with
&lt;syntaxhighlight lang="html"&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
&lt;/syntaxhighlight&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
which would produce the box to the right.
Some wikitext elements allow you to insert CSS styling directly into them. An example is the table syntax:
{{-}}
&lt;syntaxhighlight lang="wikitext"&gt;
{| style="your style here"
|-
|your table stuff
|}
&lt;/syntaxhighlight&gt;
{{Further2|[[Help:Tables]]}}

== MediaWiki existing styles ==

You may wish to use a style type that is already predefined by MediaWiki, or the site that you are visiting. You can also create a style that is unique to your page.

Vector is the default style, you can view it at: [[MediaWiki:Vector.css]]

You will give your CSS tag an existing "class"

Please put a list of existing classes here.

== Tips and tricks ==

=== Non-display ===
In an embedded page, one can hide comments in one version, and show them in another view.
One extreme "style" for a text is not displaying it, with

&lt;syntaxhighlight lang="css"&gt;
.''classname'' {display: none}
#''id'' {display: none}
&lt;/syntaxhighlight&gt;

etc.

Non-displayed links do not work (as opposed to links in a very small font).

It cannot be used to remove text in expressions for template names, parameter names, parameter values, page names in links, etc.

To view hidden text, download the Web Developer Toolbar for Firefox [https://addons.mozilla.org/en-US/firefox/addon/60 here], then choose Misc. &amp;rarr; show hidden elements in that toolbar. It will make all hidden elements appear.

=== Non-print ===

One can exclude content from being printed by declaring the content to be of the "noprint" class:
&lt;syntaxhighlight lang="html"&gt;
&lt;div class="someclass noprint"&gt;This will not appear in the print version.&lt;/div&gt;
&lt;/syntaxhighlight&gt;

== Major style blocks ==
[[File:Wikimedia major style blocks example.svg|right|thumb|An annotated desktop screenshot of [[Wikipedia:Example|an example article]], showcasing the style blocks]]
*''column-content'' – overall space within the margins of which the content exists.
* ''firstHeading'' – the class of the heading tag at the top of every page
*''contentSub'' – the name of the wiki immediately underneath the main heading, but above the body text
* ''content'' – the white background, thin bordered box which contains the main page content.
* ''bodyContent'' – the main page content within the content box

The portlet class is the style used by all the div blocks around the main content.  Identified blocks using that class:
*''p-cactions'' – id for the list of page-related tabs above the main content (page, talk, edit, etc.), top.
*''p-personal'' – id for the list of user-related links above the main content (username, talk, etc.), top.
*''p-logo'' – id for the block that contains the logo, top left.
*''p-navigation'' – id for the block that contains the navigation links on the left of the page
*''p-search'' – the block that contains the search buttons
*''p-tb'' – the block that contains the toolbox links
*''p-lang'' – the block that contains interlanguage links

The footer at the bottom of the page includes blocks with the following ids
*''footer'' – overall footer container block
*''f-poweredbyico'' – the powered by MediaWiki image that normally resides to the right of the page
*''f-list'' – id for the list that contains all the bits of text at the bottom of the page

== Style depending on a parameter or variable ==
=== Variable class or id ===

A class or id can depend on the result produced by a template or on a template parameter, e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1|def}}}"&lt;/syntaxhighlight&gt;. For one or more of the possible class names the style of that class can be defined. If the class is undefined it is ignored, so the standard style is used.

In the simplest case we have e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1}}}"&lt;/syntaxhighlight&gt; and define class abcdef. If the parameter value is "def" it applies.

If a page for general use only makes sense when styles are defined for certain classes, then these have to be specified in the page [[MediaWiki:Common.css]], which applies for all users and all skins, as far as not overridden.

=== Variable style parameter value ===

Wikitext that reads
&lt;syntaxhighlight lang="html"&gt;&lt;span style="display:{{{3|none}}}"&gt;Wed&lt;/span&gt;&lt;/syntaxhighlight&gt;
will display "Wed" if parameter 3 is defined, but its value is not "none", and displays nothing if parameter 3 is undefined or "none". If the value of parameter 3 is a display style other than "none", that style is applied.

==Wiki headings==
Wiki headings use the following default CSS:
&lt;section begin="HTML_section" /&gt;
{| class="wikitable"
|-
! Wikimarkup !! [[:en:HTML|HTML]] !! Style
|-
| {{nowrap|1=&lt;code&gt;= Heading =&lt;/code&gt;}} || {{tag|h1|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.8em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;== Heading ==&lt;/code&gt;}} || {{tag|h2|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.5em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;=== Heading ===&lt;/code&gt;}} || {{tag|h3|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.2em; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;==== Heading ====&lt;/code&gt;}} || {{tag|h4|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;===== Heading =====&lt;/code&gt;}} || {{tag|h5|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;====== Heading ======&lt;/code&gt;}} || {{tag|h6|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
|}&lt;section end="HTML_section" /&gt;

== See also ==
*[[:en:Span and div|Span and div]]
*[[:en:Cascading Style Sheets|Cascading Style Sheets]] – article on CSS
*[[Wikipedia:Customisation]] – also covers user names, preferences settings, skins, user scripting, etc.
*[[Help:User style]] – modifying style for accessibility or for additional feature testing.
*[[Wikipedia:TemplateStyles]] – modifying style for advanced visual appearance that can be applied with template.
*[[Wikipedia:Catalogue of CSS classes]] – list of classes globally defined across the site
*[[Wikipedia:WikiProject Microformats/classes]] – list of classes used in microformats employed on Wikipedia
*[[Help:User CSS for a monospaced coding font]] – both for the editing window and for display of monospaced elements like {{tag|code|o}}
*[[meta:Help:Cascading_style_sheets]]
*[[mw:Manual:CSS]] and [[mw:Manual:Interface/Stylesheets]]
*[[mw:Gallery of user styles]]
*[[m:Customization:Explaining skins]]
*[[mw:Skin projects]]

{{Wikipedia technical help|collapsed}}
{{CSS and JS MediaWiki messages|state=collapsed|style=}}

[[Category:Wikipedia features]]
[[Category:Wikipedia help]]
[[sn:Cascading Style Sheets]][[en:Cascading Style Sheets]]</text>
      <sha1>3kwj84hk6u6b9scjj9573lckaubx8dp</sha1>
    </revision>
    <revision>
      <id>359565</id>
      <parentid>3317</parentid>
      <timestamp>2026-02-20T01:49:14Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "[[sn:" to "[[Category:sn_crud|TEST"</comment>
      <origin>359565</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="13912" sha1="hdt3k2hosxzm2tlkacmw9s2hkh4rnhr" xml:space="preserve">{{Information page|H:CSS|WP:CSS}}
[[:en:Cascading Style Sheets|Cascading Style Sheets]] allows for flexible formatting of a page. They should be used instead of [[Help:Table|tables]] for non-tabular content whenever possible, because they can be [[Help:User style|manipulated by the reader]] or overridden by an author if your CSS is embedded in another page via a [[Help:Template|template]].

== Levels of CSS settings ==
Style may be chosen specifically for a piece of content, see e.g., [[Help:Table#Color; scope of parameters|color; scope of parameters]]

Alternatively, style is specified for CSS selectors, expressed in terms of elements, classes, and ID's. This is done on various levels:

Author style sheets, in this order:

''Note: See [[WP:CLASS]] for a list of all the style sheets loaded.''

===MediaWiki core style-sheets===

Per skin: [[mw:Gallery of user styles|MediaWiki Manual:Gallery of user styles]] etc. 
Typically loaded style sheets:
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/shared.css common/shared.css]
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/commonPrint.css common/commonPrint.css]

;Skin-specific main file:
e.g., [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/main.css monobook/main.css] (normal skin for PC's), [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/chick/main.css chick/main.css] (normal skin for handhelds)

;Browser-specific fixes (also skin-specific): 
Examples for Monobook:
*For Firefox: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/FF2Fixes.css monobook/FF2Fixes.css]
*For Internet Explorer: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE60Fixes.css monobook/IE60Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE70Fixes.css monobook/IE70Fixes.css]
*For Opera: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera6Fixes.css monobook/Opera6Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera7Fixes.css monobook/Opera7Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera9Fixes.css monobook/Opera9Fixes.css]

===Site-wide style-sheets===

* Site-wide for all skins (desktop view only): [[MediaWiki:Common.css]]
* Site-wide for all skins (mobile view only): [[MediaWiki:Mobile.css]]
* Site-wide per skin: [[MediaWiki:Vector.css]], [[MediaWiki:Monobook.css]], etc.
* Site-wide for user groups: [[MediaWiki:Group-user.css]], [[MediaWiki:Group-autoconfirmed.css]], etc.
* Site-wide for specialist purposes: [[MediaWiki:Print.css]], [[MediaWiki:Noscript.css]], [[MediaWiki:Filepage.css]]
* Site-wide if gadgets loaded: see [[Wikipedia:Gadget]] for more information

Note: MediaWiki sites other than English Wikipedia may use &lt;code&gt;MediaWiki:Gadget-site.css&lt;/code&gt; instead of MediaWiki:Common.css.

===Page-specific style-sheets===

Page-specific style-sheets may be introduced through [[Wikipedia:TemplateStyles|TemplateStyles]]. Pages, particularly templates, may also have inline CSS.

Some pages have their own CSS, e.g., [[MediaWiki:FileUploadWizard.css]]

===User-specific style-sheets===

* Global user-specific for all skins: [[meta:Special:MyPage/global.css]]
* User-specific for all skins: [[Special:MyPage/common.css]]
* User-specific per skin: e.g., [[Special:MyPage/vector.css]], or [[Special:MyPage/skin.css]] for your current skin 
* User-specific CSS loaded through JavaScript, e.g., loaded at [[Special:MyPage/common.js]]
* User-specific web-wide browser settings: local file referred to in the browser settings, or directly set in the browser

Note that in CSS terminology, the user-specific style sheets are ''not'' [[Help:User style|user style sheets]].

===Usage===

An HTML element may be just taken from the wikitext (see [[Help:HTML in wikitext|HTML in wikitext]]), e.g., span, or the result of translating wikitext, e.g., the &lt;code&gt;&lt;nowiki&gt;'''...'''&lt;/nowiki&gt;&lt;/code&gt; code is changed into {{tag|b}}, or part of the code for the skin.

A class may be produced by the software, e.g., ns-''namespace number'' for the HTML-element "body", and extiw for an interwiki link in the page body, or taken from the wikitext.

Similarly, an ID may be produced by the software, e.g., bodyContent, or taken from the wikitext.

In the case of conflicting style settings for a piece of content, the resulting setting depends primarily on the indication "!important". Secondarily, if both are important, the user wins, if neither is, the author wins. Tertiarily it depends on specificity. Only lastly, it depends on order between and within style sheets: the last wins. Thus, a User:''username''/monobook.css does not win from MediaWiki:Monobook.css (both author, not user) if the specificity of the latter is greater. See also [http://www.w3.org/TR/REC-CSS2/cascade.html cascade].

== Supported elements ==
MediaWiki supports most CSS, with such exceptions as the url() attribute. There were some bugs in CSS support in earlier versions.

{{Further2|the [http://www.w3.org/TR/2011/REC-CSS2-20110607/ Cascading Style Sheets Level 2 Revision 1 (CSS 2.1) Specification]}}

== CSS in wikitext ==

You can use CSS styling in HTML elements in your code (see [[Help:HTML in wikitext]] for a list of elements supported by MediaWiki) like you would in normal HTML markup.

For example, a {{tag|div}} element with a green border and its contents floated to the right would be created with
&lt;syntaxhighlight lang="html"&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
&lt;/syntaxhighlight&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
which would produce the box to the right.
Some wikitext elements allow you to insert CSS styling directly into them. An example is the table syntax:
{{-}}
&lt;syntaxhighlight lang="wikitext"&gt;
{| style="your style here"
|-
|your table stuff
|}
&lt;/syntaxhighlight&gt;
{{Further2|[[Help:Tables]]}}

== MediaWiki existing styles ==

You may wish to use a style type that is already predefined by MediaWiki, or the site that you are visiting. You can also create a style that is unique to your page.

Vector is the default style, you can view it at: [[MediaWiki:Vector.css]]

You will give your CSS tag an existing "class"

Please put a list of existing classes here.

== Tips and tricks ==

=== Non-display ===
In an embedded page, one can hide comments in one version, and show them in another view.
One extreme "style" for a text is not displaying it, with

&lt;syntaxhighlight lang="css"&gt;
.''classname'' {display: none}
#''id'' {display: none}
&lt;/syntaxhighlight&gt;

etc.

Non-displayed links do not work (as opposed to links in a very small font).

It cannot be used to remove text in expressions for template names, parameter names, parameter values, page names in links, etc.

To view hidden text, download the Web Developer Toolbar for Firefox [https://addons.mozilla.org/en-US/firefox/addon/60 here], then choose Misc. &amp;rarr; show hidden elements in that toolbar. It will make all hidden elements appear.

=== Non-print ===

One can exclude content from being printed by declaring the content to be of the "noprint" class:
&lt;syntaxhighlight lang="html"&gt;
&lt;div class="someclass noprint"&gt;This will not appear in the print version.&lt;/div&gt;
&lt;/syntaxhighlight&gt;

== Major style blocks ==
[[File:Wikimedia major style blocks example.svg|right|thumb|An annotated desktop screenshot of [[Wikipedia:Example|an example article]], showcasing the style blocks]]
*''column-content'' – overall space within the margins of which the content exists.
* ''firstHeading'' – the class of the heading tag at the top of every page
*''contentSub'' – the name of the wiki immediately underneath the main heading, but above the body text
* ''content'' – the white background, thin bordered box which contains the main page content.
* ''bodyContent'' – the main page content within the content box

The portlet class is the style used by all the div blocks around the main content.  Identified blocks using that class:
*''p-cactions'' – id for the list of page-related tabs above the main content (page, talk, edit, etc.), top.
*''p-personal'' – id for the list of user-related links above the main content (username, talk, etc.), top.
*''p-logo'' – id for the block that contains the logo, top left.
*''p-navigation'' – id for the block that contains the navigation links on the left of the page
*''p-search'' – the block that contains the search buttons
*''p-tb'' – the block that contains the toolbox links
*''p-lang'' – the block that contains interlanguage links

The footer at the bottom of the page includes blocks with the following ids
*''footer'' – overall footer container block
*''f-poweredbyico'' – the powered by MediaWiki image that normally resides to the right of the page
*''f-list'' – id for the list that contains all the bits of text at the bottom of the page

== Style depending on a parameter or variable ==
=== Variable class or id ===

A class or id can depend on the result produced by a template or on a template parameter, e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1|def}}}"&lt;/syntaxhighlight&gt;. For one or more of the possible class names the style of that class can be defined. If the class is undefined it is ignored, so the standard style is used.

In the simplest case we have e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1}}}"&lt;/syntaxhighlight&gt; and define class abcdef. If the parameter value is "def" it applies.

If a page for general use only makes sense when styles are defined for certain classes, then these have to be specified in the page [[MediaWiki:Common.css]], which applies for all users and all skins, as far as not overridden.

=== Variable style parameter value ===

Wikitext that reads
&lt;syntaxhighlight lang="html"&gt;&lt;span style="display:{{{3|none}}}"&gt;Wed&lt;/span&gt;&lt;/syntaxhighlight&gt;
will display "Wed" if parameter 3 is defined, but its value is not "none", and displays nothing if parameter 3 is undefined or "none". If the value of parameter 3 is a display style other than "none", that style is applied.

==Wiki headings==
Wiki headings use the following default CSS:
&lt;section begin="HTML_section" /&gt;
{| class="wikitable"
|-
! Wikimarkup !! [[:en:HTML|HTML]] !! Style
|-
| {{nowrap|1=&lt;code&gt;= Heading =&lt;/code&gt;}} || {{tag|h1|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.8em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;== Heading ==&lt;/code&gt;}} || {{tag|h2|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.5em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;=== Heading ===&lt;/code&gt;}} || {{tag|h3|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.2em; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;==== Heading ====&lt;/code&gt;}} || {{tag|h4|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;===== Heading =====&lt;/code&gt;}} || {{tag|h5|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;====== Heading ======&lt;/code&gt;}} || {{tag|h6|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
|}&lt;section end="HTML_section" /&gt;

== See also ==
*[[:en:Span and div|Span and div]]
*[[:en:Cascading Style Sheets|Cascading Style Sheets]] – article on CSS
*[[Wikipedia:Customisation]] – also covers user names, preferences settings, skins, user scripting, etc.
*[[Help:User style]] – modifying style for accessibility or for additional feature testing.
*[[Wikipedia:TemplateStyles]] – modifying style for advanced visual appearance that can be applied with template.
*[[Wikipedia:Catalogue of CSS classes]] – list of classes globally defined across the site
*[[Wikipedia:WikiProject Microformats/classes]] – list of classes used in microformats employed on Wikipedia
*[[Help:User CSS for a monospaced coding font]] – both for the editing window and for display of monospaced elements like {{tag|code|o}}
*[[meta:Help:Cascading_style_sheets]]
*[[mw:Manual:CSS]] and [[mw:Manual:Interface/Stylesheets]]
*[[mw:Gallery of user styles]]
*[[m:Customization:Explaining skins]]
*[[mw:Skin projects]]

{{Wikipedia technical help|collapsed}}
{{CSS and JS MediaWiki messages|state=collapsed|style=}}

[[Category:Wikipedia features]]
[[Category:Wikipedia help]]
[[Category:sn_crud|TESTCascading Style Sheets]][[en:Cascading Style Sheets]]</text>
      <sha1>hdt3k2hosxzm2tlkacmw9s2hkh4rnhr</sha1>
    </revision>
    <revision>
      <id>3600200</id>
      <parentid>359565</parentid>
      <timestamp>2026-04-05T02:13:50Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <comment>Bot: remove [[Category:Sn crud]] (crud category cleanup) [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23991619004|time triggered pipeline]]</comment>
      <origin>3600200</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="13865" sha1="34liwazgmxxll7v03bzt53hay8rwgng" xml:space="preserve">{{Information page|H:CSS|WP:CSS}}
[[:en:Cascading Style Sheets|Cascading Style Sheets]] allows for flexible formatting of a page. They should be used instead of [[Help:Table|tables]] for non-tabular content whenever possible, because they can be [[Help:User style|manipulated by the reader]] or overridden by an author if your CSS is embedded in another page via a [[Help:Template|template]].

== Levels of CSS settings ==
Style may be chosen specifically for a piece of content, see e.g., [[Help:Table#Color; scope of parameters|color; scope of parameters]]

Alternatively, style is specified for CSS selectors, expressed in terms of elements, classes, and ID's. This is done on various levels:

Author style sheets, in this order:

''Note: See [[WP:CLASS]] for a list of all the style sheets loaded.''

===MediaWiki core style-sheets===

Per skin: [[mw:Gallery of user styles|MediaWiki Manual:Gallery of user styles]] etc. 
Typically loaded style sheets:
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/shared.css common/shared.css]
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/commonPrint.css common/commonPrint.css]

;Skin-specific main file:
e.g., [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/main.css monobook/main.css] (normal skin for PC's), [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/chick/main.css chick/main.css] (normal skin for handhelds)

;Browser-specific fixes (also skin-specific): 
Examples for Monobook:
*For Firefox: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/FF2Fixes.css monobook/FF2Fixes.css]
*For Internet Explorer: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE60Fixes.css monobook/IE60Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE70Fixes.css monobook/IE70Fixes.css]
*For Opera: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera6Fixes.css monobook/Opera6Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera7Fixes.css monobook/Opera7Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera9Fixes.css monobook/Opera9Fixes.css]

===Site-wide style-sheets===

* Site-wide for all skins (desktop view only): [[MediaWiki:Common.css]]
* Site-wide for all skins (mobile view only): [[MediaWiki:Mobile.css]]
* Site-wide per skin: [[MediaWiki:Vector.css]], [[MediaWiki:Monobook.css]], etc.
* Site-wide for user groups: [[MediaWiki:Group-user.css]], [[MediaWiki:Group-autoconfirmed.css]], etc.
* Site-wide for specialist purposes: [[MediaWiki:Print.css]], [[MediaWiki:Noscript.css]], [[MediaWiki:Filepage.css]]
* Site-wide if gadgets loaded: see [[Wikipedia:Gadget]] for more information

Note: MediaWiki sites other than English Wikipedia may use &lt;code&gt;MediaWiki:Gadget-site.css&lt;/code&gt; instead of MediaWiki:Common.css.

===Page-specific style-sheets===

Page-specific style-sheets may be introduced through [[Wikipedia:TemplateStyles|TemplateStyles]]. Pages, particularly templates, may also have inline CSS.

Some pages have their own CSS, e.g., [[MediaWiki:FileUploadWizard.css]]

===User-specific style-sheets===

* Global user-specific for all skins: [[meta:Special:MyPage/global.css]]
* User-specific for all skins: [[Special:MyPage/common.css]]
* User-specific per skin: e.g., [[Special:MyPage/vector.css]], or [[Special:MyPage/skin.css]] for your current skin 
* User-specific CSS loaded through JavaScript, e.g., loaded at [[Special:MyPage/common.js]]
* User-specific web-wide browser settings: local file referred to in the browser settings, or directly set in the browser

Note that in CSS terminology, the user-specific style sheets are ''not'' [[Help:User style|user style sheets]].

===Usage===

An HTML element may be just taken from the wikitext (see [[Help:HTML in wikitext|HTML in wikitext]]), e.g., span, or the result of translating wikitext, e.g., the &lt;code&gt;&lt;nowiki&gt;'''...'''&lt;/nowiki&gt;&lt;/code&gt; code is changed into {{tag|b}}, or part of the code for the skin.

A class may be produced by the software, e.g., ns-''namespace number'' for the HTML-element "body", and extiw for an interwiki link in the page body, or taken from the wikitext.

Similarly, an ID may be produced by the software, e.g., bodyContent, or taken from the wikitext.

In the case of conflicting style settings for a piece of content, the resulting setting depends primarily on the indication "!important". Secondarily, if both are important, the user wins, if neither is, the author wins. Tertiarily it depends on specificity. Only lastly, it depends on order between and within style sheets: the last wins. Thus, a User:''username''/monobook.css does not win from MediaWiki:Monobook.css (both author, not user) if the specificity of the latter is greater. See also [http://www.w3.org/TR/REC-CSS2/cascade.html cascade].

== Supported elements ==
MediaWiki supports most CSS, with such exceptions as the url() attribute. There were some bugs in CSS support in earlier versions.

{{Further2|the [http://www.w3.org/TR/2011/REC-CSS2-20110607/ Cascading Style Sheets Level 2 Revision 1 (CSS 2.1) Specification]}}

== CSS in wikitext ==

You can use CSS styling in HTML elements in your code (see [[Help:HTML in wikitext]] for a list of elements supported by MediaWiki) like you would in normal HTML markup.

For example, a {{tag|div}} element with a green border and its contents floated to the right would be created with
&lt;syntaxhighlight lang="html"&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
&lt;/syntaxhighlight&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
which would produce the box to the right.
Some wikitext elements allow you to insert CSS styling directly into them. An example is the table syntax:
{{-}}
&lt;syntaxhighlight lang="wikitext"&gt;
{| style="your style here"
|-
|your table stuff
|}
&lt;/syntaxhighlight&gt;
{{Further2|[[Help:Tables]]}}

== MediaWiki existing styles ==

You may wish to use a style type that is already predefined by MediaWiki, or the site that you are visiting. You can also create a style that is unique to your page.

Vector is the default style, you can view it at: [[MediaWiki:Vector.css]]

You will give your CSS tag an existing "class"

Please put a list of existing classes here.

== Tips and tricks ==

=== Non-display ===
In an embedded page, one can hide comments in one version, and show them in another view.
One extreme "style" for a text is not displaying it, with

&lt;syntaxhighlight lang="css"&gt;
.''classname'' {display: none}
#''id'' {display: none}
&lt;/syntaxhighlight&gt;

etc.

Non-displayed links do not work (as opposed to links in a very small font).

It cannot be used to remove text in expressions for template names, parameter names, parameter values, page names in links, etc.

To view hidden text, download the Web Developer Toolbar for Firefox [https://addons.mozilla.org/en-US/firefox/addon/60 here], then choose Misc. &amp;rarr; show hidden elements in that toolbar. It will make all hidden elements appear.

=== Non-print ===

One can exclude content from being printed by declaring the content to be of the "noprint" class:
&lt;syntaxhighlight lang="html"&gt;
&lt;div class="someclass noprint"&gt;This will not appear in the print version.&lt;/div&gt;
&lt;/syntaxhighlight&gt;

== Major style blocks ==
[[File:Wikimedia major style blocks example.svg|right|thumb|An annotated desktop screenshot of [[Wikipedia:Example|an example article]], showcasing the style blocks]]
*''column-content'' – overall space within the margins of which the content exists.
* ''firstHeading'' – the class of the heading tag at the top of every page
*''contentSub'' – the name of the wiki immediately underneath the main heading, but above the body text
* ''content'' – the white background, thin bordered box which contains the main page content.
* ''bodyContent'' – the main page content within the content box

The portlet class is the style used by all the div blocks around the main content.  Identified blocks using that class:
*''p-cactions'' – id for the list of page-related tabs above the main content (page, talk, edit, etc.), top.
*''p-personal'' – id for the list of user-related links above the main content (username, talk, etc.), top.
*''p-logo'' – id for the block that contains the logo, top left.
*''p-navigation'' – id for the block that contains the navigation links on the left of the page
*''p-search'' – the block that contains the search buttons
*''p-tb'' – the block that contains the toolbox links
*''p-lang'' – the block that contains interlanguage links

The footer at the bottom of the page includes blocks with the following ids
*''footer'' – overall footer container block
*''f-poweredbyico'' – the powered by MediaWiki image that normally resides to the right of the page
*''f-list'' – id for the list that contains all the bits of text at the bottom of the page

== Style depending on a parameter or variable ==
=== Variable class or id ===

A class or id can depend on the result produced by a template or on a template parameter, e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1|def}}}"&lt;/syntaxhighlight&gt;. For one or more of the possible class names the style of that class can be defined. If the class is undefined it is ignored, so the standard style is used.

In the simplest case we have e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1}}}"&lt;/syntaxhighlight&gt; and define class abcdef. If the parameter value is "def" it applies.

If a page for general use only makes sense when styles are defined for certain classes, then these have to be specified in the page [[MediaWiki:Common.css]], which applies for all users and all skins, as far as not overridden.

=== Variable style parameter value ===

Wikitext that reads
&lt;syntaxhighlight lang="html"&gt;&lt;span style="display:{{{3|none}}}"&gt;Wed&lt;/span&gt;&lt;/syntaxhighlight&gt;
will display "Wed" if parameter 3 is defined, but its value is not "none", and displays nothing if parameter 3 is undefined or "none". If the value of parameter 3 is a display style other than "none", that style is applied.

==Wiki headings==
Wiki headings use the following default CSS:
&lt;section begin="HTML_section" /&gt;
{| class="wikitable"
|-
! Wikimarkup !! [[:en:HTML|HTML]] !! Style
|-
| {{nowrap|1=&lt;code&gt;= Heading =&lt;/code&gt;}} || {{tag|h1|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.8em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;== Heading ==&lt;/code&gt;}} || {{tag|h2|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.5em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;=== Heading ===&lt;/code&gt;}} || {{tag|h3|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.2em; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;==== Heading ====&lt;/code&gt;}} || {{tag|h4|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;===== Heading =====&lt;/code&gt;}} || {{tag|h5|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;====== Heading ======&lt;/code&gt;}} || {{tag|h6|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
|}&lt;section end="HTML_section" /&gt;

== See also ==
*[[:en:Span and div|Span and div]]
*[[:en:Cascading Style Sheets|Cascading Style Sheets]] – article on CSS
*[[Wikipedia:Customisation]] – also covers user names, preferences settings, skins, user scripting, etc.
*[[Help:User style]] – modifying style for accessibility or for additional feature testing.
*[[Wikipedia:TemplateStyles]] – modifying style for advanced visual appearance that can be applied with template.
*[[Wikipedia:Catalogue of CSS classes]] – list of classes globally defined across the site
*[[Wikipedia:WikiProject Microformats/classes]] – list of classes used in microformats employed on Wikipedia
*[[Help:User CSS for a monospaced coding font]] – both for the editing window and for display of monospaced elements like {{tag|code|o}}
*[[meta:Help:Cascading_style_sheets]]
*[[mw:Manual:CSS]] and [[mw:Manual:Interface/Stylesheets]]
*[[mw:Gallery of user styles]]
*[[m:Customization:Explaining skins]]
*[[mw:Skin projects]]

{{Wikipedia technical help|collapsed}}
{{CSS and JS MediaWiki messages|state=collapsed|style=}}

[[Category:Wikipedia features]]
[[Category:Wikipedia help]]
[[en:Cascading Style Sheets]]</text>
      <sha1>34liwazgmxxll7v03bzt53hay8rwgng</sha1>
    </revision>
    <revision>
      <id>3607009</id>
      <parentid>3600200</parentid>
      <timestamp>2026-04-17T02:27:35Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <comment>Bot: remove [[Category:Wikipedia features]] (crud category cleanup) [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/24543431184|time triggered pipeline]]</comment>
      <origin>3607009</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="13833" sha1="6ged0mjcii7jypq8zqslqkbncegjahi" xml:space="preserve">{{Information page|H:CSS|WP:CSS}}
[[:en:Cascading Style Sheets|Cascading Style Sheets]] allows for flexible formatting of a page. They should be used instead of [[Help:Table|tables]] for non-tabular content whenever possible, because they can be [[Help:User style|manipulated by the reader]] or overridden by an author if your CSS is embedded in another page via a [[Help:Template|template]].

== Levels of CSS settings ==
Style may be chosen specifically for a piece of content, see e.g., [[Help:Table#Color; scope of parameters|color; scope of parameters]]

Alternatively, style is specified for CSS selectors, expressed in terms of elements, classes, and ID's. This is done on various levels:

Author style sheets, in this order:

''Note: See [[WP:CLASS]] for a list of all the style sheets loaded.''

===MediaWiki core style-sheets===

Per skin: [[mw:Gallery of user styles|MediaWiki Manual:Gallery of user styles]] etc. 
Typically loaded style sheets:
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/shared.css common/shared.css]
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/commonPrint.css common/commonPrint.css]

;Skin-specific main file:
e.g., [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/main.css monobook/main.css] (normal skin for PC's), [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/chick/main.css chick/main.css] (normal skin for handhelds)

;Browser-specific fixes (also skin-specific): 
Examples for Monobook:
*For Firefox: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/FF2Fixes.css monobook/FF2Fixes.css]
*For Internet Explorer: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE60Fixes.css monobook/IE60Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE70Fixes.css monobook/IE70Fixes.css]
*For Opera: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera6Fixes.css monobook/Opera6Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera7Fixes.css monobook/Opera7Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera9Fixes.css monobook/Opera9Fixes.css]

===Site-wide style-sheets===

* Site-wide for all skins (desktop view only): [[MediaWiki:Common.css]]
* Site-wide for all skins (mobile view only): [[MediaWiki:Mobile.css]]
* Site-wide per skin: [[MediaWiki:Vector.css]], [[MediaWiki:Monobook.css]], etc.
* Site-wide for user groups: [[MediaWiki:Group-user.css]], [[MediaWiki:Group-autoconfirmed.css]], etc.
* Site-wide for specialist purposes: [[MediaWiki:Print.css]], [[MediaWiki:Noscript.css]], [[MediaWiki:Filepage.css]]
* Site-wide if gadgets loaded: see [[Wikipedia:Gadget]] for more information

Note: MediaWiki sites other than English Wikipedia may use &lt;code&gt;MediaWiki:Gadget-site.css&lt;/code&gt; instead of MediaWiki:Common.css.

===Page-specific style-sheets===

Page-specific style-sheets may be introduced through [[Wikipedia:TemplateStyles|TemplateStyles]]. Pages, particularly templates, may also have inline CSS.

Some pages have their own CSS, e.g., [[MediaWiki:FileUploadWizard.css]]

===User-specific style-sheets===

* Global user-specific for all skins: [[meta:Special:MyPage/global.css]]
* User-specific for all skins: [[Special:MyPage/common.css]]
* User-specific per skin: e.g., [[Special:MyPage/vector.css]], or [[Special:MyPage/skin.css]] for your current skin 
* User-specific CSS loaded through JavaScript, e.g., loaded at [[Special:MyPage/common.js]]
* User-specific web-wide browser settings: local file referred to in the browser settings, or directly set in the browser

Note that in CSS terminology, the user-specific style sheets are ''not'' [[Help:User style|user style sheets]].

===Usage===

An HTML element may be just taken from the wikitext (see [[Help:HTML in wikitext|HTML in wikitext]]), e.g., span, or the result of translating wikitext, e.g., the &lt;code&gt;&lt;nowiki&gt;'''...'''&lt;/nowiki&gt;&lt;/code&gt; code is changed into {{tag|b}}, or part of the code for the skin.

A class may be produced by the software, e.g., ns-''namespace number'' for the HTML-element "body", and extiw for an interwiki link in the page body, or taken from the wikitext.

Similarly, an ID may be produced by the software, e.g., bodyContent, or taken from the wikitext.

In the case of conflicting style settings for a piece of content, the resulting setting depends primarily on the indication "!important". Secondarily, if both are important, the user wins, if neither is, the author wins. Tertiarily it depends on specificity. Only lastly, it depends on order between and within style sheets: the last wins. Thus, a User:''username''/monobook.css does not win from MediaWiki:Monobook.css (both author, not user) if the specificity of the latter is greater. See also [http://www.w3.org/TR/REC-CSS2/cascade.html cascade].

== Supported elements ==
MediaWiki supports most CSS, with such exceptions as the url() attribute. There were some bugs in CSS support in earlier versions.

{{Further2|the [http://www.w3.org/TR/2011/REC-CSS2-20110607/ Cascading Style Sheets Level 2 Revision 1 (CSS 2.1) Specification]}}

== CSS in wikitext ==

You can use CSS styling in HTML elements in your code (see [[Help:HTML in wikitext]] for a list of elements supported by MediaWiki) like you would in normal HTML markup.

For example, a {{tag|div}} element with a green border and its contents floated to the right would be created with
&lt;syntaxhighlight lang="html"&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
&lt;/syntaxhighlight&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
which would produce the box to the right.
Some wikitext elements allow you to insert CSS styling directly into them. An example is the table syntax:
{{-}}
&lt;syntaxhighlight lang="wikitext"&gt;
{| style="your style here"
|-
|your table stuff
|}
&lt;/syntaxhighlight&gt;
{{Further2|[[Help:Tables]]}}

== MediaWiki existing styles ==

You may wish to use a style type that is already predefined by MediaWiki, or the site that you are visiting. You can also create a style that is unique to your page.

Vector is the default style, you can view it at: [[MediaWiki:Vector.css]]

You will give your CSS tag an existing "class"

Please put a list of existing classes here.

== Tips and tricks ==

=== Non-display ===
In an embedded page, one can hide comments in one version, and show them in another view.
One extreme "style" for a text is not displaying it, with

&lt;syntaxhighlight lang="css"&gt;
.''classname'' {display: none}
#''id'' {display: none}
&lt;/syntaxhighlight&gt;

etc.

Non-displayed links do not work (as opposed to links in a very small font).

It cannot be used to remove text in expressions for template names, parameter names, parameter values, page names in links, etc.

To view hidden text, download the Web Developer Toolbar for Firefox [https://addons.mozilla.org/en-US/firefox/addon/60 here], then choose Misc. &amp;rarr; show hidden elements in that toolbar. It will make all hidden elements appear.

=== Non-print ===

One can exclude content from being printed by declaring the content to be of the "noprint" class:
&lt;syntaxhighlight lang="html"&gt;
&lt;div class="someclass noprint"&gt;This will not appear in the print version.&lt;/div&gt;
&lt;/syntaxhighlight&gt;

== Major style blocks ==
[[File:Wikimedia major style blocks example.svg|right|thumb|An annotated desktop screenshot of [[Wikipedia:Example|an example article]], showcasing the style blocks]]
*''column-content'' – overall space within the margins of which the content exists.
* ''firstHeading'' – the class of the heading tag at the top of every page
*''contentSub'' – the name of the wiki immediately underneath the main heading, but above the body text
* ''content'' – the white background, thin bordered box which contains the main page content.
* ''bodyContent'' – the main page content within the content box

The portlet class is the style used by all the div blocks around the main content.  Identified blocks using that class:
*''p-cactions'' – id for the list of page-related tabs above the main content (page, talk, edit, etc.), top.
*''p-personal'' – id for the list of user-related links above the main content (username, talk, etc.), top.
*''p-logo'' – id for the block that contains the logo, top left.
*''p-navigation'' – id for the block that contains the navigation links on the left of the page
*''p-search'' – the block that contains the search buttons
*''p-tb'' – the block that contains the toolbox links
*''p-lang'' – the block that contains interlanguage links

The footer at the bottom of the page includes blocks with the following ids
*''footer'' – overall footer container block
*''f-poweredbyico'' – the powered by MediaWiki image that normally resides to the right of the page
*''f-list'' – id for the list that contains all the bits of text at the bottom of the page

== Style depending on a parameter or variable ==
=== Variable class or id ===

A class or id can depend on the result produced by a template or on a template parameter, e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1|def}}}"&lt;/syntaxhighlight&gt;. For one or more of the possible class names the style of that class can be defined. If the class is undefined it is ignored, so the standard style is used.

In the simplest case we have e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1}}}"&lt;/syntaxhighlight&gt; and define class abcdef. If the parameter value is "def" it applies.

If a page for general use only makes sense when styles are defined for certain classes, then these have to be specified in the page [[MediaWiki:Common.css]], which applies for all users and all skins, as far as not overridden.

=== Variable style parameter value ===

Wikitext that reads
&lt;syntaxhighlight lang="html"&gt;&lt;span style="display:{{{3|none}}}"&gt;Wed&lt;/span&gt;&lt;/syntaxhighlight&gt;
will display "Wed" if parameter 3 is defined, but its value is not "none", and displays nothing if parameter 3 is undefined or "none". If the value of parameter 3 is a display style other than "none", that style is applied.

==Wiki headings==
Wiki headings use the following default CSS:
&lt;section begin="HTML_section" /&gt;
{| class="wikitable"
|-
! Wikimarkup !! [[:en:HTML|HTML]] !! Style
|-
| {{nowrap|1=&lt;code&gt;= Heading =&lt;/code&gt;}} || {{tag|h1|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.8em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;== Heading ==&lt;/code&gt;}} || {{tag|h2|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.5em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;=== Heading ===&lt;/code&gt;}} || {{tag|h3|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.2em; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;==== Heading ====&lt;/code&gt;}} || {{tag|h4|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;===== Heading =====&lt;/code&gt;}} || {{tag|h5|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;====== Heading ======&lt;/code&gt;}} || {{tag|h6|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
|}&lt;section end="HTML_section" /&gt;

== See also ==
*[[:en:Span and div|Span and div]]
*[[:en:Cascading Style Sheets|Cascading Style Sheets]] – article on CSS
*[[Wikipedia:Customisation]] – also covers user names, preferences settings, skins, user scripting, etc.
*[[Help:User style]] – modifying style for accessibility or for additional feature testing.
*[[Wikipedia:TemplateStyles]] – modifying style for advanced visual appearance that can be applied with template.
*[[Wikipedia:Catalogue of CSS classes]] – list of classes globally defined across the site
*[[Wikipedia:WikiProject Microformats/classes]] – list of classes used in microformats employed on Wikipedia
*[[Help:User CSS for a monospaced coding font]] – both for the editing window and for display of monospaced elements like {{tag|code|o}}
*[[meta:Help:Cascading_style_sheets]]
*[[mw:Manual:CSS]] and [[mw:Manual:Interface/Stylesheets]]
*[[mw:Gallery of user styles]]
*[[m:Customization:Explaining skins]]
*[[mw:Skin projects]]

{{Wikipedia technical help|collapsed}}
{{CSS and JS MediaWiki messages|state=collapsed|style=}}

[[Category:Wikipedia help]]
[[en:Cascading Style Sheets]]</text>
      <sha1>6ged0mjcii7jypq8zqslqkbncegjahi</sha1>
    </revision>
    <revision>
      <id>3607096</id>
      <parentid>3607009</parentid>
      <timestamp>2026-04-17T02:32:13Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <comment>Bot: remove [[Category:Wikipedia help]] (crud category cleanup) [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/24543431184|time triggered pipeline]]</comment>
      <origin>3607096</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="13805" sha1="qikrifkfjxneke7aqz87fct4amthgjo" xml:space="preserve">{{Information page|H:CSS|WP:CSS}}
[[:en:Cascading Style Sheets|Cascading Style Sheets]] allows for flexible formatting of a page. They should be used instead of [[Help:Table|tables]] for non-tabular content whenever possible, because they can be [[Help:User style|manipulated by the reader]] or overridden by an author if your CSS is embedded in another page via a [[Help:Template|template]].

== Levels of CSS settings ==
Style may be chosen specifically for a piece of content, see e.g., [[Help:Table#Color; scope of parameters|color; scope of parameters]]

Alternatively, style is specified for CSS selectors, expressed in terms of elements, classes, and ID's. This is done on various levels:

Author style sheets, in this order:

''Note: See [[WP:CLASS]] for a list of all the style sheets loaded.''

===MediaWiki core style-sheets===

Per skin: [[mw:Gallery of user styles|MediaWiki Manual:Gallery of user styles]] etc. 
Typically loaded style sheets:
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/shared.css common/shared.css]
*[https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/common/commonPrint.css common/commonPrint.css]

;Skin-specific main file:
e.g., [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/main.css monobook/main.css] (normal skin for PC's), [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/chick/main.css chick/main.css] (normal skin for handhelds)

;Browser-specific fixes (also skin-specific): 
Examples for Monobook:
*For Firefox: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/FF2Fixes.css monobook/FF2Fixes.css]
*For Internet Explorer: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE60Fixes.css monobook/IE60Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/IE70Fixes.css monobook/IE70Fixes.css]
*For Opera: [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera6Fixes.css monobook/Opera6Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera7Fixes.css monobook/Opera7Fixes.css] [https://phabricator.wikimedia.org/diffusion/SVN/browse/trunk/phase3/skins/monobook/Opera9Fixes.css monobook/Opera9Fixes.css]

===Site-wide style-sheets===

* Site-wide for all skins (desktop view only): [[MediaWiki:Common.css]]
* Site-wide for all skins (mobile view only): [[MediaWiki:Mobile.css]]
* Site-wide per skin: [[MediaWiki:Vector.css]], [[MediaWiki:Monobook.css]], etc.
* Site-wide for user groups: [[MediaWiki:Group-user.css]], [[MediaWiki:Group-autoconfirmed.css]], etc.
* Site-wide for specialist purposes: [[MediaWiki:Print.css]], [[MediaWiki:Noscript.css]], [[MediaWiki:Filepage.css]]
* Site-wide if gadgets loaded: see [[Wikipedia:Gadget]] for more information

Note: MediaWiki sites other than English Wikipedia may use &lt;code&gt;MediaWiki:Gadget-site.css&lt;/code&gt; instead of MediaWiki:Common.css.

===Page-specific style-sheets===

Page-specific style-sheets may be introduced through [[Wikipedia:TemplateStyles|TemplateStyles]]. Pages, particularly templates, may also have inline CSS.

Some pages have their own CSS, e.g., [[MediaWiki:FileUploadWizard.css]]

===User-specific style-sheets===

* Global user-specific for all skins: [[meta:Special:MyPage/global.css]]
* User-specific for all skins: [[Special:MyPage/common.css]]
* User-specific per skin: e.g., [[Special:MyPage/vector.css]], or [[Special:MyPage/skin.css]] for your current skin 
* User-specific CSS loaded through JavaScript, e.g., loaded at [[Special:MyPage/common.js]]
* User-specific web-wide browser settings: local file referred to in the browser settings, or directly set in the browser

Note that in CSS terminology, the user-specific style sheets are ''not'' [[Help:User style|user style sheets]].

===Usage===

An HTML element may be just taken from the wikitext (see [[Help:HTML in wikitext|HTML in wikitext]]), e.g., span, or the result of translating wikitext, e.g., the &lt;code&gt;&lt;nowiki&gt;'''...'''&lt;/nowiki&gt;&lt;/code&gt; code is changed into {{tag|b}}, or part of the code for the skin.

A class may be produced by the software, e.g., ns-''namespace number'' for the HTML-element "body", and extiw for an interwiki link in the page body, or taken from the wikitext.

Similarly, an ID may be produced by the software, e.g., bodyContent, or taken from the wikitext.

In the case of conflicting style settings for a piece of content, the resulting setting depends primarily on the indication "!important". Secondarily, if both are important, the user wins, if neither is, the author wins. Tertiarily it depends on specificity. Only lastly, it depends on order between and within style sheets: the last wins. Thus, a User:''username''/monobook.css does not win from MediaWiki:Monobook.css (both author, not user) if the specificity of the latter is greater. See also [http://www.w3.org/TR/REC-CSS2/cascade.html cascade].

== Supported elements ==
MediaWiki supports most CSS, with such exceptions as the url() attribute. There were some bugs in CSS support in earlier versions.

{{Further2|the [http://www.w3.org/TR/2011/REC-CSS2-20110607/ Cascading Style Sheets Level 2 Revision 1 (CSS 2.1) Specification]}}

== CSS in wikitext ==

You can use CSS styling in HTML elements in your code (see [[Help:HTML in wikitext]] for a list of elements supported by MediaWiki) like you would in normal HTML markup.

For example, a {{tag|div}} element with a green border and its contents floated to the right would be created with
&lt;syntaxhighlight lang="html"&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
&lt;/syntaxhighlight&gt;
&lt;div style="float:right; border:thin solid green;"&gt;
Here comes a short paragraph that is&lt;br /&gt;
contained in a "div" element that is&lt;br /&gt;
floated to the right.
&lt;/div&gt;
which would produce the box to the right.
Some wikitext elements allow you to insert CSS styling directly into them. An example is the table syntax:
{{-}}
&lt;syntaxhighlight lang="wikitext"&gt;
{| style="your style here"
|-
|your table stuff
|}
&lt;/syntaxhighlight&gt;
{{Further2|[[Help:Tables]]}}

== MediaWiki existing styles ==

You may wish to use a style type that is already predefined by MediaWiki, or the site that you are visiting. You can also create a style that is unique to your page.

Vector is the default style, you can view it at: [[MediaWiki:Vector.css]]

You will give your CSS tag an existing "class"

Please put a list of existing classes here.

== Tips and tricks ==

=== Non-display ===
In an embedded page, one can hide comments in one version, and show them in another view.
One extreme "style" for a text is not displaying it, with

&lt;syntaxhighlight lang="css"&gt;
.''classname'' {display: none}
#''id'' {display: none}
&lt;/syntaxhighlight&gt;

etc.

Non-displayed links do not work (as opposed to links in a very small font).

It cannot be used to remove text in expressions for template names, parameter names, parameter values, page names in links, etc.

To view hidden text, download the Web Developer Toolbar for Firefox [https://addons.mozilla.org/en-US/firefox/addon/60 here], then choose Misc. &amp;rarr; show hidden elements in that toolbar. It will make all hidden elements appear.

=== Non-print ===

One can exclude content from being printed by declaring the content to be of the "noprint" class:
&lt;syntaxhighlight lang="html"&gt;
&lt;div class="someclass noprint"&gt;This will not appear in the print version.&lt;/div&gt;
&lt;/syntaxhighlight&gt;

== Major style blocks ==
[[File:Wikimedia major style blocks example.svg|right|thumb|An annotated desktop screenshot of [[Wikipedia:Example|an example article]], showcasing the style blocks]]
*''column-content'' – overall space within the margins of which the content exists.
* ''firstHeading'' – the class of the heading tag at the top of every page
*''contentSub'' – the name of the wiki immediately underneath the main heading, but above the body text
* ''content'' – the white background, thin bordered box which contains the main page content.
* ''bodyContent'' – the main page content within the content box

The portlet class is the style used by all the div blocks around the main content.  Identified blocks using that class:
*''p-cactions'' – id for the list of page-related tabs above the main content (page, talk, edit, etc.), top.
*''p-personal'' – id for the list of user-related links above the main content (username, talk, etc.), top.
*''p-logo'' – id for the block that contains the logo, top left.
*''p-navigation'' – id for the block that contains the navigation links on the left of the page
*''p-search'' – the block that contains the search buttons
*''p-tb'' – the block that contains the toolbox links
*''p-lang'' – the block that contains interlanguage links

The footer at the bottom of the page includes blocks with the following ids
*''footer'' – overall footer container block
*''f-poweredbyico'' – the powered by MediaWiki image that normally resides to the right of the page
*''f-list'' – id for the list that contains all the bits of text at the bottom of the page

== Style depending on a parameter or variable ==
=== Variable class or id ===

A class or id can depend on the result produced by a template or on a template parameter, e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1|def}}}"&lt;/syntaxhighlight&gt;. For one or more of the possible class names the style of that class can be defined. If the class is undefined it is ignored, so the standard style is used.

In the simplest case we have e.g. &lt;syntaxhighlight lang="wikitext" inline&gt;class="abc{{{1}}}"&lt;/syntaxhighlight&gt; and define class abcdef. If the parameter value is "def" it applies.

If a page for general use only makes sense when styles are defined for certain classes, then these have to be specified in the page [[MediaWiki:Common.css]], which applies for all users and all skins, as far as not overridden.

=== Variable style parameter value ===

Wikitext that reads
&lt;syntaxhighlight lang="html"&gt;&lt;span style="display:{{{3|none}}}"&gt;Wed&lt;/span&gt;&lt;/syntaxhighlight&gt;
will display "Wed" if parameter 3 is defined, but its value is not "none", and displays nothing if parameter 3 is undefined or "none". If the value of parameter 3 is a display style other than "none", that style is applied.

==Wiki headings==
Wiki headings use the following default CSS:
&lt;section begin="HTML_section" /&gt;
{| class="wikitable"
|-
! Wikimarkup !! [[:en:HTML|HTML]] !! Style
|-
| {{nowrap|1=&lt;code&gt;= Heading =&lt;/code&gt;}} || {{tag|h1|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.8em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;== Heading ==&lt;/code&gt;}} || {{tag|h2|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.5em; font-family: 'Linux Libertine',Georgia,Times,serif; margin-top: 1em; margin-bottom: 0.25em; line-height: 1.3; padding: 0; border-bottom: 1px solid #AAAAAA;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;=== Heading ===&lt;/code&gt;}} || {{tag|h3|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 1.2em; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;==== Heading ====&lt;/code&gt;}} || {{tag|h4|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;===== Heading =====&lt;/code&gt;}} || {{tag|h5|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
| {{nowrap|1=&lt;code&gt;====== Heading ======&lt;/code&gt;}} || {{tag|h6|o}} ||
&lt;syntaxhighlight lang="css"&gt;
color: #000000; background: none; overflow: hidden; page-break-after: avoid; font-size: 100%; font-weight: bold; margin-top: 0.3em; margin-bottom: 0; line-height: 1.6; padding-top: 0.5em; padding-bottom: 0;
&lt;/syntaxhighlight&gt;
|-
|}&lt;section end="HTML_section" /&gt;

== See also ==
*[[:en:Span and div|Span and div]]
*[[:en:Cascading Style Sheets|Cascading Style Sheets]] – article on CSS
*[[Wikipedia:Customisation]] – also covers user names, preferences settings, skins, user scripting, etc.
*[[Help:User style]] – modifying style for accessibility or for additional feature testing.
*[[Wikipedia:TemplateStyles]] – modifying style for advanced visual appearance that can be applied with template.
*[[Wikipedia:Catalogue of CSS classes]] – list of classes globally defined across the site
*[[Wikipedia:WikiProject Microformats/classes]] – list of classes used in microformats employed on Wikipedia
*[[Help:User CSS for a monospaced coding font]] – both for the editing window and for display of monospaced elements like {{tag|code|o}}
*[[meta:Help:Cascading_style_sheets]]
*[[mw:Manual:CSS]] and [[mw:Manual:Interface/Stylesheets]]
*[[mw:Gallery of user styles]]
*[[m:Customization:Explaining skins]]
*[[mw:Skin projects]]

{{Wikipedia technical help|collapsed}}
{{CSS and JS MediaWiki messages|state=collapsed|style=}}

[[en:Cascading Style Sheets]]</text>
      <sha1>qikrifkfjxneke7aqz87fct4amthgjo</sha1>
    </revision>
  </page>
</mediawiki>
