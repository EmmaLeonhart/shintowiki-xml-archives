<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Module:IDs</title>
    <ns>828</ns>
    <id>56323</id>
    <revision>
      <id>2938480</id>
      <timestamp>2024-12-20T17:39:02Z</timestamp>
      <contributor>
        <username>d&gt;TomT0m</username>
        <id>0</id>
      </contributor>
      <comment>added require strict</comment>
      <origin>2938480</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2565" sha1="j8r703c7mudhtz797a45kqlfckmg578" xml:space="preserve">
local entities_prefixes = "PQ" ;
local pq_pattern = "[" .. entities_prefixes .. "][1-9][0-9]*" ;
local q_pattern = "[" .. "Q" .. "][1-9][0-9]*" ;
local pattern = "^" .. pq_pattern .."$" ;

local entities_uri_pattern = { "https?://www.wikidata.org/wiki/", "https?://www.wikidata.org/entity/"};
local entities_wikilink_pattern_bounds = { "[[][[]", "|.*[]][]]"};

local p = {
	entities_prefixes = entities_prefixes;
	pq_pattern = pq_pattern ;
    pattern =  "^" .. pq_pattern .."$" ; -- pattern ;
    entities_uri_pattern = entities_uri_pattern;
}

p.is_validID = function(id)
	return mw.ustring.match(mw.text.trim(id), p.pattern) ~= nil
end

p.is_numberID = function(id)
	return mw.ustring.match(mw.text.trim(id), "^[1-9][0-9]*$") ~= nil
end

p.is_uriID = function(id)
	
	return mw.ustring.match(mw.text.trim(id), 
		"^https?://www.wikidata.org/(entity|wiki)/" .. p.pq_pattern .. "$" ) ~= nil;
end

p.is_wikilinkQID = function(id)
	return mw.ustring.match(mw.text.trim(id), 
		entities_wikilink_pattern_bounds[1] .. q_pattern .. entities_wikilink_pattern_bounds[2] ) ~= nil;
end

p.raw_number_from_StringID = function(id)
	-- string_id assumed to be valid
	return mw.ustring.gmatch(mw.text.trim(id), "[0-9]*$")()
end

p.id_normalize = function(idstring)
	local id = mw.text.trim(idstring)
	id = mw.ustring.gsub(id, p.entities_uri_pattern[1], "")
	id = mw.ustring.gsub(id, p.entities_uri_pattern[2], "")
	if p.is_wikilinkQID(id) then
		id = mw.ustring.gsub(id, "^" .. entities_wikilink_pattern_bounds[1], "")
		id = mw.ustring.gsub(id, entities_wikilink_pattern_bounds[2] .. "$", "")
	end
	return id
end

-- handle the case where just a number is given as id and an item number is required
p.qid_normalize = function(idstring)
	if p.is_numberID(idstring) then
		return "Q" .. idstring
	else
		return p.id_normalize(idstring)
	end
end


p.number = function(frame)
 	return p.raw_number_from_StringID(frame.args[1])
end

p.valid = function(frame)
	id = p.id_normalize(frame.args[1])
 	if p.is_validID(id) then
 		return "OK"
 	else
 		return nil
 	end
end

-- returns a P/Q number from a supposed to be entity id

p.isNumberID = function(frame)
 	if p.is_numberID(frame.args[1]) then
 		return "OK"
 	else
 		return nil
 	end
end


p.normalize = function(frame)
	return p.id_normalize(frame.args[1]);
end

-- case where a Qid is expected
p.normalizeQ = function(frame)
	local normedID = p.qid_normalize(frame.args[1]);
	if p.is_validID(normedID) then
		return normedID
	else
		error("«" .. frame.args[1] .. "» cannot be transformed into a Qid")
	end
	
end


return p;</text>
      <sha1>j8r703c7mudhtz797a45kqlfckmg578</sha1>
    </revision>
    <revision>
      <id>2938481</id>
      <parentid>2938480</parentid>
      <timestamp>2025-08-11T09:31:22Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: importing wikidata q template</comment>
      <origin>2938480</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2565" sha1="j8r703c7mudhtz797a45kqlfckmg578" xml:space="preserve">
local entities_prefixes = "PQ" ;
local pq_pattern = "[" .. entities_prefixes .. "][1-9][0-9]*" ;
local q_pattern = "[" .. "Q" .. "][1-9][0-9]*" ;
local pattern = "^" .. pq_pattern .."$" ;

local entities_uri_pattern = { "https?://www.wikidata.org/wiki/", "https?://www.wikidata.org/entity/"};
local entities_wikilink_pattern_bounds = { "[[][[]", "|.*[]][]]"};

local p = {
	entities_prefixes = entities_prefixes;
	pq_pattern = pq_pattern ;
    pattern =  "^" .. pq_pattern .."$" ; -- pattern ;
    entities_uri_pattern = entities_uri_pattern;
}

p.is_validID = function(id)
	return mw.ustring.match(mw.text.trim(id), p.pattern) ~= nil
end

p.is_numberID = function(id)
	return mw.ustring.match(mw.text.trim(id), "^[1-9][0-9]*$") ~= nil
end

p.is_uriID = function(id)
	
	return mw.ustring.match(mw.text.trim(id), 
		"^https?://www.wikidata.org/(entity|wiki)/" .. p.pq_pattern .. "$" ) ~= nil;
end

p.is_wikilinkQID = function(id)
	return mw.ustring.match(mw.text.trim(id), 
		entities_wikilink_pattern_bounds[1] .. q_pattern .. entities_wikilink_pattern_bounds[2] ) ~= nil;
end

p.raw_number_from_StringID = function(id)
	-- string_id assumed to be valid
	return mw.ustring.gmatch(mw.text.trim(id), "[0-9]*$")()
end

p.id_normalize = function(idstring)
	local id = mw.text.trim(idstring)
	id = mw.ustring.gsub(id, p.entities_uri_pattern[1], "")
	id = mw.ustring.gsub(id, p.entities_uri_pattern[2], "")
	if p.is_wikilinkQID(id) then
		id = mw.ustring.gsub(id, "^" .. entities_wikilink_pattern_bounds[1], "")
		id = mw.ustring.gsub(id, entities_wikilink_pattern_bounds[2] .. "$", "")
	end
	return id
end

-- handle the case where just a number is given as id and an item number is required
p.qid_normalize = function(idstring)
	if p.is_numberID(idstring) then
		return "Q" .. idstring
	else
		return p.id_normalize(idstring)
	end
end


p.number = function(frame)
 	return p.raw_number_from_StringID(frame.args[1])
end

p.valid = function(frame)
	id = p.id_normalize(frame.args[1])
 	if p.is_validID(id) then
 		return "OK"
 	else
 		return nil
 	end
end

-- returns a P/Q number from a supposed to be entity id

p.isNumberID = function(frame)
 	if p.is_numberID(frame.args[1]) then
 		return "OK"
 	else
 		return nil
 	end
end


p.normalize = function(frame)
	return p.id_normalize(frame.args[1]);
end

-- case where a Qid is expected
p.normalizeQ = function(frame)
	local normedID = p.qid_normalize(frame.args[1]);
	if p.is_validID(normedID) then
		return normedID
	else
		error("«" .. frame.args[1] .. "» cannot be transformed into a Qid")
	end
	
end


return p;</text>
      <sha1>j8r703c7mudhtz797a45kqlfckmg578</sha1>
    </revision>
    <revision>
      <id>56324</id>
      <timestamp>2025-11-22T14:38:02Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was Text replacement - "↵↵↵" to "
"</comment>
      <origin>56324</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2559" sha1="fng6wnh0y5ngaxn832c34ydkpbdftpx" xml:space="preserve">
local entities_prefixes = "PQ" ;
local pq_pattern = "[" .. entities_prefixes .. "][1-9][0-9]*" ;
local q_pattern = "[" .. "Q" .. "][1-9][0-9]*" ;
local pattern = "^" .. pq_pattern .."$" ;

local entities_uri_pattern = { "https?://www.wikidata.org/wiki/", "https?://www.wikidata.org/entity/"};
local entities_wikilink_pattern_bounds = { "[[][[]", "|.*[]][]]"};

local p = {
	entities_prefixes = entities_prefixes;
	pq_pattern = pq_pattern ;
    pattern =  "^" .. pq_pattern .."$" ; -- pattern ;
    entities_uri_pattern = entities_uri_pattern;
}

p.is_validID = function(id)
	return mw.ustring.match(mw.text.trim(id), p.pattern) ~= nil
end

p.is_numberID = function(id)
	return mw.ustring.match(mw.text.trim(id), "^[1-9][0-9]*$") ~= nil
end

p.is_uriID = function(id)
	
	return mw.ustring.match(mw.text.trim(id), 
		"^https?://www.wikidata.org/(entity|wiki)/" .. p.pq_pattern .. "$" ) ~= nil;
end

p.is_wikilinkQID = function(id)
	return mw.ustring.match(mw.text.trim(id), 
		entities_wikilink_pattern_bounds[1] .. q_pattern .. entities_wikilink_pattern_bounds[2] ) ~= nil;
end

p.raw_number_from_StringID = function(id)
	-- string_id assumed to be valid
	return mw.ustring.gmatch(mw.text.trim(id), "[0-9]*$")()
end

p.id_normalize = function(idstring)
	local id = mw.text.trim(idstring)
	id = mw.ustring.gsub(id, p.entities_uri_pattern[1], "")
	id = mw.ustring.gsub(id, p.entities_uri_pattern[2], "")
	if p.is_wikilinkQID(id) then
		id = mw.ustring.gsub(id, "^" .. entities_wikilink_pattern_bounds[1], "")
		id = mw.ustring.gsub(id, entities_wikilink_pattern_bounds[2] .. "$", "")
	end
	return id
end

-- handle the case where just a number is given as id and an item number is required
p.qid_normalize = function(idstring)
	if p.is_numberID(idstring) then
		return "Q" .. idstring
	else
		return p.id_normalize(idstring)
	end
end
p.number = function(frame)
 	return p.raw_number_from_StringID(frame.args[1])
end

p.valid = function(frame)
	id = p.id_normalize(frame.args[1])
 	if p.is_validID(id) then
 		return "OK"
 	else
 		return nil
 	end
end

-- returns a P/Q number from a supposed to be entity id

p.isNumberID = function(frame)
 	if p.is_numberID(frame.args[1]) then
 		return "OK"
 	else
 		return nil
 	end
end
p.normalize = function(frame)
	return p.id_normalize(frame.args[1]);
end

-- case where a Qid is expected
p.normalizeQ = function(frame)
	local normedID = p.qid_normalize(frame.args[1]);
	if p.is_validID(normedID) then
		return normedID
	else
		error("«" .. frame.args[1] .. "» cannot be transformed into a Qid")
	end
	
end
return p;</text>
      <sha1>fng6wnh0y5ngaxn832c34ydkpbdftpx</sha1>
    </revision>
  </page>
</mediawiki>
