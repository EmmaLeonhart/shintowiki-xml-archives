<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Template:If/doc</title>
    <ns>10</ns>
    <id>23314</id>
    <revision>
      <id>2058333</id>
      <timestamp>2025-05-10T21:17:45Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: create automatic enwiki redirect</comment>
      <origin>2058333</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="75" sha1="j0ihcr94caij2t5hmwe09voiuvg50wp" xml:space="preserve">#redirect[[:en:Template:If/doc]]
[[Category:automatic wikipedia redirects]]</text>
      <sha1>j0ihcr94caij2t5hmwe09voiuvg50wp</sha1>
    </revision>
    <revision>
      <id>2058334</id>
      <parentid>2058333</parentid>
      <timestamp>2025-05-11T01:48:02Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "[[Category:automatic wikipedia redirects]]" to "&lt;nowiki&gt;[[Category:Automatic wikipedia redirects]]&lt;/nowiki&gt;"</comment>
      <origin>2058334</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="92" sha1="14zbkt617jjjs1wpaaeza4xib643fnw" xml:space="preserve">#redirect[[:en:Template:If/doc]]
&lt;nowiki&gt;[[Category:Automatic wikipedia redirects]]&lt;/nowiki&gt;</text>
      <sha1>14zbkt617jjjs1wpaaeza4xib643fnw</sha1>
    </revision>
    <revision>
      <id>2058335</id>
      <parentid>2058334</parentid>
      <timestamp>2025-05-19T05:42:17Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: sync with enwiki</comment>
      <origin>2058335</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="6553" sha1="14gieox0iozyleiqwwsol35c1zdtteb" xml:space="preserve">{{stack|{{Documentation subpage}}{{risky template}}{{cascade-protected template}}}}
With this template the [[Help:parser function|parser functions]] of the [[mw:Help:Extension:ParserFunctions|ParserFunctions]] collection with names starting with "#if" can be used in a way such that they do not [[m:Manual:Newlines_and_spaces#Trimming_on_expansion|strip spaces and newlines]] from the "then" and "else" part. Spaces still do not affect the outcome of the condition. Parameter 1 selects the if-type as "eq", "expr", "exist" or "error" (for #iferror), or empty "||" for a simple if-there (for #if). The template can be repeatedly nested 6 or 7 levels, one inside the other, because the outer-most is completed before running either the then/else inner levels.

This template can be substituted, when the expression or comparison will not change. Functionally, a lead-space character is stored, internally, as a simple blank character. Note, in many cases a null nowiki tag ("{{xtag|nowiki|s}}") could be used, without [[Template:If]], to allow a lead-space (such as "{{xtag|nowiki|s}}&amp;nbsp;xx") anywhere, but the internal storage puts a 43-character marker for "{{xtag|nowiki|s}}" in string length. However, there is no extra expansion depth for a nowiki-tag, such as in trailing space, "zz&amp;nbsp;{{xtag|nowiki|s}}".

==Usage==
'''Where p is output for a true statement and q is output for a false statement.'''

*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if||x| p | q }}&lt;/syntaxhighlight&gt; gives "{{if||x| p | q }}". Notice the empty first parameter, seen as two adjacent vertical bars.
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|expr|2&lt;3| p | q }}&lt;/syntaxhighlight&gt; gives " p "
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|eq| u |u| p | q }}&lt;/syntaxhighlight&gt; gives "{{if|eq| u |u| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|exist| Help:Link | p | q }}&lt;/syntaxhighlight&gt; gives "{{if|exist| Help:Link | p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|error|{{#expr:x}}| p | q }}&lt;/syntaxhighlight&gt; gives "{{if|error|{{#expr:x}}| p | q }}"

Compare:
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#if:x| p | q }}&lt;/syntaxhighlight&gt; gives "{{#if:x| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr:2&lt;3| p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifexpr:2&lt;3| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: u |u| p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifeq: u |u| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexist: Help:Link | p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifexist: Help:Link | p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror:{{#expr:x}}| p | q }}&lt;/syntaxhighlight&gt; gives "{{#iferror:{{#expr:x}}| p | q }}"

For example, with a template named ''Template:Template name'' containing the code &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{parameter}}} |u| p | q }}&lt;/syntaxhighlight&gt;, entering the code &lt;syntaxhighlight lang="wikitext" inline&gt;{{Template name|parameter=u}}&lt;/syntaxhighlight&gt; into an article gives "p", but &lt;syntaxhighlight lang="wikitext" inline&gt;{{Template name|parameter=v}}&lt;/syntaxhighlight&gt; gives "q".

The text of either the then-clause or else-clause is only processed and expanded when triggered. Hence, any templates in use are only expanded once the then-clause or else-clause is matched, otherwise they are skipped as merely paired braces, "&amp;#123;&amp;#123;" with "}}".

===Indentation===
If indenting the markup, care must be taken to avoid extra newlines when indenting "}}" on the next line. An extra bar pipe "|" can be added after the else-clause to complete that text, and allow "}}" to then be placed anywhere without adding a newline into the else-clause. For example:
:::* &lt;syntaxhighlight lang="wikitext"&gt;{{if|{{{1|}}}
    |then found parameter 1|else no parameter 1| ← extra "|" ends else-clause
}}&lt;/syntaxhighlight &gt;
When the else-clause is indented to the next line, a newline (CR/LF) is added:
:::* &lt;syntaxhighlight lang="wikitext"&gt;{{if|{{{1|}}}
    |then found parameter 1 ← extra newline here
    |else noparameter 1|
}}&lt;/syntaxhighlight&gt;
To indent the else-clause, split an [[HTML]]-form comment, as "&lt;code&gt;&amp;lt;!--&lt;/code&gt;" with next line as "&lt;code&gt;--&amp;gt;|else...&lt;/code&gt;". Unless each then-clause and else-clause is carefully tested, to watch for extra newlines, then the results are likely to cause broken lines, with extra line breaks for each newline. For that reason, a global edit with simple search-and-replace of "&lt;code&gt;{#if:&lt;/code&gt;" to "&lt;code&gt;{if||&lt;/code&gt;" is likely to leave newline problems, wherever the original markup was wrapped to indent either the else-clause or "}}" of each if-structure. Indenting the then-clause is not a problem.

==Performance considerations==
Because [[Template:If]] must prepare the parameters for &lt;code&gt;#if, #ifeq, #ifexpr,&lt;/code&gt; (etc.) there is a slight overhead when using it. Each nested use adds 5 levels to the template expansion depth, so 7 nested if-templates would use 35 levels (5*7) of the [[m:Help:Expansion_depth|41-level limit]].

'''Using P-if syntax:''' A similar if-structure can be coded without '''Template:If''', by using the {{tlx|P1}} and {{tlx|P2}} templates in a "P-if" structure. [[Template:P1]] always returns parameter 1, and P2 returns the 2nd. So, a comparison of 4 versus 3 can be coded using P-if syntax form:
:: Example of &lt;code&gt;#ifexpr&lt;/code&gt;: &lt;syntaxhighlight lang="wikitext" inline&gt;"{{P{{#ifexpr: 4 &gt; 3|1|2}}| then 4 greater| else 4 lower}}"&lt;/syntaxhighlight&gt;
:: Result for &lt;code&gt;#ifexpr&lt;/code&gt;: &amp;nbsp; "{{P{{#ifexpr: 4 &gt; 3|1|2}}| then 4 greater| else 4 lower}}" &amp;larr; ''the comparison invoked'' {&lt;nowiki/&gt;{P1}}.
The expansion depth of a P-if is only 4 levels deep, but nesting of the then-clause or else-clause might be more confusing than using nested levels of Template:if.

:: Example of &lt;code&gt;#if&lt;/code&gt;: &lt;syntaxhighlight lang="wikitext" inline&gt;"{{P{{#if:{{{2|x}}}|1|2}}| then {2} set| else {2} empty}}"&lt;/syntaxhighlight&gt;
:: Result for &lt;code&gt;#if&lt;/code&gt;: "{{P{{#if:{{{2|}}}|1|2}}| then {2} set| else {2} empty}}" &amp;larr; ''the comparison invoked'' {&lt;nowiki/&gt;{P2}}.

==See also==
*[[m:Template:If]]
*[[Template:Ifnotempty]] - for simplicity and to reduce the limitations due to the expansion depth limit, this is a separate template corresponding to #if only.
*[[Template:Ifexist not redirect]] - useful because &lt;syntaxhighlight lang="wikitext" inline&gt;{{if|exist| testLink | yesRtn | noRtn }}&lt;/syntaxhighlight&gt; returns yesRtn if testLink is a redirect
*[[Help:Conditional expressions]]
&lt;includeonly&gt;{{Sandbox other||
[[Category:If-then-else templates]]
}}&lt;/includeonly&gt;</text>
      <sha1>14gieox0iozyleiqwwsol35c1zdtteb</sha1>
    </revision>
    <revision>
      <id>2058336</id>
      <parentid>2058335</parentid>
      <timestamp>2025-05-20T19:32:23Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>2058336</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="6562" sha1="iamzbgih7e58fqyuqhsu4jsz19rvbo9" xml:space="preserve">{{stack|{{Documentation subpage}}{{risky template}}{{cascade-protected template}}}}
With this template the [[Help:parser function|parser functions]] of the [[mw:Help:Extension:ParserFunctions|ParserFunctions]] collection with names starting with "#if" can be used in a way such that they do not [[m:Manual:Newlines_and_spaces#Trimming_on_expansion|strip spaces and newlines]] from the "then" and "else" part. Spaces still do not affect the outcome of the condition. Parameter 1 selects the if-type as "eq", "expr", "exist" or "error" (for #iferror), or empty "||" for a simple if-there (for #if). The template can be repeatedly nested 6 or 7 levels, one inside the other, because the outer-most is completed before running either the then/else inner levels.

This template can be substituted, when the expression or comparison will not change. Functionally, a lead-space character is stored, internally, as a simple blank character. Note, in many cases a null nowiki tag ("{{xtag|nowiki|s}}") could be used, without [[Template:If]], to allow a lead-space (such as "{{xtag|nowiki|s}}&amp;nbsp;xx") anywhere, but the internal storage puts a 43-character marker for "{{xtag|nowiki|s}}" in string length. However, there is no extra expansion depth for a nowiki-tag, such as in trailing space, "zz&amp;nbsp;{{xtag|nowiki|s}}".

==Usage==
'''Where p is output for a true statement and q is output for a false statement.'''

*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if||x| p | q }}&lt;/syntaxhighlight&gt; gives "{{if||x| p | q }}". Notice the empty first parameter, seen as two adjacent vertical bars.
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|expr|2&lt;3| p | q }}&lt;/syntaxhighlight&gt; gives " p "
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|eq| u |u| p | q }}&lt;/syntaxhighlight&gt; gives "{{if|eq| u |u| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|exist| Help:Link | p | q }}&lt;/syntaxhighlight&gt; gives "{{if|exist| Help:Link | p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|error|{{#expr:x}}| p | q }}&lt;/syntaxhighlight&gt; gives "{{if|error|{{#expr:x}}| p | q }}"

Compare:
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#if:x| p | q }}&lt;/syntaxhighlight&gt; gives "{{#if:x| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr:2&lt;3| p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifexpr:2&lt;3| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: u |u| p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifeq: u |u| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexist: Help:Link | p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifexist: Help:Link | p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror:{{#expr:x}}| p | q }}&lt;/syntaxhighlight&gt; gives "{{#iferror:{{#expr:x}}| p | q }}"

For example, with a template named ''Template:Template name'' containing the code &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{parameter}}} |u| p | q }}&lt;/syntaxhighlight&gt;, entering the code &lt;syntaxhighlight lang="wikitext" inline&gt;{{Template name|parameter=u}}&lt;/syntaxhighlight&gt; into an article gives "p", but &lt;syntaxhighlight lang="wikitext" inline&gt;{{Template name|parameter=v}}&lt;/syntaxhighlight&gt; gives "q".

The text of either the then-clause or else-clause is only processed and expanded when triggered. Hence, any templates in use are only expanded once the then-clause or else-clause is matched, otherwise they are skipped as merely paired braces, "&amp;#123;&amp;#123;" with "}}".

===Indentation===
If indenting the markup, care must be taken to avoid extra newlines when indenting "}}" on the next line. An extra bar pipe "|" can be added after the else-clause to complete that text, and allow "}}" to then be placed anywhere without adding a newline into the else-clause. For example:
:::* &lt;syntaxhighlight lang="wikitext"&gt;{{if|{{{1|}}}
    |then found parameter 1|else no parameter 1| ← extra "|" ends else-clause
}}&lt;/syntaxhighlight &gt;
When the else-clause is indented to the next line, a newline (CR/LF) is added:
:::* &lt;syntaxhighlight lang="wikitext"&gt;{{if|{{{1|}}}
    |then found parameter 1 ← extra newline here
    |else noparameter 1|
}}&lt;/syntaxhighlight&gt;
To indent the else-clause, split an [[:en:HTML|HTML]]-form comment, as "&lt;code&gt;&amp;lt;!--&lt;/code&gt;" with next line as "&lt;code&gt;--&amp;gt;|else...&lt;/code&gt;". Unless each then-clause and else-clause is carefully tested, to watch for extra newlines, then the results are likely to cause broken lines, with extra line breaks for each newline. For that reason, a global edit with simple search-and-replace of "&lt;code&gt;{#if:&lt;/code&gt;" to "&lt;code&gt;{if||&lt;/code&gt;" is likely to leave newline problems, wherever the original markup was wrapped to indent either the else-clause or "}}" of each if-structure. Indenting the then-clause is not a problem.

==Performance considerations==
Because [[Template:If]] must prepare the parameters for &lt;code&gt;#if, #ifeq, #ifexpr,&lt;/code&gt; (etc.) there is a slight overhead when using it. Each nested use adds 5 levels to the template expansion depth, so 7 nested if-templates would use 35 levels (5*7) of the [[m:Help:Expansion_depth|41-level limit]].

'''Using P-if syntax:''' A similar if-structure can be coded without '''Template:If''', by using the {{tlx|P1}} and {{tlx|P2}} templates in a "P-if" structure. [[Template:P1]] always returns parameter 1, and P2 returns the 2nd. So, a comparison of 4 versus 3 can be coded using P-if syntax form:
:: Example of &lt;code&gt;#ifexpr&lt;/code&gt;: &lt;syntaxhighlight lang="wikitext" inline&gt;"{{P{{#ifexpr: 4 &gt; 3|1|2}}| then 4 greater| else 4 lower}}"&lt;/syntaxhighlight&gt;
:: Result for &lt;code&gt;#ifexpr&lt;/code&gt;: &amp;nbsp; "{{P{{#ifexpr: 4 &gt; 3|1|2}}| then 4 greater| else 4 lower}}" &amp;larr; ''the comparison invoked'' {&lt;nowiki/&gt;{P1}}.
The expansion depth of a P-if is only 4 levels deep, but nesting of the then-clause or else-clause might be more confusing than using nested levels of Template:if.

:: Example of &lt;code&gt;#if&lt;/code&gt;: &lt;syntaxhighlight lang="wikitext" inline&gt;"{{P{{#if:{{{2|x}}}|1|2}}| then {2} set| else {2} empty}}"&lt;/syntaxhighlight&gt;
:: Result for &lt;code&gt;#if&lt;/code&gt;: "{{P{{#if:{{{2|}}}|1|2}}| then {2} set| else {2} empty}}" &amp;larr; ''the comparison invoked'' {&lt;nowiki/&gt;{P2}}.

==See also==
*[[m:Template:If]]
*[[Template:Ifnotempty]] - for simplicity and to reduce the limitations due to the expansion depth limit, this is a separate template corresponding to #if only.
*[[Template:Ifexist not redirect]] - useful because &lt;syntaxhighlight lang="wikitext" inline&gt;{{if|exist| testLink | yesRtn | noRtn }}&lt;/syntaxhighlight&gt; returns yesRtn if testLink is a redirect
*[[Help:Conditional expressions]]
&lt;includeonly&gt;{{Sandbox other||
[[Category:If-then-else templates]]
}}&lt;/includeonly&gt;</text>
      <sha1>iamzbgih7e58fqyuqhsu4jsz19rvbo9</sha1>
    </revision>
    <revision>
      <id>2058337</id>
      <parentid>2058336</parentid>
      <timestamp>2025-11-08T01:52:02Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: tag as missing wikidata (no wikidata found via interwikis)</comment>
      <origin>2058337</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="6625" sha1="trkmriduw42vlfbmg1pdg83gu9br6g9" xml:space="preserve">{{stack|{{Documentation subpage}}{{risky template}}{{cascade-protected template}}}}
With this template the [[Help:parser function|parser functions]] of the [[mw:Help:Extension:ParserFunctions|ParserFunctions]] collection with names starting with "#if" can be used in a way such that they do not [[m:Manual:Newlines_and_spaces#Trimming_on_expansion|strip spaces and newlines]] from the "then" and "else" part. Spaces still do not affect the outcome of the condition. Parameter 1 selects the if-type as "eq", "expr", "exist" or "error" (for #iferror), or empty "||" for a simple if-there (for #if). The template can be repeatedly nested 6 or 7 levels, one inside the other, because the outer-most is completed before running either the then/else inner levels.

This template can be substituted, when the expression or comparison will not change. Functionally, a lead-space character is stored, internally, as a simple blank character. Note, in many cases a null nowiki tag ("{{xtag|nowiki|s}}") could be used, without [[Template:If]], to allow a lead-space (such as "{{xtag|nowiki|s}}&amp;nbsp;xx") anywhere, but the internal storage puts a 43-character marker for "{{xtag|nowiki|s}}" in string length. However, there is no extra expansion depth for a nowiki-tag, such as in trailing space, "zz&amp;nbsp;{{xtag|nowiki|s}}".

==Usage==
'''Where p is output for a true statement and q is output for a false statement.'''

*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if||x| p | q }}&lt;/syntaxhighlight&gt; gives "{{if||x| p | q }}". Notice the empty first parameter, seen as two adjacent vertical bars.
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|expr|2&lt;3| p | q }}&lt;/syntaxhighlight&gt; gives " p "
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|eq| u |u| p | q }}&lt;/syntaxhighlight&gt; gives "{{if|eq| u |u| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|exist| Help:Link | p | q }}&lt;/syntaxhighlight&gt; gives "{{if|exist| Help:Link | p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|error|{{#expr:x}}| p | q }}&lt;/syntaxhighlight&gt; gives "{{if|error|{{#expr:x}}| p | q }}"

Compare:
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#if:x| p | q }}&lt;/syntaxhighlight&gt; gives "{{#if:x| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr:2&lt;3| p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifexpr:2&lt;3| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: u |u| p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifeq: u |u| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexist: Help:Link | p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifexist: Help:Link | p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror:{{#expr:x}}| p | q }}&lt;/syntaxhighlight&gt; gives "{{#iferror:{{#expr:x}}| p | q }}"

For example, with a template named ''Template:Template name'' containing the code &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{parameter}}} |u| p | q }}&lt;/syntaxhighlight&gt;, entering the code &lt;syntaxhighlight lang="wikitext" inline&gt;{{Template name|parameter=u}}&lt;/syntaxhighlight&gt; into an article gives "p", but &lt;syntaxhighlight lang="wikitext" inline&gt;{{Template name|parameter=v}}&lt;/syntaxhighlight&gt; gives "q".

The text of either the then-clause or else-clause is only processed and expanded when triggered. Hence, any templates in use are only expanded once the then-clause or else-clause is matched, otherwise they are skipped as merely paired braces, "&amp;#123;&amp;#123;" with "}}".

===Indentation===
If indenting the markup, care must be taken to avoid extra newlines when indenting "}}" on the next line. An extra bar pipe "|" can be added after the else-clause to complete that text, and allow "}}" to then be placed anywhere without adding a newline into the else-clause. For example:
:::* &lt;syntaxhighlight lang="wikitext"&gt;{{if|{{{1|}}}
    |then found parameter 1|else no parameter 1| ← extra "|" ends else-clause
}}&lt;/syntaxhighlight &gt;
When the else-clause is indented to the next line, a newline (CR/LF) is added:
:::* &lt;syntaxhighlight lang="wikitext"&gt;{{if|{{{1|}}}
    |then found parameter 1 ← extra newline here
    |else noparameter 1|
}}&lt;/syntaxhighlight&gt;
To indent the else-clause, split an [[:en:HTML|HTML]]-form comment, as "&lt;code&gt;&amp;lt;!--&lt;/code&gt;" with next line as "&lt;code&gt;--&amp;gt;|else...&lt;/code&gt;". Unless each then-clause and else-clause is carefully tested, to watch for extra newlines, then the results are likely to cause broken lines, with extra line breaks for each newline. For that reason, a global edit with simple search-and-replace of "&lt;code&gt;{#if:&lt;/code&gt;" to "&lt;code&gt;{if||&lt;/code&gt;" is likely to leave newline problems, wherever the original markup was wrapped to indent either the else-clause or "}}" of each if-structure. Indenting the then-clause is not a problem.

==Performance considerations==
Because [[Template:If]] must prepare the parameters for &lt;code&gt;#if, #ifeq, #ifexpr,&lt;/code&gt; (etc.) there is a slight overhead when using it. Each nested use adds 5 levels to the template expansion depth, so 7 nested if-templates would use 35 levels (5*7) of the [[m:Help:Expansion_depth|41-level limit]].

'''Using P-if syntax:''' A similar if-structure can be coded without '''Template:If''', by using the {{tlx|P1}} and {{tlx|P2}} templates in a "P-if" structure. [[Template:P1]] always returns parameter 1, and P2 returns the 2nd. So, a comparison of 4 versus 3 can be coded using P-if syntax form:
:: Example of &lt;code&gt;#ifexpr&lt;/code&gt;: &lt;syntaxhighlight lang="wikitext" inline&gt;"{{P{{#ifexpr: 4 &gt; 3|1|2}}| then 4 greater| else 4 lower}}"&lt;/syntaxhighlight&gt;
:: Result for &lt;code&gt;#ifexpr&lt;/code&gt;: &amp;nbsp; "{{P{{#ifexpr: 4 &gt; 3|1|2}}| then 4 greater| else 4 lower}}" &amp;larr; ''the comparison invoked'' {&lt;nowiki/&gt;{P1}}.
The expansion depth of a P-if is only 4 levels deep, but nesting of the then-clause or else-clause might be more confusing than using nested levels of Template:if.

:: Example of &lt;code&gt;#if&lt;/code&gt;: &lt;syntaxhighlight lang="wikitext" inline&gt;"{{P{{#if:{{{2|x}}}|1|2}}| then {2} set| else {2} empty}}"&lt;/syntaxhighlight&gt;
:: Result for &lt;code&gt;#if&lt;/code&gt;: "{{P{{#if:{{{2|}}}|1|2}}| then {2} set| else {2} empty}}" &amp;larr; ''the comparison invoked'' {&lt;nowiki/&gt;{P2}}.

==See also==
*[[m:Template:If]]
*[[Template:Ifnotempty]] - for simplicity and to reduce the limitations due to the expansion depth limit, this is a separate template corresponding to #if only.
*[[Template:Ifexist not redirect]] - useful because &lt;syntaxhighlight lang="wikitext" inline&gt;{{if|exist| testLink | yesRtn | noRtn }}&lt;/syntaxhighlight&gt; returns yesRtn if testLink is a redirect
*[[Help:Conditional expressions]]
&lt;includeonly&gt;{{Sandbox other||
[[Category:If-then-else templates]]
}}&lt;/includeonly&gt;
&lt;noinclude&gt;[[Category:Templates without wikidata]]&lt;/noinclude&gt;</text>
      <sha1>trkmriduw42vlfbmg1pdg83gu9br6g9</sha1>
    </revision>
    <revision>
      <id>23315</id>
      <timestamp>2025-11-08T04:37:52Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was Text replacement - "↵&lt;noinclude&gt;[[Category:Templates without wikidata]]&lt;/noinclude&gt;" to ""</comment>
      <origin>23315</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="6562" sha1="iamzbgih7e58fqyuqhsu4jsz19rvbo9" xml:space="preserve">{{stack|{{Documentation subpage}}{{risky template}}{{cascade-protected template}}}}
With this template the [[Help:parser function|parser functions]] of the [[mw:Help:Extension:ParserFunctions|ParserFunctions]] collection with names starting with "#if" can be used in a way such that they do not [[m:Manual:Newlines_and_spaces#Trimming_on_expansion|strip spaces and newlines]] from the "then" and "else" part. Spaces still do not affect the outcome of the condition. Parameter 1 selects the if-type as "eq", "expr", "exist" or "error" (for #iferror), or empty "||" for a simple if-there (for #if). The template can be repeatedly nested 6 or 7 levels, one inside the other, because the outer-most is completed before running either the then/else inner levels.

This template can be substituted, when the expression or comparison will not change. Functionally, a lead-space character is stored, internally, as a simple blank character. Note, in many cases a null nowiki tag ("{{xtag|nowiki|s}}") could be used, without [[Template:If]], to allow a lead-space (such as "{{xtag|nowiki|s}}&amp;nbsp;xx") anywhere, but the internal storage puts a 43-character marker for "{{xtag|nowiki|s}}" in string length. However, there is no extra expansion depth for a nowiki-tag, such as in trailing space, "zz&amp;nbsp;{{xtag|nowiki|s}}".

==Usage==
'''Where p is output for a true statement and q is output for a false statement.'''

*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if||x| p | q }}&lt;/syntaxhighlight&gt; gives "{{if||x| p | q }}". Notice the empty first parameter, seen as two adjacent vertical bars.
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|expr|2&lt;3| p | q }}&lt;/syntaxhighlight&gt; gives " p "
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|eq| u |u| p | q }}&lt;/syntaxhighlight&gt; gives "{{if|eq| u |u| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|exist| Help:Link | p | q }}&lt;/syntaxhighlight&gt; gives "{{if|exist| Help:Link | p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|error|{{#expr:x}}| p | q }}&lt;/syntaxhighlight&gt; gives "{{if|error|{{#expr:x}}| p | q }}"

Compare:
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#if:x| p | q }}&lt;/syntaxhighlight&gt; gives "{{#if:x| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr:2&lt;3| p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifexpr:2&lt;3| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: u |u| p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifeq: u |u| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexist: Help:Link | p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifexist: Help:Link | p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror:{{#expr:x}}| p | q }}&lt;/syntaxhighlight&gt; gives "{{#iferror:{{#expr:x}}| p | q }}"

For example, with a template named ''Template:Template name'' containing the code &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{parameter}}} |u| p | q }}&lt;/syntaxhighlight&gt;, entering the code &lt;syntaxhighlight lang="wikitext" inline&gt;{{Template name|parameter=u}}&lt;/syntaxhighlight&gt; into an article gives "p", but &lt;syntaxhighlight lang="wikitext" inline&gt;{{Template name|parameter=v}}&lt;/syntaxhighlight&gt; gives "q".

The text of either the then-clause or else-clause is only processed and expanded when triggered. Hence, any templates in use are only expanded once the then-clause or else-clause is matched, otherwise they are skipped as merely paired braces, "&amp;#123;&amp;#123;" with "}}".

===Indentation===
If indenting the markup, care must be taken to avoid extra newlines when indenting "}}" on the next line. An extra bar pipe "|" can be added after the else-clause to complete that text, and allow "}}" to then be placed anywhere without adding a newline into the else-clause. For example:
:::* &lt;syntaxhighlight lang="wikitext"&gt;{{if|{{{1|}}}
    |then found parameter 1|else no parameter 1| ← extra "|" ends else-clause
}}&lt;/syntaxhighlight &gt;
When the else-clause is indented to the next line, a newline (CR/LF) is added:
:::* &lt;syntaxhighlight lang="wikitext"&gt;{{if|{{{1|}}}
    |then found parameter 1 ← extra newline here
    |else noparameter 1|
}}&lt;/syntaxhighlight&gt;
To indent the else-clause, split an [[:en:HTML|HTML]]-form comment, as "&lt;code&gt;&amp;lt;!--&lt;/code&gt;" with next line as "&lt;code&gt;--&amp;gt;|else...&lt;/code&gt;". Unless each then-clause and else-clause is carefully tested, to watch for extra newlines, then the results are likely to cause broken lines, with extra line breaks for each newline. For that reason, a global edit with simple search-and-replace of "&lt;code&gt;{#if:&lt;/code&gt;" to "&lt;code&gt;{if||&lt;/code&gt;" is likely to leave newline problems, wherever the original markup was wrapped to indent either the else-clause or "}}" of each if-structure. Indenting the then-clause is not a problem.

==Performance considerations==
Because [[Template:If]] must prepare the parameters for &lt;code&gt;#if, #ifeq, #ifexpr,&lt;/code&gt; (etc.) there is a slight overhead when using it. Each nested use adds 5 levels to the template expansion depth, so 7 nested if-templates would use 35 levels (5*7) of the [[m:Help:Expansion_depth|41-level limit]].

'''Using P-if syntax:''' A similar if-structure can be coded without '''Template:If''', by using the {{tlx|P1}} and {{tlx|P2}} templates in a "P-if" structure. [[Template:P1]] always returns parameter 1, and P2 returns the 2nd. So, a comparison of 4 versus 3 can be coded using P-if syntax form:
:: Example of &lt;code&gt;#ifexpr&lt;/code&gt;: &lt;syntaxhighlight lang="wikitext" inline&gt;"{{P{{#ifexpr: 4 &gt; 3|1|2}}| then 4 greater| else 4 lower}}"&lt;/syntaxhighlight&gt;
:: Result for &lt;code&gt;#ifexpr&lt;/code&gt;: &amp;nbsp; "{{P{{#ifexpr: 4 &gt; 3|1|2}}| then 4 greater| else 4 lower}}" &amp;larr; ''the comparison invoked'' {&lt;nowiki/&gt;{P1}}.
The expansion depth of a P-if is only 4 levels deep, but nesting of the then-clause or else-clause might be more confusing than using nested levels of Template:if.

:: Example of &lt;code&gt;#if&lt;/code&gt;: &lt;syntaxhighlight lang="wikitext" inline&gt;"{{P{{#if:{{{2|x}}}|1|2}}| then {2} set| else {2} empty}}"&lt;/syntaxhighlight&gt;
:: Result for &lt;code&gt;#if&lt;/code&gt;: "{{P{{#if:{{{2|}}}|1|2}}| then {2} set| else {2} empty}}" &amp;larr; ''the comparison invoked'' {&lt;nowiki/&gt;{P2}}.

==See also==
*[[m:Template:If]]
*[[Template:Ifnotempty]] - for simplicity and to reduce the limitations due to the expansion depth limit, this is a separate template corresponding to #if only.
*[[Template:Ifexist not redirect]] - useful because &lt;syntaxhighlight lang="wikitext" inline&gt;{{if|exist| testLink | yesRtn | noRtn }}&lt;/syntaxhighlight&gt; returns yesRtn if testLink is a redirect
*[[Help:Conditional expressions]]
&lt;includeonly&gt;{{Sandbox other||
[[Category:If-then-else templates]]
}}&lt;/includeonly&gt;</text>
      <sha1>iamzbgih7e58fqyuqhsu4jsz19rvbo9</sha1>
    </revision>
    <revision>
      <id>168486</id>
      <parentid>23315</parentid>
      <timestamp>2026-02-05T22:24:52Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: Adding dummy category to make categories work after wiki restoration</comment>
      <origin>168486</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="6585" sha1="ddc9ia2getgg36yxk5s4kh2v2knw2oj" xml:space="preserve">{{stack|{{Documentation subpage}}{{risky template}}{{cascade-protected template}}}}
With this template the [[Help:parser function|parser functions]] of the [[mw:Help:Extension:ParserFunctions|ParserFunctions]] collection with names starting with "#if" can be used in a way such that they do not [[m:Manual:Newlines_and_spaces#Trimming_on_expansion|strip spaces and newlines]] from the "then" and "else" part. Spaces still do not affect the outcome of the condition. Parameter 1 selects the if-type as "eq", "expr", "exist" or "error" (for #iferror), or empty "||" for a simple if-there (for #if). The template can be repeatedly nested 6 or 7 levels, one inside the other, because the outer-most is completed before running either the then/else inner levels.

This template can be substituted, when the expression or comparison will not change. Functionally, a lead-space character is stored, internally, as a simple blank character. Note, in many cases a null nowiki tag ("{{xtag|nowiki|s}}") could be used, without [[Template:If]], to allow a lead-space (such as "{{xtag|nowiki|s}}&amp;nbsp;xx") anywhere, but the internal storage puts a 43-character marker for "{{xtag|nowiki|s}}" in string length. However, there is no extra expansion depth for a nowiki-tag, such as in trailing space, "zz&amp;nbsp;{{xtag|nowiki|s}}".

==Usage==
'''Where p is output for a true statement and q is output for a false statement.'''

*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if||x| p | q }}&lt;/syntaxhighlight&gt; gives "{{if||x| p | q }}". Notice the empty first parameter, seen as two adjacent vertical bars.
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|expr|2&lt;3| p | q }}&lt;/syntaxhighlight&gt; gives " p "
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|eq| u |u| p | q }}&lt;/syntaxhighlight&gt; gives "{{if|eq| u |u| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|exist| Help:Link | p | q }}&lt;/syntaxhighlight&gt; gives "{{if|exist| Help:Link | p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|error|{{#expr:x}}| p | q }}&lt;/syntaxhighlight&gt; gives "{{if|error|{{#expr:x}}| p | q }}"

Compare:
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#if:x| p | q }}&lt;/syntaxhighlight&gt; gives "{{#if:x| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr:2&lt;3| p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifexpr:2&lt;3| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: u |u| p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifeq: u |u| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexist: Help:Link | p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifexist: Help:Link | p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror:{{#expr:x}}| p | q }}&lt;/syntaxhighlight&gt; gives "{{#iferror:{{#expr:x}}| p | q }}"

For example, with a template named ''Template:Template name'' containing the code &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{parameter}}} |u| p | q }}&lt;/syntaxhighlight&gt;, entering the code &lt;syntaxhighlight lang="wikitext" inline&gt;{{Template name|parameter=u}}&lt;/syntaxhighlight&gt; into an article gives "p", but &lt;syntaxhighlight lang="wikitext" inline&gt;{{Template name|parameter=v}}&lt;/syntaxhighlight&gt; gives "q".

The text of either the then-clause or else-clause is only processed and expanded when triggered. Hence, any templates in use are only expanded once the then-clause or else-clause is matched, otherwise they are skipped as merely paired braces, "&amp;#123;&amp;#123;" with "}}".

===Indentation===
If indenting the markup, care must be taken to avoid extra newlines when indenting "}}" on the next line. An extra bar pipe "|" can be added after the else-clause to complete that text, and allow "}}" to then be placed anywhere without adding a newline into the else-clause. For example:
:::* &lt;syntaxhighlight lang="wikitext"&gt;{{if|{{{1|}}}
    |then found parameter 1|else no parameter 1| ← extra "|" ends else-clause
}}&lt;/syntaxhighlight &gt;
When the else-clause is indented to the next line, a newline (CR/LF) is added:
:::* &lt;syntaxhighlight lang="wikitext"&gt;{{if|{{{1|}}}
    |then found parameter 1 ← extra newline here
    |else noparameter 1|
}}&lt;/syntaxhighlight&gt;
To indent the else-clause, split an [[:en:HTML|HTML]]-form comment, as "&lt;code&gt;&amp;lt;!--&lt;/code&gt;" with next line as "&lt;code&gt;--&amp;gt;|else...&lt;/code&gt;". Unless each then-clause and else-clause is carefully tested, to watch for extra newlines, then the results are likely to cause broken lines, with extra line breaks for each newline. For that reason, a global edit with simple search-and-replace of "&lt;code&gt;{#if:&lt;/code&gt;" to "&lt;code&gt;{if||&lt;/code&gt;" is likely to leave newline problems, wherever the original markup was wrapped to indent either the else-clause or "}}" of each if-structure. Indenting the then-clause is not a problem.

==Performance considerations==
Because [[Template:If]] must prepare the parameters for &lt;code&gt;#if, #ifeq, #ifexpr,&lt;/code&gt; (etc.) there is a slight overhead when using it. Each nested use adds 5 levels to the template expansion depth, so 7 nested if-templates would use 35 levels (5*7) of the [[m:Help:Expansion_depth|41-level limit]].

'''Using P-if syntax:''' A similar if-structure can be coded without '''Template:If''', by using the {{tlx|P1}} and {{tlx|P2}} templates in a "P-if" structure. [[Template:P1]] always returns parameter 1, and P2 returns the 2nd. So, a comparison of 4 versus 3 can be coded using P-if syntax form:
:: Example of &lt;code&gt;#ifexpr&lt;/code&gt;: &lt;syntaxhighlight lang="wikitext" inline&gt;"{{P{{#ifexpr: 4 &gt; 3|1|2}}| then 4 greater| else 4 lower}}"&lt;/syntaxhighlight&gt;
:: Result for &lt;code&gt;#ifexpr&lt;/code&gt;: &amp;nbsp; "{{P{{#ifexpr: 4 &gt; 3|1|2}}| then 4 greater| else 4 lower}}" &amp;larr; ''the comparison invoked'' {&lt;nowiki/&gt;{P1}}.
The expansion depth of a P-if is only 4 levels deep, but nesting of the then-clause or else-clause might be more confusing than using nested levels of Template:if.

:: Example of &lt;code&gt;#if&lt;/code&gt;: &lt;syntaxhighlight lang="wikitext" inline&gt;"{{P{{#if:{{{2|x}}}|1|2}}| then {2} set| else {2} empty}}"&lt;/syntaxhighlight&gt;
:: Result for &lt;code&gt;#if&lt;/code&gt;: "{{P{{#if:{{{2|}}}|1|2}}| then {2} set| else {2} empty}}" &amp;larr; ''the comparison invoked'' {&lt;nowiki/&gt;{P2}}.

==See also==
*[[m:Template:If]]
*[[Template:Ifnotempty]] - for simplicity and to reduce the limitations due to the expansion depth limit, this is a separate template corresponding to #if only.
*[[Template:Ifexist not redirect]] - useful because &lt;syntaxhighlight lang="wikitext" inline&gt;{{if|exist| testLink | yesRtn | noRtn }}&lt;/syntaxhighlight&gt; returns yesRtn if testLink is a redirect
*[[Help:Conditional expressions]]
&lt;includeonly&gt;{{Sandbox other||
[[Category:If-then-else templates]]
}}&lt;/includeonly&gt;
[[Category:Templates]]</text>
      <sha1>ddc9ia2getgg36yxk5s4kh2v2knw2oj</sha1>
    </revision>
    <revision>
      <id>3567134</id>
      <parentid>168486</parentid>
      <timestamp>2026-03-23T20:41:42Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <comment>Bot: wrap 2 stray tag(s) in noinclude [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/23458359115|commit triggered pipeline]]</comment>
      <origin>3567134</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="6654" sha1="twdnri1s0l6u9n6mnnoh3g91ikgx2lp" xml:space="preserve">{{stack|{{Documentation subpage}}{{risky template}}{{cascade-protected template}}}}
With this template the [[Help:parser function|parser functions]] of the [[mw:Help:Extension:ParserFunctions|ParserFunctions]] collection with names starting with "#if" can be used in a way such that they do not [[m:Manual:Newlines_and_spaces#Trimming_on_expansion|strip spaces and newlines]] from the "then" and "else" part. Spaces still do not affect the outcome of the condition. Parameter 1 selects the if-type as "eq", "expr", "exist" or "error" (for #iferror), or empty "||" for a simple if-there (for #if). The template can be repeatedly nested 6 or 7 levels, one inside the other, because the outer-most is completed before running either the then/else inner levels.

This template can be substituted, when the expression or comparison will not change. Functionally, a lead-space character is stored, internally, as a simple blank character. Note, in many cases a null nowiki tag ("{{xtag|nowiki|s}}") could be used, without [[Template:If]], to allow a lead-space (such as "{{xtag|nowiki|s}}&amp;nbsp;xx") anywhere, but the internal storage puts a 43-character marker for "{{xtag|nowiki|s}}" in string length. However, there is no extra expansion depth for a nowiki-tag, such as in trailing space, "zz&amp;nbsp;{{xtag|nowiki|s}}".

==Usage==
'''Where p is output for a true statement and q is output for a false statement.'''

*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if||x| p | q }}&lt;/syntaxhighlight&gt; gives "{{if||x| p | q }}". Notice the empty first parameter, seen as two adjacent vertical bars.
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|expr|2&lt;3| p | q }}&lt;/syntaxhighlight&gt; gives " p "
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|eq| u |u| p | q }}&lt;/syntaxhighlight&gt; gives "{{if|eq| u |u| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|exist| Help:Link | p | q }}&lt;/syntaxhighlight&gt; gives "{{if|exist| Help:Link | p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{if|error|{{#expr:x}}| p | q }}&lt;/syntaxhighlight&gt; gives "{{if|error|{{#expr:x}}| p | q }}"

Compare:
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#if:x| p | q }}&lt;/syntaxhighlight&gt; gives "{{#if:x| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr:2&lt;3| p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifexpr:2&lt;3| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: u |u| p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifeq: u |u| p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexist: Help:Link | p | q }}&lt;/syntaxhighlight&gt; gives "{{#ifexist: Help:Link | p | q }}"
*&lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror:{{#expr:x}}| p | q }}&lt;/syntaxhighlight&gt; gives "{{#iferror:{{#expr:x}}| p | q }}"

For example, with a template named ''Template:Template name'' containing the code &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{parameter}}} |u| p | q }}&lt;/syntaxhighlight&gt;, entering the code &lt;syntaxhighlight lang="wikitext" inline&gt;{{Template name|parameter=u}}&lt;/syntaxhighlight&gt; into an article gives "p", but &lt;syntaxhighlight lang="wikitext" inline&gt;{{Template name|parameter=v}}&lt;/syntaxhighlight&gt; gives "q".

The text of either the then-clause or else-clause is only processed and expanded when triggered. Hence, any templates in use are only expanded once the then-clause or else-clause is matched, otherwise they are skipped as merely paired braces, "&amp;#123;&amp;#123;" with "}}".

===Indentation===
If indenting the markup, care must be taken to avoid extra newlines when indenting "}}" on the next line. An extra bar pipe "|" can be added after the else-clause to complete that text, and allow "}}" to then be placed anywhere without adding a newline into the else-clause. For example:
:::* &lt;syntaxhighlight lang="wikitext"&gt;{{if|{{{1|}}}
    |then found parameter 1|else no parameter 1| ← extra "|" ends else-clause
}}&lt;/syntaxhighlight &gt;
When the else-clause is indented to the next line, a newline (CR/LF) is added:
:::* &lt;syntaxhighlight lang="wikitext"&gt;{{if|{{{1|}}}
    |then found parameter 1 ← extra newline here
    |else noparameter 1|
}}&lt;/syntaxhighlight&gt;
To indent the else-clause, split an [[:en:HTML|HTML]]-form comment, as "&lt;code&gt;&amp;lt;!--&lt;/code&gt;" with next line as "&lt;code&gt;--&amp;gt;|else...&lt;/code&gt;". Unless each then-clause and else-clause is carefully tested, to watch for extra newlines, then the results are likely to cause broken lines, with extra line breaks for each newline. For that reason, a global edit with simple search-and-replace of "&lt;code&gt;{#if:&lt;/code&gt;" to "&lt;code&gt;{if||&lt;/code&gt;" is likely to leave newline problems, wherever the original markup was wrapped to indent either the else-clause or "}}" of each if-structure. Indenting the then-clause is not a problem.

==Performance considerations==
Because [[Template:If]] must prepare the parameters for &lt;code&gt;#if, #ifeq, #ifexpr,&lt;/code&gt; (etc.) there is a slight overhead when using it. Each nested use adds 5 levels to the template expansion depth, so 7 nested if-templates would use 35 levels (5*7) of the [[m:Help:Expansion_depth|41-level limit]].

'''Using P-if syntax:''' A similar if-structure can be coded without '''Template:If''', by using the {{tlx|P1}} and {{tlx|P2}} templates in a "P-if" structure. [[Template:P1]] always returns parameter 1, and P2 returns the 2nd. So, a comparison of 4 versus 3 can be coded using P-if syntax form:
:: Example of &lt;code&gt;#ifexpr&lt;/code&gt;: &lt;syntaxhighlight lang="wikitext" inline&gt;"{{P{{#ifexpr: 4 &gt; 3|1|2}}| then 4 greater| else 4 lower}}"&lt;/syntaxhighlight&gt;
:: Result for &lt;code&gt;#ifexpr&lt;/code&gt;: &amp;nbsp; "{{P{{#ifexpr: 4 &gt; 3|1|2}}| then 4 greater| else 4 lower}}" &amp;larr; ''the comparison invoked'' {&lt;nowiki/&gt;{P1}}.
The expansion depth of a P-if is only 4 levels deep, but nesting of the then-clause or else-clause might be more confusing than using nested levels of Template:if.

:: Example of &lt;code&gt;#if&lt;/code&gt;: &lt;syntaxhighlight lang="wikitext" inline&gt;"{{P{{#if:{{{2|x}}}|1|2}}| then {2} set| else {2} empty}}"&lt;/syntaxhighlight&gt;
:: Result for &lt;code&gt;#if&lt;/code&gt;: "{{P{{#if:{{{2|}}}|1|2}}| then {2} set| else {2} empty}}" &amp;larr; ''the comparison invoked'' {&lt;nowiki/&gt;{P2}}.

==See also==
*[[m:Template:If]]
*[[Template:Ifnotempty]] - for simplicity and to reduce the limitations due to the expansion depth limit, this is a separate template corresponding to #if only.
*[[Template:Ifexist not redirect]] - useful because &lt;syntaxhighlight lang="wikitext" inline&gt;{{if|exist| testLink | yesRtn | noRtn }}&lt;/syntaxhighlight&gt; returns yesRtn if testLink is a redirect
*[[Help:Conditional expressions]]
&lt;includeonly&gt;{{Sandbox other||
}}&lt;/includeonly&gt;
&lt;noinclude&gt;
[[Category:If-then-else templates]]
[[Category:Templates]]
[[Category:Templates fixed with noinclude]]
&lt;/noinclude&gt;</text>
      <sha1>twdnri1s0l6u9n6mnnoh3g91ikgx2lp</sha1>
    </revision>
  </page>
</mediawiki>
